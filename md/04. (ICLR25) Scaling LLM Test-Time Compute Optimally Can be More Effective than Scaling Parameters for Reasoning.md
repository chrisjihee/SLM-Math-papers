# 📑 SCALING LLM TEST-TIME COMPUTE OPTIMALLY CAN BE MORE EFFECTIVE THAN SCALING PARAMETERS FOR REASONING

---

## 📘 Abstract (Paragraph 1)

Enabling LLMs to improve their outputs by using more test-time compute is a critical step towards building self-improving agents that can operate on open-ended natural language. In this paper, we scale up inference-time computation in LLMs, with a focus on answering: if an LLM is allowed to use a fixed but non-trivial amount of inference-time compute, how much can it improve its performance on a challenging prompt? Answering this question has implications not only on performance, but also on the future of LLM pretraining and how to tradeoff inference-time and pre-training compute. Little research has attempted to understand the scaling behaviors of test-time inference methods, with current work largely providing negative results for a number of these strategies.

---

### ✅ 문장 1

Enabling LLMs to improve their outputs by using more test-time compute is a critical step towards building self-improving agents that can operate on open-ended natural language.

**단위 해석**
더 많은 test-time compute(테스트 시간 연산)를 사용하여 LLM이 결과물을 개선할 수 있도록 하는 것은, 개방형 자연어(open-ended natural language) 환경에서 동작할 수 있는 self-improving agents(자기 개선 에이전트)를 구축하기 위한 중요한 단계입니다.

**보조 설명**

* **Test-time compute:** 모델 학습이 끝난 후, 추론(Inference) 단계에서 추가적인 연산 자원을 투입하는 것을 의미합니다. (예: CoT 생성, 다수 샘플링 후 검증 등)
* **Self-improving agents:** 사람의 개입 없이 스스로 성능을 향상시키는 AI 에이전트를 말하며, 이 논문에서는 추론 시 더 많은 연산을 통해 더 나은 답을 찾아내는 능력을 그 핵심으로 보고 있습니다.

---

### ✅ 문장 2

In this paper, we scale up inference-time computation in LLMs, with a focus on answering: if an LLM is allowed to use a fixed but non-trivial amount of inference-time compute, how much can it improve its performance on a challenging prompt?

**단위 해석**
본 논문에서는 LLM의 inference-time computation(추론 시간 연산)을 확장(scale up)하며, 다음 질문에 답하는 데 초점을 맞춥니다: 만약 LLM에게 고정되어 있지만 적지 않은(non-trivial) 양의 추론 연산을 사용하도록 허용한다면, 어려운 프롬프트에 대한 성능을 얼마나 향상시킬 수 있을까?

**보조 설명**

* **Fixed but non-trivial amount:** 단순히 한 번의 추론(Greedy decoding)이 아니라, 시스템적으로 유의미한 양의 연산 자원(토큰 생성, 탐색 등)을 할당한다는 의미입니다.

---

### ✅ 문장 3

Answering this question has implications not only on performance, but also on the future of LLM pretraining and how to tradeoff inference-time and pre-training compute.

**단위 해석**
이 질문에 답하는 것은 성능뿐만 아니라 LLM pretraining(사전 학습)의 미래, 그리고 inference-time(추론 시간)과 pre-training(사전 학습) 연산 간의 트레이드오프(tradeoff)를 어떻게 할 것인지에 대해서도 시사점을 줍니다.

**보조 설명**

* **Tradeoff:** 모델의 파라미터 수를 늘려 사전 학습 비용을 높일 것인지, 아니면 모델을 작게 유지하되 추론 시 더 많은 연산을 수행할 것인지에 대한 비용 효율성 문제를 다룹니다.

---

### ✅ 문장 4

Little research has attempted to understand the scaling behaviors of test-time inference methods, with current work largely providing negative results for a number of these strategies.

**단위 해석**
Test-time inference 방법들의 scaling behavior(확장 거동)를 이해하려는 연구는 거의 없었으며, 현재의 연구들은 이러한 전략들에 대해 대체로 부정적인 결과를 보여주고 있습니다.

**보조 설명**

* 기존 연구들에서는 단순히 추론 연산을 늘린다고 해서 성능이 비례하여 증가하지 않거나, 특정 시점부터는 효과가 없다는 결과가 많았음을 지적합니다.

---

## 📘 Abstract (Paragraph 2)

In this work, we analyze two primary mechanisms to scale test-time computation: (1) searching against dense, process-based verifier reward models (PRMs); and (2) updating the model's distribution over a response adaptively, given the prompt at test time. We find that in both cases, the effectiveness of different approaches to scaling test-time compute critically varies depending on the difficulty of the prompt. This observation motivates applying a "compute-optimal" scaling strategy, which acts to, as effectively as possible, allocate test-time compute per prompt in an adaptive manner. Using this compute-optimal strategy, we can improve the efficiency of test-time compute scaling for math reasoning problems by more than 4x compared to a best-of-N baseline. Additionally, in a FLOPs-matched evaluation, we find that on problems where a smaller base model attains somewhat non-trivial success rates, test-time compute can be used to outperform a 14x larger model.

---

### ✅ 문장 5

In this work, we analyze two primary mechanisms to scale test-time computation: (1) searching against dense, process-based verifier reward models (PRMs); and (2) updating the model's distribution over a response adaptively, given the prompt at test time.

**단위 해석**
본 연구에서는 test-time computation을 확장하기 위한 두 가지 주요 메커니즘을 분석합니다: (1) Dense(밀집된) Process-based Verifier Reward Models (PRMs, 과정 중심 보상 모델)을 활용한 탐색(searching), 그리고 (2) 테스트 시 주어지는 프롬프트에 따라 응답에 대한 모델의 분포를 적응적으로(adaptively) 업데이트하는 것입니다.

**보조 설명**

* **PRM (Process Reward Model):** 최종 결과(Outcome)만 평가하는 것이 아니라, 풀이 과정의 각 단계(Step)를 평가하는 보상 모델입니다.
* **Updating distribution:** 모델이 답변을 생성하는 확률 분포 자체를 개선하는 행위(예: 답변을 수정(revision)하도록 하여 더 나은 답변을 생성하게 함)를 의미합니다.

---

### ✅ 문장 6

We find that in both cases, the effectiveness of different approaches to scaling test-time compute critically varies depending on the difficulty of the prompt.

**단위 해석**
우리는 두 경우 모두에서, test-time compute를 확장하는 다양한 접근 방식의 효율성이 프롬프트의 난이도(difficulty)에 따라 결정적으로 달라진다는 것을 발견했습니다.

**보조 설명**

* **Key Insight:** 모든 문제에 똑같은 전략(예: 무조건 많이 생성하기)을 쓰는 것이 아니라, 문제의 난이도에 따라 효과적인 전략이 다르다는 것이 논문의 핵심 주장입니다.

---

### ✅ 문장 7

This observation motivates applying a "compute-optimal" scaling strategy, which acts to, as effectively as possible, allocate test-time compute per prompt in an adaptive manner.

**단위 해석**
이러한 관찰은 각 프롬프트마다 적응적인 방식으로 test-time compute를 가능한 한 효과적으로 할당하는 "compute-optimal(연산 최적화)" scaling 전략을 적용하도록 동기를 부여합니다.

---

### ✅ 문장 8

Using this compute-optimal strategy, we can improve the efficiency of test-time compute scaling for math reasoning problems by more than 4x compared to a best-of-N baseline.

**단위 해석**
이 compute-optimal 전략을 사용함으로써, 우리는 수학 추론 문제에서 Best-of-N 베이스라인과 비교하여 test-time compute 확장의 효율성을 4배 이상 향상시킬 수 있습니다.

**보조 설명**

* **Best-of-N:** N개의 답변을 생성한 뒤 가장 좋은 것을 선택하는 가장 단순하고 강력한 베이스라인 방법입니다. 이보다 4배 적은 연산으로 같은 성능을 낸다는 의미입니다.

---

### ✅ 문장 9

Additionally, in a FLOPs-matched evaluation, we find that on problems where a smaller base model attains somewhat non-trivial success rates, test-time compute can be used to outperform a 14x larger model.

**단위 해석**
추가적으로, FLOPs-matched(연산량 일치) 평가에서, 작은 베이스 모델이 어느 정도 유의미한 성공률을 보이는 문제들의 경우, test-time compute를 사용하여 14배 더 큰 모델보다 더 나은 성능을 낼 수 있음을 발견했습니다.

**보조 설명**

* **FLOPs-matched:** 공정한 비교를 위해 사용된 총 연산량(Floating Point Operations)을 동일하게 맞춘 상태에서의 비교입니다.
* 작은 모델 + 많은 추론 연산 > 큰 모델 + 적은 추론 연산 (단, 모델이 아예 못 푸는 문제가 아닐 경우)

---

# 📑 1 INTRODUCTION

---

## 📘 Introduction (Paragraph 1)

Given a challenging input, can we enable LLMs to most effectively make use of additional computation at test-time to improve their responses? In theory, additional test-time compute should enable an LLM to do better than what it was trained to do in zero-shot. Such a capability at test-time bears the potential to unlock agentic and reasoning abilities. Additionally, if pre-trained model size can be traded off for additional inference compute, this would enable the deployment of smaller on-device models in place of datacenter scale LLMs. Automating the inference-time improvement of model outputs also provides a path towards a general self-improvement algorithm that can function with reduced human supervision.

---

### ✅ 문장 10 (그룹)

Given a challenging input, can we enable LLMs to most effectively make use of additional computation at test-time to improve their responses? In theory, additional test-time compute should enable an LLM to do better than what it was trained to do in zero-shot.

**단위 해석**
어려운 입력이 주어졌을 때, LLMs가 답변을 개선하기 위해 test-time에서의 추가 연산을 가장 효과적으로 활용하도록 할 수 있을까요? 이론적으로, 추가적인 test-time compute는 LLM이 zero-shot으로 학습된 것보다 더 나은 성능을 낼 수 있게 해야 합니다.

---

### ✅ 문장 11

Such a capability at test-time bears the potential to unlock agentic and reasoning abilities.

**단위 해석**
Test-time에서의 이러한 능력은 에이전트적(agentic) 능력과 추론(reasoning) 능력을 잠금 해제할 잠재력을 지니고 있습니다.

**보조 설명**

* 단순한 패턴 매칭이 아니라, 복잡한 문제를 계획하고 수정하며 해결하는 'System 2'적 사고 능력을 의미합니다.

---

### ✅ 문장 12

Additionally, if pre-trained model size can be traded off for additional inference compute, this would enable the deployment of smaller on-device models in place of datacenter scale LLMs.

**단위 해석**
또한, 만약 사전 학습된 모델의 크기를 추가적인 inference compute와 맞바꿀(trade off) 수 있다면, 이는 데이터센터 규모의 LLM 대신 더 작은 온디바이스(on-device) 모델을 배포하는 것을 가능하게 할 것입니다.

---

### ✅ 문장 13

Automating the inference-time improvement of model outputs also provides a path towards a general self-improvement algorithm that can function with reduced human supervision.

**단위 해석**
모델 출력의 inference-time 개선을 자동화하는 것은 또한 인간의 감독을 줄이면서 작동할 수 있는 일반적인 자기 개선(self-improvement) 알고리즘을 향한 길을 제시합니다.

---

## 📘 Introduction (Paragraph 2)

Prior work studying inference-time computation provides mixed results. On the one hand, some prior work shows that current LLMs can use test-time computation to improve their outputs, on the other hand, several other works show that the effectiveness of these methods on complex tasks such as math reasoning remains limited. However, reasoning is a domain we should expect to benefit from test-time compute, since reasoning involves drawing inferences from existing knowledge as opposed to acquiring new knowledge. Therefore, the disagreement in these prior findings motivates the need for a systematic analysis of different approaches for scaling test-time compute.

---

### ✅ 문장 14

Prior work studying inference-time computation provides mixed results. On the one hand, some prior work shows that current LLMs can use test-time computation to improve their outputs, on the other hand, several other works show that the effectiveness of these methods on complex tasks such as math reasoning remains limited.

**단위 해석**
Inference-time computation을 연구한 이전 연구들은 엇갈린 결과(mixed results)를 보여줍니다. 한편으로는 현재의 LLM들이 test-time computation을 사용하여 출력을 개선할 수 있다는 연구가 있는 반면, 다른 여러 연구들은 수학 추론과 같은 복잡한 과제에서 이러한 방법들의 효과가 여전히 제한적임을 보여줍니다.

---

### ✅ 문장 15

However, reasoning is a domain we should expect to benefit from test-time compute, since reasoning involves drawing inferences from existing knowledge as opposed to acquiring new knowledge.

**단위 해석**
그러나, 추론(Reasoning)은 새로운 지식을 습득하는 것이 아니라 기존 지식으로부터 추론을 이끌어내는(drawing inferences) 것이기 때문에, test-time compute의 이점을 누릴 것으로 기대해야 하는 영역입니다.

**보조 설명**

* **Reasoning vs Knowledge:** 지식은 학습 시 주입되어야 하지만, 추론은 이미 알고 있는 지식을 조합하고 논리적으로 연결하는 과정이므로, 시간을 들여 생각(연산)하면 더 잘할 수 있어야 한다는 논리입니다.

---

### ✅ 문장 16

Therefore, the disagreement in these prior findings motivates the need for a systematic analysis of different approaches for scaling test-time compute.

**단위 해석**
따라서, 이러한 이전 연구 결과들의 불일치는 test-time compute를 확장하는 다양한 접근 방식에 대한 체계적인 분석의 필요성을 제기합니다.

---

## 📘 Introduction (Figure 1 Summary)

> Figure 1: Summary of results. Left: Compute-optimal scaling for revisions and search. We compare the compute-optimal scaling policy with PaLM 2-S* against baselines in the revision (top) and PRM search (bottom) settings. In the revision setting, we find that compute-optimal scaling outperforms best-of-N with 4x less compute. Similarly, with PRM search, compute-optimal scaling shows large early improvements over best-of-N, nearly outperforming best-of-N with 4x less compute at points (see Sec. 5 and 6). Right: Comparing test-time compute and parameter scaling. We compare compute-optimal test-time scaling with PaLM 2-S* against a x larger model without additional test-time compute. We expect X tokens of pretraining for both models and Y tokens of inference. A larger model, multiplies the FLOPs for both. If we were to apply additional test-time compute to the smaller model, to match this FLOPs multiplier, we see that for the revisions (top) when , test-time compute is preferable to pretraining. As inference to pretraining ratio increases, test-time compute is preferable on easy questions. However, on hard questions, pretraining is preferable.

---

### ✅ 문장 17 (Figure 1 Caption 분석)

Figure 1: Summary of results. Left: Compute-optimal scaling for revisions and search. We compare the compute-optimal scaling policy with PaLM 2-S* against baselines in the revision (top) and PRM search (bottom) settings.

**단위 해석**
그림 1: 결과 요약. 왼쪽: 수정(revisions) 및 탐색(search)에 대한 Compute-optimal scaling. 우리는 PaLM 2-S* 모델을 사용한 compute-optimal scaling 정책을 revision(상단) 및 PRM search(하단) 설정에서의 베이스라인과 비교합니다.

---

### ✅ 문장 18 (Figure 1 Caption 계속)

In the revision setting, we find that compute-optimal scaling outperforms best-of-N with 4x less compute. Similarly, with PRM search, compute-optimal scaling shows large early improvements over best-of-N, nearly outperforming best-of-N with 4x less compute at points (see Sec. 5 and 6).

**단위 해석**
Revision 설정에서, compute-optimal scaling은 4배 더 적은 연산으로 best-of-N보다 뛰어난 성능을 보임을 발견했습니다. 마찬가지로, PRM search에서도 compute-optimal scaling은 best-of-N 대비 초기에 큰 향상을 보이며, 특정 지점들에서는 4배 더 적은 연산으로 best-of-N을 거의 능가합니다 (섹션 5와 6 참조).

---

### ✅ 문장 19 (Figure 1 Caption 계속)

Right: Comparing test-time compute and parameter scaling. We compare compute-optimal test-time scaling with PaLM 2-S* against a x larger model without additional test-time compute. ... If we were to apply additional test-time compute to the smaller model, to match this FLOPs multiplier, we see that for the revisions (top) when , test-time compute is preferable to pretraining.

**단위 해석**
오른쪽: Test-time compute와 파라미터 스케일링 비교. 우리는 PaLM 2-S*의 compute-optimal test-time scaling을 추가적인 test-time compute가 없는 약 14배 더 큰 모델과 비교합니다. ... 만약 이 FLOPs 승수(multiplier)를 맞추기 위해 더 작은 모델에 추가적인 test-time compute를 적용한다면,  (추론 토큰)가  (학습 토큰)보다 훨씬 작은 경우()에는 revision(상단)에 대해 test-time compute가 사전 학습(pretraining)보다 더 선호됨을 알 수 있습니다.

---

### ✅ 문장 20 (Figure 1 Caption 마지막)

As inference to pretraining ratio increases, test-time compute is preferable on easy questions. However, on hard questions, pretraining is preferable.

**단위 해석**
추론 대 사전 학습 비율이 증가함에 따라, 쉬운 문제들에서는 test-time compute가 선호됩니다. 하지만, 어려운 문제들에서는 사전 학습(모델 크기 키우기)이 더 선호됩니다.

**보조 설명**

* **Key Insight:** 어려운 문제는 단순히 시간을 더 쓴다고 해결되는 것이 아니라, 모델 자체의 지능(파라미터/학습량)이 더 필요하다는 점을 시사합니다.

---

## 📘 Introduction (Paragraph 3)

In this paper we understand the pros and cons of scaling up test-time compute, and how it compares with scaling up pre-training compute. While the simplest approach for scaling test-time compute is best-of-N sampling — sampling N outputs in "parallel" from a base LLM and selecting the one that scores the highest per a learned verifier or a reward model — there are many other ways we could conceivably scale up test-time compute. We unify methods into those that modify either the proposal distribution from which responses are sampled (for e.g., by asking the base model to revise its responses) or those that alter how the verifier is used for searching directly in the output space (e.g. by training a PRM).

---

### ✅ 문장 21

In this paper we understand the pros and cons of scaling up test-time compute, and how it compares with scaling up pre-training compute.

**단위 해석**
이 논문에서 우리는 test-time compute 확장의 장단점을 이해하고, 이것이 pre-training compute 확장과 어떻게 비교되는지 알아봅니다.

---

### ✅ 문장 22

While the simplest approach for scaling test-time compute is best-of-N sampling — sampling N outputs in "parallel" from a base LLM and selecting the one that scores the highest per a learned verifier or a reward model — there are many other ways we could conceivably scale up test-time compute.

**단위 해석**
Test-time compute를 확장하는 가장 간단한 접근 방식은 Best-of-N sampling — 베이스 LLM으로부터 "병렬로(parallel)" N개의 출력을 샘플링하고 학습된 verifier(검증기)나 보상 모델에 의해 가장 높은 점수를 받은 것을 선택하는 것 — 이지만, 우리가 상상할 수 있는 test-time compute 확장 방법은 그 외에도 많이 있습니다.

---

### ✅ 문장 23

We unify methods into those that modify either the proposal distribution from which responses are sampled (for e.g., by asking the base model to revise its responses) or those that alter how the verifier is used for searching directly in the output space (e.g. by training a PRM).

**단위 해석**
우리는 방법론들을, 응답이 샘플링되는 proposal distribution(제안 분포)을 수정하는 방식(예: 베이스 모델에게 자신의 응답을 수정하도록 요청하는 것) 또는 출력 공간에서 직접 탐색하기 위해 verifier가 사용되는 방식을 변경하는 방식(예: PRM 학습)으로 통합하여 분류합니다.

**보조 설명**

* **Proposal Distribution Modification:** 모델이 처음부터 더 나은 답을 내놓도록 유도하거나(Revisions), 분포 자체를 개선함.
* **Verifier Optimization:** 모델이 내놓은 답들 중에서 정답을 찾는 탐색 알고리즘(Search)을 개선함 (PRM 활용).

---

## 📘 Introduction (Paragraph 4)

To understand the benefits of scaling up test-time compute, we carry out experiments on MATH using PaLM-2 models fine-tuned to either revise incorrect answers (e.g. improving the proposal distribution; Section 6) or verify the correctness of individual steps in an answer using a process-based reward model (PRM) (Section 5). We find that the efficacy of different test-time strategies depends on both the nature of the specific problem at hand and the base LLM used. For example, on "easier" problems, for which the base LLM can already produce reasonable-looking responses, allowing the model to sequentially revise its initial answer (i.e., modifying the proposal distribution) is a more effective use of compute than reranking N independent answers sampled in parallel. On the other hand, on difficult problems which require searching over many high-level strategies, re-sampling new responses independently in parallel or deploying tree-search against a process reward model is more effective. This underscores the need to deploy an adaptive "compute-optimal" strategy for scaling test-time compute, wherein the specific approach for utilizing test-time compute is selected depending on the prompt, so as to make the best use of additional computation.

---

### ✅ 문장 24

To understand the benefits of scaling up test-time compute, we carry out experiments on MATH using PaLM-2 models fine-tuned to either revise incorrect answers (e.g. improving the proposal distribution; Section 6) or verify the correctness of individual steps in an answer using a process-based reward model (PRM) (Section 5).

**단위 해석**
Test-time compute 확장의 이점을 이해하기 위해, 우리는 틀린 답변을 수정하도록(예: 제안 분포 개선; 섹션 6) fine-tuning되거나, process-based reward model (PRM)을 사용하여 답변의 개별 단계의 정확성을 검증하도록(섹션 5) fine-tuning된 PaLM-2 모델을 사용하여 MATH 데이터셋에 대해 실험을 수행합니다.

---

### ✅ 문장 25

We find that the efficacy of different test-time strategies depends on both the nature of the specific problem at hand and the base LLM used.

**단위 해석**
우리는 서로 다른 test-time 전략들의 효율성이 당면한 특정 문제의 본질과 사용된 베이스 LLM에 따라 달라진다는 것을 발견했습니다.

---

### ✅ 문장 26

For example, on "easier" problems, for which the base LLM can already produce reasonable-looking responses, allowing the model to sequentially revise its initial answer (i.e., modifying the proposal distribution) is a more effective use of compute than reranking N independent answers sampled in parallel.

**단위 해석**
예를 들어, 베이스 LLM이 이미 그럴듯한(reasonable-looking) 응답을 생성할 수 있는 "더 쉬운" 문제들의 경우, 모델이 자신의 초기 답변을 순차적으로 수정하도록 허용하는 것(즉, 제안 분포 수정)이 병렬로 샘플링된 N개의 독립적인 답변을 재순위(reranking) 매기는 것보다 더 효율적인 연산 사용법입니다.

**보조 설명**

* **쉬운 문제:** 모델이 거의 맞출 뻔했거나 실수가 적은 문제는, 다시 생성하기보다 고쳐 쓰는(Revision) 게 낫다는 뜻입니다.

---

### ✅ 문장 27

On the other hand, on difficult problems which require searching over many high-level strategies, re-sampling new responses independently in parallel or deploying tree-search against a process reward model is more effective.

**단위 해석**
반면, 많은 고차원적 전략에 대한 탐색이 필요한 어려운 문제들의 경우, 새로운 응답을 병렬로 독립적으로 다시 샘플링하거나 PRM을 상대로 tree-search(트리 탐색)를 배포하는 것이 더 효과적입니다.

**보조 설명**

* **어려운 문제:** 처음 접근 방식이 완전히 틀렸을 수 있으므로, 수정보다는 아예 다양한 시도를 해보는 것(Parallel sampling/Search)이 낫다는 뜻입니다.

---

### ✅ 문장 28

This underscores the need to deploy an adaptive "compute-optimal" strategy for scaling test-time compute, wherein the specific approach for utilizing test-time compute is selected depending on the prompt, so as to make the best use of additional computation.

**단위 해석**
이는 test-time compute 확장을 위해 적응적인 "compute-optimal" 전략을 배포해야 할 필요성을 강조하며, 여기서는 추가 연산을 최적으로 활용하기 위해 프롬프트에 따라 test-time compute를 활용하는 구체적인 접근 방식이 선택됩니다.

---

## 📘 Introduction (Paragraph 5 & 6)

We also show that a notion of question difficulty (Section 4) from the perspective of the base LLM can be used to predict the efficacy of test-time computation, enabling us to practically instantiate this "compute-optimal" scaling. By appropriately allocating test-time compute in this way, we are able to greatly improve test-time compute scaling, surpassing the performance of a best-of-N baseline while only using ~4x less computation with both revisions and search (Sections 5 and 6).

Using our scaling strategy, we then study to what extent test-time computation can substitute for additional pretraining. Specifically, we conduct a FLOPs-matched comparison between a smaller model with test-time compute and pretraining a 14x larger model. We find that on easy and intermediate questions, additional test-time compute is often preferable to scaling pretraining. This finding suggests that rather than focusing purely on scaling pretraining, in some settings it is more efficient to pretrain smaller models with less compute, and then apply test-time compute to improve outputs. That said, with the most challenging questions, we observe few benefits from scaling up test-time compute. Instead, on these questions, it is more effective to make progress by applying additional pretraining, demonstrating that current approaches to scaling test-time compute may not be 1-to-1 exchangeable with scaling pretraining. Overall, this suggests that even with a fairly naïve methodology, scaling up test-time computation can already be preferable to pretraining in some settings, with only more improvements as test-time strategies mature. Longer term, this hints at a future where fewer FLOPs are spent during pretraining and more FLOPs are spent at inference.

---

### ✅ 문장 29

We also show that a notion of question difficulty (Section 4) from the perspective of the base LLM can be used to predict the efficacy of test-time computation, enabling us to practically instantiate this "compute-optimal" scaling.

**단위 해석**
우리는 또한 베이스 LLM 관점에서의 문제 난이도(Section 4) 개념이 test-time computation의 효용성을 예측하는 데 사용될 수 있음을 보여주며, 이를 통해 이 "compute-optimal" scaling을 실질적으로 구현할 수 있게 합니다.

---

### ✅ 문장 30

By appropriately allocating test-time compute in this way, we are able to greatly improve test-time compute scaling, surpassing the performance of a best-of-N baseline while only using ~4x less computation with both revisions and search (Sections 5 and 6).

**단위 해석**
이런 방식으로 test-time compute를 적절히 할당함으로써, 우리는 revision과 search 모두에서(섹션 5와 6), 약 4배 더 적은 연산량만으로도 Best-of-N 베이스라인의 성능을 능가하며 test-time compute scaling을 크게 개선할 수 있었습니다.

---

### ✅ 문장 31

Using our scaling strategy, we then study to what extent test-time computation can substitute for additional pretraining. Specifically, we conduct a FLOPs-matched comparison between a smaller model with test-time compute and pretraining a 14x larger model.

**단위 해석**
우리의 scaling 전략을 사용하여, 우리는 test-time computation이 추가적인 pretraining을 어느 정도까지 대체할 수 있는지 연구합니다. 구체적으로, 우리는 test-time compute를 사용하는 더 작은 모델과 14배 더 큰 모델을 pretraining하는 것 사이의 FLOPs-matched 비교를 수행합니다.

---

### ✅ 문장 32

We find that on easy and intermediate questions, additional test-time compute is often preferable to scaling pretraining. This finding suggests that rather than focusing purely on scaling pretraining, in some settings it is more efficient to pretrain smaller models with less compute, and then apply test-time compute to improve outputs.

**단위 해석**
우리는 쉬운 문제와 중간 난이도 문제에서, 추가적인 test-time compute가 pretraining을 확장하는 것보다 종종 더 선호된다는 것을 발견했습니다. 이 발견은 순수하게 pretraining 확장에만 집중하기보다, 어떤 환경에서는 적은 연산으로 작은 모델을 pretrain한 뒤 출력을 개선하기 위해 test-time compute를 적용하는 것이 더 효율적임을 시사합니다.

---

### ✅ 문장 33

That said, with the most challenging questions, we observe few benefits from scaling up test-time compute. Instead, on these questions, it is more effective to make progress by applying additional pretraining, demonstrating that current approaches to scaling test-time compute may not be 1-to-1 exchangeable with scaling pretraining.

**단위 해석**
그렇기는 하지만, 가장 도전적인(어려운) 문제들의 경우, test-time compute를 확장하는 것에서 이점을 거의 관찰하지 못했습니다. 대신, 이러한 문제들에서는 추가적인 pretraining을 적용함으로써 진전을 이루는 것이 더 효과적이었으며, 이는 현재의 test-time compute 확장 접근 방식들이 pretraining 확장과 1대1로 교환 가능하지 않을 수 있음을 보여줍니다.

---

### ✅ 문장 34

Overall, this suggests that even with a fairly naïve methodology, scaling up test-time computation can already be preferable to pretraining in some settings, with only more improvements as test-time strategies mature. Longer term, this hints at a future where fewer FLOPs are spent during pretraining and more FLOPs are spent at inference.

**단위 해석**
종합적으로, 이는 꽤 단순한(naïve) 방법론을 사용하더라도 일부 환경에서는 test-time computation을 확장하는 것이 이미 pretraining보다 선호될 수 있으며, test-time 전략이 성숙해짐에 따라 더 많은 개선이 있을 것임을 시사합니다. 장기적으로, 이것은 pretraining에는 더 적은 FLOPs가 사용되고 inference에는 더 많은 FLOPs가 사용되는 미래를 암시합니다.

---

**Introduction** 섹션까지 읽어드렸습니다. 이어서 **Section 2: UNIFIED PERSPECTIVE ON TEST-TIME COMPUTE: PROPOSER & VERIFIER** 및 **Section 3: HOW TO SCALE TEST-TIME COMPUTATION OPTIMALLY**로 계속 진행하시겠습니까?