---

# 📑 Abstract

---

## 📘 단락 본문 텍스트

Deploying large language models (LLMs) is challenging because they are memory inefficient and compute-intensive for practical applications. In reaction, researchers train smaller task-specific models by either finetuning with human labels or distilling using LLM-generated labels. However, finetuning and distillation require large amounts of training data to achieve comparable performance to LLMs. We introduce *Distilling step-by-step*, a new mechanism that (a) trains smaller models that outperform LLMs, and (b) achieves so by leveraging less training data needed by finetuning or distillation. Our method extracts LLM rationales as additional supervision for training small models within a multi-task framework. We present three findings across 4 NLP benchmarks: First, compared to both finetuning and distillation, our mechanism achieves better performance with much fewer labeled/unlabeled training examples. Second, compared to few-shot prompted LLMs, we achieve better performance using substantially smaller model sizes. Third, we reduce both the model size and the amount of data required to outperform LLMs; our finetuned 770M T5 model outperforms the few-shot prompted 540B PaLM model using only 80% of available data on a benchmark, whereas standard finetuning the same T5 model struggles to match even by using 100% of the dataset. Source code is available at: [https://github.com/google-research/distilling-step-by-step](https://github.com/google-research/distilling-step-by-step).  &#x20;

---

### ✅ 문장 1

Deploying large language models (LLMs) is challenging because they are memory inefficient and compute-intensive for practical applications.

**단위 해석**
대형 언어 모델은 메모리 효율이 낮고 연산 비용이 커서 실제 서비스에 배포하기 어렵다는 진단입니다.

**보조 설명**

* “memory inefficient”는 같은 작업을 수행하는 데 필요한 GPU 메모리가 매우 크다는 뜻입니다.
* “compute-intensive”는 추론 단계에서의 FLOPs 요구량과 레이턴시 부담을 가리킵니다.
* 실제 프로덕션 환경(지연시간 SLA, 비용 제약)에서 특히 문제가 됩니다.

---

### ✅ 문장 2

In reaction, researchers train smaller task-specific models by either finetuning with human labels or distilling using LLM-generated labels.

**단위 해석**
대안으로, 연구자들은 인간 라벨로 파인튜닝하거나 LLM이 만든 라벨로 증류하여 더 작은 특화 모델을 학습합니다.

**보조 설명**

* finetuning: 사전학습 모델을 다운스트림 데이터로 추가 학습.
* distilling: 큰 모델(teacher)의 출력을 작은 모델(student) 학습 신호로 사용.
* “task-specific”은 범용 대신 과제 특화 성능에 초점을 둠을 의미.

---

### ✅ 문장 3

However, finetuning and distillation require large amounts of training data to achieve comparable performance to LLMs.

**단위 해석**
하지만 LLM에 비슷하게 성능을 내려면 파인튜닝도 증류도 많은 학습 데이터가 필요합니다.

**보조 설명**

* 파인튜닝은 고품질 “사람” 라벨이 많이 필요해 비용이 큼.
* 증류는 대량의 비라벨 데이터가 필요(teacher로부터 의사 라벨 생성).
* 데이터 수급이 현실적 병목이 됩니다.

---

### ✅ 문장 4

We introduce *Distilling step-by-step*, a new mechanism that (a) trains smaller models that outperform LLMs, and (b) achieves so by leveraging less training data needed by finetuning or distillation.

**단위 해석**
저자들은 *Distilling step-by-step*이라는 새 메커니즘을 제안합니다. 이 방법은 (a) 더 작은 모델로 LLM보다 높은 성능을 내고, (b) 파인튜닝/증류보다 더 적은 학습 데이터로 그 목표를 이룹니다.

**보조 설명**

* 핵심 가치는 **작은 모델** + **적은 데이터** + **더 높은 성능**의 동시 달성.
* “메커니즘”은 학습 절차/프레임워크 수준의 제안을 의미.

---

### ✅ 문장 5

Our method extracts LLM rationales as additional supervision for training small models within a multi-task framework.

**단위 해석**
이 방법은 멀티태스크 프레임워크 안에서 LLM이 생성한 \*\*rationale(추론 과정·설명)\*\*을 추가 감독 신호로 사용해 작은 모델을 학습합니다.

**보조 설명**

* rationale: 정답 라벨에 이르는 중간 사고 과정(Chain-of-Thought).
* 라벨만 학습하는 것보다, “왜” 정답인지에 대한 텍스트도 함께 학습.
* 멀티태스크: `[label]`과 `[rationale]` 두 가지 출력을 하나의 모델이 학습.

---

### ✅ 문장 6

We present three findings across 4 NLP benchmarks: First, compared to both finetuning and distillation, our mechanism achieves better performance with much fewer labeled/unlabeled training examples.

**단위 해석**
4개 NLP 벤치마크에서 세 가지 결과를 보고합니다. 첫째, 파인튜닝/증류와 비교해 훨씬 적은 (라벨·비라벨) 예제로도 더 좋은 성능을 냅니다.

**보조 설명**

* 데이터 효율(data efficiency) 향상이 핵심.
* 라벨 유무와 무관하게 이점이 있음을 명시.

---

### ✅ 문장 7

Second, compared to few-shot prompted LLMs, we achieve better performance using substantially smaller model sizes.

**단위 해석**
둘째, few-shot 프롬프트로 구동한 LLM과 비교해, 훨씬 **작은 모델**로도 더 좋은 성능을 냅니다.

**보조 설명**

* few-shot LLM은 강력하지만 파라미터 수가 커서 배포 부담이 큼.
* 작은 모델이 LLM을 능가한다는 점이 실무적 의미가 큼(비용·레이턴시 절감).

---

### ✅ 문장 8

Third, we reduce both the model size and the amount of data required to outperform LLMs; our finetuned 770M T5 model outperforms the few-shot prompted 540B PaLM model using only 80% of available data on a benchmark, whereas standard finetuning the same T5 model struggles to match even by using 100% of the dataset.

**단위 해석**
셋째, LLM을 능가하기 위해 **모델 크기**와 **데이터 양**을 모두 줄였습니다. 구체적으로 770M T5는 어떤 벤치마크에서 이용 가능한 데이터의 80%만으로도 few-shot 540B PaLM을 능가했으며, 같은 T5를 표준 파인튜닝하면 100% 데이터를 써도 따라잡기 어렵습니다.

**보조 설명**

* “770M vs 540B”는 약 **700배 이상** 작은 모델이 LLM을 이긴 사례.
* “80% vs 100%”는 **데이터 효율**의 정량적 증거.
* T5(엔코더-디코더)와 PaLM(거대 디코더 계열) 비교 맥락.

---

### ✅ 문장 9

Source code is available at: [https://github.com/google-research/distilling-step-by-step](https://github.com/google-research/distilling-step-by-step).

**단위 해석**
소스 코드는 위 깃허브 저장소에서 공개됩니다.

**보조 설명**

* 재현 가능성(reproducibility)을 높이는 실무적 포인트.
* 구현을 참고해 자체 데이터/도메인으로 실험 가능.

---

다음 섹션 **1 Introduction**으로 넘어가서 같은 방식으로 계속 진행할까요?

---

# 📑 1 Introduction

---

## 📘 단락 본문 텍스트

Despite the impressive few-shot ability offered by large language models (LLMs) (Brown et al., 2020; Chowdhery et al., 2022; Thoppilan et al., 2022; Hoffmann et al., 2022; Smith et al., 2022b; Zhang et al., 2022), these models are challenging to deploy in real world applications due to their sheer size. Serving a single 175 billion LLM requires at least 350 GB GPU memory using specialized infrastructure (Zheng et al., 2022). To make matters worse, today’s state-of-the-art LLMs are composed of over 500B parameters (Chowdhery et al., 2022), requiring significantly more memory and compute. Such computational requirements are far beyond affordable for most product teams, especially for applications that require low latency performance.

---

### ✅ 문장 1

Despite the impressive few-shot ability offered by large language models (LLMs) (Brown et al., 2020; Chowdhery et al., 2022; Thoppilan et al., 2022; Hoffmann et al., 2022; Smith et al., 2022b; Zhang et al., 2022), these models are challenging to deploy in real world applications due to their sheer size.

**단위 해석**
대형 언어 모델은 few-shot 성능이 인상적이지만, 규모가 매우 커서 실제 환경에 배포하기 어렵습니다.

**보조 설명**

* “few-shot ability”는 적은 예시만으로도 잘 수행하는 능력.
* “sheer size”는 모델 파라미터 수와 메모리 요구량을 모두 포함.
* 연구적 성과와 실무 적용성 사이의 간극을 강조.

---

### ✅ 문장 2

Serving a single 175 billion LLM requires at least 350 GB GPU memory using specialized infrastructure (Zheng et al., 2022).

**단위 해석**
175B 파라미터 LLM 하나를 서비스하려면 최소 350GB의 GPU 메모리가 필요하며, 이를 위해 특수 인프라가 요구됩니다.

**보조 설명**

* 모델 크기가 GPU 메모리 요구량과 직결됨.
* 일반적인 클라우드 환경에서는 매우 비싸고 세팅이 복잡.
* 실제 배포 시 **모델 병렬화, 메모리 최적화**가 필요.

---

### ✅ 문장 3

To make matters worse, today’s state-of-the-art LLMs are composed of over 500B parameters (Chowdhery et al., 2022), requiring significantly more memory and compute.

**단위 해석**
더 나아가, 최신 SOTA LLM은 500B 이상 파라미터로 구성되어, 훨씬 더 많은 메모리와 연산 자원을 필요로 합니다.

**보조 설명**

* 500B는 175B보다 **약 3배 규모**.
* 파라미터 수 증가 → 메모리 요구량·추론 속도 모두 악화.
* 최신 모델일수록 배포 비용 장벽이 커짐.

---

### ✅ 문장 4

Such computational requirements are far beyond affordable for most product teams, especially for applications that require low latency performance.

**단위 해석**
이러한 연산 요구 사항은 대부분의 제품 개발팀이 감당하기 어렵고, 특히 저지연이 필요한 애플리케이션에서는 더욱 부담스럽습니다.

**보조 설명**

* 비용뿐 아니라 **응답 속도**가 중요한 서비스에서 문제 심각.
* LLM API 호출조차 레이턴시가 길어질 수 있음.
* 경량 모델의 필요성을 강조하는 맥락.

---

## 📘 단락 본문 텍스트

To circumvent these deployment challenges of large models, practitioners often choose to deploy smaller specialized models instead. These smaller models are trained using one of two common paradigms: finetuning or distillation. Finetuning updates a pretrained smaller model (e.g. BERT (Devlin et al., 2018) or T5 (Raffel et al., 2020)) using downstream human annotated data (Howard and Ruder, 2018). Distillation trains the same smaller models with labels generated by a larger LLM (Tang et al., 2019; Wang et al., 2021; Smith et al., 2022a; Arora et al., 2022). Unfortunately, these paradigms reduce model size at a cost: to achieve comparable performance to LLMs, finetuning requires expensive human labels, and distillation requires large amounts of unlabeled data which can be hard to obtain (Tang et al., 2019; Liang et al., 2020).

---

### ✅ 문장 5

To circumvent these deployment challenges of large models, practitioners often choose to deploy smaller specialized models instead.

**단위 해석**
이러한 대규모 모델 배포 문제를 해결하기 위해, 실무자들은 대신 더 작은 특화 모델을 배포하는 방법을 선택합니다.

**보조 설명**

* 특화 모델은 특정 태스크에서만 동작하도록 설계.
* 메모리·속도·비용 최적화 가능.
* 범용성 대신 효율성을 우선.

---

### ✅ 문장 6

These smaller models are trained using one of two common paradigms: finetuning or distillation.

**단위 해석**
이 작은 모델들은 주로 파인튜닝이나 증류라는 두 가지 방법 중 하나로 학습됩니다.

**보조 설명**

* 두 접근 모두 대형 모델 대비 파라미터 수가 적음.
* 학습 절차와 데이터 요구량에서 차이가 있음.

---

### ✅ 문장 7

Finetuning updates a pretrained smaller model (e.g. BERT (Devlin et al., 2018) or T5 (Raffel et al., 2020)) using downstream human annotated data (Howard and Ruder, 2018).

**단위 해석**
파인튜닝은 사전학습된 작은 모델(BERT, T5 등)을 다운스트림 인간 라벨 데이터로 업데이트하는 방법입니다.

**보조 설명**

* 다운스트림 데이터는 목표 태스크에 맞춘 도메인 데이터.
* 라벨 품질이 성능에 직접적 영향을 줌.

---

### ✅ 문장 8

Distillation trains the same smaller models with labels generated by a larger LLM (Tang et al., 2019; Wang et al., 2021; Smith et al., 2022a; Arora et al., 2022).

**단위 해석**
증류는 대형 LLM이 생성한 라벨을 이용해 동일한 작은 모델을 학습시키는 방식입니다.

**보조 설명**

* teacher 모델 → label 생성 → student 모델 학습.
* 인간 라벨 없이도 가능하지만, teacher 품질에 의존.

---

### ✅ 문장 9

Unfortunately, these paradigms reduce model size at a cost: to achieve comparable performance to LLMs, finetuning requires expensive human labels, and distillation requires large amounts of unlabeled data which can be hard to obtain (Tang et al., 2019; Liang et al., 2020).

**단위 해석**
그러나 모델 크기를 줄이는 대가가 있습니다. LLM과 비슷한 성능을 내려면 파인튜닝은 값비싼 인간 라벨이 필요하고, 증류는 확보하기 어려운 대량의 비라벨 데이터가 필요합니다.

**보조 설명**

* 데이터 수급·품질이 병목.
* “cost”는 금전·시간·리소스 모두를 포함.
* 현실적으로 두 방법 모두 제약이 큼.

---

다음 단락부터는 **본 연구의 제안 방법 Distilling step-by-step 소개**로 이어집니다.
이어서 진행할까요?

---

## 📘 단락 본문 텍스트

In this work, we introduce *Distilling step-by-step*, a new simple mechanism for training smaller models with less training data. Our mechanism reduces the amount of training data required for both finetuning and distillation of LLMs into smaller model sizes. Core to our mechanism is changing our perspective from viewing LLMs as a source of noisy labels to viewing them as agents that can reason: LLMs can produce natural language rationales justifying their predicted labels (Wei et al., 2022; Kojima et al., 2022). For example, when asked “Jesse’s room is 11 feet long and 15 feet wide. If she already has 16 square feet of carpet. How much more carpet does she need to cover the whole floor?”, an LLM can be prompted by chain-of-thought (CoT) technique (Wei et al., 2022) to provide intermediate rationales “Area = length × width. Jesse’s room has 11 × 15 square feet.” that better connects the input to the final answer “(11 × 15) – 16”. These rationales can contain relevant task knowledge, such as “Area = length × width”, that may originally require many data for small task-specific models to learn. We thus utilize these extracted rationales as additional, richer information to train small models through a multi-task training setup, with both label prediction and rationale prediction tasks (Raffel et al., 2020; Narang et al., 2020).

---

### ✅ 문장 10

In this work, we introduce *Distilling step-by-step*, a new simple mechanism for training smaller models with less training data.

**단위 해석**
본 연구에서는 더 적은 학습 데이터로 작은 모델을 학습하는 새로운 간단한 방법인 *Distilling step-by-step*을 제안합니다.

**보조 설명**

* "simple mechanism" → 구현과 이해가 비교적 단순한 학습 절차.
* 목표는 데이터 효율성과 성능을 동시에 개선.

---

### ✅ 문장 11

Our mechanism reduces the amount of training data required for both finetuning and distillation of LLMs into smaller model sizes.

**단위 해석**
이 방법은 LLM을 작은 모델로 파인튜닝하거나 증류할 때 필요한 데이터 양을 줄입니다.

**보조 설명**

* 파인튜닝과 증류 모두에 적용 가능.
* 데이터 절감은 학습 비용·시간 절감과 직결.

---

### ✅ 문장 12

Core to our mechanism is changing our perspective from viewing LLMs as a source of noisy labels to viewing them as agents that can reason: LLMs can produce natural language rationales justifying their predicted labels (Wei et al., 2022; Kojima et al., 2022).

**단위 해석**
이 방법의 핵심은 LLM을 단순히 ‘노이즈 라벨 공급원’으로 보지 않고, ‘추론할 수 있는 에이전트’로 보는 관점 전환입니다. LLM은 자신이 예측한 라벨을 정당화하는 자연어 근거(rationale)를 생성할 수 있습니다.

**보조 설명**

* noisy labels → teacher 모델 예측이 항상 정확하지 않음을 의미.
* reasoning agent → 중간 추론 과정을 제공할 수 있는 존재.
* rationale → chain-of-thought 형식의 텍스트 근거.

---

### ✅ 문장 13

For example, when asked “Jesse’s room is 11 feet long and 15 feet wide. If she already has 16 square feet of carpet. How much more carpet does she need to cover the whole floor?”, an LLM can be prompted by chain-of-thought (CoT) technique (Wei et al., 2022) to provide intermediate rationales “Area = length × width. Jesse’s room has 11 × 15 square feet.” that better connects the input to the final answer “(11 × 15) – 16”.

**단위 해석**
예를 들어, “제시의 방은 길이 11피트, 너비 15피트입니다. 이미 16평방피트의 카펫이 있다면, 전체 바닥을 덮기 위해 얼마나 더 필요한가?”라는 질문을 LLM에 CoT 기법으로 입력하면, LLM은 “면적 = 길이 × 너비. 제시의 방은 11 × 15 평방피트입니다.”라는 중간 근거를 제시하여 최종 답 “(11 × 15) – 16”과 입력을 더 잘 연결해 줍니다.

**보조 설명**

* chain-of-thought → 복잡한 문제 해결 시 중간 단계 reasoning을 출력하도록 유도.
* rationale은 문제 해결 과정의 명시적 텍스트.
* 이렇게 하면 모델이 ‘정답만 외우는 것’이 아니라 ‘문제를 푸는 방식’을 학습.

---

### ✅ 문장 14

These rationales can contain relevant task knowledge, such as “Area = length × width”, that may originally require many data for small task-specific models to learn.

**단위 해석**
이러한 rationale은 “면적 = 길이 × 너비”처럼 작은 특화 모델이 원래는 많은 데이터를 통해서만 학습할 수 있는 중요한 과제 지식을 포함할 수 있습니다.

**보조 설명**

* task knowledge → 문제 해결에 필수적인 공식·규칙·개념.
* LLM의 reasoning을 통해 이런 지식을 저비용으로 획득.

---

### ✅ 문장 15

We thus utilize these extracted rationales as additional, richer information to train small models through a multi-task training setup, with both label prediction and rationale prediction tasks (Raffel et al., 2020; Narang et al., 2020).

**단위 해석**
따라서 우리는 이러한 추출된 rationale을 추가적이고 풍부한 정보로 사용하여, 라벨 예측과 rationale 예측을 동시에 수행하는 멀티태스크 학습 방식으로 작은 모델을 훈련합니다.

**보조 설명**

* 멀티태스크 → 모델이 두 출력(라벨·설명)을 모두 생성.
* rationale 학습은 라벨 예측 성능을 간접적으로 강화.
* T5 구조처럼 텍스트-투-텍스트 모델에 잘 맞음.

---

다음은 **본 방법이 성능과 데이터 효율에서 기존 방법을 능가한다는 주장**을 담은 서술로 이어집니다. 계속 진행할까요?

---

## 📘 단락 본문 텍스트

*Distilling step-by-step* allows us to learn task-specific smaller models that outperform LLMs using over 500× less model parameters, and it does so with far fewer training examples compared to traditional finetuning or distillation (Figure 1). Our results show three promising empirical conclusions across 4 NLP benchmarks. First, compared to both finetuning and distillation, our resulting models achieve better performance with over 50% less training examples on average across datasets (and up to over 85% reduction). Second, our models outperform LLMs with much smaller model sizes (up to 2000× smaller), drastically reducing the computation cost required for model deployment. Third, we simultaneously reduce the model size as well as the amount of data required to outperform LLMs. We surpass the performance of 540B parameter LLMs using a 770M T5 model; this smaller model only uses 80% of a labeled dataset that would otherwise be required if using an existing finetuning method. When only unlabeled data is present, our small models still perform on par or better than LLMs. We outperform 540B PaLM’s performance with only a 11B T5 model. We further show that when a smaller model performs worse than an LLM, *Distilling step-by-step* can more efficiently leverage additional unlabeled data to match the LLM performance compared to the standard distillation approach.

---

### ✅ 문장 16

*Distilling step-by-step* allows us to learn task-specific smaller models that outperform LLMs using over 500× less model parameters, and it does so with far fewer training examples compared to traditional finetuning or distillation (Figure 1).

**단위 해석**
*Distilling step-by-step*은 파라미터 수를 500배 이상 줄인 작은 태스크 특화 모델로도 LLM을 능가하게 하며, 전통적인 파인튜닝·증류보다 훨씬 적은 학습 예제로 이를 달성합니다(Figure 1 참조).

**보조 설명**

* 500× 축소는 배포 비용·응답 속도에 엄청난 이점을 제공.
* 데이터 효율성과 모델 경량화를 동시에 달성.

---

### ✅ 문장 17

Our results show three promising empirical conclusions across 4 NLP benchmarks.

**단위 해석**
실험 결과, 4개의 NLP 벤치마크 전반에서 세 가지 유망한 실증적 결론을 얻었습니다.

**보조 설명**

* 성능 검증이 단일 데이터셋이 아니라 범용적으로 이루어졌음을 강조.

---

### ✅ 문장 18

First, compared to both finetuning and distillation, our resulting models achieve better performance with over 50% less training examples on average across datasets (and up to over 85% reduction).

**단위 해석**
첫째, 파인튜닝·증류 대비 평균 50% 이상, 최대 85%까지 적은 학습 예제로 더 나은 성능을 냅니다.

**보조 설명**

* 데이터 절감 폭이 매우 큼.
* 실무에서 데이터 수집·라벨링 부담을 크게 줄일 수 있음.

---

### ✅ 문장 19

Second, our models outperform LLMs with much smaller model sizes (up to 2000× smaller), drastically reducing the computation cost required for model deployment.

**단위 해석**
둘째, 모델 크기를 최대 2000배 줄이면서도 LLM을 능가하여, 배포에 필요한 연산 비용을 대폭 절감합니다.

**보조 설명**

* "2000× smaller"는 파라미터 기준.
* 메모리, 전력, 하드웨어 요구사항 모두 완화.

---

### ✅ 문장 20

Third, we simultaneously reduce the model size as well as the amount of data required to outperform LLMs.

**단위 해석**
셋째, LLM을 능가하는 데 필요한 모델 크기와 데이터 양을 동시에 줄였습니다.

**보조 설명**

* 단일 지표 최적화가 아니라 다방면 효율성 개선.
* 실무 채택 가능성을 크게 높임.

---

### ✅ 문장 21

We surpass the performance of 540B parameter LLMs using a 770M T5 model; this smaller model only uses 80% of a labeled dataset that would otherwise be required if using an existing finetuning method.

**단위 해석**
540B 파라미터 LLM보다 성능이 뛰어난 770M T5 모델을 구현했으며, 이는 기존 파인튜닝에 비해 라벨 데이터 80%만 사용했습니다.

**보조 설명**

* 약 700배 작은 모델이 더 높은 성능.
* 데이터 절감과 성능 향상이 동시에 입증.

---

### ✅ 문장 22

When only unlabeled data is present, our small models still perform on par or better than LLMs.

**단위 해석**
라벨 없는 데이터만 있을 때도, 작은 모델이 LLM과 동등하거나 더 잘 수행합니다.

**보조 설명**

* 비라벨 데이터 환경에서도 효과적.
* teacher LLM 없이도 강한 성능 유지.

---

### ✅ 문장 23

We outperform 540B PaLM’s performance with only a 11B T5 model.

**단위 해석**
11B T5 모델만으로도 540B PaLM보다 나은 성능을 냅니다.

**보조 설명**

* 약 49배 작은 모델로 동일 태스크에서 우위 확보.

---

### ✅ 문장 24

We further show that when a smaller model performs worse than an LLM, *Distilling step-by-step* can more efficiently leverage additional unlabeled data to match the LLM performance compared to the standard distillation approach.

**단위 해석**
또한, 작은 모델이 LLM보다 성능이 낮을 경우, *Distilling step-by-step*은 표준 증류법보다 비라벨 데이터를 더 효율적으로 활용해 LLM 성능에 근접하게 할 수 있음을 보여줍니다.

**보조 설명**

* 데이터 추가 시 효율성이 기존보다 높음.
* 데이터 확보가 가능한 환경에서 성능 보정 가능.

---

다음은 **2 Related work** 섹션으로 넘어가 관련 연구를 검토하는 부분입니다. 계속 진행할까요?

