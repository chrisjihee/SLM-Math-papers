# 📑 Abstract

---

## 📘 단락 본문 텍스트

Deploying large language models (LLMs) is challenging because they are memory inefficient and compute-intensive for practical applications. In reaction, researchers train smaller task-specific models by either finetuning with human labels or distilling using LLM-generated labels. However, finetuning and distillation require large amounts of training data to achieve comparable performance to LLMs. We introduce Distilling step-by-step, a new mechanism that (a) trains smaller models that outperform LLMs, and (b) achieves so by leveraging less training data needed by finetuning or distillation. Our method extracts LLM rationales as additional supervision for training small models within a multi-task framework. We present three findings across 4 NLP benchmarks: First, compared to both finetuning and distillation, our mechanism achieves better performance with much fewer labeled/unlabeled training examples. Second, compared to few-shot prompted LLMs, we achieve better performance using substantially smaller model sizes. Third, we reduce both the model size and the amount of data required to outperform LLMs; our finetuned 770M T5 model outperforms the few-shot prompted 540B PaLM model using only 80% of available data on a benchmark, whereas standard finetuning the same T5 model struggles to match even by using 100% of the dataset.

---

### ✅ 문장 그룹 1

Deploying large language models (LLMs) is challenging because they are memory inefficient and compute-intensive for practical applications.

**단위 해석**
대형 언어 모델(LLMs)을 실제로 배포하는 것은 어려운데, 그 이유는 메모리 효율성이 낮고 실제 응용에서 연산 비용이 매우 크기 때문이다.

**보조 설명**

* LLM은 수백억\~수천억 파라미터를 가지기 때문에 GPU 메모리와 연산량 요구가 엄청나다.
* 이 문장은 논문 전반의 문제의식(대형 모델의 활용 어려움)을 명확히 제시한다.

---

### ✅ 문장 그룹 2

In reaction, researchers train smaller task-specific models by either finetuning with human labels or distilling using LLM-generated labels.

**단위 해석**
이에 대한 대안으로 연구자들은 작은 과제 특화 모델(task-specific model)을 훈련하는데, 방법은 (1) 사람이 라벨링한 데이터를 사용한 finetuning, (2) LLM이 생성한 라벨을 사용하는 distillation 방식이 있다.

**보조 설명**

* finetuning: 기존 모델을 특정 작업에 맞게 재학습시키는 방법.
* distillation: 큰 모델이 생성한 결과를 작은 모델이 학습하도록 하여 지식을 전달하는 방법.

---

### ✅ 문장 그룹 3

However, finetuning and distillation require large amounts of training data to achieve comparable performance to LLMs.

**단위 해석**
그러나 finetuning과 distillation 모두 LLM과 비슷한 성능을 내려면 대량의 학습 데이터가 필요하다.

**보조 설명**

* 작은 모델은 데이터 효율이 떨어지기 때문에 더 많은 라벨 혹은 준라벨이 요구된다.
* 이것이 실질적으로 학습 비용을 높이는 문제점이다.

---

### ✅ 문장 그룹 4

We introduce Distilling step-by-step, a new mechanism that (a) trains smaller models that outperform LLMs, and (b) achieves so by leveraging less training data needed by finetuning or distillation.

**단위 해석**
우리는 **Distilling step-by-step**이라는 새로운 메커니즘을 제안한다. 이 방법은 (a) 더 작은 모델을 훈련해서 LLM보다 높은 성능을 내며, (b) finetuning이나 distillation에 비해 더 적은 학습 데이터를 사용해서 이 성과를 달성한다.

**보조 설명**

* 논문의 핵심 기여를 요약하는 부분.
* 단순히 모델 크기 축소뿐 아니라 **데이터 효율성**에서도 장점을 가진다.

---

### ✅ 문장 그룹 5

Our method extracts LLM rationales as additional supervision for training small models within a multi-task framework.

**단위 해석**
우리의 방법은 LLM이 생성한 **rationales**(추론 과정 설명)을 추가적인 감독 신호로 활용하여, 작은 모델을 **멀티태스크 프레임워크** 안에서 학습시킨다.

**보조 설명**

* rationale: 모델이 답을 내리기 위해 제시하는 중간 단계의 설명이나 chain-of-thought.
* 단순히 결과(label)만 전달받는 것이 아니라 reasoning 과정을 함께 학습한다는 점이 핵심.

---

### ✅ 문장 그룹 6

We present three findings across 4 NLP benchmarks: First, compared to both finetuning and distillation, our mechanism achieves better performance with much fewer labeled/unlabeled training examples.

**단위 해석**
우리는 4개의 NLP 벤치마크에서 세 가지 발견을 제시한다. 첫째, finetuning과 distillation 모두와 비교했을 때, 우리의 방법은 훨씬 적은 수의 라벨링/비라벨링 학습 예제를 사용해도 더 나은 성능을 낸다.

**보조 설명**

* 첫 번째 성과: **데이터 효율성** 개선.
* labeled/unlabeled 모두 절감 효과가 있다는 점을 강조.

---

### ✅ 문장 그룹 7

Second, compared to few-shot prompted LLMs, we achieve better performance using substantially smaller model sizes.

**단위 해석**
둘째, few-shot 프롬프트를 사용하는 LLM들과 비교했을 때, 훨씬 작은 모델 크기를 사용하면서도 더 나은 성능을 달성한다.

**보조 설명**

* LLM은 few-shot 프롬프트를 통해 강력한 성능을 낼 수 있지만, 여전히 모델 크기가 매우 크다.
* 작은 모델이 LLM을 능가하는 성능을 보인다는 점은 산업 적용에서 큰 장점.

---

### ✅ 문장 그룹 8

Third, we reduce both the model size and the amount of data required to outperform LLMs; our finetuned 770M T5 model outperforms the few-shot prompted 540B PaLM model using only 80% of available data on a benchmark, whereas standard finetuning the same T5 model struggles to match even by using 100% of the dataset.

**단위 해석**
셋째, 우리는 모델 크기와 필요한 데이터 양을 모두 줄이면서 LLM을 능가한다. 예를 들어, 우리가 finetuning한 **770M T5 모델**은 하나의 벤치마크에서, **540B 파라미터 PaLM 모델**을 few-shot prompting으로 사용한 것보다 높은 성능을 보인다. 이때 필요한 데이터는 전체의 80%에 불과하다. 반면, 같은 T5 모델을 표준 finetuning 방식으로 학습시킬 경우 전체 데이터(100%)를 사용해도 PaLM의 성능에 미치지 못한다.

**보조 설명**

* 대표적인 성과 비교: **770M vs 540B** → 약 700배 이상 작은 모델이 성능 우위를 점함.
* 단순히 작은 모델이라는 장점뿐 아니라 **데이터 사용량도 줄였음**을 강조.
* "outperform"이라는 표현에서 실제로 LLM 성능을 넘어섰음을 보여줌.

---

👉 Abstract 부분을 모두 해석하고 설명드렸습니다.
다음은 **1 Introduction** 섹션으로 넘어가서 같은 방식으로 이어갈까요?
좋습니다. 이제 **1 Introduction** 섹션으로 넘어가서, 동일한 방식으로 문장 그룹별 해석과 보조 설명을 드리겠습니다.

---

# 📑 1 Introduction

---

## 📘 단락 본문 텍스트

Despite the impressive few-shot ability offered by large language models (LLMs) (Brown et al., 2020 ; Chowdhery et al., 2022 ; Thoppilan et al., 2022 ; Hoffmann et al., 2022 ; Smith et al., 2022b ; Zhang et al., 2022 ), these models are challenging to deploy in real world applications due to their sheer size. Serving a single 175 billion LLM requires at least 350 GB GPU memory using specialized infrastructure (Zheng et al., 2022 ). To make matters worse, today’s state-of-the-art LLMs are composed of over 500 B parameters (Chowdhery et al. 2022 ), requiring significantly more memory and compute. Such computational requirements are far beyond affordable for most product teams, especially for applications that require low latency performance.

---

### ✅ 문장 그룹 1

Despite the impressive few-shot ability offered by large language models (LLMs) (Brown et al., 2020 ; Chowdhery et al., 2022 ; Thoppilan et al., 2022 ; Hoffmann et al., 2022 ; Smith et al., 2022b ; Zhang et al., 2022 ), these models are challenging to deploy in real world applications due to their sheer size.

**단위 해석**
대형 언어 모델(LLMs)이 제공하는 **few-shot 능력**은 인상적이지만 (여러 선행 연구 인용), 이러한 모델은 **거대한 규모** 때문에 실제 응용에서 배포하기 어렵다.

**보조 설명**

* few-shot: 극히 적은 예시만으로도 높은 성능을 보이는 능력.
* "sheer size"는 단순히 크다는 의미가 아니라, 규모 자체가 배포를 가로막는 핵심 장애 요인임을 강조.

---

### ✅ 문장 그룹 2

Serving a single 175 billion LLM requires at least 350 GB GPU memory using specialized infrastructure (Zheng et al., 2022 ).

**단위 해석**
파라미터 1750억(175B) 규모의 LLM 하나를 서비스하려면, 특수한 인프라를 사용하더라도 최소 350GB의 GPU 메모리가 필요하다.

**보조 설명**

* 일반적인 GPU(예: A100 80GB)를 여러 장 병렬로 사용해야 하는 수준.
* "specialized infrastructure"는 분산 GPU 클러스터, 고속 네트워크 등을 의미.

---

### ✅ 문장 그룹 3

To make matters worse, today’s state-of-the-art LLMs are composed of over 500 B parameters (Chowdhery et al. 2022 ), requiring significantly more memory and compute.

**단위 해석**
더 나쁜 것은, 현재 최첨단 LLM들은 **5000억(500B)** 개가 넘는 파라미터로 구성되어 있어, 훨씬 더 많은 메모리와 연산을 요구한다는 점이다.

**보조 설명**

* 500B 모델은 GPT-4, PaLM 등 최신 초거대 모델 수준.
* 350GB 이상의 요구 사항이 기본인데, 이보다 훨씬 많은 자원이 필요.

---

### ✅ 문장 그룹 4

Such computational requirements are far beyond affordable for most product teams, especially for applications that require low latency performance.

**단위 해석**
이러한 연산 요구는 대부분의 제품 팀이 감당할 수 있는 범위를 훨씬 초과한다. 특히, **낮은 지연 시간(low latency)** 성능이 필요한 응용 분야에서는 더욱 그렇다.

**보조 설명**

* 예: 챗봇, 검색, 실시간 추천 시스템 등.
* 연구실 단위에서는 실험적으로 가능할지 몰라도, 상용 서비스에는 부적합.

---

## 📘 단락 본문 텍스트

To circumvent these deployment challenges of large models, practitioners often choose to deploy smaller specialized models instead. These smaller models are trained using one of two common paradigms: finetuning or distillation. Finetuning updates a pretrained smaller model (e.g. BERT (Devlin et al., 2018 ) or T5 (Raffel et al., 2020 )) using downstream human annotated data (Howard and Ruder, 2018 ). Distillation trains the same smaller models with labels generated by a larger LLM (Tang et al., 2019 ; Wang et al., 2021 ; Smith et al., 2022a ; Arora et al., 2022 ). Unfortunately, these paradigms reduce model size at a cost: to achieve comparable performance to LLMs, finetuning requires expensive human labels, and distillation requires large amounts of unlabeled data which can be hard to obtain (Tang et al., 2019 ; Liang et al., 2020 ).

---

### ✅ 문장 그룹 5

To circumvent these deployment challenges of large models, practitioners often choose to deploy smaller specialized models instead.

**단위 해석**
이러한 대형 모델 배포의 어려움을 피하기 위해, 실제 개발자들은 보통 더 작은 \*\*특화 모델(specialized model)\*\*을 배포하는 방식을 선택한다.

**보조 설명**

* "circumvent" → 정면 돌파가 아니라 우회하는 접근.
* 예: 대형 GPT 대신 작은 BERT 파생 모델을 특정 업무에 활용.

---

### ✅ 문장 그룹 6

These smaller models are trained using one of two common paradigms: finetuning or distillation.

**단위 해석**
이 작은 모델들은 일반적으로 두 가지 방식으로 훈련된다: **finetuning** 또는 **distillation**.

**보조 설명**

* 논문에서 자주 반복되는 핵심 대비 구조: finetuning vs distillation.

---

### ✅ 문장 그룹 7

Finetuning updates a pretrained smaller model (e.g. BERT (Devlin et al., 2018 ) or T5 (Raffel et al., 2020 )) using downstream human annotated data (Howard and Ruder, 2018 ).

**단위 해석**
finetuning은 사전에 학습된 작은 모델(예: BERT, T5)을 **다운스트림 태스크용 인간 주석 데이터**를 이용해 업데이트하는 방식이다.

**보조 설명**

* "downstream" → 특정 응용 과제(분류, QA 등)에 맞춰 재학습.
* 비용: 인간 라벨링이 필요하므로 인건비와 시간이 많이 든다.

---

### ✅ 문장 그룹 8

Distillation trains the same smaller models with labels generated by a larger LLM (Tang et al., 2019 ; Wang et al., 2021 ; Smith et al., 2022a ; Arora et al., 2022 ).

**단위 해석**
distillation은 같은 작은 모델을 훈련하되, 이때 **큰 LLM이 생성한 라벨**을 사용한다.

**보조 설명**

* 교사 모델(teacher) → 학생 모델(student) 구조.
* 장점: 사람이 직접 라벨링할 필요는 줄지만, 여전히 대량 데이터 필요.

---

### ✅ 문장 그룹 9

Unfortunately, these paradigms reduce model size at a cost: to achieve comparable performance to LLMs, finetuning requires expensive human labels, and distillation requires large amounts of unlabeled data which can be hard to obtain (Tang et al., 2019 ; Liang et al., 2020 ).

**단위 해석**
그러나 이러한 패러다임은 모델 크기를 줄여주기는 하지만 대가가 따른다. LLM과 비슷한 성능을 내려면, finetuning은 값비싼 인간 라벨이 필요하고, distillation은 대량의 비라벨링 데이터가 필요한데 이것도 확보하기 어렵다.

**보조 설명**

* 결국 비용 문제: 사람이 직접 라벨링 → 고비용, 비라벨 데이터 수집 → 데이터 확보 난이도.
* 데이터 효율성 문제가 이 논문의 핵심 배경.

---

👉 여기까지가 **1 Introduction**의 첫 두 단락입니다.
다음 단락부터는 저자들이 본격적으로 **Distilling step-by-step** 개념을 소개하기 시작하는데, 이어서 진행할까요?
좋습니다. 이제 **1 Introduction**의 이어지는 부분(세 번째 단락부터)을 같은 방식으로 해석과 설명을 드리겠습니다.

---

## 📘 단락 본문 텍스트

In this work, we introduce Distilling step-by-step, a new simple mechanism for training smaller models with less training data. Our mechanism reduces the amount of training data required for both finetuning and distillation of LLMs into smaller model sizes. Core to our mechanism is changing our perspective from viewing LLMs as a source of noisy labels to viewing them as agents that can reason: LLMs can produce natural language rationales justifying their predicted labels (Wei et al., 2022 ; Kojima et al., 2022 ). For example, when asked “ Jesse’s room is 11 feet long and 15 feet wide. If she already has 16 square feet of carpet. How much more carpet does she need to cover the whole floor? ”, an LLM can be prompted by chain-of-thought (CoT) technique (Wei et al., 2022 ) to provide intermediate rationales “ Area = length × width. Jesse’s room has 11×15 square feet. ” that better connects the input to the final answer “ (11×15)-16 ”. These rationales can contain relevant task knowledge, such as “ Area = length × width ”, that may originally require many data for small task-specific models to learn. We thus utilize these extracted rationales as additional, richer information to train small models through a multi-task training setup, with both label prediction and rationale prediction tasks (Raffel et al., 2020 ; Narang et al., 2020 ).

---

### ✅ 문장 그룹 10

In this work, we introduce Distilling step-by-step, a new simple mechanism for training smaller models with less training data.

**단위 해석**
본 연구에서는 **Distilling step-by-step**이라는 새로운 간단한 메커니즘을 제안하는데, 이 방법은 더 적은 학습 데이터로 작은 모델을 훈련할 수 있게 해준다.

**보조 설명**

* "simple mechanism" → 접근법은 단순하지만 효과적임을 강조.
* 핵심 기여 소개.

---

### ✅ 문장 그룹 11

Our mechanism reduces the amount of training data required for both finetuning and distillation of LLMs into smaller model sizes.

**단위 해석**
이 메커니즘은 LLM을 작은 모델로 변환하는 과정에서, finetuning과 distillation 모두에 필요한 학습 데이터 양을 줄여준다.

**보조 설명**

* 데이터 절감 효과는 라벨링된 데이터와 라벨링되지 않은 데이터 모두에 적용 가능.
* 문제의 본질적 비용을 줄여주는 방식.

---

### ✅ 문장 그룹 12

Core to our mechanism is changing our perspective from viewing LLMs as a source of noisy labels to viewing them as agents that can reason: LLMs can produce natural language rationales justifying their predicted labels (Wei et al., 2022 ; Kojima et al., 2022 ).

**단위 해석**
이 메커니즘의 핵심은 LLM을 단순히 **잡음 섞인 라벨(noisy labels)** 공급원으로 보는 것이 아니라, **추론할 수 있는 에이전트**로 보는 관점 전환에 있다. 즉, LLM은 자신이 예측한 라벨을 정당화하는 **자연어 rationale**을 생성할 수 있다.

**보조 설명**

* noisy label: LLM이 만든 라벨은 완벽하지 않고 오류가 섞여 있다는 의미.
* rationale: 답을 뒷받침하는 설명(중간 단계 reasoning).
* 관점 전환: 단순히 “정답”이 아니라 “풀이 과정”을 같이 활용.

---

### ✅ 문장 그룹 13

For example, when asked “ Jesse’s room is 11 feet long and 15 feet wide. If she already has 16 square feet of carpet. How much more carpet does she need to cover the whole floor? ”, an LLM can be prompted by chain-of-thought (CoT) technique (Wei et al., 2022 ) to provide intermediate rationales “ Area = length × width. Jesse’s room has 11×15 square feet. ” that better connects the input to the final answer “ (11×15)-16 ”.

**단위 해석**
예를 들어, LLM에 다음과 같이 물었을 때:
“Jesse의 방은 길이 11피트, 너비 15피트이다. 이미 16 평방피트의 카펫을 가지고 있다면, 전체 바닥을 덮기 위해 얼마나 더 필요한가?”
→ CoT(chain-of-thought) 기법으로 LLM을 프롬프트하면, LLM은 중간 추론 과정을 다음과 같이 제공할 수 있다:
“면적 = 길이 × 너비. Jesse의 방은 11×15 평방피트이다.”
이 설명은 최종 답 “(11×15)-16”과 입력을 더 잘 연결해 준다.

**보조 설명**

* chain-of-thought: 모델이 단순히 정답만 주지 않고, 단계별 reasoning을 출력하도록 유도하는 프롬프트 방법.
* 예시는 수학적 문제에서 rationale이 어떻게 유용한지를 보여줌.

---

### ✅ 문장 그룹 14

These rationales can contain relevant task knowledge, such as “ Area = length × width ”, that may originally require many data for small task-specific models to learn.

**단위 해석**
이러한 rationales에는 “면적 = 길이 × 너비”와 같은 **과제 관련 지식**이 포함될 수 있는데, 이는 원래 작은 과제 특화 모델이 학습하려면 대량의 데이터가 필요했을 지식이다.

**보조 설명**

* rationale은 단순한 설명이 아니라 **도메인 지식 압축본**으로 작동할 수 있음.
* 적은 데이터로도 추론 규칙을 배울 수 있는 계기.

---

### ✅ 문장 그룹 15

We thus utilize these extracted rationales as additional, richer information to train small models through a multi-task training setup, with both label prediction and rationale prediction tasks (Raffel et al., 2020 ; Narang et al., 2020 ).

**단위 해석**
따라서 우리는 이렇게 추출한 rationales를 **추가적이고 더 풍부한 정보**로 활용하여 작은 모델을 학습시킨다. 구체적으로는 **멀티태스크 학습 구조**에서, 라벨 예측과 rationale 예측 두 가지 과제를 동시에 학습하도록 한다.

**보조 설명**

* multi-task setup: 하나의 입력으로 두 가지 출력을 동시에 예측하도록 학습.
* label prediction = 정답 예측, rationale prediction = reasoning 과정 생성.
* rationale 예측은 테스트 시 필수는 아니지만, 학습 단계에서 모델 이해를 돕는다.

---

👉 여기까지가 세 번째 단락입니다. 이어서 **네 번째 단락(결과 요약)** 부분을 계속 해석해드릴까요?
좋습니다! 이제 **1 Introduction**의 네 번째 단락(결과 요약 부분)을 이어서 해석과 설명을 드리겠습니다.

---

## 📘 단락 본문 텍스트

Distilling step-by-step allows us to learn task-specific smaller models that outperform LLMs using over 500× less model parameters, and it does so with far fewer training examples compared to traditional finetuning or distillation (Figure 1 ). Our results show three promising empirical conclusions across 4 NLP benchmarks. First, compared to both finetuning and distillation, our resulting models achieve better performance with over 50% less training examples on average across datasets (and up to over 85% reduction). Second, our models outperform LLMs with much smaller model sizes (up to 2000× smaller), drastically reducing the computation cost required for model deployment. Third, we simultaneously reduce the model size as well as the amount of data required to outperform LLMs. We surpass the performance of 540 B parameter LLMs using a 770 M T5 model; this smaller model only uses 80% of a labeled dataset that would otherwise be required if using an existing finetuning method. When only unlabeled data is present, our small models still perform on par or better than LLMs. We outperform 540 B PaLM’s performance with only a 11 B T5 model. We further show that when a smaller model performs worse than an LLM, Distilling step-by-step can more efficiently leverage additional unlabeled data to match the LLM performance compared to the standard distillation approach.

---

### ✅ 문장 그룹 16

Distilling step-by-step allows us to learn task-specific smaller models that outperform LLMs using over 500× less model parameters, and it does so with far fewer training examples compared to traditional finetuning or distillation (Figure 1 ).

**단위 해석**
Distilling step-by-step은 우리가 **LLM보다 뛰어난 성능을 내는 과제 특화 작은 모델**을 학습할 수 있게 해주며, 이는 LLM보다 **500배 이상 적은 파라미터**만으로 가능하다. 또한 전통적인 finetuning이나 distillation보다 훨씬 적은 학습 예제를 사용한다 (그림 1 참조).

**보조 설명**

* "500× less" → 같은 성능을 내는데 필요한 파라미터 수가 500분의 1 이하.
* 데이터 효율성과 모델 경량화를 동시에 달성.

---

### ✅ 문장 그룹 17

Our results show three promising empirical conclusions across 4 NLP benchmarks.

**단위 해석**
우리의 결과는 4개의 NLP 벤치마크에서 세 가지 유망한 실험적 결론을 보여준다.

**보조 설명**

* 이 단락은 구체적인 성과를 요약하는 구조.
* 이어지는 First, Second, Third가 핵심 포인트.

---

### ✅ 문장 그룹 18

First, compared to both finetuning and distillation, our resulting models achieve better performance with over 50% less training examples on average across datasets (and up to over 85% reduction).

**단위 해석**
첫째, finetuning과 distillation 모두와 비교했을 때, 우리의 모델은 평균적으로 **50% 이상 적은 학습 예제**만으로 더 나은 성능을 낸다. (최대 85%까지 학습 데이터 절감 가능)

**보조 설명**

* 데이터 절감 효과는 **절반 이상**.
* 일부 과제에서는 거의 **1/7 수준의 데이터**로도 성능 확보.

---

### ✅ 문장 그룹 19

Second, our models outperform LLMs with much smaller model sizes (up to 2000× smaller), drastically reducing the computation cost required for model deployment.

**단위 해석**
둘째, 우리의 모델은 훨씬 작은 크기(최대 2000배 더 작음)로도 LLM보다 뛰어난 성능을 내며, 이로 인해 모델 배포에 필요한 계산 비용이 크게 줄어든다.

**보조 설명**

* 예: 540B 모델 대신 220M 모델이 같은 혹은 더 나은 성능 달성.
* 실제 서비스 배포 가능성이 크게 올라감.

---

### ✅ 문장 그룹 20

Third, we simultaneously reduce the model size as well as the amount of data required to outperform LLMs.

**단위 해석**
셋째, 우리는 모델 크기와 데이터 양을 동시에 줄이면서 LLM을 능가한다.

**보조 설명**

* 단순히 작은 모델이 아니라 **데이터 효율성 + 모델 경량화** 두 마리 토끼를 잡음.
* 기존 방법에서는 하나만 줄이면 다른 쪽이 늘어나는 trade-off가 있었음.

---

### ✅ 문장 그룹 21

We surpass the performance of 540 B parameter LLMs using a 770 M T5 model; this smaller model only uses 80% of a labeled dataset that would otherwise be required if using an existing finetuning method.

**단위 해석**
우리는 540B 파라미터 LLM보다 더 나은 성능을 **770M T5 모델**로 달성했다. 이 작은 모델은 기존 finetuning 방법이었다면 전체 라벨링 데이터가 필요했을 텐데, **그중 80%만 사용**해도 충분했다.

**보조 설명**

* 540B vs 770M → 약 700배 이상 작은 모델.
* 데이터도 20% 절감.

---

### ✅ 문장 그룹 22

When only unlabeled data is present, our small models still perform on par or better than LLMs.

**단위 해석**
라벨이 없는 데이터만 존재할 때에도, 우리의 작은 모델은 LLM과 비슷하거나 그 이상의 성능을 낸다.

**보조 설명**

* unlabeled setting에서도 효율적이라는 점은 매우 실용적.
* 실제 응용에서 라벨링된 데이터보다 unlabeled 데이터가 훨씬 많기 때문에 강점이 됨.

---

### ✅ 문장 그룹 23

We outperform 540 B PaLM’s performance with only a 11 B T5 model.

**단위 해석**
우리는 단지 **11B T5 모델**만으로도 **540B PaLM 모델**의 성능을 넘어섰다.

**보조 설명**

* 540B vs 11B → 약 50배 이상 차이가 나는 모델 크기.
* 매우 작은 모델로도 초대형 모델의 성능을 초과 가능.

---

### ✅ 문장 그룹 24

We further show that when a smaller model performs worse than an LLM, Distilling step-by-step can more efficiently leverage additional unlabeled data to match the LLM performance compared to the standard distillation approach.

**단위 해석**
또한, 작은 모델이 LLM보다 성능이 낮을 때조차도, Distilling step-by-step은 **추가적인 unlabeled 데이터**를 기존 distillation보다 훨씬 효율적으로 활용하여 LLM 성능에 도달할 수 있음을 보였다.

**보조 설명**

* 즉, 성능이 부족한 경우에도 추가 데이터 투입 효율성이 높음.
* “데이터를 얼마나 잘 활용할 수 있느냐”에서 기존 distillation보다 우위.

---

👉 여기까지가 **1 Introduction** 전체입니다.
이제 다음 섹션은 **2 Related work** 인데, 이어서 계속 해석해드릴까요?
좋습니다! 이제 **2 Related work** 섹션으로 넘어가겠습니다. 원문을 그대로 출력하고, 이어서 문장 그룹별 해석과 설명을 드리겠습니다.

---

# 📑 2 Related work

---

## 📘 단락 본문 텍스트

Our work distills task-specific knowledge of LLMs into smaller specialist models by leveraging the emergent reasoning capabilities of today’s LLMs. We draw on knowledge distillation research and methods that learn from both human-generated rationales and LLM-generated rationales.

---

### ✅ 문장 그룹 1

Our work distills task-specific knowledge of LLMs into smaller specialist models by leveraging the emergent reasoning capabilities of today’s LLMs.

**단위 해석**
우리의 연구는 최신 LLM들이 가진 \*\*emergent reasoning capability(자발적 추론 능력)\*\*을 활용하여, LLM의 과제 특화 지식을 작은 특화 모델로 distill한다.

**보조 설명**

* emergent reasoning: 대형 모델에서만 나타나는 새로운 추론 능력.
* 단순히 정답 라벨을 복제하는 것이 아니라 reasoning까지 전달한다는 점이 중요.

---

### ✅ 문장 그룹 2

We draw on knowledge distillation research and methods that learn from both human-generated rationales and LLM-generated rationales.

**단위 해석**
우리는 **지식 증류(knowledge distillation)** 연구와, **인간이 만든 rationale** 및 **LLM이 생성한 rationale**을 활용하는 방법들에 기반한다.

**보조 설명**

* 즉, 이 논문은 두 연구 전통의 교차점에 위치.
* rationale 기반 학습은 사람 설명 활용 연구와 LLM 설명 활용 연구 두 가지가 존재.

---

## 📘 단락 본문 텍스트

**Knowledge distillation from large models.** Knowledge distillation has been successfully used to transfer knowledge from larger, more competent teacher models into smaller student models affordable for practical applications (Buciluǎ et al., 2006 ; Hinton et al., 2015 ; Beyer et al., 2022 ; West et al., 2021 ; Fu et al., 2023 ). It supports learning from limited labeled data, since the larger teacher model is often used to generate a training dataset with noisy pseudo labels (Chen et al., 2020 ; Iliopoulos et al., 2022 ; Wang et al., 2021 ; Smith et al., 2022a ; Arora et al., 2022 ; Agrawal et al., 2022 ). The one limitation that knowledge distillation often faces is its reliance on large amounts of unlabelled data required to create a useful noisy training dataset. Although prior work has explored using data augmentation techniques to reduce this hunger for data (Tang et al., 2019 ; Liang et al., 2020 ; Srinivas and Fleuret, 2018 ; Milli et al., 2019 ), we propose an alternative approach: we reduce the need for large unlabeled data by distilling not just labels but also the teacher’s rationales.

---

### ✅ 문장 그룹 3

**Knowledge distillation from large models.** Knowledge distillation has been successfully used to transfer knowledge from larger, more competent teacher models into smaller student models affordable for practical applications (Buciluǎ et al., 2006 ; Hinton et al., 2015 ; Beyer et al., 2022 ; West et al., 2021 ; Fu et al., 2023 ).

**단위 해석**
**대형 모델로부터의 지식 증류.** Knowledge distillation은 더 크고 성능이 뛰어난 \*\*교사 모델(teacher model)\*\*의 지식을, 실용적으로 활용 가능한 작은 \*\*학생 모델(student model)\*\*로 성공적으로 전이하는 데 사용되어 왔다.

**보조 설명**

* Hinton (2015) 논문 → distillation 개념의 대표 연구.
* 핵심은 큰 모델의 성능을 작은 모델로 최대한 전달.

---

### ✅ 문장 그룹 4

It supports learning from limited labeled data, since the larger teacher model is often used to generate a training dataset with noisy pseudo labels (Chen et al., 2020 ; Iliopoulos et al., 2022 ; Wang et al., 2021 ; Smith et al., 2022a ; Arora et al., 2022 ; Agrawal et al., 2022 ).

**단위 해석**
지식 증류는 라벨링된 데이터가 제한된 경우에도 학습을 지원하는데, 이는 대형 교사 모델이 종종 \*\*noisy pseudo label(잡음이 섞인 준라벨)\*\*을 생성하여 학습 데이터셋을 만들어내기 때문이다.

**보조 설명**

* pseudo label: 사람이 직접 만든 정답이 아닌, 모델이 자동 생성한 라벨.
* noisy: 정확하지 않을 수 있음.

---

### ✅ 문장 그룹 5

The one limitation that knowledge distillation often faces is its reliance on large amounts of unlabelled data required to create a useful noisy training dataset.

**단위 해석**
지식 증류가 자주 직면하는 한 가지 한계는, **유용한 noisy 학습 데이터셋**을 만들기 위해 대량의 unlabeled 데이터에 의존해야 한다는 점이다.

**보조 설명**

* distillation은 데이터 확보가 쉽지 않은 환경에서는 비효율적.

---

### ✅ 문장 그룹 6

Although prior work has explored using data augmentation techniques to reduce this hunger for data (Tang et al., 2019 ; Liang et al., 2020 ; Srinivas and Fleuret, 2018 ; Milli et al., 2019 ), we propose an alternative approach: we reduce the need for large unlabeled data by distilling not just labels but also the teacher’s rationales.

**단위 해석**
이전 연구에서는 데이터에 대한 갈증을 줄이기 위해 **데이터 증강(data augmentation)** 기법을 사용하기도 했지만, 우리는 다른 접근을 제안한다: 라벨뿐 아니라 **교사 모델의 rationale**까지 distill하여, 대량의 unlabeled 데이터 필요성을 줄이는 것이다.

**보조 설명**

* 기존: 데이터 양 늘려서 해결.
* 제안: 데이터 자체가 아니라 \*\*정보량(라벨 + reasoning)\*\*을 증류.

---

## 📘 단락 본문 텍스트

**Learning with human rationales.** While utilizing LLM-generated rationales is a new exciting area of investigation, using human-generated rationales has a rich history (Hase and Bansal, 2021 ). For instance, human rationales can be used to regularize model behavior (Ross et al., 2017 ); it can be used as additional inputs to guide a model’s predictions (Rajani et al., 2019 ); it can be used to improve overall model performance (Zaidan et al., 2007 ; Zhang et al., 2016 ; Camburu et al., 2018 ; Hancock et al., 2019 ; Pruthi et al., 2022 ); and human rationales can be used as gold standard labels to make models more interpretable by generating similar rationales (Wiegreffe et al., 2021 ; Narang et al., 2020 ; Eisenstein et al., 2022 ). Unfortunately, human rationales are expensive.

---

### ✅ 문장 그룹 7

**Learning with human rationales.** While utilizing LLM-generated rationales is a new exciting area of investigation, using human-generated rationales has a rich history (Hase and Bansal, 2021 ).

**단위 해석**
**인간 rationale을 활용한 학습.** LLM이 생성한 rationale을 활용하는 연구는 새로운 흥미로운 분야이지만, **인간이 만든 rationale**을 활용하는 연구는 오랜 역사를 가지고 있다.

**보조 설명**

* 인간 rationale: 사람이 직접 문제 풀이 과정이나 근거를 설명한 것.
* 교육, 심리학, NLP 등 다양한 분야에서 연구됨.

---

### ✅ 문장 그룹 8

For instance, human rationales can be used to regularize model behavior (Ross et al., 2017 ); it can be used as additional inputs to guide a model’s predictions (Rajani et al., 2019 ); it can be used to improve overall model performance (Zaidan et al., 2007 ; Zhang et al., 2016 ; Camburu et al., 2018 ; Hancock et al., 2019 ; Pruthi et al., 2022 ); and human rationales can be used as gold standard labels to make models more interpretable by generating similar rationales (Wiegreffe et al., 2021 ; Narang et al., 2020 ; Eisenstein et al., 2022 ).

**단위 해석**
예를 들어, 인간 rationale은 (1) 모델의 동작을 정규화(regularize)하는 데 쓰일 수 있고, (2) 모델 예측을 안내하는 추가 입력으로 쓰일 수 있으며, (3) 모델의 전체 성능을 향상시키는 데 쓰일 수 있고, (4) 유사한 rationale을 생성하게 만들어 모델을 더 해석 가능하게 하는 **gold standard label**로 쓰일 수 있다.

**보조 설명**

* human rationale → 라벨만이 아니라 “이유”까지 제공하는 학습 자원.
* interpretable AI의 중요한 도구로 여겨짐.

---

### ✅ 문장 그룹 9

Unfortunately, human rationales are expensive.

**단위 해석**
그러나 인간 rationale은 **비용이 많이 든다**.

**보조 설명**

* 라벨링보다 훨씬 부담: 단순 정답 체크보다 reasoning 설명 작성이 더 어려움.
* 따라서 대규모 데이터셋 확보가 어렵다.

---

👉 여기까지 **2 Related work** 중 **knowledge distillation**과 **human rationales** 부분을 다뤘습니다.
다음은 **Learning with LLM generated rationales** 부분인데, 이어서 계속 진행할까요?
좋습니다! 이어서 요청하신 원문 **Learning with LLM generated rationales** 부분을 해석 및 설명드리겠습니다.

---

## 📘 단락 본문 텍스트

Learning with LLM generated rationales. Today’s LLMs are capable of explaining their predictions by generating high-quality reasoning steps (Wei et al., 2022 ; Kojima et al., 2022 ). These reasoning steps have been used to augment input prompts to LLMs, improving their few-shot or zero-shot performance (Wei et al., 2022 ; Kojima et al., 2022 ; Wang et al., 2022b ); reasoning steps have also been used as additional finetuning data “self-improve” LLMs (Zelikman et al., 2022 ; Huang et al., 2022 ). Unfortunately, regardless of how LLMs are improved, their large size limits their utility in most test-time applications.

---

### ✅ 문장 그룹 1

Learning with LLM generated rationales. Today’s LLMs are capable of explaining their predictions by generating high-quality reasoning steps (Wei et al., 2022 ; Kojima et al., 2022 ).

**단위 해석**
**LLM이 생성한 rationale을 활용한 학습.** 오늘날의 LLM들은 자신의 예측을 설명하기 위해 **고품질의 reasoning step**을 생성할 수 있다.

**보조 설명**

* reasoning step = chain-of-thought과 같은 중간 추론 단계.
* 단순 정답이 아니라 “왜 그런 답이 나왔는지”를 제시하는 능력.

---

### ✅ 문장 그룹 2

These reasoning steps have been used to augment input prompts to LLMs, improving their few-shot or zero-shot performance (Wei et al., 2022 ; Kojima et al., 2022 ; Wang et al., 2022b ); reasoning steps have also been used as additional finetuning data “self-improve” LLMs (Zelikman et al., 2022 ; Huang et al., 2022 ).

**단위 해석**
이러한 reasoning step은 LLM의 입력 프롬프트를 보강하여 **few-shot 혹은 zero-shot 성능을 개선**하는 데 사용되었고, 또 다른 연구에서는 이 reasoning step을 **추가 finetuning 데이터**로 활용해 LLM 스스로를 개선(“self-improve”)하는 데 사용하기도 했다.

**보조 설명**

* few-shot, zero-shot → 적거나 전혀 없는 예시로도 잘 수행.
* reasoning step을 프롬프트에 포함시키면 LLM 스스로 사고 과정을 따라가 성능 향상.
* self-improve → LLM이 스스로 생성한 rationale을 다시 학습하여 강화.

---

### ✅ 문장 그룹 3

Unfortunately, regardless of how LLMs are improved, their large size limits their utility in most test-time applications.

**단위 해석**
그러나 LLM이 어떤 방식으로 개선되든, **거대한 크기** 때문에 대부분의 테스트 시 응용에서 활용성은 여전히 제한된다.

**보조 설명**

* 즉, reasoning step 활용 자체는 LLM 성능 향상에 기여하지만, 여전히 배포 비용 문제는 해결되지 않는다.
* 논문이 작은 모델 훈련으로 전환하는 이유를 강조.

---

## 📘 단락 본문 텍스트

By contrast, we leverage generated rationales as informative supervision to train smaller task-specific models, i.e. models that can be deployed without incurring large computation or memory costs. Several concurrent works have also proposed a similar idea to ours – that of using extracted rationales as supervision (Wang et al., 2022a ; Ho et al., 2022 ; Magister et al., 2022 ; Li et al., 2023 ). Amongst them, PINTO (Wang et al., 2022a ) relies on an LLM to generate rationales at test-time, and thus does not fully solve deployment challenges. Compared with Ho et al. ( 2022 ) and Magister et al. ( 2022 ), we go beyond their experiments to provide a granular study by varying training dataset size, exploring downstream model sizes, and demonstrating the effectiveness of our method on fully unlabeled datasets.

---

### ✅ 문장 그룹 4

By contrast, we leverage generated rationales as informative supervision to train smaller task-specific models, i.e. models that can be deployed without incurring large computation or memory costs.

**단위 해석**
반대로, 우리는 LLM이 생성한 rationale을 \*\*유용한 감독 신호(informative supervision)\*\*로 활용하여 작은 과제 특화 모델을 훈련한다. 즉, 큰 연산·메모리 비용을 들이지 않고도 배포 가능한 모델을 목표로 한다.

**보조 설명**

* 핵심 차별점: rationale을 “LLM 성능 향상”이 아니라 “작은 모델 학습”에 활용.
* deployment practicality 강조.

---

### ✅ 문장 그룹 5

Several concurrent works have also proposed a similar idea to ours – that of using extracted rationales as supervision (Wang et al., 2022a ; Ho et al., 2022 ; Magister et al., 2022 ; Li et al., 2023 ).

**단위 해석**
여러 동시대 연구들도 유사한 아이디어를 제안했는데, 그것은 **추출된 rationale을 감독 신호로 활용**하는 것이다.

**보조 설명**

* 동시 연구(concurrent works) → 비슷한 시기 독립적으로 진행된 관련 연구들.
* 저자들은 기존 연구들과의 차이를 이후에 명확히 설명하려 한다.

---

### ✅ 문장 그룹 6

Amongst them, PINTO (Wang et al., 2022a ) relies on an LLM to generate rationales at test-time, and thus does not fully solve deployment challenges.

**단위 해석**
그중 \*\*PINTO(Wang et al., 2022a)\*\*는 테스트 시점에 LLM이 rationale을 생성하도록 의존하기 때문에, 배포상의 문제를 완전히 해결하지 못한다.

**보조 설명**

* PINTO는 LLM이 여전히 inference 과정에 필요 → 계산 부담 유지.
* 저자들의 방법과 대비되는 점: 우리 방법은 inference 시 LLM 불필요.

---

### ✅ 문장 그룹 7

Compared with Ho et al. ( 2022 ) and Magister et al. ( 2022 ), we go beyond their experiments to provide a granular study by varying training dataset size, exploring downstream model sizes, and demonstrating the effectiveness of our method on fully unlabeled datasets.

**단위 해석**
Ho et al. (2022) 및 Magister et al. (2022)와 비교했을 때, 우리는 그들의 실험을 넘어 **훈련 데이터셋 크기를 다양하게 조정**하고, **다운스트림 모델 크기를 탐색**하며, **완전히 라벨 없는 데이터셋**에서도 우리의 방법이 효과적임을 보여주는 정밀한 연구를 제공한다.

**보조 설명**

* 기존 연구: 작은 규모/제한된 실험.
* 본 연구: 데이터 크기, 모델 크기, 라벨링 여부 등 다양한 조건을 탐구.
* 기여: 더 포괄적이고 실증적인 검증 제공.

---

👉 여기까지 **2 Related work** 전체를 마쳤습니다.
다음은 본격적인 방법론 설명인 **3 Distilling step-by-step** 섹션인데, 이어서 진행할까요?
좋습니다! 이제 **3 Distilling step-by-step** 섹션으로 넘어가겠습니다. 원문을 그대로 제시하고, 이어서 문장 그룹별 해석과 설명을 드리겠습니다.

---

# 📑 3 Distilling step-by-step

---

## 📘 단락 본문 텍스트

We propose a new paradigm, Distilling step-by-step, that leverages the ability of LLMs to reason about their predictions to train smaller models in a data-efficient way. Our overall framework is illustrated in Figure 2. Our paradigm has two simple steps: First, given an LLM and an unlabeled dataset, we prompt the LLM to generate output labels along with rationales to justify the labels. Rationales are natural language explanations that provide support for the model’s predicted label (see Figure 2). Second, we leverage these rationales in addition to the task labels to train smaller downstream models. Intuitively, rationales provide richer, more detailed information about why an input is mapped to a specific output label, and often contain relevant task knowledge that may be hard to infer solely from the original inputs.

---

### ✅ 문장 그룹 1

We propose a new paradigm, Distilling step-by-step, that leverages the ability of LLMs to reason about their predictions to train smaller models in a data-efficient way.

**단위 해석**
우리는 새로운 패러다임인 **Distilling step-by-step**을 제안한다. 이 방법은 LLM의 **예측에 대한 추론 능력**을 활용하여 작은 모델을 **데이터 효율적으로(data-efficient)** 훈련시킨다.

**보조 설명**

* 단순히 라벨 복사(copying labels)가 아니라, reasoning을 적극 활용.
* 데이터 효율성은 이 논문의 핵심 기여.

---

### ✅ 문장 그룹 2

Our overall framework is illustrated in Figure 2.

**단위 해석**
우리의 전체 프레임워크는 그림 2에 나타나 있다.

**보조 설명**

* 프레임워크 구성: (1) rationale 추출, (2) rationale을 활용한 학습.

---

### ✅ 문장 그룹 3

Our paradigm has two simple steps: First, given an LLM and an unlabeled dataset, we prompt the LLM to generate output labels along with rationales to justify the labels.

**단위 해석**
우리의 패러다임은 두 가지 간단한 단계로 이루어진다. 첫째, LLM과 unlabeled 데이터셋이 주어지면, LLM을 프롬프트하여 **출력 라벨**과 그것을 정당화하는 **rationale**을 생성한다.

**보조 설명**

* 기존 distillation은 라벨만 → 여기서는 라벨 + rationale.
* rationale = natural language justification.

---

### ✅ 문장 그룹 4

Rationales are natural language explanations that provide support for the model’s predicted label (see Figure 2).

**단위 해석**
rationale은 모델이 예측한 라벨을 뒷받침하는 **자연어 설명**이다 (그림 2 참고).

**보조 설명**

* reasoning trace와 유사 개념.
* chain-of-thought prompting을 통해 쉽게 얻을 수 있음.

---

### ✅ 문장 그룹 5

Second, we leverage these rationales in addition to the task labels to train smaller downstream models.

**단위 해석**
둘째, 우리는 이러한 rationale을 태스크 라벨과 함께 활용하여 더 작은 다운스트림 모델을 학습한다.

**보조 설명**

* 학습 데이터 = (입력, 라벨, rationale).
* multi-task 학습 구조로 확장 가능.

---

### ✅ 문장 그룹 6

Intuitively, rationales provide richer, more detailed information about why an input is mapped to a specific output label, and often contain relevant task knowledge that may be hard to infer solely from the original inputs.

**단위 해석**
직관적으로, rationale은 **입력이 특정 출력 라벨로 매핑되는 이유**에 대한 더 풍부하고 세부적인 정보를 제공한다. 또한, 이는 원래 입력만으로는 추론하기 어려운 **과제 관련 지식**을 종종 포함한다.

**보조 설명**

* 예: “면적 = 길이 × 너비” 같은 규칙은 라벨만 보면 알기 어려움.
* rationale은 작은 모델에게 학습 지름길(shortcut)을 제공.

---

## 📘 단락 본문 텍스트

### 3.1 Extracting rationales from LLMs

Recent studies observe one intriguing emerging property of LLMs: their ability to generate rationales that support their predictions (Wei et al., 2022 ; Kojima et al., 2022). While the studies have largely focused on how to elicit such reasoning capability from LLMs (Nye et al., 2021 ; Wei et al., 2022 ; Kojima et al., 2022), we use them in training smaller downstream models.

---

### ✅ 문장 그룹 7

Recent studies observe one intriguing emerging property of LLMs: their ability to generate rationales that support their predictions (Wei et al., 2022 ; Kojima et al., 2022).

**단위 해석**
최근 연구들은 LLM의 흥미로운 새로운 특성을 관찰했는데, 그것은 자신의 예측을 뒷받침하는 **rationale 생성 능력**이다.

**보조 설명**

* emergent property → 대규모 모델에서만 나타나는 새로운 행동 양상.
* reasoning 능력은 작은 모델에는 잘 나타나지 않는 특성.

---

### ✅ 문장 그룹 8

While the studies have largely focused on how to elicit such reasoning capability from LLMs (Nye et al., 2021 ; Wei et al., 2022 ; Kojima et al., 2022), we use them in training smaller downstream models.

**단위 해석**
기존 연구는 주로 LLM으로부터 이런 reasoning 능력을 어떻게 \*\*이끌어내는가(elicit)\*\*에 집중했지만, 우리는 이를 작은 다운스트림 모델을 학습시키는 데 활용한다.

**보조 설명**

* “elicit” → 특정 프롬프트 기법(CoT 등)으로 reasoning을 뽑아내는 것.
* 본 논문은 단순 연구가 아니라 **작은 모델 학습**으로 연결.

---

👉 여기까지 **3 Distilling step-by-step**의 첫 부분(개요 + 3.1 서두)입니다.
다음은 **3.1 Extracting rationales from LLMs**의 구체적인 설명(Chain-of-Thought prompting 과정 등)이 이어지는데, 계속 진행할까요?
좋습니다! 요청하신 포맷에 맞춰 **3.1 Extracting rationales from LLMs**의 이어지는 문단을 처리하겠습니다. (수식은 `$…$` 대신 `\(...\)`로 표기해 렌더링합니다.)

---

## 📘 단락 본문 텍스트

Specifically, we utilize Chain-of-Thought (CoT) prompting (Wei et al., 2022) to elicit and extract rationales from LLMs. As illustrated in Figure 3, given an unlabeled dataset $x_{i}\in D$, we first curate a prompt template $p$ that articulates how the task should be solved. Each prompt is a triplet $(x^{\mathrm{p}}, r^{\mathrm{p}}, y^{\mathrm{p}})$, where $x^{\mathrm{p}}$ is an example input, $y^{\mathrm{p}}$ is its corresponding label and $r^{\mathrm{p}}$ is a user-provided rationale that explains why $x^{\mathrm{p}}$ can be categorized as $y^{\mathrm{p}}$. We append each input $x_{i}$ to $p$ and use it as an input to prompt the LLM to generate rationales and labels for each $x_{i}\in D$. With the demonstrations seen in $p$, the LLM is able to mimic the triplet demonstration to generate the rationale $\hat{r}_{i}$ and output $\hat{y}_{i}$ for $x_{i}$.

---

### ✅ 문장 그룹 9

Specifically, we utilize Chain-of-Thought (CoT) prompting (Wei et al., 2022) to elicit and extract rationales from LLMs.

**단위 해석**
구체적으로, 우리는 LLM으로부터 rationale을 **이끌어내고(elicit) 추출**하기 위해 **Chain-of-Thought(CoT) prompting**을 사용한다.

**보조 설명**

* CoT 프롬프트는 모델이 단계적 추론을 **텍스트로 드러내도록** 유도하는 기법.
* 여기서의 초점은 “정답”이 아니라 **추론 과정**을 얻는 데 있다.

---

### ✅ 문장 그룹 10

As illustrated in Figure 3, given an unlabeled dataset $x_{i}\in D$, we first curate a prompt template $p$ that articulates how the task should be solved.

**단위 해석**
그림 3에 나타난 것처럼, **라벨이 없는 데이터셋** $x_{i}\in D$가 주어지면, 먼저 **과제를 어떻게 해결할지**를 설명하는 **프롬프트 템플릿 $p$** 를 만든다.

**보조 설명**

* $x_i$는 개별 입력, $D$는 데이터셋 전체를 의미.
* “curate a prompt template” → 예시, 지시문, 형식을 신중히 구성한다는 뜻.

---

### ✅ 문장 그룹 11

Each prompt is a triplet $(x^{\mathrm{p}}, r^{\mathrm{p}}, y^{\mathrm{p}})$, where $x^{\mathrm{p}}$ is an example input, $y^{\mathrm{p}}$ is its corresponding label and $r^{\mathrm{p}}$ is a user-provided rationale that explains why $x^{\mathrm{p}}$ can be categorized as $y^{\mathrm{p}}$.

**단위 해석**
각 프롬프트는 **삼중항(triplet)** $(x^{\mathrm{p}}, r^{\mathrm{p}}, y^{\mathrm{p}})$으로 구성된다. 여기서 $x^{\mathrm{p}}$는 **예시 입력**, $y^{\mathrm{p}}$는 그에 대응하는 **라벨**, $r^{\mathrm{p}}$는 왜 $x^{\mathrm{p}}$가 $y^{\mathrm{p}}$로 **분류되는지 설명하는 사용자 제공 rationale**이다.

**보조 설명**

* $(x^{\mathrm{p}}, r^{\mathrm{p}}, y^{\mathrm{p}})$ 예시를 \*\*데모(demonstration)\*\*로써 프롬프트에 포함.
* 사용자 제공 $r^{\mathrm{p}}$는 초기 **few-shot 예제**에서 제공되는 고품질 설명일 수 있다.

---

### ✅ 문장 그룹 12

We append each input $x_{i}$ to $p$ and use it as an input to prompt the LLM to generate rationales and labels for each $x_{i}\in D$.

**단위 해석**
각 입력 $x_{i}$를 템플릿 $p$에 **이어붙여(append)** LLM에 투입하면, LLM이 $x_{i}\in D$ 각각에 대해 **rationale과 라벨을 생성**한다.

**보조 설명**

* 실무적으로는: \[데모 트리플렛 여러 개] + \[새 입력 $x_i$] 형태로 프롬프트 구성.
* 이렇게 얻은 **$\hat{r}_i$, $\hat{y}_i$** 가 이후 작은 모델 학습 데이터가 된다.

---

### ✅ 문장 그룹 13

With the demonstrations seen in $p$, the LLM is able to mimic the triplet demonstration to generate the rationale $\hat{r}_{i}$ and output $\hat{y}_{i}$ for $x_{i}$.

**단위 해석**
템플릿 $p$에 있는 데모를 본 LLM은 그 **트리플렛 형식**을 모방하여, 입력 $x_{i}$에 대해 **rationale $\hat{r}_{i}$** 와 **라벨 $\hat{y}_{i}$** 를 생성한다.

**보조 설명**

* “mimic the triplet” → few-shot 예시의 **패턴을 학습**해 유사한 형식으로 출력.
* 결과적으로, (입력, 생성 rationale, 생성 라벨)의 **풍부한 학습 표본**을 대량 확보 가능.

---

다음으로 **3.2 Training smaller models with rationales**(멀티태스크 학습 설계와 손실 함수 $\mathcal{L}$ 구성) 부분으로 넘어갈까요?
좋습니다! 요청하신 원문을 **생략 없이** 제시하고, 이어서 문장 그룹별 해석과 보조 설명을 제공하겠습니다. (수식은 `$…$` 대신 `\(...\)` 또는 `\[...\]`로 표기하고, (1)–(4)는 **수식 번호**로 수식 뒤에 표기합니다.)

---

# 📑 3.2 Training smaller models with rationales

---

## 📘 단락 본문 텍스트

We first describe the current framework for learning task-specific models. With this framework in place, we extend it to incorporate rationales into the training process. Formally, we denote a dataset as $\mathcal{D}=\{(x_{i},y_{i})\}_{i=1}^{N}$ where each $x_{i}$ represents an input and $y_{i}$ is the corresponding desired output label. While our framework supports inputs and outputs of any modality, our experiments limits $x$ and $y$ to be natural language. This text-to-text framework (Raffel et al., 2020) encompasses a variety of NLP tasks: classification, natural language inference, question answering and more.

---

### ✅ 문장 그룹 14

We first describe the current framework for learning task-specific models. With this framework in place, we extend it to incorporate rationales into the training process.

**단위 해석**
먼저 과제 특화 모델을 학습하는 **기존 프레임워크**를 설명하고, 그 위에 **rationale을 통합**하는 확장 방식을 제시하겠다는 뜻입니다.

**보조 설명**

* 구조 소개 → 확장 순서로 설명 전개를 예고.
* 핵심 목표: 기존 파이프라인에 **rationale 감독 신호**를 넣는 것.

---

### ✅ 문장 그룹 15

Formally, we denote a dataset as $\mathcal{D}=\{(x_{i},y_{i})\}_{i=1}^{N}$ where each $x_{i}$ represents an input and $y_{i}$ is the corresponding desired output label.

**단위 해석**
형식적으로, 데이터셋을 $\mathcal{D}=\{(x_{i},y_{i})\}_{i=1}^{N}$로 두며, $x_i$는 입력, $y_i$는 그에 대응하는 목표 라벨입니다.

**보조 설명**

* 표준 **감독학습 표기**.
* 이후 손실함수 정의를 위한 기초 표기.

---

### ✅ 문장 그룹 16

While our framework supports inputs and outputs of any modality, our experiments limits $x$ and $y$ to be natural language.

**단위 해석**
프레임워크는 **모달리티 제약이 없지만**, 본 논문의 실험에서는 $x$, $y$를 **자연어**로 제한합니다.

**보조 설명**

* 일반화 가능성을 열어두되, 실증은 **text-to-text**로 수행.

---

### ✅ 문장 그룹 17

This text-to-text framework (Raffel et al., 2020) encompasses a variety of NLP tasks: classification, natural language inference, question answering and more.

**단위 해석**
이 **text-to-text** 프레임워크(Raffel et al., 2020)는 분류, 자연어 추론(NLI), 질의응답(QA) 등 다양한 NLP 작업을 포괄합니다.

**보조 설명**

* T5 패러다임을 전제로 함.
* 이후 각 벤치마크에 공통 적용 가능.

---

## 📘 단락 본문 텍스트

Standard finetuning and task distillation. The most common practice to train a task-specific model is to finetune a pretrained model with supervised data (Howard and Ruder, 2018). In the absence of human-annotated labels, task-specific distillation (Hinton et al., 2015; Tang et al., 2019) uses LLM teachers to generates pseudo noisy training labels, $\hat{y}_{i}$ in place of $y_{i}$ (Wang et al., 2021; Smith et al., 2022a; Arora et al., 2022).

---

### ✅ 문장 그룹 18

Standard finetuning and task distillation. The most common practice to train a task-specific model is to finetune a pretrained model with supervised data (Howard and Ruder, 2018).

**단위 해석**
**표준 finetuning과 태스크 증류.** 과제 특화 모델을 훈련하는 가장 일반적인 방법은 **사전학습 모델을 감독 데이터로 미세조정**하는 것입니다.

**보조 설명**

* 감독 데이터 = **인간 라벨**.
* 산업·연구에서 가장 널리 쓰이는 기본 방법.

---

### ✅ 문장 그룹 19

In the absence of human-annotated labels, task-specific distillation (Hinton et al., 2015; Tang et al., 2019) uses LLM teachers to generates pseudo noisy training labels, $\hat{y}_{i}$ in place of $y_{i}$ (Wang et al., 2021; Smith et al., 2022a; Arora et al., 2022).

**단위 해석**
**인간 라벨이 없을 때**는 태스크 증류를 사용하여, LLM 교사가 만든 **준라벨 $\hat{y}_i$** 로 **정답 $y_i$** 를 대신합니다.

**보조 설명**

* pseudo/noisy 라벨 → 정확도는 완벽하지 않지만 **대규모 데이터 확보**가 가능.
* 교사–학생(teacher–student) 구조의 전형.

---

## 📘 단락 본문 텍스트

For both scenarios, the smaller model $f$ is trained to minimize the label prediction loss:

$$
\displaystyle\mathcal{L}_{\mathrm{label}}=\frac{1}{N}\sum_{i=1}^{N}\ell\big(f(x_{i}),\hat{y}_{i}\big),
$$

(1)
where $\ell$ is the cross-entropy loss between the predicted and target tokens. Note that for ease of exposition, we overload $\hat{y}_{i}$ in Eq. 1 to be either human-annotated labels $y_{i}$ for the standard finetuning case, or LLM-predicted labels $\hat{y}_{i}$ for the model distillation case.

---

### ✅ 문장 그룹 20

For both scenarios, the smaller model $f$ is trained to minimize the label prediction loss: $\mathcal{L}_{\mathrm{label}}=\frac{1}{N}\sum_{i=1}^{N}\ell(f(x_{i}),\hat{y}_{i})$. (1)

**단위 해석**
두 경우(finetuning, distillation) 모두 작은 모델 $f$는 **라벨 예측 손실 $\mathcal{L}_{\mathrm{label}}$** 을 최소화하도록 훈련됩니다. (1)

**보조 설명**

* $\ell$: 토큰 단위 **크로스 엔트로피**.
* 평균 손실로 **데이터 전체**에 대해 학습.

---

### ✅ 문장 그룹 21

where $\ell$ is the cross-entropy loss between the predicted and target tokens.

**단위 해석**
여기서 $\ell$은 **예측 토큰과 정답 토큰 간**의 크로스 엔트로피 손실입니다.

**보조 설명**

* 시퀀스-투-시퀀스 학습의 표준 손실.

---

### ✅ 문장 그룹 22

Note that for ease of exposition, we overload $\hat{y}_{i}$ in Eq. 1 to be either human-annotated labels $y_{i}$ for the standard finetuning case, or LLM-predicted labels $\hat{y}_{i}$ for the model distillation case.

**단위 해석**
설명의 편의를 위해, (1)식의 $\hat{y}_i$ 표기는 **finetuning일 때는 $y_i$** (인간 라벨), **distillation일 때는 $\hat{y}_i$** (LLM 라벨)을 **겸해** 쓰는 표기 과부하임을 밝힙니다.

**보조 설명**

* 같은 기호를 문맥에 따라 다르게 해석.

---

## 📘 단락 본문 텍스트

Multi-task learning with rationales. To create a more explicit connection between $x_{i}$’s to $\hat{y}_{i}$’s, we use extracted rationales $\hat{r}_{i}$ as additional supervision. There are several ways to incorporate rationales into the downstream model’s training process. One straightforward approach is feed $\hat{r}_{i}$ as an additional input—as proposed by other concurrent research (Rajani et al., 2019; Wang et al., 2022a). In other words, the $f(x_{i},\hat{r}_{i})\rightarrow\hat{y}_{i}$ is trained with both text and rationale $[x,r]$ as inputs:

$$
\displaystyle\mathcal{L}=\frac{1}{N}\sum_{i=1}^{N}\ell\big(f(x_{i},\hat{r}_{i}),\hat{y}_{i}\big).
$$

(2)
Unfortunately, this design requires an LLM to first generate a rationale before the $f$ can make a prediction. The LLM is still necessary during deployment, limited its deployability.

---

### ✅ 문장 그룹 23

Multi-task learning with rationales. To create a more explicit connection between $x_{i}$’s to $\hat{y}_{i}$’s, we use extracted rationales $\hat{r}_{i}$ as additional supervision.

**단위 해석**
**rationale 기반 멀티태스크 학습.** 입력 $x_i$와 출력 $\hat{y}_i$ 사이의 **명시적 연결**을 강화하기 위해, **추출된 rationale $\hat{r}_i$** 를 **추가 감독**으로 사용합니다.

**보조 설명**

* 목표: **왜** 그 라벨인지까지 학습에 반영.

---

### ✅ 문장 그룹 24

There are several ways to incorporate rationales into the downstream model’s training process. One straightforward approach is feed $\hat{r}_{i}$ as an additional input—as proposed by other concurrent research (Rajani et al., 2019; Wang et al., 2022a).

**단위 해석**
rationale을 학습에 넣는 방법은 여러 가지가 있는데, 가장 직관적인 방법은 **$\hat{r}_i$를 추가 입력**으로 투입하는 것입니다(선행 동시 연구 참조).

**보조 설명**

* 입력을 $[x, r]$로 확장하는 방식.

---

### ✅ 문장 그룹 25

In other words, the $f(x_{i},\hat{r}_{i})\rightarrow\hat{y}_{i}$ is trained with both text and rationale $[x,r]$ as inputs: $\mathcal{L}=\frac{1}{N}\sum_{i=1}^{N}\ell(f(x_{i},\hat{r}_{i}),\hat{y}_{i})$. (2)

**단위 해석**
즉, 모델 $f$는 입력 $[x, r]$를 받아 $\hat{y}_i$를 예측하도록 학습되며, 손실은 (2)와 같습니다.

**보조 설명**

* **테스트 시**에도 rationale 입력이 **필요해지는** 설계라는 점이 한계.

---

### ✅ 문장 그룹 26

Unfortunately, this design requires an LLM to first generate a rationale before the $f$ can make a prediction. The LLM is still necessary during deployment, limited its deployability.

**단위 해석**
하지만 이 설계는 $f$가 예측하기 **전에** LLM이 먼저 rationale을 **생성해야** 하므로, **배포 시점에도 LLM이 필요**해져 **배포성(deployability)** 이 제한됩니다.

**보조 설명**

* 지연·비용 증가 → 실사용 제약.

---

## 📘 단락 본문 텍스트

In this work, instead of using rationales as additional model inputs, we frame learning with rationales as a multi-task problem. Specifically, we train the model $f(x_{i})\rightarrow(\hat{y}_{i},\hat{r}_{i})$ to not only predict the task labels but also generate the corresponding rationales given the text inputs:

$$
\displaystyle\mathcal{L}=\mathcal{L}_{\mathrm{label}}+\lambda\mathcal{L}_{\mathrm{rationale}},
$$

(3)
where $\mathcal{L}_{\mathrm{label}}$ is the label prediction loss in Eq. 1 and $\mathcal{L}_{\mathrm{rationale}}$ is the rationale generation loss :

$$
\displaystyle\mathcal{L}_{\mathrm{rationale}}=\frac{1}{N}\sum_{i=1}^{N}\ell\big(f(x_{i}),\hat{r}_{i}\big).
$$

(4)
The rationale generation loss enables the model to learn to generate the intermediate reasoning steps for the prediction, and could therefore guide the model in better predicting the resultant label. This is our proposed Distilling step-by-step. Compared with Eq. 2, the rationale $\hat{r}_{i}$ is not required in the test time, which removes the need for an LLM at test-time.

---

### ✅ 문장 그룹 27

In this work, instead of using rationales as additional model inputs, we frame learning with rationales as a multi-task problem.

**단위 해석**
본 논문은 rationale을 **입력으로 넣지 않고**, **멀티태스크 학습 문제**로 정식화합니다.

**보조 설명**

* 입력 단순화 → **배포 시 LLM 불필요** 목표.

---

### ✅ 문장 그룹 28

Specifically, we train the model $f(x_{i})\rightarrow(\hat{y}_{i},\hat{r}_{i})$ to not only predict the task labels but also generate the corresponding rationales given the text inputs: $\mathcal{L}=\mathcal{L}_{\mathrm{label}}+\lambda\mathcal{L}_{\mathrm{rationale}}$. (3)

**단위 해석**
구체적으로, $f$는 입력 $x_i$로부터 **라벨 $\hat{y}_i$** 와 **rationale $\hat{r}_i$** 를 **동시에** 생성하도록 학습되며, 전체 손실은 (3)과 같습니다.

**보조 설명**

* $\lambda$: rationale 손실의 **가중치**.

---

### ✅ 문장 그룹 29

where $\mathcal{L}_{\mathrm{label}}$ is the label prediction loss in Eq. 1 and $\mathcal{L}_{\mathrm{rationale}}$ is the rationale generation loss : $\mathcal{L}_{\mathrm{rationale}}=\frac{1}{N}\sum_{i=1}^{N}\ell(f(x_{i}),\hat{r}_{i})$. (4)

**단위 해석**
여기서 $\mathcal{L}_{\mathrm{label}}$은 (1)의 라벨 손실, $\mathcal{L}_{\mathrm{rationale}}$은 **rationale 생성 손실**로 (4)와 같습니다.

**보조 설명**

* rationale 생성 자체를 **학습 신호**로 활용.

---

### ✅ 문장 그룹 30

The rationale generation loss enables the model to learn to generate the intermediate reasoning steps for the prediction, and could therefore guide the model in better predicting the resultant label.

**단위 해석**
rationale 생성 손실은 모델이 **중간 추론 단계**를 학습하도록 만들며, 그 결과 **라벨 예측의 정확도 향상**을 도울 수 있습니다.

**보조 설명**

* reasoning을 배우면 **정답도 잘 맞춘다**는 가설을 구현.

---

### ✅ 문장 그룹 31

This is our proposed Distilling step-by-step. Compared with Eq. 2, the rationale $\hat{r}_{i}$ is not required in the test time, which removes the need for an LLM at test-time.

**단위 해석**
이것이 우리가 제안하는 **Distilling step-by-step**입니다. (2)와 달리 **테스트 시 $\hat{r}_i$가 필요 없으므로**, **LLM이 불필요**해집니다.

**보조 설명**

* 실서비스 배포성 향상: **지연·비용** 절감.

---

## 📘 단락 본문 텍스트

We prepend “task prefixes” ( \[label] , \[rationale] ) to the input examples and train the smaller model to output $\hat{y}_{i}$ when \[label] is provided and to produce $\hat{r}_{i}$ with \[rationale] (Raffel et al., 2020).

---

### ✅ 문장 그룹 32

We prepend “task prefixes” ( \[label] , \[rationale] ) to the input examples and train the smaller model to output $\hat{y}_{i}$ when \[label] is provided and to produce $\hat{r}_{i}$ with \[rationale] (Raffel et al., 2020).

**단위 해석**
입력에 **태스크 프리픽스**( \[label], \[rationale] )를 앞에 붙여, \[label]이 주어지면 $\hat{y}_i$를, \[rationale]이 주어지면 $\hat{r}_i$를 출력하도록 작은 모델을 학습합니다.

**보조 설명**

* T5의 **prefix-tuning 스타일**과 유사한 작업 지시 방식.
* **하나의 모델**이 라벨 생성과 rationale 생성을 **모두 수행**.

---

여기까지 **3.2 Training smaller models with rationales**를 마쳤습니다.
이어서 **4 Experiments** 섹션으로 넘어가서 같은 방식으로 계속 진행할까요?
좋습니다! 요청하신 원문을 **생략 없이** 제시하고, 이어서 문장 그룹별 해석과 보조 설명을 제공하겠습니다. (수식은 LaTeX 수식으로 표현하고, (1)–(5)는 **수식 번호**로 표기합니다.)

---

# 📑 4 Experiments

---

## 📘 단락 본문 텍스트

We empirically validate the effectiveness of Distilling step-by-step. First, we show that when compared to standard finetuning and task distillation approaches, Distilling step-by-step achieves better performance with much fewer number of training examples, substantially improving the data efficiency to learn small task-specific models (Sec. 4.1). Second, we show that Distilling step-by-step surpasses the performance of LLMs with much smaller model size, drastically lowering the deployment cost compared to LLMs (Sec. 4.2). Third, we investigate the minimum resources required, w\.r.t. both number of training examples and model size, for Distilling step-by-step to outperform LLMs. We show that Distilling step-by-step outperforms LLMs by using less data and smaller model, simultaneously improving both data- and deployability-efficiency (Sec. 4.3). Finally, we perform ablation studies to understand the influence of different components and design choices in the Distilling step-by-step framework (Sec. 4.4).

---

### ✅ 문장 그룹 1

We empirically validate the effectiveness of Distilling step-by-step.

**단위 해석**
우리는 **Distilling step-by-step의 효과성을 실증적으로 검증**한다.

**보조 설명**

* 실험적 검증의 전체 목표를 선언.

---

### ✅ 문장 그룹 2

First, we show that when compared to standard finetuning and task distillation approaches, Distilling step-by-step achieves better performance with much fewer number of training examples, substantially improving the data efficiency to learn small task-specific models (Sec. 4.1).

**단위 해석**
첫째, 표준 finetuning 및 task distillation 접근법과 비교했을 때, Distilling step-by-step은 **훨씬 적은 학습 예시로 더 나은 성능**을 달성하여 작은 과제 특화 모델 학습의 **데이터 효율성**을 크게 향상시킨다 (4.1절).

---

### ✅ 문장 그룹 3

Second, we show that Distilling step-by-step surpasses the performance of LLMs with much smaller model size, drastically lowering the deployment cost compared to LLMs (Sec. 4.2).

**단위 해석**
둘째, Distilling step-by-step은 **훨씬 작은 모델 크기로도 LLM보다 뛰어난 성능**을 보여주며, 따라서 LLM 대비 배포 비용을 크게 낮춘다 (4.2절).

---

### ✅ 문장 그룹 4

Third, we investigate the minimum resources required, w\.r.t. both number of training examples and model size, for Distilling step-by-step to outperform LLMs.

**단위 해석**
셋째, Distilling step-by-step이 LLM을 능가하기 위해 필요한 최소 자원—**학습 예제 수와 모델 크기**—를 조사한다.

---

### ✅ 문장 그룹 5

We show that Distilling step-by-step outperforms LLMs by using less data and smaller model, simultaneously improving both data- and deployability-efficiency (Sec. 4.3).

**단위 해석**
우리는 Distilling step-by-step이 더 적은 데이터와 더 작은 모델을 사용해도 LLM을 능가함을 보여주며, 동시에 **데이터 효율성과 배포 효율성**을 개선한다 (4.3절).

---

### ✅ 문장 그룹 6

Finally, we perform ablation studies to understand the influence of different components and design choices in the Distilling step-by-step framework (Sec. 4.4).

**단위 해석**
마지막으로, Distilling step-by-step 프레임워크에서 **구성 요소와 설계 선택**이 미치는 영향을 이해하기 위해 ablation 연구를 수행한다 (4.4절).

---

## 📘 단락 본문 텍스트

**Setup.** In the experiments, we consider the 540B PaLM model (Chowdhery et al., 2022) as the LLM. For task-specific downstream models, we use T5 models (Raffel et al., 2020) where we initialize the models with pretrained weights obtained from publicly available sources. For CoT prompting, we follow Wei et al. (2022) when available, and curate our own examples for new datasets. We include more implementation details in Appendix A.1.

---

### ✅ 문장 그룹 7

**Setup.** In the experiments, we consider the 540B PaLM model (Chowdhery et al., 2022) as the LLM.

**단위 해석**
**실험 세팅.** 우리는 실험에서 **540B 파라미터 PaLM 모델**을 LLM으로 사용한다.

---

### ✅ 문장 그룹 8

For task-specific downstream models, we use T5 models (Raffel et al., 2020) where we initialize the models with pretrained weights obtained from publicly available sources.

**단위 해석**
다운스트림 과제용 모델로는 T5 모델을 사용하며, 공개된 사전학습 가중치로 초기화한다.

---

### ✅ 문장 그룹 9

For CoT prompting, we follow Wei et al. (2022) when available, and curate our own examples for new datasets.

**단위 해석**
CoT 프롬프트는 가능한 경우 Wei et al. (2022)을 따르고, 새로운 데이터셋에는 직접 예시를 작성(curate)한다.

---

### ✅ 문장 그룹 10

We include more implementation details in Appendix A.1.

**단위 해석**
구현 세부 사항은 부록 A.1에 추가로 제시한다.

---

## 📘 단락 본문 텍스트

**Datasets.** We conduct the experiments on 4 popular benchmark datasets across 3 different NLP tasks: e-SNLI (Camburu et al., 2018) and ANLI (Nie et al., 2020) for natural language inference; CQA (Talmor et al., 2019; Rajani et al., 2019) for commonsense question answering; SVAMP (Patel et al., 2021) for arithmetic math word problems. We include more dataset details in Appendix A.2.

---

### ✅ 문장 그룹 11

**Datasets.** We conduct the experiments on 4 popular benchmark datasets across 3 different NLP tasks: e-SNLI (Camburu et al., 2018) and ANLI (Nie et al., 2020) for natural language inference; CQA (Talmor et al., 2019; Rajani et al., 2019) for commonsense question answering; SVAMP (Patel et al., 2021) for arithmetic math word problems.

**단위 해석**
**데이터셋.** 우리는 3가지 NLP 태스크에서 4개의 널리 쓰이는 벤치마크 데이터셋으로 실험을 수행한다:

* 자연어 추론(NLI): e-SNLI, ANLI
* 상식적 질의응답: CQA
* 산술적 수학 단어문제: SVAMP

---

### ✅ 문장 그룹 12

We include more dataset details in Appendix A.2.

**단위 해석**
데이터셋에 대한 더 많은 세부 사항은 부록 A.2에 포함되어 있다.

---

👉 여기까지 **4 Experiments**의 개요, 세팅, 데이터셋 부분을 다뤘습니다.
이제 이어지는 **4.1 Reducing training data**부터 자세히 들어가야 하는데, 계속 진행할까요?
좋습니다! 이제 **4.1 Reducing training data** 부분을 원문 그대로 제시하고, 이어서 문장 그룹별 해석과 보조 설명을 드리겠습니다.

---

# 📑 4.1 Reducing training data

---

## 📘 단락 본문 텍스트

We compare Distilling step-by-step to two most common methods in learning task-specific models: (1) Standard finetuning when human-labeled examples are available, and (2) Standard task distillation when only unlabeled examples are available. Specifically, standard finetuning refers to the prevailing pretrain-then-finetune paradigm that finetunes a model with ground-truth labels via standard label supervision (Howard and Ruder, 2018). On the other hand, when only unlabeled examples are available, standard task distillation learns the task-specific model by treating a teacher LLM’s predicted labels as ground-truths (Hinton et al., 2015; Chen et al., 2020; Wang et al., 2021; Smith et al., 2022a; Arora et al., 2022).

---

### ✅ 문장 그룹 13

We compare Distilling step-by-step to two most common methods in learning task-specific models: (1) Standard finetuning when human-labeled examples are available, and (2) Standard task distillation when only unlabeled examples are available.

**단위 해석**
우리는 Distilling step-by-step을 두 가지 대표적인 과제 특화 모델 학습 방식과 비교한다:
(1) 사람이 라벨링한 예시가 있을 때의 **표준 finetuning**,
(2) 비라벨 데이터만 있을 때의 **표준 task distillation**.

**보조 설명**

* 비교 대상 = 전통적 두 가지 방법.
* 새로운 방법의 장점을 실험적으로 드러내려는 구조.

---

### ✅ 문장 그룹 14

Specifically, standard finetuning refers to the prevailing pretrain-then-finetune paradigm that finetunes a model with ground-truth labels via standard label supervision (Howard and Ruder, 2018).

**단위 해석**
구체적으로, 표준 finetuning은 **사전학습 후 finetuning** 패러다임을 말하며, 이는 모델을 **정답 라벨(ground-truth)** 로 감독 학습하는 것이다.

**보조 설명**

* "pretrain-then-finetune" → 대규모 사전학습 후 특정 태스크 데이터로 조정.
* NLP에서 가장 널리 쓰이는 접근법.

---

### ✅ 문장 그룹 15

On the other hand, when only unlabeled examples are available, standard task distillation learns the task-specific model by treating a teacher LLM’s predicted labels as ground-truths (Hinton et al., 2015; Chen et al., 2020; Wang et al., 2021; Smith et al., 2022a; Arora et al., 2022).

**단위 해석**
반대로, **라벨이 전혀 없을 경우** 표준 task distillation은 **교사 LLM이 예측한 라벨**을 정답으로 간주하여 과제 특화 모델을 학습한다.

**보조 설명**

* distillation은 사람이 라벨을 안 달아줘도 가능.
* 그러나 LLM 라벨은 noisy → 품질 저하 가능성.

---

## 📘 단락 본문 텍스트

In the following set of experiments, we fix the task-specific models to be 220M T5-Base models, and compare the task performances achieved by different methods under varying number of available training examples.

---

### ✅ 문장 그룹 16

In the following set of experiments, we fix the task-specific models to be 220M T5-Base models, and compare the task performances achieved by different methods under varying number of available training examples.

**단위 해석**
이후 실험에서는 **220M T5-Base 모델**을 과제 특화 모델로 고정하고, **가용 학습 예제 수를 달리하면서** 서로 다른 방법들의 성능을 비교한다.

**보조 설명**

* 비교 조건 = 같은 모델 크기, 다른 학습 데이터 양.
* 방법 간 **데이터 효율성** 차이가 드러날 예정.

---

## 📘 단락 본문 텍스트

**Distilling step-by-step outperforms standard finetuning with much less labeled examples.** When finetuned with human-labeled examples, Figure 4 shows that Distilling step-by-step consistently achieves better performance than standard finetuning across varying numbers of labeled examples used. Furthermore, we see that Distilling step-by-step can achieve the same performance as standard finetuning with much less labeled examples. In particular, by using only 12.5% of the full e-SNLI dataset, Distilling step-by-step can outperform standard finetuning trained with 100% of the full dataset. Similarly, we achieve 75%, 25%, and 20% reduction in training examples required to outperform standard finetuning on ANLI, CQA, and SVAMP respectively.

---

### ✅ 문장 그룹 17

**Distilling step-by-step outperforms standard finetuning with much less labeled examples.** When finetuned with human-labeled examples, Figure 4 shows that Distilling step-by-step consistently achieves better performance than standard finetuning across varying numbers of labeled examples used.

**단위 해석**
**라벨 데이터가 적어도 Distilling step-by-step은 표준 finetuning보다 뛰어나다.** Figure 4에 따르면, 인간 라벨 데이터를 사용했을 때 Distilling step-by-step은 사용된 라벨 수가 달라져도 항상 표준 finetuning보다 좋은 성능을 보인다.

---

### ✅ 문장 그룹 18

Furthermore, we see that Distilling step-by-step can achieve the same performance as standard finetuning with much less labeled examples.

**단위 해석**
게다가 Distilling step-by-step은 **훨씬 적은 라벨 예제만으로도** 표준 finetuning과 동일한 성능을 달성할 수 있다.

---

### ✅ 문장 그룹 19

In particular, by using only 12.5% of the full e-SNLI dataset, Distilling step-by-step can outperform standard finetuning trained with 100% of the full dataset.

**단위 해석**
특히, 전체 e-SNLI 데이터셋의 **12.5%만 사용해도** Distilling step-by-step은 **100% 데이터로 학습한 표준 finetuning**을 능가한다.

**보조 설명**

* 데이터 절감 효과를 수치로 강조.
* 약 **1/8 데이터로 전체 학습보다 우수**.

---

### ✅ 문장 그룹 20

Similarly, we achieve 75%, 25%, and 20% reduction in training examples required to outperform standard finetuning on ANLI, CQA, and SVAMP respectively.

**단위 해석**
마찬가지로, ANLI에서는 75%, CQA에서는 25%, SVAMP에서는 20%의 **데이터 절감**으로 표준 finetuning을 능가한다.

**보조 설명**

* 절감률은 과제별로 다르지만 모두 개선.

---

## 📘 단락 본문 텍스트

**Distilling step-by-step outperforms standard distillation with much less unlabeled examples.** When only unlabeled data is available, we compare Distilling step-by-step to standard task distillation. In Figure 5, we observe an overall similar trend to the finetuning setup. Specifically, we see that Distilling step-by-step outperforms standard task distillation on all 4 datasets under different numbers of unlabeled data used. We as well see that Distilling step-by-step requires much less unlabeled data to outperform standard task distillation. For instance, we need only 12.5% of the full unlabeled dataset to outperform the performance achieved by standard task distillation using 100% of the training examples on e-SNLI dataset.

---

### ✅ 문장 그룹 21

**Distilling step-by-step outperforms standard distillation with much less unlabeled examples.** When only unlabeled data is available, we compare Distilling step-by-step to standard task distillation.

**단위 해석**
**비라벨 데이터만 있을 때도 Distilling step-by-step은 표준 distillation을 능가한다.** 라벨 없는 데이터만 사용할 경우, 우리는 Distilling step-by-step을 표준 task distillation과 비교한다.

---

### ✅ 문장 그룹 22

In Figure 5, we observe an overall similar trend to the finetuning setup.

**단위 해석**
Figure 5에서 우리는 finetuning 경우와 **비슷한 추세**를 관찰한다.

---

### ✅ 문장 그룹 23

Specifically, we see that Distilling step-by-step outperforms standard task distillation on all 4 datasets under different numbers of unlabeled data used.

**단위 해석**
구체적으로, Distilling step-by-step은 **4개 데이터셋 모두**에서 unlabeled 데이터 수가 달라져도 **표준 distillation보다 우수**하다.

---

### ✅ 문장 그룹 24

We as well see that Distilling step-by-step requires much less unlabeled data to outperform standard task distillation.

**단위 해석**
또한 Distilling step-by-step은 **훨씬 적은 unlabeled 데이터**만으로도 표준 distillation을 능가한다.

---

### ✅ 문장 그룹 25

For instance, we need only 12.5% of the full unlabeled dataset to outperform the performance achieved by standard task distillation using 100% of the training examples on e-SNLI dataset.

**단위 해석**
예를 들어, e-SNLI 데이터셋에서는 전체 unlabeled 데이터의 **12.5%만 사용해도**, 전체 데이터(100%)로 학습한 표준 distillation 성능을 능가한다.

---

👉 여기까지 **4.1 Reducing training data** 부분을 정리했습니다.
다음은 **4.2 Reducing model size** 실험 결과 설명으로 이어지는데, 계속 진행할까요?
좋습니다! 이제 **4.2 Reducing model size** 부분을 원문 그대로 제시하고, 이어서 문장 그룹별 해석과 설명을 드리겠습니다.

---

# 📑 4.2 Reducing model size

---

## 📘 단락 본문 텍스트

In the following set of experiments, we hold the training set size fixed (using $100\%$ of the datasets), and compare varying sizes of small T5 models trained with Distilling step-by-step and standard approaches to LLMs. Specifically, we consider $3$ different sizes of T5 models, i.e., 220M T5-Base, 770M T5-Large, and 11B T5-XXL. For LLMs, we include two baseline methods: (1) Few-shot CoT (Wei et al., 2022), and (2) PINTO tuning (Wang et al., 2022a). Few-shot CoT directly utilizes CoT demonstrations to prompt the 540B PaLM to generate intermediate steps before predicting the final labels without any further finetuning of the LLM. PINTO tuning refers to our extension of Wang et al. (2022a) to handle tasks beyond question-answering, which are not studied by Wang et al. (2022a). Here, we finetune a 220M T5-Base model on top of the outputs generated from the PaLM model, which can be viewed as a finetuning method for LLMs with additional parameters (Zhang et al., 2020; Lester et al., 2021).

We present the experimental results under the two broad scenarios of having access to labeled datasets or unlabeled datasets in Figure 6 and Figure 7, respectively. We plot each method by their deployed model sizes for prediction (x-axis), and their corresponding task performances (y-axis).

---

### ✅ 문장 그룹 26

In the following set of experiments, we hold the training set size fixed (using $100\%$ of the datasets), and compare varying sizes of small T5 models trained with Distilling step-by-step and standard approaches to LLMs.

**단위 해석**
다음 실험에서는 **훈련 데이터셋 크기를 고정(100%)** 하고, Distilling step-by-step 및 표준 접근법으로 학습된 다양한 크기의 작은 T5 모델들을 LLM과 비교한다.

---

### ✅ 문장 그룹 27

Specifically, we consider $3$ different sizes of T5 models, i.e., 220M T5-Base, 770M T5-Large, and 11B T5-XXL.

**단위 해석**
구체적으로, 우리는 **3가지 T5 모델 크기**를 고려한다: 220M T5-Base, 770M T5-Large, 11B T5-XXL.

---

### ✅ 문장 그룹 28

For LLMs, we include two baseline methods: (1) Few-shot CoT (Wei et al., 2022), and (2) PINTO tuning (Wang et al., 2022a).

**단위 해석**
LLM 비교군으로는 두 가지 기준 방법을 포함한다: (1) Few-shot CoT, (2) PINTO tuning.

---

### ✅ 문장 그룹 29

Few-shot CoT directly utilizes CoT demonstrations to prompt the 540B PaLM to generate intermediate steps before predicting the final labels without any further finetuning of the LLM.

**단위 해석**
Few-shot CoT는 **540B PaLM**에 CoT 데모를 직접 입력하여 **추론 단계를 생성한 후 최종 라벨을 예측**하도록 하며, 추가적인 finetuning은 없다.

---

### ✅ 문장 그룹 30

PINTO tuning refers to our extension of Wang et al. (2022a) to handle tasks beyond question-answering, which are not studied by Wang et al. (2022a).

**단위 해석**
PINTO tuning은 Wang et al. (2022a)의 방식을 확장하여 **질의응답 이외의 태스크까지 처리**하도록 한 것이다.

---

### ✅ 문장 그룹 31

Here, we finetune a 220M T5-Base model on top of the outputs generated from the PaLM model, which can be viewed as a finetuning method for LLMs with additional parameters (Zhang et al., 2020; Lester et al., 2021).

**단위 해석**
여기서 우리는 PaLM이 생성한 출력 위에 **220M T5-Base 모델을 finetuning**하며, 이는 **LLM에 추가 파라미터를 붙여 finetuning하는 방식**으로 볼 수 있다.

---

## 📘 단락 본문 텍스트

We present the experimental results under the two broad scenarios of having access to labeled datasets or unlabeled datasets in Figure 6 and Figure 7, respectively. We plot each method by their deployed model sizes for prediction (x-axis), and their corresponding task performances (y-axis).

---

### ✅ 문장 그룹 32

We present the experimental results under the two broad scenarios of having access to labeled datasets or unlabeled datasets in Figure 6 and Figure 7, respectively.

**단위 해석**
실험 결과는 **라벨이 있는 경우**와 **라벨이 없는 경우** 두 가지 시나리오로 나누어 Figure 6과 Figure 7에 제시한다.

---

### ✅ 문장 그룹 33

We plot each method by their deployed model sizes for prediction (x-axis), and their corresponding task performances (y-axis).

**단위 해석**
각 방법은 **배포된 모델 크기(x축)** 와 **태스크 성능(y축)** 으로 시각화된다.

---

## 📘 단락 본문 텍스트

**Distilling step-by-step improves over standard baselines across varying model sizes used.** In Figure 6 and Figure 7 respectively, we see that Distilling step-by-step consistently improves over standard finetuning and standard distillation across all sizes of T5 models. The improvements are most pronounced on ANLI, where Distilling step-by-step outperforms standard finetuning and distillation by an average of $8\%$ and $13\%$ on task accuracy respectively.

---

### ✅ 문장 그룹 34

**Distilling step-by-step improves over standard baselines across varying model sizes used.** In Figure 6 and Figure 7 respectively, we see that Distilling step-by-step consistently improves over standard finetuning and standard distillation across all sizes of T5 models.

**단위 해석**
**모델 크기가 달라져도 Distilling step-by-step은 표준 기법보다 개선된다.** Figure 6과 7에서 보듯, Distilling step-by-step은 모든 크기의 T5 모델에서 표준 finetuning과 distillation보다 꾸준히 우수하다.

---

### ✅ 문장 그룹 35

The improvements are most pronounced on ANLI, where Distilling step-by-step outperforms standard finetuning and distillation by an average of $8\%$ and $13\%$ on task accuracy respectively.

**단위 해석**
가장 두드러진 개선은 ANLI에서 나타나며, 여기서 Distilling step-by-step은 표준 finetuning보다 평균 $8\%$, 표준 distillation보다 평균 $13\%$ 높은 정확도를 달성한다.

---

👉 여기까지가 **4.2 절의 첫 번째 결과 블록**입니다.
이어서 남은 부분 **“Distilling step-by-step outperforms LLMs by using much smaller task-specific models”** 부터 진행할까요?
좋습니다! 사용자가 주신 지시사항과 유의사항을 그대로 따라, 이어지는 **4.2 절의 두 번째 부분**부터 상세히 진행하겠습니다. (문장 번호는 이번 섹션 내에서 계속 이어갑니다. 즉, 앞에서 35번까지 했으니 이번엔 36번부터 시작합니다.)

---

## 📘 단락 본문 텍스트

**Distilling step-by-step outperforms LLMs by using much smaller task-specific models.** In Figure 6 when human-labeled datasets are available, Distilling step-by-step can always outperform Few-shot CoT and PINTO tuning on all 4 datasets considered, by using much smaller T5 models. For instance, we can achieve better performances than 540B PaLM model’s Few-shot CoT with 220M (over 2000× smaller) T5 model on e-SNLI, 770M (over 700× smaller) T5 models on ANLI and SVAMP, and 11B (over 45× smaller) T5 model on CQA. These results hold true even by further finetuning the 540B PaLM model on available labeled data with PINTO tuning.

---

### ✅ 문장 그룹 36

**Distilling step-by-step outperforms LLMs by using much smaller task-specific models.** In Figure 6 when human-labeled datasets are available, Distilling step-by-step can always outperform Few-shot CoT and PINTO tuning on all 4 datasets considered, by using much smaller T5 models.

**단위 해석**
**Distilling step-by-step은 훨씬 작은 task-specific 모델로도 LLM을 능가한다.** Figure 6에서, 사람이 라벨링한 데이터셋을 사용할 경우 Distilling step-by-step은 네 개의 모든 데이터셋에서 **Few-shot CoT**와 **PINTO tuning**을 항상 능가하며, 훨씬 작은 T5 모델로 이를 달성한다.

**보조 설명**

* 핵심 메시지: “작은 모델로도 큰 LLM을 이긴다.”
* LLM(540B)과 비교할 때 수천 배 작은 T5 모델이 outperform 가능.
* 라벨 데이터가 있을 때 이 현상이 뚜렷함.

---

### ✅ 문장 그룹 37

For instance, we can achieve better performances than 540B PaLM model’s Few-shot CoT with 220M (over 2000× smaller) T5 model on e-SNLI, 770M (over 700× smaller) T5 models on ANLI and SVAMP, and 11B (over 45× smaller) T5 model on CQA.

**단위 해석**
예를 들어, e-SNLI에서는 **220M T5 모델(2000배 이상 더 작음)** 만으로도 540B PaLM의 Few-shot CoT보다 더 나은 성능을 얻을 수 있다. ANLI와 SVAMP에서는 **770M T5 모델(700배 이상 더 작음)** 로, CQA에서는 **11B T5 모델(45배 이상 더 작음)** 로 PaLM Few-shot CoT보다 더 나은 성능을 달성할 수 있다.

**보조 설명**

* 데이터셋별 최소 모델 크기 조건이 다름.
* 데이터셋의 난이도와 특성에 따라 필요한 모델 크기가 달라짐.
* 그러나 공통적으로 LLM보다 훨씬 작은 모델이 더 나은 성능.

---

### ✅ 문장 그룹 38

These results hold true even by further finetuning the 540B PaLM model on available labeled data with PINTO tuning.

**단위 해석**
이 결과는 540B PaLM 모델을 라벨 데이터로 **추가 finetuning(PINTO tuning)** 하더라도 여전히 유효하다.

**보조 설명**

* PINTO tuning을 적용해도 PaLM은 작은 T5 모델에 못 미침.
* Distilling step-by-step의 **효율성 우위**를 강조.

---

## 📘 단락 본문 텍스트

In Figure 7, by only utilizing unlabeled examples, Distilling step-by-step also outperforms the teacher LLM on 3 out of 4 datasets. Specifically, Distilling step-by-step surpasses the 540B PaLM model’s Few-shot CoT performance by using 11B T5 with less than 3% of PaLM’s size. On SVAMP where the distilled model underperforms, we hypothesize that the performance gap is due to the relatively small number of data points in the dataset (i.e., 800). In reaction, we propose to augment the dataset with additional unlabeled examples to close the performance gap as shown in next.

---

### ✅ 문장 그룹 39

In Figure 7, by only utilizing unlabeled examples, Distilling step-by-step also outperforms the teacher LLM on 3 out of 4 datasets.

**단위 해석**
Figure 7에서, **unlabeled 데이터만 사용했을 때도** Distilling step-by-step은 4개 데이터셋 중 3개에서 교사 LLM보다 우수하다.

**보조 설명**

* 사람 라벨이 없어도 효과적임.
* 단순한 증류보다 나은 이유 = rationale 학습이 보조적 신호로 작용.

---

### ✅ 문장 그룹 40

Specifically, Distilling step-by-step surpasses the 540B PaLM model’s Few-shot CoT performance by using 11B T5 with less than 3% of PaLM’s size.

**단위 해석**
구체적으로, Distilling step-by-step은 **11B T5 모델(540B PaLM의 크기 대비 3% 미만)** 만으로도 540B PaLM Few-shot CoT 성능을 능가한다.

**보조 설명**

* 3% 크기의 모델이 성능을 초과 달성.
* 비용 절감, 배포 효율성 측면에서 큰 장점.

---

### ✅ 문장 그룹 41

On SVAMP where the distilled model underperforms, we hypothesize that the performance gap is due to the relatively small number of data points in the dataset (i.e., 800).

**단위 해석**
SVAMP에서는 distilled 모델 성능이 부족한데, 이는 데이터셋에 포함된 예시가 상대적으로 적기(약 800개) 때문이라고 가설을 세운다.

**보조 설명**

* 데이터 부족 문제 = 학습 성능 저하.
* rationale distillation도 데이터 크기에 의존.

---

### ✅ 문장 그룹 42

In reaction, we propose to augment the dataset with additional unlabeled examples to close the performance gap as shown in next.

**단위 해석**
이에 대응하여, 우리는 추가적인 **unlabeled 예시**로 데이터셋을 증강하여 성능 격차를 줄일 것을 제안하며, 이는 다음 부분에서 보여준다.

**보조 설명**

* 데이터 증강(data augmentation) 전략 제안.
* unlabeled 데이터가 많아지면 증류 효율성이 올라갈 것으로 기대.

---

👉 여기까지가 **4.2 절의 두 번째 결과 블록**이었습니다.
다음은 이어지는 **“Unlabeled data augmentation further improves Distilling step-by-step”** 단락부터 설명해야 합니다.
계속 이어서 진행할까요?
좋습니다! 이제 **4.2 절의 세 번째 블록: *Unlabeled data augmentation further improves Distilling step-by-step*** 부분부터 진행하겠습니다. (문장 번호는 이어서 43번부터 시작합니다.)

---

## 📘 단락 본문 텍스트

**Unlabeled data augmentation further improves Distilling step-by-step.** We augment the SVAMP training set with unlabeled examples from the ASDiv dataset (Miao et al., 2020). ASDiv dataset contains a total of 2,305 examples, where each example is a math word problem similar to the ones in SVAMP. In Figure 7 on SVAMP, we show the performances of Distilling step-by-step and standard task distillation using 11B T5 model after augmenting the training set with ASDiv. We see the data augmentation much improves the performance for both Distilling step-by-step and standard task distillation. However, even with the added unlabeled examples, standard task distillation still underperforms Few-shot CoT. On the other hand, Distilling step-by-step is able to much more efficiently exploit the value of the added examples to achieve the same performance level of Few-shot CoT, again, using a T5 model of size less than 3% of the 540B PaLM.

---

### ✅ 문장 그룹 43

**Unlabeled data augmentation further improves Distilling step-by-step.** We augment the SVAMP training set with unlabeled examples from the ASDiv dataset (Miao et al., 2020).

**단위 해석**
**Unlabeled 데이터 증강은 Distilling step-by-step 성능을 추가적으로 개선한다.** 우리는 SVAMP 학습 세트를 **ASDiv 데이터셋의 unlabeled 예시**로 증강한다.

**보조 설명**

* SVAMP의 데이터 부족 문제를 해결하기 위한 접근.
* ASDiv는 같은 유형(수학 문제)의 데이터셋.

---

### ✅ 문장 그룹 44

ASDiv dataset contains a total of 2,305 examples, where each example is a math word problem similar to the ones in SVAMP.

**단위 해석**
ASDiv 데이터셋은 총 **2,305개 예시**를 포함하며, 각 예시는 SVAMP와 유사한 **수학 서술형(word problem)** 문제이다.

**보조 설명**

* 데이터셋 유사성 확보 → transfer 효과 기대.
* 단순히 데이터 수 늘리기보다 질적 유사성이 중요.

---

### ✅ 문장 그룹 45

In Figure 7 on SVAMP, we show the performances of Distilling step-by-step and standard task distillation using 11B T5 model after augmenting the training set with ASDiv.

**단위 해석**
Figure 7의 SVAMP 결과에서, 우리는 **ASDiv로 증강된 학습 세트**를 사용하여 11B T5 모델로 학습한 **Distilling step-by-step**과 **표준 task distillation**의 성능을 보여준다.

**보조 설명**

* 데이터 증강 후 두 방법 모두 성능 개선.
* 그러나 성능 차이는 여전히 존재.

---

### ✅ 문장 그룹 46

We see the data augmentation much improves the performance for both Distilling step-by-step and standard task distillation.

**단위 해석**
데이터 증강은 Distilling step-by-step과 표준 task distillation 모두에서 성능을 크게 개선한다.

**보조 설명**

* unlabeled 데이터 활용 가치 확인.
* 소규모 데이터셋 문제를 보완하는 효과.

---

### ✅ 문장 그룹 47

However, even with the added unlabeled examples, standard task distillation still underperforms Few-shot CoT.

**단위 해석**
그러나 unlabeled 예시를 추가했음에도 불구하고, **표준 task distillation은 여전히 Few-shot CoT보다 성능이 낮다.**

**보조 설명**

* 단순 label 증류 접근의 한계.
* rationale 기반 학습의 차별성을 강조.

---

### ✅ 문장 그룹 48

On the other hand, Distilling step-by-step is able to much more efficiently exploit the value of the added examples to achieve the same performance level of Few-shot CoT, again, using a T5 model of size less than 3% of the 540B PaLM.

**단위 해석**
반면, Distilling step-by-step은 추가된 예시의 가치를 훨씬 더 효과적으로 활용하여 **Few-shot CoT와 동등한 성능 수준**에 도달할 수 있으며, 이때 사용한 T5 모델 크기는 **540B PaLM의 3% 미만**이다.

**보조 설명**

* 핵심: 작은 모델이 큰 LLM 성능에 도달.
* unlabeled augmentation을 효율적으로 활용하는 능력 차이.
* 배포 효율성과 학습 효율성 모두에서 탁월함.

---

👉 여기까지가 **4.2 절의 세 번째 블록** (*Unlabeled data augmentation further improves Distilling step-by-step*) 입니다.
다음은 **4.3 Outperforming LLMs using minimum model size and least training data** 부분으로 넘어갑니다.
계속 이어서 진행할까요?
좋습니다! 이제 **4.3 Outperforming LLMs using minimum model size and least training data** 부분을 이어서 진행하겠습니다. (문장 번호는 49번부터 시작합니다.)

---

# 📑 4.3 Outperforming LLMs using minimum model size and least training data

---

## 📘 단락 본문 텍스트

Here, using the LLM’s performance as an anchor point, we explore the most efficient resource requirements in terms of both number of training examples and deployed model size, that Distilling step-by-step and standard finetuning/distillation need to outperform the LLM. We present the results, again under human-labeled setting and unlabeled setting, in Figure 8 and Figure 9 respectively. We visualize the results by plotting different resultant models by (1) the number of training examples used (x-axis), (2) the final task performance achieved (y-axis), and (3) the size of the model (visualized by the size of the shaded area).

---

### ✅ 문장 그룹 49

Here, using the LLM’s performance as an anchor point, we explore the most efficient resource requirements in terms of both number of training examples and deployed model size, that Distilling step-by-step and standard finetuning/distillation need to outperform the LLM.

**단위 해석**
여기서는 LLM의 성능을 **기준점(anchor point)** 으로 삼아, Distilling step-by-step 및 표준 finetuning/증류가 LLM을 능가하기 위해 필요한 가장 효율적인 자원 요구 사항(훈련 예제 수와 배포 모델 크기 모두)을 탐구한다.

**보조 설명**

* 자원 효율성(Resource efficiency)에 대한 실험.
* “얼마나 작은 모델 + 얼마나 적은 데이터로 LLM을 이길 수 있나”를 확인.

---

### ✅ 문장 그룹 50

We present the results, again under human-labeled setting and unlabeled setting, in Figure 8 and Figure 9 respectively.

**단위 해석**
그 결과는 **사람 라벨 데이터셋**과 **unlabeled 데이터셋** 두 설정으로 나누어 Figure 8과 Figure 9에 각각 제시된다.

**보조 설명**

* 비교 조건은 앞 절과 동일 → 라벨 유무 기준.

---

### ✅ 문장 그룹 51

We visualize the results by plotting different resultant models by (1) the number of training examples used (x-axis), (2) the final task performance achieved (y-axis), and (3) the size of the model (visualized by the size of the shaded area).

**단위 해석**
결과는 다음 세 가지 축으로 시각화된다:
(1) 사용된 훈련 예제 수 (x축),
(2) 최종 태스크 성능 (y축),
(3) 모델 크기 (음영 영역의 크기로 표시).

**보조 설명**

* 버블 차트(bubble chart) 형식으로 결과를 표현했음을 시사.
* 직관적으로 **데이터량–성능–모델 크기**의 trade-off 확인 가능.

---

## 📘 단락 본문 텍스트

**Distilling step-by-step outperforms LLMs with much smaller models by using less data.** On all datasets in Figure 8, we see that Distilling step-by-step outperforms PaLM’s Few-shot CoT with much smaller T5 models using only a subset of the available training examples. Specifically, on e-SNLI, Distilling step-by-step can achieve better performance than Few-shot CoT with a model over 2000× smaller (220M T5) and only 0.1% of the full dataset. In Figure 9 where only unlabeled datasets are available, we observe the same trend that Distilling step-by-step can, at most time, outperform Few-shot CoT with smaller model as well as less data. For instance, on ANLI, Distilling step-by-step outperforms the LLM with a 45× smaller model and 50% of the full unlabeled set.

---

### ✅ 문장 그룹 52

**Distilling step-by-step outperforms LLMs with much smaller models by using less data.** On all datasets in Figure 8, we see that Distilling step-by-step outperforms PaLM’s Few-shot CoT with much smaller T5 models using only a subset of the available training examples.

**단위 해석**
**Distilling step-by-step은 훨씬 작은 모델과 더 적은 데이터만으로도 LLM을 능가한다.** Figure 8의 모든 데이터셋에서, Distilling step-by-step은 PaLM Few-shot CoT보다 작은 T5 모델과 적은 훈련 예제만으로도 더 나은 성능을 달성한다.

**보조 설명**

* 데이터 효율성과 파라미터 효율성 모두에서 우수.
* Distilling step-by-step의 가장 핵심적인 성과 중 하나.

---

### ✅ 문장 그룹 53

Specifically, on e-SNLI, Distilling step-by-step can achieve better performance than Few-shot CoT with a model over 2000× smaller (220M T5) and only 0.1% of the full dataset.

**단위 해석**
구체적으로, e-SNLI에서 Distilling step-by-step은 **2000배 이상 작은 모델(220M T5)** 과 전체 데이터셋의 **0.1%만 사용**하고도 Few-shot CoT보다 나은 성능을 달성한다.

**보조 설명**

* 데이터 효율성(data efficiency)의 극단적인 사례.
* LLM이 반드시 필요하지 않음을 보여주는 대표적 결과.

---

### ✅ 문장 그룹 54

In Figure 9 where only unlabeled datasets are available, we observe the same trend that Distilling step-by-step can, at most time, outperform Few-shot CoT with smaller model as well as less data.

**단위 해석**
Figure 9의 unlabeled 데이터셋 실험에서도 동일한 경향을 보인다. Distilling step-by-step은 대부분의 경우 작은 모델과 더 적은 데이터만으로도 Few-shot CoT를 능가한다.

**보조 설명**

* 라벨이 없어도 효과적이라는 점이 중요.
* rationale이 pseudo-label 학습의 한계를 극복하는 데 도움.

---

### ✅ 문장 그룹 55

For instance, on ANLI, Distilling step-by-step outperforms the LLM with a 45× smaller model and 50% of the full unlabeled set.

**단위 해석**
예를 들어, ANLI에서 Distilling step-by-step은 **45배 더 작은 모델**과 **unlabeled 데이터의 절반(50%)만**으로도 LLM을 능가한다.

**보조 설명**

* 데이터 절반만 사용했음에도 성능 초과.
* 모델 크기와 데이터량 두 축 모두에서 효율적.

---

👉 여기까지가 **4.3 절의 첫 번째 결과 블록**입니다.
다음은 이어지는 **“Standard finetuning and distillation require more data and larger model”** 단락부터 설명해야 합니다.
계속 이어서 진행할까요?
좋습니다! 이어서 **4.3 절 두 번째 블록: *Standard finetuning and distillation require more data and larger model*** 부분부터 진행하겠습니다. (문장 번호는 이어서 56번부터 시작합니다.)

---

## 📘 단락 본문 텍스트

**Standard finetuning and distillation require more data and larger model.** Finally, in Figure 8 and Figure 9, we see that standard finetuning and distillation often need either more data or larger models to match LLM’s performance. For instance, on e-SNLI in Figure 8, we observe that Distilling step-by-step outperform the LLM using only 0.1% of the dataset while standard finetuning requires more data to match the performance. Furthermore, on ANLI in Figure 8, we observe that Distilling step-by-step can outperform PaLM using 770M model with only 80% of the training set while standard finetuning struggles to match the LLM even using the full dataset and thus requires larger model to close the performance gap.

---

### ✅ 문장 그룹 56

**Standard finetuning and distillation require more data and larger model.** Finally, in Figure 8 and Figure 9, we see that standard finetuning and distillation often need either more data or larger models to match LLM’s performance.

**단위 해석**
**표준 finetuning과 distillation은 더 많은 데이터와 더 큰 모델을 필요로 한다.** Figure 8과 Figure 9에서 보듯, 표준 finetuning과 distillation은 LLM의 성능을 맞추기 위해 종종 더 많은 데이터나 더 큰 모델이 필요하다.

**보조 설명**

* 기존 접근법의 **비효율성**을 강조.
* rationale 기반 학습과 비교했을 때 대비가 분명해짐.

---

### ✅ 문장 그룹 57

For instance, on e-SNLI in Figure 8, we observe that Distilling step-by-step outperform the LLM using only 0.1% of the dataset while standard finetuning requires more data to match the performance.

**단위 해석**
예를 들어, Figure 8의 e-SNLI에서 Distilling step-by-step은 **데이터셋의 0.1%만 사용**하고도 LLM보다 우수한 성능을 보인다. 반면, 표준 finetuning은 같은 성능을 내기 위해 훨씬 더 많은 데이터가 필요하다.

**보조 설명**

* 극적인 데이터 효율성 차이 사례.
* 데이터 레이블링 비용을 줄일 수 있다는 실질적 장점.

---

### ✅ 문장 그룹 58

Furthermore, on ANLI in Figure 8, we observe that Distilling step-by-step can outperform PaLM using 770M model with only 80% of the training set while standard finetuning struggles to match the LLM even using the full dataset and thus requires larger model to close the performance gap.

**단위 해석**
게다가, Figure 8의 ANLI에서 Distilling step-by-step은 **770M 모델과 전체 학습 데이터의 80%만 사용**하고도 PaLM을 능가한다. 반면, 표준 finetuning은 **전체 데이터셋을 다 사용해도 LLM 성능을 따라잡지 못하며**, 결국 더 큰 모델이 필요하다.

**보조 설명**

* Distilling step-by-step은 **중간 크기 모델(770M)** 로도 충분.
* 표준 finetuning은 데이터와 모델 크기 모두에서 한계가 있음.
* rationale 학습의 장점이 다시 한번 부각됨.

---

👉 여기까지로 **4.3 절**이 완료되었습니다.
이제 다음은 **4.4 Further ablation studies** 부분으로 넘어가야 합니다.
계속 이어서 진행할까요?
좋습니다! 이제 마지막 부분인 **4.4 Further ablation studies** 를 진행하겠습니다. (문장 번호는 이어서 59번부터 시작합니다.)

---

# 📑 4.4 Further ablation studies

---

## 📘 단락 본문 텍스트

So far, we have focused on showing the effectiveness of Distilling step-by-step on reducing the training data required for finetuning or distilling smaller task-specific models. In this section, we perform further studies to understand the influence of different components in the Distilling step-by-step framework. Specifically, we study (1) how different LLMs, from which the rationales are extracted, affect the effectiveness of Distilling step-by-step, and (2) how the multi-task training approach compares to other potential design choices in training small task-specific models with LLM rationales. Here, we fix the small task-specific models to be 220M T5 models, and utilize 100% of the data on all datasets.

---

### ✅ 문장 그룹 59

So far, we have focused on showing the effectiveness of Distilling step-by-step on reducing the training data required for finetuning or distilling smaller task-specific models.

**단위 해석**
지금까지 우리는 Distilling step-by-step이 **소규모 task-specific 모델을 finetuning하거나 distillation하는 데 필요한 훈련 데이터 양을 줄이는 효과**에 초점을 맞춰왔다.

**보조 설명**

* 핵심 관심사 = 데이터 효율성.
* 지금까지의 섹션은 **실용적 효율성** 강조.

---

### ✅ 문장 그룹 60

In this section, we perform further studies to understand the influence of different components in the Distilling step-by-step framework.

**단위 해석**
이번 섹션에서는 Distilling step-by-step 프레임워크의 **다양한 구성 요소들이 어떤 영향을 주는지**를 이해하기 위해 추가 연구를 수행한다.

**보조 설명**

* “ablation study”의 목적: 어떤 요소가 성능에 기여하는지 분석.

---

### ✅ 문장 그룹 61

Specifically, we study (1) how different LLMs, from which the rationales are extracted, affect the effectiveness of Distilling step-by-step, and (2) how the multi-task training approach compares to other potential design choices in training small task-specific models with LLM rationales.

**단위 해석**
구체적으로, (1) rationales을 추출하는 **LLM의 종류 차이**가 Distilling step-by-step의 효과에 어떤 영향을 미치는지, (2) **multi-task 학습 접근법**이 다른 가능한 설계 선택지들과 비교하여 어떤 차이를 보이는지를 연구한다.

**보조 설명**

* 두 가지 주요 축: **LLM 선택 효과** vs **훈련 방식 설계 효과**.

---

### ✅ 문장 그룹 62

Here, we fix the small task-specific models to be 220M T5 models, and utilize 100% of the data on all datasets.

**단위 해석**
여기서는 소규모 task-specific 모델을 **220M T5 모델**로 고정하고, 모든 데이터셋에서 **100% 데이터**를 사용한다.

**보조 설명**

* ablation 연구는 변인을 통제해야 함 → 모델 크기와 데이터량 고정.

---

## 📘 단락 본문 텍스트

**Distilling step-by-step works with different sizes of decently trained LLMs.** In addition to using 540B PaLM as the LLM, here we consider a relatively smaller LLM, 20B GPT-NeoX model Black et al. (2022), from which we extract rationales for Distilling step-by-step. In Table 1, we see that when coupled with LLMs of different sizes, Distilling step-by-step can still provide performance improvements compared to standard finetuning. However, the performance lift is smaller when rationales are extracted from the 20B GPT-NeoX model instead of from the 540B PaLM. This can be due to the fact that the larger PaLM model provides higher-quality rationales that are more beneficial for learning the task.

---

### ✅ 문장 그룹 63

**Distilling step-by-step works with different sizes of decently trained LLMs.** In addition to using 540B PaLM as the LLM, here we consider a relatively smaller LLM, 20B GPT-NeoX model Black et al. (2022), from which we extract rationales for Distilling step-by-step.

**단위 해석**
**Distilling step-by-step은 다양한 크기의 충분히 훈련된 LLM에서도 동작한다.** 540B PaLM을 LLM으로 사용하는 것 외에도, 여기서는 상대적으로 작은 LLM인 **20B GPT-NeoX 모델 (Black et al., 2022)** 에서 rationale을 추출하여 Distilling step-by-step에 활용한다.

**보조 설명**

* LLM 규모 축소 시 성능 변화 측정.
* GPT-NeoX는 오픈소스 대형 모델.

---

### ✅ 문장 그룹 64

In Table 1, we see that when coupled with LLMs of different sizes, Distilling step-by-step can still provide performance improvements compared to standard finetuning.

**단위 해석**
Table 1에서 보듯, 다양한 크기의 LLM과 결합했을 때도 Distilling step-by-step은 표준 finetuning과 비교해 여전히 성능 향상을 제공한다.

**보조 설명**

* LLM 크기가 달라도 Distilling step-by-step의 상대적 우위는 유지.

---

### ✅ 문장 그룹 65

However, the performance lift is smaller when rationales are extracted from the 20B GPT-NeoX model instead of from the 540B PaLM.

**단위 해석**
그러나 540B PaLM 대신 **20B GPT-NeoX**에서 rationale을 추출하면, 성능 향상 폭은 더 작아진다.

**보조 설명**

* 작은 LLM은 rationale 품질이 떨어질 가능성이 큼.
* rationale 품질과 성능 사이에 직접적인 상관관계 존재.

---

### ✅ 문장 그룹 66

This can be due to the fact that the larger PaLM model provides higher-quality rationales that are more beneficial for learning the task.

**단위 해석**
이는 더 큰 PaLM 모델이 **더 고품질의 rationale**을 제공하고, 그것이 학습에 더 유리하기 때문일 수 있다.

**보조 설명**

* rationale의 품질이 학습 성능에 직접적 영향을 줌.
* rationale 기반 학습의 핵심 포인트: **설명 품질이 곧 성능 품질**.

---

👉 여기까지가 ablation의 첫 번째 연구 결과였습니다.
다음은 이어지는 **“Multi-task training is much more effective than single-task rationale and label joint prediction”** 단락부터 설명해야 합니다.
계속 이어서 진행할까요?
좋습니다! 안내하신 지시사항과 유의사항을 그대로 지켜, 이어지는 **4.4 Further ablation studies** 내 해당 단락을 처리하겠습니다.
(이 섹션에서는 이전 번호에서 **연속해** 문장 번호를 부여합니다: 앞서 66까지 사용했으므로 이번에는 **67번**부터 시작합니다.)

---

## 📘 단락 본문 텍스트

**Multi-task training is much more effective than single-task rationale and label joint prediction.** There are different possible ways to train task-specific models with LLM-rationales as output supervisions. One straightforward approach is to concatenate the rationale $\hat{r}_{i}$ and label $\hat{y}_{i}$ into a single sequence $[\hat{r}_{i},\hat{y}_{i}]$ and treat the entire sequence as the target output in training small models, as considered in Magister et al. ( 2022 ); Ho et al. ( 2022 ) :

$$
\displaystyle\mathcal{L}_{\mathrm{single}}=\frac{1}{N}\sum_{i=1}^{N}\ell\big(f(x_{i}),[\hat{r}_{i},\hat{y}_{i}]\big).
$$

(5)

---

### ✅ 문장 그룹 67

**Multi-task training is much more effective than single-task rationale and label joint prediction.** There are different possible ways to train task-specific models with LLM-rationales as output supervisions.

**단위 해석**
**멀티태스크 학습이 rationale·라벨을 하나로 묶어 예측하는 단일태스크(조인트) 방식보다 훨씬 효과적**이라는 주장으로 문단을 연다. LLM이 생성한 rationale을 **출력 감독 신호**로 활용해 과제 특화 모델을 훈련하는 방법은 여럿 있을 수 있음을 밝힌다.

**보조 설명**

* 여기서 “single-task joint prediction”은 “rationale + label”을 **하나의 시퀀스**로 만들어 **한 번에** 생성하도록 학습하는 접근.
* 저자들은 곧 이 **단일태스크 방식의 한계**를 보이고, 멀티태스크가 낫다고 결론내릴 것임.

---

### ✅ 문장 그룹 68

One straightforward approach is to concatenate the rationale $\hat{r}_{i}$ and label $\hat{y}_{i}$ into a single sequence $[\hat{r}_{i},\hat{y}_{i}]$ and treat the entire sequence as the target output in training small models, as considered in Magister et al. ( 2022 ); Ho et al. ( 2022 ).

**단위 해석**
가장 직관적인 접근은 **rationale $\hat{r}_i$** 와 **라벨 $\hat{y}_i$** 를 **단일 시퀀스 $[\hat{r}_i,\hat{y}_i]$** 로 연결해, 작은 모델 학습 시 **전체 시퀀스를 목표 출력**으로 두는 것이다(선행 연구 Magister et al., Ho et al. 참고).

**보조 설명**

* 입력은 $x_i$, 출력은 “rationale 다음 label” 형식의 단일 문자열.
* 구현은 간단하지만, **라벨 정확도를 해칠 위험**이 있음(뒤에서 설명).

---

### ✅ 문장 그룹 69

$$
\displaystyle\mathcal{L}_{\mathrm{single}}=\frac{1}{N}\sum_{i=1}^{N}\ell\big(f(x_{i}),[\hat{r}_{i},\hat{y}_{i}]\big).
$$

(5)

**단위 해석**
단일태스크 학습의 손실 함수 정의. 모델 $f(x_i)$의 출력과 **결합 타깃** $[\hat{r}_i,\hat{y}_i]$ 사이의 **크로스 엔트로피 손실**을 표본 평균으로 최소화한다. (식 번호 **(5)**)

**보조 설명**

* $\ell$: 시퀀스-투-시퀀스 토큰 수준 손실.
* rationale 토큰과 라벨 토큰이 **같은 목표 시퀀스**에 섞여 있어, **라벨 토큰에 대한 그라디언트가 약화/왜곡**될 수 있음.

---

## 📘 단락 본문 텍스트

In Table 2 , we compare this single-task training approach to our proposed multi-task training approach for utilizing LLM-rationales. We see that not only multi-task training consistently leads to better performance, single-task training with LLM-rationales can at times leads to worse performance than standard finetuning, e.g., on ANLI and CQA. In fact, similar results have also been observed in Wiegreffe et al. ( 2021 ); Magister et al. ( 2022 ); Ho et al. ( 2022 ) that simply treating rationale and label predictions as a single joint task may harm the model’s performance on label prediction. This validates our use of the multi-task training approach, and highlights the need to treat the rationales carefully so as to unleash their actual benefits.

---

### ✅ 문장 그룹 70

In Table 2 , we compare this single-task training approach to our proposed multi-task training approach for utilizing LLM-rationales.

**단위 해석**
Table 2에서, 저자들은 이 **단일태스크 방식**과 자신들이 제안한 **멀티태스크 방식**(LLM-rationale 활용)을 비교한다.

**보조 설명**

* 공정 비교: 동일 모델/데이터 조건에서 **학습 방식만** 달리해 성능 비교.

---

### ✅ 문장 그룹 71

We see that not only multi-task training consistently leads to better performance, single-task training with LLM-rationales can at times leads to worse performance than standard finetuning, e.g., on ANLI and CQA.

**단위 해석**
멀티태스크 학습은 **꾸준히 더 나은 성능**을 보일 뿐 아니라, 단일태스크(조인트) 방식은 때때로 **표준 finetuning보다도 성능이 떨어질 수 있음**을(예: ANLI, CQA) 관찰했다.

**보조 설명**

* 조인트 방식은 rationale 생성 목표가 **라벨 학습을 방해**할 수 있음(출력 공간이 길어지고 최적화가 어려워짐).
* 멀티태스크는 \*\*\[label] / \[rationale]\*\*로 **목표를 분리**해 간섭을 줄임.

---

### ✅ 문장 그룹 72

In fact, similar results have also been observed in Wiegreffe et al. ( 2021 ); Magister et al. ( 2022 ); Ho et al. ( 2022 ) that simply treating rationale and label predictions as a single joint task may harm the model’s performance on label prediction.

**단위 해석**
실제로, **rationale과 라벨 예측을 하나의 조인트 태스크로 취급**하면 **라벨 예측 성능을 해칠 수 있다**는 유사한 결과가 여러 선행 연구에서도 보고되었다.

**보조 설명**

* 독립 재현 근거 제시: 주장에 대한 **문헌적 일치** 확보.
* 원인 가설: 학습 신호의 **경합(conflict)**, 디코더 **노출편향** 증가 등.

---

### ✅ 문장 그룹 73

This validates our use of the multi-task training approach, and highlights the need to treat the rationales carefully so as to unleash their actual benefits.

**단위 해석**
이는 **멀티태스크 학습**을 택한 저자들의 결정이 타당함을 뒷받침하며, rationale의 실제 이점을 **최대로 끌어내려면** 이를 **신중히 다루어야 함**을 강조한다.

**보조 설명**

* 실무 팁: 출력 헤드/프리픽스 분리, 손실 가중치 $\lambda$ 튜닝, **라벨 우선 최적화** 전략 등이 유효.
* 핵심 메시지: rationale은 **입력**이 아니라 **보조적 출력(학습 신호)** 로 쓰는 편이 실전 성능에 더 좋다.

---

여기까지 **4.4 Further ablation studies**의 마지막 단락까지 정리했습니다.
이제 다음 섹션 **5 Discussion**으로 넘어갈까요?
