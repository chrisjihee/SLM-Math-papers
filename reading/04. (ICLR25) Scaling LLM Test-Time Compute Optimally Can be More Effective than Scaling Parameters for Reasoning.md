# 0 Abstract

## 📘 단락 0-1
Enabling LLMs to improve their outputs by using more test-time compute is a critical step towards building self-improving agents that can operate on open-ended natural language. In this paper, we scale up inference-time computation in LLMs, with a focus on answering: if an LLM is allowed to use a fixed but non-trivial amount of inference-time compute, how much can it improve its performance on a challenging prompt? Answering this question has implications not only on performance, but also on the future of LLM pretraining and how to tradeoff inference-time and pre-training compute. Little research has attempted to understand the scaling behaviors of test-time inference methods, with current work largely providing negative results for a number of these strategies. In this work, we analyze two primary mechanisms to scale test-time computation: (1) searching against dense, process-based verifier reward models (PRMs); and (2) updating the model's distribution over a response adaptively, given the prompt at test time. We find that in both cases, the effectiveness of different approaches to scaling test-time compute critically varies depending on the difficulty of the prompt. This observation motivates applying a "compute-optimal" scaling strategy, which acts to, as effectively as possible, allocate test-time compute per prompt in an adaptive manner. Using this compute-optimal strategy, we can improve the efficiency of test-time compute scaling for math reasoning problems by more than 4x compared to a best-of-N baseline. Additionally, in a FLOPs-matched evaluation, we find that on problems where a smaller base model attains somewhat non-trivial success rates, test-time compute can be used to outperform a 14x larger model.

---

### ✅ 문장 0-1-1
Enabling LLMs to improve their outputs by using more test-time compute is a critical step towards building self-improving agents that can operate on open-ended natural language.

**🔍 단위 해석**
LLM이 더 많은 '테스트 시간 연산(test-time compute)'을 사용하여 출력 품질을 개선하도록 하는 것은, 개방형 자연어를 다룰 수 있는 '자기 개선 에이전트(self-improving agents)'를 구축하는 데 있어 매우 중요한 단계입니다.

**💡 보조 설명**
* **[Test-time compute]:** 모델의 학습(Training) 단계가 아닌, 실제 입력에 대해 답을 생성하는 추론(Inference) 단계에서 추가적인 계산 자원(시간, 메모리 등)을 투입하여 성능을 높이는 기술을 의미합니다.
* **[Self-improving agents]:** 스스로 오류를 수정하거나 더 나은 답을 찾아내어 성능을 향상시키는 AI 에이전트입니다.

---

### ✅ 문장 0-1-2
In this paper, we scale up inference-time computation in LLMs, with a focus on answering: if an LLM is allowed to use a fixed but non-trivial amount of inference-time compute, how much can it improve its performance on a challenging prompt?

**🔍 단위 해석**
본 논문에서는 LLM의 추론 시간 연산을 확장(scale up)하며, 다음 질문에 답하는 데 초점을 맞춥니다: "만약 LLM이 고정되어 있지만 적지 않은(non-trivial) 양의 추론 연산을 사용할 수 있다면, 어려운 프롬프트에 대한 성능을 얼마나 향상시킬 수 있는가?"

---

### ✅ 문장 0-1-3
Answering this question has implications not only on performance, but also on the future of LLM pretraining and how to tradeoff inference-time and pre-training compute.

**🔍 단위 해석**
이 질문에 대한 답은 단순한 성능뿐만 아니라, LLM 사전 학습(Pretraining)의 미래와 '추론 시간 연산' 대 '사전 학습 연산' 간의 트레이드오프(tradeoff)를 어떻게 조절할지에 대한 시사점을 줍니다.

**💡 보조 설명**
* **[Tradeoff]:** 한정된 컴퓨팅 자원을 모델을 더 크게 만드는 데(Pretraining) 쓸지, 아니면 작은 모델로 추론 시 더 많이 생각하게 하는 데(Inference-time) 쓸지 결정하는 균형점을 의미합니다.

---

### ✅ 문장 0-1-4
Little research has attempted to understand the scaling behaviors of test-time inference methods, with current work largely providing negative results for a number of these strategies.

**🔍 단위 해석**
테스트 시간 추론 방법론의 스케일링 거동(scaling behaviors)을 이해하려는 연구는 거의 없었으며, 현재의 연구들은 대부분 이러한 전략들에 대해 부정적인 결과를 보여주고 있습니다.

---

### ✅ 문장 0-1-5
In this work, we analyze two primary mechanisms to scale test-time computation: (1) searching against dense, process-based verifier reward models (PRMs); and (2) updating the model's distribution over a response adaptively, given the prompt at test time.

**🔍 단위 해석**
본 연구에서는 테스트 시간 연산을 확장하기 위한 두 가지 주요 메커니즘을 분석합니다: (1) 밀집된(dense) 프로세스 기반 검증 보상 모델(PRM)을 활용한 탐색(search), 그리고 (2) 테스트 시간에 주어진 프롬프트에 맞춰 모델의 응답 분포를 적응적으로 업데이트하는 방법입니다.

**💡 보조 설명**
* **[PRM (Process-based Reward Models)]:** 최종 결과만 평가하는 것이 아니라, 풀이 과정의 각 단계(step-by-step)를 평가하여 보상을 주는 모델입니다.
* **[Mechanism (2)]:** 주로 모델이 스스로 답변을 수정(Revision)하거나 개선하는 방식을 의미합니다.

---

### ✅ 문장 0-1-6
We find that in both cases, the effectiveness of different approaches to scaling test-time compute critically varies depending on the difficulty of the prompt.

**🔍 단위 해석**
우리는 두 경우 모두, 테스트 시간 연산을 확장하는 다양한 접근 방식의 효율성이 프롬프트의 '난이도(difficulty)'에 따라 결정적으로 달라진다는 것을 발견했습니다.

---

### ✅ 문장 0-1-7
This observation motivates applying a "compute-optimal" scaling strategy, which acts to, as effectively as possible, allocate test-time compute per prompt in an adaptive manner.

**🔍 단위 해석**
이러한 관찰은 "연산 최적화(compute-optimal)" 스케일링 전략의 적용을 유도하며, 이는 각 프롬프트에 대해 가능한 한 효과적으로 테스트 시간 연산을 적응적으로 할당하는 역할을 합니다.

**💡 보조 설명**
* **[Compute-optimal strategy]:** 문제의 난이도에 따라 '탐색(Search)'을 할지 '수정(Revision)'을 할지, 혹은 연산량을 얼마나 투입할지 동적으로 결정하는 전략입니다.

---

### ✅ 문장 0-1-8
Using this compute-optimal strategy, we can improve the efficiency of test-time compute scaling for math reasoning problems by more than 4x compared to a best-of-N baseline.

**🔍 단위 해석**
이 연산 최적화 전략을 사용함으로써, 우리는 수학 추론 문제에서 'Best-of-N' 베이스라인과 비교하여 테스트 시간 연산 스케일링의 효율성을 4배 이상 향상시킬 수 있습니다.

**💡 보조 설명**
* **[Best-of-N]:** 동일한 프롬프트에 대해 N개의 답변을 생성한 뒤, 가장 높은 점수를 받은 답변을 선택하는 단순한 방법입니다. 본 논문은 이보다 4배 적은 연산으로 동등한 성능을 냄을 주장합니다.

---

### ✅ 문장 0-1-9
Additionally, in a FLOPs-matched evaluation, we find that on problems where a smaller base model attains somewhat non-trivial success rates, test-time compute can be used to outperform a 14x larger model.

**🔍 단위 해석**
또한, FLOPs(연산량)를 일치시킨 평가에서, 작은 베이스 모델이 어느 정도 유의미한 성공률을 보이는 문제들의 경우, 테스트 시간 연산을 활용하면 14배 더 큰 모델보다 더 나은 성능을 낼 수 있음을 확인했습니다.

**💡 보조 설명**
* **[FLOPs-matched]:** 공정한 비교를 위해 총 연산량(Floating Point Operations)을 동일하게 맞춘 상태에서 비교하는 것입니다. 즉, "작은 모델 + 많은 생각" vs "큰 모델 + 즉각적인 답"의 대결입니다.

---

# 1 INTRODUCTION

## 📘 단락 1-1
Given a challenging input, can we enable LLMs to most effectively make use of additional computation at test-time to improve their responses? In theory, additional test-time compute should enable an LLM to do better than what it was trained to do in zero-shot. Such a capability at test-time bears the potential to unlock agentic and reasoning abilities. Additionally, if pre-trained model size can be traded off for additional inference compute, this would enable the deployment of smaller on-device models in place of datacenter scale LLMs. Automating the inference-time improvement of model outputs also provides a path towards a general self-improvement algorithm that can function with reduced human supervision.

---

### ✅ 문장 1-1-1
Given a challenging input, can we enable LLMs to most effectively make use of additional computation at test-time to improve their responses?

**🔍 단위 해석**
어려운 입력이 주어졌을 때, 우리는 LLM이 응답을 개선하기 위해 테스트 시간에 추가적인 연산을 가장 효과적으로 활용하도록 할 수 있을까요?

---

### ✅ 문장 1-1-2
In theory, additional test-time compute should enable an LLM to do better than what it was trained to do in zero-shot.

**🔍 단위 해석**
이론적으로, 추가적인 테스트 시간 연산은 LLM이 제로샷(zero-shot, 추가 학습 없이 바로 답하기)으로 수행하도록 훈련된 것보다 더 나은 성능을 낼 수 있게 해야 합니다.

---

### ✅ 문장 1-1-3
Such a capability at test-time bears the potential to unlock agentic and reasoning abilities.

**🔍 단위 해석**
테스트 시간에서의 이러한 능력은 에이전트적 능력(agentic abilities)과 추론 능력(reasoning abilities)을 잠금 해제할 잠재력을 지니고 있습니다.

---

### ✅ 문장 1-1-4
Additionally, if pre-trained model size can be traded off for additional inference compute, this would enable the deployment of smaller on-device models in place of datacenter scale LLMs.

**🔍 단위 해석**
게다가, 만약 사전 학습된 모델의 크기를 추가적인 추론 연산과 교환(trade-off)할 수 있다면, 이는 데이터센터 규모의 거대 LLM 대신 더 작은 온디바이스(on-device) 모델을 배포하는 것을 가능하게 할 것입니다.

---

### ✅ 문장 1-1-5
Automating the inference-time improvement of model outputs also provides a path towards a general self-improvement algorithm that can function with reduced human supervision.

**🔍 단위 해석**
모델 출력의 추론 시간 개선을 자동화하는 것은 또한 인간의 감독을 줄이면서 작동할 수 있는 일반적인 '자기 개선 알고리즘'으로 나아가는 길을 제공합니다.

---

## 📘 단락 1-2
Prior work studying inference-time computation provides mixed results. On the one hand, some prior work shows that current LLMs can use test-time computation to improve their outputs, on the other hand, several other works show that the effectiveness of these methods on complex tasks such as math reasoning remains limited. However, reasoning is a domain we should expect to benefit from test-time compute, since reasoning involves drawing inferences from existing knowledge as opposed to acquiring new knowledge. Therefore, the disagreement in these prior findings motivates the need for a systematic analysis of different approaches for scaling test-time compute.

---

### ✅ 문장 1-2-1
Prior work studying inference-time computation provides mixed results.

**🔍 단위 해석**
추론 시간 연산을 연구한 기존 연구들은 엇갈린 결과(mixed results)를 내놓고 있습니다.

---

### ✅ 문장 1-2-2
On the one hand, some prior work shows that current LLMs can use test-time computation to improve their outputs, on the other hand, several other works show that the effectiveness of these methods on complex tasks such as math reasoning remains limited.

**🔍 단위 해석**
한편으로는 현재의 LLM들이 테스트 시간 연산을 통해 출력을 개선할 수 있음을 보여주는 연구가 있는 반면, 다른 한편으로는 수학 추론과 같은 복잡한 과제에서는 이러한 방법들의 효과가 제한적임을 보여주는 연구들도 있습니다.

---

### ✅ 문장 1-2-3
However, reasoning is a domain we should expect to benefit from test-time compute, since reasoning involves drawing inferences from existing knowledge as opposed to acquiring new knowledge.

**🔍 단위 해석**
그러나 '추론(reasoning)'은 새로운 지식을 습득하는 것이 아니라 기존 지식으로부터 논리적 결론을 도출하는 것이므로, 테스트 시간 연산의 혜택을 볼 것으로 기대해야 하는 영역입니다.

---

### ✅ 문장 1-2-4
Therefore, the disagreement in these prior findings motivates the need for a systematic analysis of different approaches for scaling test-time compute.

**🔍 단위 해석**
따라서, 이러한 기존 연구 결과들의 불일치는 테스트 시간 연산을 확장하는 다양한 접근 방식에 대한 체계적인 분석의 필요성을 제기합니다.

---

## 📘 단락 1-3
In this paper we understand the pros and cons of scaling up test-time compute, and how it compares with scaling up pre-training compute. While the simplest approach for scaling test-time compute is best-of-N sampling — sampling N outputs in "parallel" from a base LLM and selecting the one that scores the highest per a learned verifier or a reward model — there are many other ways we could conceivably scale up test-time compute. We unify methods into those that modify either the proposal distribution from which responses are sampled (for e.g., by asking the base model to revise its responses) or those that alter how the verifier is used for searching directly in the output space (e.g. by training a PRM).

---

### ✅ 문장 1-3-1
In this paper we understand the pros and cons of scaling up test-time compute, and how it compares with scaling up pre-training compute.

**🔍 단위 해석**
본 논문에서 우리는 테스트 시간 연산 확장의 장단점을 이해하고, 이것이 사전 학습(pre-training) 연산 확장과 어떻게 비교되는지 파악합니다.

---

### ✅ 문장 1-3-2
While the simplest approach for scaling test-time compute is best-of-N sampling — sampling N outputs in "parallel" from a base LLM and selecting the one that scores the highest per a learned verifier or a reward model — there are many other ways we could conceivably scale up test-time compute.

**🔍 단위 해석**
테스트 시간 연산을 확장하는 가장 간단한 방법은 'Best-of-N 샘플링'(베이스 LLM에서 병렬로 N개의 출력을 샘플링하고 학습된 검증기나 보상 모델을 통해 가장 높은 점수의 답을 선택하는 것)이지만, 그 외에도 우리가 상상할 수 있는 확장 방법은 많이 있습니다.

**💡 보조 설명**
* **[Best-of-N sampling]:** 여러 개의 후보 답안을 생성한 뒤 가장 좋은 것을 고르는 기법입니다. (PDF Figure 2 좌측 참조)
* **[PDF 참조]:** Figure 2의 'Best-of-N' 다이어그램은 질문에서 여러 개의 답변 경로가 병렬로 뻗어 나가고, 그중 하나를 선택(Select)하는 과정을 시각화하고 있습니다.

---

### ✅ 문장 1-3-3
We unify methods into those that modify either the proposal distribution from which responses are sampled (for e.g., by asking the base model to revise its responses) or those that alter how the verifier is used for searching directly in the output space (e.g. by training a PRM).

**🔍 단위 해석**
우리는 방법론들을 '응답이 샘플링되는 제안 분포(proposal distribution)를 수정하는 방식'(예: 베이스 모델에게 응답을 수정하도록 요청)과 '검증기(verifier)가 출력 공간을 직접 탐색하는 방식을 변경하는 방식'(예: PRM 학습)으로 통합하여 분류합니다.

**💡 보조 설명**
* **[Proposal Distribution]:** 모델이 다음에 어떤 토큰(단어)을 내뱉을지에 대한 확률 분포입니다. '수정(Revision)'을 요청하면 이 분포가 바뀝니다.
* **[Verifier/PRM]:** 생성된 답안을 평가하는 모델입니다. 이를 활용해 트리 탐색(Tree Search) 등을 수행할 수 있습니다.

---

## 📘 단락 1-4
To understand the benefits of scaling up test-time compute, we carry out experiments on MATH using PaLM-2 models fine-tuned to either revise incorrect answers (e.g. improving the proposal distribution; Section 6) or verify the correctness of individual steps in an answer using a process-based reward model (PRM) (Section 5). We find that the efficacy of different test-time strategies depends on both the nature of the specific problem at hand and the base LLM used. For example, on "easier" problems, for which the base LLM can already produce reasonable-looking responses, allowing the model to sequentially revise its initial answer (i.e., modifying the proposal distribution) is a more effective use of compute than reranking N independent answers sampled in parallel. On the other hand, on difficult problems which require searching over many high-level strategies, re-sampling new responses independently in parallel or deploying tree-search against a process reward model is more effective.

---

### ✅ 문장 1-4-1
To understand the benefits of scaling up test-time compute, we carry out experiments on MATH using PaLM-2 models fine-tuned to either revise incorrect answers (e.g. improving the proposal distribution; Section 6) or verify the correctness of individual steps in an answer using a process-based reward model (PRM) (Section 5).

**🔍 단위 해석**
테스트 시간 연산 확장의 이점을 이해하기 위해, 우리는 오답을 수정하도록 미세 조정된(제안 분포 개선) PaLM-2 모델이나, 답변의 각 단계별 정확성을 검증하는 프로세스 기반 보상 모델(PRM)을 사용하여 MATH 데이터셋에 대한 실험을 수행합니다.

---

### ✅ 문장 1-4-2
We find that the efficacy of different test-time strategies depends on both the nature of the specific problem at hand and the base LLM used.

**🔍 단위 해석**
우리는 다양한 테스트 시간 전략의 효용성이 당면한 특정 문제의 본질과 사용된 베이스 LLM에 따라 달라진다는 것을 발견했습니다.

---

### ✅ 문장 1-4-3
For example, on "easier" problems, for which the base LLM can already produce reasonable-looking responses, allowing the model to sequentially revise its initial answer (i.e., modifying the proposal distribution) is a more effective use of compute than reranking N independent answers sampled in parallel.

**🔍 단위 해석**
예를 들어, 베이스 LLM이 이미 그럴듯한 응답을 생성할 수 있는 "더 쉬운" 문제들의 경우, 초기 답변을 순차적으로 수정(revise)하도록 하는 것(즉, 제안 분포를 수정하는 것)이 N개의 독립적인 답변을 병렬로 샘플링하여 재순위(reranking) 매기는 것보다 더 효율적인 연산 활용법입니다.

**💡 보조 설명**
* **[Sequential Revision]:** 한 번 답을 내고 끝내는 것이 아니라, "이게 맞나? 다시 고쳐보자"를 반복하는 방식입니다. 쉬운 문제에서 실수를 줄이는 데 효과적입니다.

---

### ✅ 문장 1-4-4
On the other hand, on difficult problems which require searching over many high-level strategies, re-sampling new responses independently in parallel or deploying tree-search against a process reward model is more effective.

**🔍 단위 해석**
반면, 다양한 고차원적 전략을 탐색해야 하는 "어려운" 문제들의 경우, 새로운 응답을 병렬로 독립적으로 재샘플링하거나 프로세스 보상 모델(PRM)을 활용한 트리 탐색(tree-search)을 배포하는 것이 더 효과적입니다.

---

## 📘 단락 1-5
This underscores the need to deploy an adaptive "compute-optimal" strategy for scaling test-time compute, wherein the specific approach for utilizing test-time compute is selected depending on the prompt, so as to make the best use of additional computation. We also show that a notion of question difficulty (Section 4) from the perspective of the base LLM can be used to predict the efficacy of test-time computation, enabling us to practically instantiate this "compute-optimal" scaling. By appropriately allocating test-time compute in this way, we are able to greatly improve test-time compute scaling, surpassing the performance of a best-of-N baseline while only using ~ 4x less computation with both revisions and search.

---

### ✅ 문장 1-5-1
This underscores the need to deploy an adaptive "compute-optimal" strategy for scaling test-time compute, wherein the specific approach for utilizing test-time compute is selected depending on the prompt, so as to make the best use of additional computation.

**🔍 단위 해석**
이는 테스트 시간 연산 확장을 위해 적응형 "연산 최적화(compute-optimal)" 전략을 배포할 필요성을 강조하며, 여기서 추가적인 연산을 최적으로 활용하기 위해 프롬프트에 따라 구체적인 연산 활용 접근법이 선택됩니다.

---

### ✅ 문장 1-5-2
We also show that a notion of question difficulty (Section 4) from the perspective of the base LLM can be used to predict the efficacy of test-time computation, enabling us to practically instantiate this "compute-optimal" scaling.

**🔍 단위 해석**
우리는 또한 베이스 LLM 관점에서의 '문제 난이도(question difficulty)' 개념(섹션 4)이 테스트 시간 연산의 효율성을 예측하는 데 사용될 수 있음을 보여주며, 이를 통해 "연산 최적화" 스케일링을 실제로 구현할 수 있게 합니다.

---

### ✅ 문장 1-5-3
By appropriately allocating test-time compute in this way, we are able to greatly improve test-time compute scaling, surpassing the performance of a best-of-N baseline while only using ~ 4x less computation with both revisions and search.

**🔍 단위 해석**
이러한 방식으로 테스트 시간 연산을 적절히 할당함으로써, 우리는 수정(revisions)과 탐색(search) 모두에서 약 4배 더 적은 연산을 사용하면서도 Best-of-N 베이스라인의 성능을 능가하는 등 연산 스케일링 효율을 크게 개선할 수 있었습니다.

**💡 보조 설명**
* **[PDF 참조]:** Figure 1 (Left) 그래프는 'Compute Optimal' 전략(파란색 선)이 'Best-of-N'(빨간 점선)보다 훨씬 가파르게 성능이 향상됨을 보여줍니다. 특히 적은 예산(x축의 앞부분)에서 더 높은 정확도(y축)를 달성합니다.

---

# 2 UNIFIED PERSPECTIVE ON TEST-TIME COMPUTE: PROPOSER & VERIFIER

## 📘 단락 2-1
We first provide an unified abstraction of test-time compute to situate contemporary approaches. We view the use of test-time compute through the lens of modifying the model's distribution on a given prompt, adaptively at test-time. Ideally, test-time compute should allow for the ability to express more complex distributions than naïvely sampling from the LLM. In general, there are two knobs to modify an LLM's distribution: (1) at the input level: by augmenting the given prompt with an additional set of tokens that the LLM conditions on to obtain a modified proposal distribution, or (2) at the output level: by sampling multiple candidates from the standard LLM and performing surgery on them, using some post-hoc verifiers or scorers. This process is reminiscent of Markov chain Monte Carlo (MCMC) sampling from a complex distribution by combining a simple proposal distribution and a score function. Modifying the proposal distribution by altering inputs tokens and using a verifier form the two independent axes of our study.

---

### ✅ 문장 2-1-1
We first provide an unified abstraction of test-time compute to situate contemporary approaches.

**🔍 단위 해석**
우리는 먼저 최신 접근 방식들의 위치를 정립하기 위해 테스트 시간 연산에 대한 통합된 추상화를 제공합니다.

---

### ✅ 문장 2-1-2
We view the use of test-time compute through the lens of modifying the model's distribution on a given prompt, adaptively at test-time.

**🔍 단위 해석**
우리는 테스트 시간 연산의 사용을, 주어진 프롬프트에 대한 모델의 분포를 테스트 시간에 적응적으로 수정하는 관점에서 바라봅니다.

---

### ✅ 문장 2-1-3
Ideally, test-time compute should allow for the ability to express more complex distributions than naïvely sampling from the LLM.

**🔍 단위 해석**
이상적으로, 테스트 시간 연산은 LLM에서 단순하게(naïvely) 샘플링하는 것보다 더 복잡한 분포를 표현할 수 있는 능력을 허용해야 합니다.

---

### ✅ 문장 2-1-4
In general, there are two knobs to modify an LLM's distribution: (1) at the input level: by augmenting the given prompt with an additional set of tokens that the LLM conditions on to obtain a modified proposal distribution, or (2) at the output level: by sampling multiple candidates from the standard LLM and performing surgery on them, using some post-hoc verifiers or scorers.

**🔍 단위 해석**
일반적으로 LLM의 분포를 수정하는 데는 두 가지 조절 장치(knobs)가 있습니다: (1) 입력 레벨: LLM이 조건부로 삼을 추가 토큰 세트로 주어진 프롬프트를 증강하여 수정된 '제안 분포(proposal distribution)'를 얻는 방식, 또는 (2) 출력 레벨: 표준 LLM에서 여러 후보를 샘플링한 뒤 사후 검증기(post-hoc verifiers)나 채점기를 사용하여 이들을 수술(surgery, 조작/선별)하는 방식입니다.

**💡 보조 설명**
* **[Proposal Distribution]:** 모델이 다음에 생성할 단어들에 대한 확률 분포입니다.
* **[Knobs]:** 라디오 볼륨을 조절하듯, 사용자가 조작할 수 있는 설정 변수를 의미합니다. 여기서는 '입력 프롬프트 수정'과 '출력 결과 검증' 두 가지를 말합니다.

---

### ✅ 문장 2-1-5
This process is reminiscent of Markov chain Monte Carlo (MCMC) sampling from a complex distribution by combining a simple proposal distribution and a score function.

**🔍 단위 해석**
이 과정은 단순한 제안 분포와 점수 함수(score function)를 결합하여 복잡한 분포에서 샘플링을 수행하는 마르코프 체인 몬테카를로(MCMC) 방식을 연상시킵니다.

**💡 보조 설명**
* **[MCMC]:** 복잡한 확률 분포에서 표본을 추출하기 위한 알고리즘으로, 현재 상태에서 다음 상태를 확률적으로 결정하며 이동합니다. LLM의 추론 과정을 이와 유사하게 해석한 것입니다.

---

### ✅ 문장 2-1-6
Modifying the proposal distribution by altering inputs tokens and using a verifier form the two independent axes of our study.

**🔍 단위 해석**
입력 토큰을 변경하여 제안 분포를 수정하는 것과 검증기를 사용하는 것은 본 연구의 두 가지 독립적인 축을 형성합니다.

---

## 📘 단락 2-2
(1) Modifying the proposal distribution. One way to improve the proposal distribution is to directly optimize the model for a given reasoning task via RL-inspired finetuning methods such as STaR or $ReST^{EM}$. These techniques specifically finetune the model to directly improve the proposal distribution, rather than generating additional tokens at test-time. Instead, techniques such as self-critique enable the model to improve its own proposals at test time by instructing it to critique and revise its outputs iteratively. Since prompting off-the-shelf models is not effective at enabling effective revisions at test time, we specifically finetune models to iteratively revise their answers for complex reasoning, using Best-of-N guidance.

---

### ✅ 문장 2-2-1
(1) Modifying the proposal distribution. One way to improve the proposal distribution is to directly optimize the model for a given reasoning task via RL-inspired finetuning methods such as STaR or $ReST^{EM}$.

**🔍 단위 해석**
(1) 제안 분포 수정. 제안 분포를 개선하는 한 가지 방법은 STaR나 $ReST^{EM}$과 같은 강화학습(RL) 영감 기반의 미세 조정(finetuning) 방법을 통해 특정 추론 작업에 대해 모델을 직접 최적화하는 것입니다.

---

### ✅ 문장 2-2-2
These techniques specifically finetune the model to directly improve the proposal distribution, rather than generating additional tokens at test-time.

**🔍 단위 해석**
이러한 기법들은 테스트 시간에 추가 토큰을 생성하는 대신, 제안 분포 자체를 직접 개선하도록 모델을 미세 조정하는 데 특화되어 있습니다.

---

### ✅ 문장 2-2-3
Instead, techniques such as self-critique enable the model to improve its own proposals at test time by instructing it to critique and revise its outputs iteratively.

**🔍 단위 해석**
반면, '자기 비판(self-critique)'과 같은 기법들은 모델에게 자신의 출력을 반복적으로 비판하고 수정하도록 지시함으로써 테스트 시간에 스스로 제안을 개선할 수 있게 합니다.

---

### ✅ 문장 2-2-4
Since prompting off-the-shelf models is not effective at enabling effective revisions at test time, we specifically finetune models to iteratively revise their answers for complex reasoning, using Best-of-N guidance.

**🔍 단위 해석**
기성(off-the-shelf) 모델에 프롬프트만 사용하는 것은 테스트 시간에 효과적인 수정을 가능하게 하는 데 효율적이지 않으므로, 우리는 Best-of-N 가이드를 사용하여 복잡한 추론을 위해 답변을 반복적으로 수정하도록 모델을 특별히 미세 조정합니다.

**💡 보조 설명**
* **[Off-the-shelf models]:** 추가적인 훈련 없이 가져다 바로 쓸 수 있는 사전 학습된 모델을 말합니다.
* **[Best-of-N guidance]:** N개의 샘플 중 가장 좋은 것을 정답으로 간주하여 학습 신호로 사용하는 방식입니다.

---

## 📘 단락 2-3
(2) Optimizing the verifier. The verifier selects the best answer from the proposal distribution. The most canonical way to use such a verifier is by applying best-of-N sampling, wherein we sample N solutions and then select the best one with a verifier. This approach can be further improved by training a process-based reward model (PRM), which produces a prediction of the correctness of each intermediate step in a solution. We can then utilize these per-step predictions to perform tree search over the solution space, enabling a more effective modification of the proposal distribution.

---

### ✅ 문장 2-3-1
(2) Optimizing the verifier. The verifier selects the best answer from the proposal distribution.

**🔍 단위 해석**
(2) 검증기 최적화. 검증기는 제안 분포에서 나온 답변 중 가장 좋은 것을 선택합니다.

---

### ✅ 문장 2-3-2
The most canonical way to use such a verifier is by applying best-of-N sampling, wherein we sample N solutions and then select the best one with a verifier.

**🔍 단위 해석**
이러한 검증기를 사용하는 가장 표준적인(canonical) 방법은 'Best-of-N 샘플링'을 적용하는 것인데, 이는 N개의 솔루션을 샘플링한 후 검증기로 가장 좋은 하나를 선택하는 방식입니다.

---

### ✅ 문장 2-3-3
This approach can be further improved by training a process-based reward model (PRM), which produces a prediction of the correctness of each intermediate step in a solution.

**🔍 단위 해석**
이 접근법은 솔루션의 각 중간 단계(intermediate step)에 대한 정답 여부를 예측하는 '프로세스 기반 보상 모델(PRM)'을 훈련시킴으로써 더욱 개선될 수 있습니다.

---

### ✅ 문장 2-3-4
We can then utilize these per-step predictions to perform tree search over the solution space, enabling a more effective modification of the proposal distribution.

**🔍 단위 해석**
그러면 우리는 이러한 단계별 예측을 활용하여 솔루션 공간에서 트리 탐색(tree search)을 수행할 수 있으며, 이를 통해 제안 분포를 더욱 효과적으로 수정할 수 있게 됩니다.

**💡 보조 설명**
* **[Tree search]:** 답변 생성 과정을 나뭇가지처럼 여러 갈래로 펼쳐서 탐색하는 알고리즘(예: Beam Search, Lookahead Search)입니다. PDF의 Figure 2가 이 과정을 시각화하고 있습니다.

---

# 3 HOW TO SCALE TEST-TIME COMPUTATION OPTIMALLY

## 📘 단락 3-1
Using this unified view of different methods, we would like to understand and characterize how to most effectively use test-time computation to improve performance on a given prompt by answering the question below. When either refining the proposal distribution or searching against a verifier, there are numerous choices on how to allocate test-time compute. For example, when using a model finetuned for revisions as the proposal distribution and an ORM verifier, we could either spend the full test-time budget on generating N independent samples in parallel from the model and then apply best-of-N, or we could sample N revisions in sequence using a revision model and then select the best answer in the sequence with an ORM, or strike a balance between these extremes. Intuitively, we might expect that problems where the initial samples are more likely to be on the right track to benefit more from revisions. On the other hand, problems that require exploration over high-level problem solving strategies might benefit from sampling more independent answers in parallel. Finally, in the case of verifiers, we also can choose between different search algorithms (e.g. beam-search, lookahead-search, best-of-N), each of which may exhibit different properties depending on the quality of the verifier and proposal distribution at hand. More sophisticated search procedures might be more useful in harder problems compared to a much simpler best-of-N or majority baseline.

---

### ✅ 문장 3-1-1
Using this unified view of different methods, we would like to understand and characterize how to most effectively use test-time computation to improve performance on a given prompt by answering the question below.

**🔍 단위 해석**
다양한 방법론에 대한 이러한 통합된 관점을 바탕으로, 우리는 아래 질문에 답함으로써 주어진 프롬프트에 대해 성능을 향상시키기 위해 테스트 시간 연산을 가장 효과적으로 사용하는 방법을 이해하고 규명하고자 합니다.

---

### ✅ 문장 3-1-2
When either refining the proposal distribution or searching against a verifier, there are numerous choices on how to allocate test-time compute.

**🔍 단위 해석**
제안 분포를 정제하든 검증기를 대상으로 탐색을 하든, 테스트 시간 연산을 어떻게 할당할지에 대해서는 수많은 선택지가 존재합니다.

---

### ✅ 문장 3-1-3
For example, when using a model finetuned for revisions as the proposal distribution and an ORM verifier, we could either spend the full test-time budget on generating N independent samples in parallel from the model and then apply best-of-N, or we could sample N revisions in sequence using a revision model and then select the best answer in the sequence with an ORM, or strike a balance between these extremes.

**🔍 단위 해석**
예를 들어, 제안 분포로써 수정을 위해 미세 조정된 모델과 ORM 검증기를 사용할 때, 우리는 전체 테스트 시간 예산을 모델에서 N개의 독립적인 샘플을 병렬로 생성한 후 Best-of-N을 적용하는 데 쓸 수도 있고, 혹은 수정 모델을 사용하여 순차적으로 N번의 수정을 샘플링한 후 ORM으로 그 시퀀스 내에서 최고의 답을 고를 수도 있으며, 또는 이 양극단 사이에서 균형을 맞출 수도 있습니다.

**💡 보조 설명**
* **[ORM (Outcome-based Reward Model)]:** 결과 기반 보상 모델. PRM과 달리 최종 결과만 보고 점수를 매깁니다.
* **[Sequential Revision vs Parallel Sampling]:** 순차적 수정은 깊이(Depth)를, 병렬 샘플링은 너비(Width)를 강조하는 전략입니다.

---

### ✅ 문장 3-1-4
Intuitively, we might expect that problems where the initial samples are more likely to be on the right track to benefit more from revisions.

**🔍 단위 해석**
직관적으로, 초기 샘플들이 정답에 가까울(on the right track) 가능성이 높은 문제들은 수정(revisions)을 통해 더 많은 이득을 볼 것이라 예상할 수 있습니다.

---

### ✅ 문장 3-1-5
On the other hand, problems that require exploration over high-level problem solving strategies might benefit from sampling more independent answers in parallel.

**🔍 단위 해석**
반면, 고차원적인 문제 해결 전략에 대한 탐색이 필요한 문제들은 더 많은 독립적인 답변을 병렬로 샘플링하는 것에서 이득을 볼 수 있습니다.

---

### ✅ 문장 3-1-6
Finally, in the case of verifiers, we also can choose between different search algorithms (e.g. beam-search, lookahead-search, best-of-N), each of which may exhibit different properties depending on the quality of the verifier and proposal distribution at hand.

**🔍 단위 해석**
마지막으로 검증기의 경우에도, 우리는 다양한 탐색 알고리즘(예: 빔 서치, 룩어헤드 서치, Best-of-N) 중에서 선택할 수 있으며, 이들 각각은 사용 가능한 검증기와 제안 분포의 품질에 따라 다른 특성을 보일 수 있습니다.

---

### ✅ 문장 3-1-7
More sophisticated search procedures might be more useful in harder problems compared to a much simpler best-of-N or majority baseline.

**🔍 단위 해석**
더 정교한 탐색 절차들은 훨씬 단순한 Best-of-N이나 다수결(majority) 베이스라인에 비해 더 어려운 문제들에서 유용할 수 있습니다.

---

## 📘 단락 3-2 (Problem setup)
**Problem setup** We are given a prompt and a test-time compute budget within which to solve the problem. Under the abstraction above, there are different knobs we can tune when utilizing test-time computation. How can we determine the most effective way to utilize test-time compute for a given prompt? And how well would this do against simply utilizing a much bigger pretrained model?

---

### ✅ 문장 3-2-1
**Problem setup** We are given a prompt and a test-time compute budget within which to solve the problem.

**🔍 단위 해석**
**문제 설정** 우리에게는 문제를 해결하기 위한 프롬프트와 테스트 시간 연산 예산(compute budget)이 주어집니다.

---

### ✅ 문장 3-2-2
Under the abstraction above, there are different knobs we can tune when utilizing test-time computation.

**🔍 단위 해석**
앞서 언급한 추상화 하에서, 테스트 시간 연산을 활용할 때 우리가 조절할 수 있는 다양한 설정들(knobs)이 존재합니다.

---

### ✅ 문장 3-2-3
How can we determine the most effective way to utilize test-time compute for a given prompt?

**🔍 단위 해석**
주어진 프롬프트에 대해 테스트 시간 연산을 활용하는 가장 효과적인 방법을 어떻게 결정할 수 있을까요?

---

### ✅ 문장 3-2-4
And how well would this do against simply utilizing a much bigger pretrained model?

**🔍 단위 해석**
그리고 이것이 단순히 훨씬 더 큰 사전 학습된 모델을 사용하는 것과 비교했을 때 얼마나 좋은 성능을 낼까요?

---

# 3.1 COMPUTE-OPTIMAL TEST-TIME SCALING STRATEGY

## 📘 단락 3.1-1
Per the discussion above, we would like to prescribe the optimal allocation of our test-time compute budget onto a given problem. To this end, for any given approach of utilizing test-time compute (e.g., revisions and search against a verifier in this paper, some combination or other methods in general), we define the "test-time compute-optimal scaling strategy" as the strategy that chooses hyperparameters appearing in a given approach for maximal performance benefits on a given prompt at test time.

---

### ✅ 문장 3.1-1-1
Per the discussion above, we would like to prescribe the optimal allocation of our test-time compute budget onto a given problem.

**🔍 단위 해석**
위의 논의에 따라, 우리는 주어진 문제에 대해 테스트 시간 연산 예산의 최적 할당(optimal allocation)을 처방(prescribe)하고자 합니다.

---

### ✅ 문장 3.1-1-2
To this end, for any given approach of utilizing test-time compute (e.g., revisions and search against a verifier in this paper, some combination or other methods in general), we define the "test-time compute-optimal scaling strategy" as the strategy that chooses hyperparameters appearing in a given approach for maximal performance benefits on a given prompt at test time.

**🔍 단위 해석**
이를 위해, 테스트 시간 연산을 활용하는 어떠한 접근 방식(본 논문의 수정 및 검증기 탐색, 혹은 일반적인 다른 조합 등)에 대해서든, 우리는 "테스트 시간 연산 최적화 스케일링 전략"을 '테스트 시간에 주어진 프롬프트에 대해 최대의 성능 이득을 얻기 위해 해당 접근법의 하이퍼파라미터를 선택하는 전략'으로 정의합니다.

---

## 📘 단락 3.1-2
Formally, define $Target(\theta,N,q)$ as the distribution over natural language output tokens induced by the model for a given prompt q, using test-time compute hyper-parameters $\theta$, and a compute budget of N. We would like to select the hyper-parameters $\theta$ which maximize the accuracy of the target distribution for a given problem. We express this formally as:
$$\theta_{q,y^*(q)}^{*}(N)=argmax_{\theta}(\mathbb{E}_{y\sim Target(\theta,N,q)}[\mathbb{I}_{y=y^*(q)}]) \quad (1)$$
where $y^*(q)$ denotes the ground-truth correct response for input query q, and $\theta_{q,y^*(q)}^{*}(N)$ represents the test-time compute-optimal scaling strategy for the problem q with compute budget N. We note that our definition of test-time compute-optimal scaling differs slightly from that of concurrent work (Wu et al., 2024) in that our notion of scaling is question dependent.

---

### ✅ 문장 3.1-2-1
Formally, define $Target(\theta,N,q)$ as the distribution over natural language output tokens induced by the model for a given prompt q, using test-time compute hyper-parameters $\theta$, and a compute budget of N.

**🔍 단위 해석**
형식적으로, $Target(\theta,N,q)$를 테스트 시간 연산 하이퍼파라미터 $\theta$와 연산 예산 $N$을 사용하여 주어진 프롬프트 $q$에 대해 모델이 유도한 자연어 출력 토큰들의 분포로 정의합니다.

---

### ✅ 문장 3.1-2-2
We would like to select the hyper-parameters $\theta$ which maximize the accuracy of the target distribution for a given problem.

**🔍 단위 해석**
우리는 주어진 문제에 대해 타겟 분포의 정확도(accuracy)를 최대화하는 하이퍼파라미터 $\theta$를 선택하고자 합니다.

---

### ✅ 문장 3.1-2-3
We express this formally as:
$$\theta_{q,y^*(q)}^{*}(N)=argmax_{\theta}(\mathbb{E}_{y\sim Target(\theta,N,q)}[\mathbb{I}_{y=y^*(q)}]) \quad (1)$$

**🔍 단위 해석**
이를 수식으로 표현하면 위 식 (1)과 같습니다.

**💡 보조 설명**
* **[수식 해석]:** 예산 $N$과 문제 $q$가 주어졌을 때, 정답 $y^*(q)$를 맞출 확률(기댓값 $\mathbb{E}$)이 가장 높은 파라미터 $\theta$를 찾는 식입니다. $\mathbb{I}$는 지시 함수(Indicator function)로 정답이면 1, 아니면 0입니다.

---

### ✅ 문장 3.1-2-4
where $y^*(q)$ denotes the ground-truth correct response for input query q, and $\theta_{q,y^*(q)}^{*}(N)$ represents the test-time compute-optimal scaling strategy for the problem q with compute budget N.

**🔍 단위 해석**
여기서 $y^*(q)$는 입력 쿼리 $q$에 대한 실제 정답(ground-truth correct response)을 나타내며, $\theta_{q,y^*(q)}^{*}(N)$은 연산 예산 $N$을 가진 문제 $q$에 대한 테스트 시간 연산 최적화 스케일링 전략을 나타냅니다.

---

### ✅ 문장 3.1-2-5
We note that our definition of test-time compute-optimal scaling differs slightly from that of concurrent work (Wu et al., 2024) in that our notion of scaling is question dependent.

**🔍 단위 해석**
우리의 테스트 시간 연산 최적화 스케일링 정의는 스케일링 개념이 '질문 의존적(question dependent)'이라는 점에서 동시대의 다른 연구(Wu et al., 2024)와 약간 다르다는 점을 주목합니다.

---

# 3.2 QUESTION DIFFICULTY IS A GOOD APPROXIMATION FOR THE OPTIMAL STRATEGY

## 📘 단락 3.2-1
In order to effectively analyze the test-time scaling properties of the different mechanisms discussed in Section 2 (e.g. proposal distribution and verifier), we will prescribe an approximation to this optimal strategy $\theta_{q,y^*(q)}^{*}(N)$ as a function of a statistic of a given prompt. Our approximation estimates a notion of difficulty for a given prompt. The compute-optimal strategy is then defined as a function of the difficulty of a prompt. Despite being only an heuristic approach to solve Equation 1, we find that it can still induce substantial improvements in performance over a baseline strategy of allocating this inference-time compute in an ad-hoc manner.

---

### ✅ 문장 3.2-1-1
In order to effectively analyze the test-time scaling properties of the different mechanisms discussed in Section 2 (e.g. proposal distribution and verifier), we will prescribe an approximation to this optimal strategy $\theta_{q,y^*(q)}^{*}(N)$ as a function of a statistic of a given prompt.

**🔍 단위 해석**
섹션 2에서 논의된 다양한 메커니즘(예: 제안 분포 및 검증기)의 테스트 시간 스케일링 특성을 효과적으로 분석하기 위해, 우리는 이 최적 전략 $\theta_{q,y^*(q)}^{*}(N)$에 대한 근사치를 주어진 프롬프트의 통계량에 대한 함수로서 처방할 것입니다.

---

### ✅ 문장 3.2-1-2
Our approximation estimates a notion of difficulty for a given prompt.

**🔍 단위 해석**
우리의 근사치는 주어진 프롬프트에 대한 '난이도(difficulty)' 개념을 추정합니다.

---

### ✅ 문장 3.2-1-3
The compute-optimal strategy is then defined as a function of the difficulty of a prompt.

**🔍 단위 해석**
그러면 연산 최적화 전략은 프롬프트의 난이도에 대한 함수로 정의됩니다.

---

### ✅ 문장 3.2-1-4
Despite being only an heuristic approach to solve Equation 1, we find that it can still induce substantial improvements in performance over a baseline strategy of allocating this inference-time compute in an ad-hoc manner.

**🔍 단위 해석**
이것이 식 (1)을 풀기 위한 휴리스틱(heuristic) 접근법일 뿐임에도 불구하고, 우리는 이것이 추론 시간 연산을 임의로(ad-hoc) 할당하는 베이스라인 전략보다 상당한 성능 향상을 유도할 수 있음을 발견했습니다.

---

## 📘 단락 3.2-2
Our estimate of question difficulty assigns a given question to one of five discrete difficulty levels. We then use these bins to estimate $\theta_{q,y^*(q)}^{*}(N)$ on a validation set (given a compute budget), and apply the optimal strategy on the test set. Thus, question difficulty acts as a sufficient statistic for designing the compute-optimal strategy. For example, to optimally allocate test-time compute between parallel best-of-N and sequential sampling, we first pre-compute the accuracy of both techniques within each difficulty bin using a held-out set. Given a new test question, we then determine the difficulty bin it belongs to and select the best performing strategy within that bin.

---

### ✅ 문장 3.2-2-1
Our estimate of question difficulty assigns a given question to one of five discrete difficulty levels.

**🔍 단위 해석**
우리의 문제 난이도 추정은 주어진 질문을 5개의 이산적인(discrete) 난이도 레벨 중 하나로 할당합니다.

---

### ✅ 문장 3.2-2-2
We then use these bins to estimate $\theta_{q,y^*(q)}^{*}(N)$ on a validation set (given a compute budget), and apply the optimal strategy on the test set.

**🔍 단위 해석**
그런 다음 우리는 이 구간(bins)들을 사용하여 검증 세트에서(주어진 연산 예산 내에서) $\theta_{q,y^*(q)}^{*}(N)$를 추정하고, 테스트 세트에 최적 전략을 적용합니다.

---

### ✅ 문장 3.2-2-3
Thus, question difficulty acts as a sufficient statistic for designing the compute-optimal strategy.

**🔍 단위 해석**
따라서 문제 난이도는 연산 최적화 전략을 설계하기 위한 충분통계량(sufficient statistic) 역할을 합니다.

---

### ✅ 문장 3.2-2-4
For example, to optimally allocate test-time compute between parallel best-of-N and sequential sampling, we first pre-compute the accuracy of both techniques within each difficulty bin using a held-out set.

**🔍 단위 해석**
예를 들어, 병렬 Best-of-N과 순차적 샘플링 간에 테스트 시간 연산을 최적으로 할당하기 위해, 우리는 먼저 보류된(held-out) 세트를 사용하여 각 난이도 구간 내에서 두 기법의 정확도를 미리 계산합니다.

---

### ✅ 문장 3.2-2-5
Given a new test question, we then determine the difficulty bin it belongs to and select the best performing strategy within that bin.

**🔍 단위 해석**
새로운 테스트 질문이 주어지면, 그것이 속한 난이도 구간을 결정하고 해당 구간 내에서 가장 성능이 좋은 전략을 선택합니다.

---

## 📘 단락 3.2-3 (Defining question difficulty)
Defining question difficulty. Following Lightman et al. (2023), we define question difficulty as a function of the given base LLM. Specifically, we bin the model's pass@1 rate estimated from 2048 samples on each test question into five quantiles, each corresponding to increasing difficulty levels. We find this notion of model-specific difficulty bins to be more predictive of the efficacy of using test-time compute compared to the hand-labeled difficulty bins in the MATH dataset.

---

### ✅ 문장 3.2-3-1
Defining question difficulty.

**🔍 단위 해석**
문제 난이도의 정의.

---

### ✅ 문장 3.2-3-2
Following Lightman et al. (2023), we define question difficulty as a function of the given base LLM.

**🔍 단위 해석**
Lightman et al. (2023)을 따르며, 우리는 문제 난이도를 주어진 베이스 LLM의 함수로 정의합니다.

---

### ✅ 문장 3.2-3-3
Specifically, we bin the model's pass@1 rate estimated from 2048 samples on each test question into five quantiles, each corresponding to increasing difficulty levels.

**🔍 단위 해석**
구체적으로, 우리는 각 테스트 질문에 대해 2048개의 샘플로부터 추정된 모델의 pass@1(한 번에 맞출 확률) 비율을 5개의 분위수(quantiles)로 나누어, 각각을 증가하는 난이도 레벨에 대응시킵니다.

**💡 보조 설명**
* **[Pass@1 rate]:** 모델이 답안을 하나 생성했을 때 정답일 확률입니다. 이것이 낮을수록 모델에게 '어려운' 문제입니다.

---

### ✅ 문장 3.2-3-4
We find this notion of model-specific difficulty bins to be more predictive of the efficacy of using test-time compute compared to the hand-labeled difficulty bins in the MATH dataset.

**🔍 단위 해석**
우리는 이러한 '모델별 난이도 구간' 개념이 MATH 데이터셋의 사람이 라벨링한 난이도 구간보다 테스트 시간 연산 사용의 효용성을 예측하는 데 더 효과적(predictive)이라는 것을 발견했습니다.

---

## 📘 단락 3.2-4
That said, we note that assessing difficulty as described assumes oracle access to a correctness checker, which is unavailable at deployment. To enable a realistic estimate of difficulty, we approximate difficulty via a model-predicted notion of difficulty, which constructs the bins by averaging the score of a learned verifier on the same 2048 samples per problem. We refer to this setting as model-predicted difficulty and the setting which relies on ground-truth correctness as oracle difficulty. Predicted difficulty removes the reliance on ground truth labels, but still incurs computational cost. Our experiments do not account for this cost largely for simplicity, since our goal is to present some of the first results of what is in fact possible by effectively allocating test-time compute.

---

### ✅ 문장 3.2-4-1
That said, we note that assessing difficulty as described assumes oracle access to a correctness checker, which is unavailable at deployment.

**🔍 단위 해석**
그렇긴 하지만, 앞서 설명한 난이도 평가는 배포 시점에는 사용할 수 없는 정답 확인기(correctness checker)에 대한 '오라클 접근(oracle access)'을 가정하고 있음을 지적합니다.

**💡 보조 설명**
* **[Oracle access]:** 정답을 미리 알고 있는 신적인 존재(오라클)에게 물어보는 것처럼, 현실에서는 불가능한 이상적인 조건을 말합니다.

---

### ✅ 문장 3.2-4-2
To enable a realistic estimate of difficulty, we approximate difficulty via a model-predicted notion of difficulty, which constructs the bins by averaging the score of a learned verifier on the same 2048 samples per problem.

**🔍 단위 해석**
현실적인 난이도 추정을 가능하게 하기 위해, 우리는 '모델이 예측한 난이도(model-predicted difficulty)' 개념을 통해 난이도를 근사하며, 이는 문제당 동일한 2048개 샘플에 대해 학습된 검증기의 점수 평균을 내어 구간을 구성합니다.

---

### ✅ 문장 3.2-4-3
We refer to this setting as model-predicted difficulty and the setting which relies on ground-truth correctness as oracle difficulty.

**🔍 단위 해석**
우리는 이 설정을 '모델 예측 난이도'라고 부르며, 실제 정답(ground-truth correctness)에 의존하는 설정을 '오라클 난이도'라고 부릅니다.

---

### ✅ 문장 3.2-4-4
Predicted difficulty removes the reliance on ground truth labels, but still incurs computational cost.

**🔍 단위 해석**
예측된 난이도는 실제 정답 라벨에 대한 의존성은 제거하지만, 여전히 연산 비용이 발생합니다.

---

### ✅ 문장 3.2-4-5
Our experiments do not account for this cost largely for simplicity, since our goal is to present some of the first results of what is in fact possible by effectively allocating test-time compute.

**🔍 단위 해석**
우리의 목표는 테스트 시간 연산을 효과적으로 할당함으로써 실제로 무엇이 가능한지에 대한 초기 결과들을 제시하는 것이므로, 단순함을 위해 실험에서는 대체로 이 비용을 고려하지 않았습니다.

---

# 4 EXPERIMENTAL SETUP

## 📘 단락 4-1
We first outline our experimental setup for conducting this analysis with multiple verifier design choices and proposal distributions, followed by the analysis results in the subsequent sections.

---

### ✅ 문장 4-1-1
We first outline our experimental setup for conducting this analysis with multiple verifier design choices and proposal distributions, followed by the analysis results in the subsequent sections.

**🔍 단위 해석**
우리는 먼저 다양한 검증기 설계 선택과 제안 분포를 사용하여 이 분석을 수행하기 위한 실험 설정을 개략적으로 설명하고, 이어지는 섹션에서 분석 결과를 제시합니다.

---

## 📘 단락 4-2
**Datasets.** We expect test-time compute to be most helpful when models already have all the basic "knowledge" needed to answer a query, and instead the primary challenge is about drawing (complex) inferences from this knowledge. To this end, we focus on the MATH (Hendrycks et al., 2021) benchmark, which consists of high-school competition level math problems with a range of difficulty levels. For all experiments, we use the dataset split consisting of 12k train and 500 test questions.

---

### ✅ 문장 4-2-1
**Datasets.** We expect test-time compute to be most helpful when models already have all the basic "knowledge" needed to answer a query, and instead the primary challenge is about drawing (complex) inferences from this knowledge.

**🔍 단위 해석**
**데이터셋.** 우리는 모델이 쿼리에 답하는 데 필요한 모든 기본 "지식"을 이미 갖추고 있고, 대신 주된 과제가 이 지식으로부터 (복잡한) 추론을 이끌어내는 것일 때 테스트 시간 연산이 가장 도움이 될 것으로 기대합니다.

---

### ✅ 문장 4-2-2
To this end, we focus on the MATH (Hendrycks et al., 2021) benchmark, which consists of high-school competition level math problems with a range of difficulty levels.

**🔍 단위 해석**
이를 위해, 우리는 다양한 난이도의 고등학교 경시대회 수준 수학 문제들로 구성된 MATH 벤치마크에 집중합니다.

---

### ✅ 문장 4-2-3
For all experiments, we use the dataset split consisting of 12k train and 500 test questions.

**🔍 단위 해석**
모든 실험에서 우리는 12,000개의 훈련 질문과 500개의 테스트 질문으로 구성된 데이터셋 분할을 사용합니다.

---

## 📘 단락 4-3
**Models.** We use the PaLM 2-S* (Anil et al., 2023) (Codey) model. We chose this model, as it is representative of the capabilities of many contemporary LLMs, and is small enough to efficiently run many experiments on. Most importantly, this model attains a non-trivial performance on MATH (but not saturated). For these reasons, we expect this model to provide a good test-bed.

---

### ✅ 문장 4-3-1
**Models.** We use the PaLM 2-S* (Anil et al., 2023) (Codey) model.

**🔍 단위 해석**
**모델.** 우리는 PaLM 2-S* (Codey) 모델을 사용합니다.

---

### ✅ 문장 4-3-2
We chose this model, as it is representative of the capabilities of many contemporary LLMs, and is small enough to efficiently run many experiments on.

**🔍 단위 해석**
우리는 이 모델이 많은 현대 LLM들의 능력을 대표하며, 효율적으로 많은 실험을 실행할 수 있을 만큼 충분히 작기 때문에 선택했습니다.

---

### ✅ 문장 4-3-3
Most importantly, this model attains a non-trivial performance on MATH (but not saturated).

**🔍 단위 해석**
가장 중요한 점은, 이 모델이 MATH 데이터셋에서 무시할 수 없는(non-trivial) 수준의 성능을 달성하지만, 성능이 포화(saturated, 더 이상 오를 데가 없는 상태)되지는 않았다는 것입니다.

**💡 보조 설명**
* **[Saturated]:** 모델이 너무 똑똑해서 이미 100점에 가깝거나, 너무 멍청해서 0점인 경우 '테스트 시간 연산'의 효과를 측정하기 어렵습니다. PaLM 2-S*는 적절히 어렵게 느끼는 모델이라는 의미입니다.

---

### ✅ 문장 4-3-4
For these reasons, we expect this model to provide a good test-bed.

**🔍 단위 해석**
이러한 이유들로 인해, 우리는 이 모델이 좋은 테스트 베드(test-bed)를 제공할 것으로 기대합니다.

---

# 5 SCALING TEST-TIME COMPUTE VIA VERIFIERS

## 📘 단락 5-1
In this section, we study how test-time compute can be most effectively scaled by searching against a verifier and keeping the proposal distribution fixed to the base LM. Specifically, we study different search approaches with PRMs and analyze their test-time compute scaling properties, but first we provide a brief overview of how a PRM can be trained.

---

### ✅ 문장 5-1-1
In this section, we study how test-time compute can be most effectively scaled by searching against a verifier and keeping the proposal distribution fixed to the base LM.

**🔍 단위 해석**
이 섹션에서는 제안 분포를 베이스 LM으로 고정한 채, 검증기(verifier)를 대상으로 탐색(search)을 수행함으로써 테스트 시간 연산을 가장 효과적으로 확장하는 방법을 연구합니다.

---

### ✅ 문장 5-1-2
Specifically, we study different search approaches with PRMs and analyze their test-time compute scaling properties, but first we provide a brief overview of how a PRM can be trained.

**🔍 단위 해석**
구체적으로, 우리는 PRM(프로세스 보상 모델)을 사용한 다양한 탐색 접근법을 연구하고 그들의 테스트 시간 연산 스케일링 특성을 분석하며, 그에 앞서 PRM이 어떻게 훈련될 수 있는지에 대한 간략한 개요를 제공합니다.

---

## 📘 단락 5.1-1
**5.1 TRAINING VERIFIERS AMENABLE TO SEARCH**
We follow the approach of Wang et al. (2023), which supervises the PRM using estimates of per-step correctness obtained from running Monte Carlo rollouts from each step in the solution. Our PRM's per-step predictions therefore correspond to value estimates of reward-to-go for the base model's sampling policy, similar to recent work (Wang et al., 2023; Setlur et al., 2024). We also compared to an ORM baseline (Appendix H) but found that our PRM consistently outperforms the ORM. Hence, all of the search experiments in this section use a PRM model. Additional details are in Appendix F.

---

### ✅ 문장 5.1-1-1
We follow the approach of Wang et al. (2023), which supervises the PRM using estimates of per-step correctness obtained from running Monte Carlo rollouts from each step in the solution.

**🔍 단위 해석**
우리는 Wang et al. (2023)의 접근 방식을 따르는데, 이는 솔루션의 각 단계에서 '몬테카를로 롤아웃(Monte Carlo rollouts)'을 실행하여 얻은 단계별 정확성 추정치를 사용하여 PRM을 지도 학습시킵니다.

**💡 보조 설명**
* **[Monte Carlo rollouts]:** 현재 단계에서 끝까지 여러 번 시뮬레이션(롤아웃)을 돌려봐서, 최종적으로 정답에 도달하는 비율이 얼마나 되는지 확인하는 방법입니다.

---

### ✅ 문장 5.1-1-2
Our PRM's per-step predictions therefore correspond to value estimates of reward-to-go for the base model's sampling policy, similar to recent work (Wang et al., 2023; Setlur et al., 2024).

**🔍 단위 해석**
따라서 우리 PRM의 단계별 예측값은 베이스 모델의 샘플링 정책에 대한 '잔여 보상(reward-to-go)'의 가치 추정치에 해당하며, 이는 최근 연구들과 유사합니다.

**💡 보조 설명**
* **[Reward-to-go]:** 현재 상태에서 앞으로 얻을 수 있을 것으로 기대되는 미래 보상의 총합입니다. 여기서는 "이 단계에서 시작해서 결국 정답을 맞힐 확률"을 의미합니다.

---

### ✅ 문장 5.1-1-3
We also compared to an ORM baseline (Appendix H) but found that our PRM consistently outperforms the ORM.

**🔍 단위 해석**
우리는 또한 ORM(결과 기반 보상 모델) 베이스라인과도 비교했으나(부록 H), 우리의 PRM이 ORM보다 일관되게 더 나은 성능을 보임을 발견했습니다.

---

### ✅ 문장 5.1-1-4
Hence, all of the search experiments in this section use a PRM model. Additional details are in Appendix F.

**🔍 단위 해석**
따라서 이 섹션의 모든 탐색 실험은 PRM 모델을 사용합니다. 추가적인 세부 사항은 부록 F에 있습니다.

---

## 📘 단락 5.1-2
**Answer aggregation.** At test time, PRMs can be used to score each individual step appearing in a set of solutions sampled from the base model. To pick out the best answer from N samples with the PRM, we need a function that can aggregate across all the per-step scores for each answer to determine the best candidate for the correct answer. To do this, we take the PRM's prediction at the last step as representative of the full-answer score and then follow Li et al. (2023) by applying "best-of-N weighted" selection across answers. We include more detail on these decisions in Appendix G.

---

### ✅ 문장 5.1-2-1
**Answer aggregation.** At test time, PRMs can be used to score each individual step appearing in a set of solutions sampled from the base model.

**🔍 단위 해석**
**답변 집계.** 테스트 시간에, PRM은 베이스 모델에서 샘플링된 솔루션 집합에 나타나는 각 개별 단계(step)에 점수를 매기는 데 사용될 수 있습니다.

---

### ✅ 문장 5.1-2-2
To pick out the best answer from N samples with the PRM, we need a function that can aggregate across all the per-step scores for each answer to determine the best candidate for the correct answer.

**🔍 단위 해석**
PRM을 사용하여 N개의 샘플 중에서 최고의 답변을 골라내기 위해서는, 각 답변의 모든 단계별 점수들을 통합(aggregate)하여 정답이 될 최고의 후보를 결정할 수 있는 함수가 필요합니다.

---

### ✅ 문장 5.1-2-3
To do this, we take the PRM's prediction at the last step as representative of the full-answer score and then follow Li et al. (2023) by applying "best-of-N weighted" selection across answers.

**🔍 단위 해석**
이를 위해, 우리는 '마지막 단계(last step)'에서의 PRM 예측값을 전체 답변 점수의 대표값으로 취하고, Li et al. (2023)을 따라 답변들 간에 "가중치가 적용된 Best-of-N(best-of-N weighted)" 선택 방식을 적용합니다.

---

### ✅ 문장 5.1-2-4
We include more detail on these decisions in Appendix G.

**🔍 단위 해석**
이러한 결정에 대한 더 자세한 내용은 부록 G에 포함되어 있습니다.

---

## 📘 단락 5.2-1
**5.2 SEARCH METHODS AGAINST A PRM**
We optimize the PRM at test time via tree search methods. We study three search approaches that sample outputs from a few-shot prompted base LLM (see Appendix J). An illustration is shown in Figure 2. We note that for all search algorithms, we use the same PRM verifier, enabling an even comparison. We include additional details about our different search methods in Appendix C.

---

### ✅ 문장 5.2-1-1
**5.2 SEARCH METHODS AGAINST A PRM**
We optimize the PRM at test time via tree search methods.

**🔍 단위 해석**
**5.2 PRM을 활용한 탐색 방법**
우리는 트리 탐색(tree search) 방법을 통해 테스트 시간에 PRM을 최적화합니다.

---

### ✅ 문장 5.2-1-2
We study three search approaches that sample outputs from a few-shot prompted base LLM (see Appendix J).

**🔍 단위 해석**
우리는 퓨샷(few-shot) 프롬프트가 적용된 베이스 LLM으로부터 출력을 샘플링하는 세 가지 탐색 접근법을 연구합니다(부록 J 참조).

---

### ✅ 문장 5.2-1-3
An illustration is shown in Figure 2.

**🔍 단위 해석**
이에 대한 예시가 그림 2에 나와 있습니다.

**💡 보조 설명**
* **[PDF 참조]:** Figure 2는 **Best-of-N**(단순 다수 샘플링), **Beam Search**(단계별로 상위 M개만 유지하며 가지치기), **Lookahead Search**(각 단계에서 k-step 앞을 미리 내다보고 결정) 세 가지 방식의 구조적 차이를 시각적으로 비교하고 있습니다.

---

### ✅ 문장 5.2-1-4
We note that for all search algorithms, we use the same PRM verifier, enabling an even comparison.

**🔍 단위 해석**
모든 탐색 알고리즘에 대해 동일한 PRM 검증기를 사용하여 공정한 비교가 가능하도록 했음을 밝힙니다.

---

### ✅ 문장 5.2-1-5
We include additional details about our different search methods in Appendix C.

**🔍 단위 해석**
다양한 탐색 방법에 대한 추가 세부 사항은 부록 C에 포함되어 있습니다.

---

## 📘 단락 5.2-2
**Best-of-N weighted.** We sample N answers independently from the base LLM and then select the best answer according to the PRM's final answer judgment.
**Beam search.** Beam search optimizes the PRM by searching over its per-step predictions. Our implementation is similar to BFS-V (Yao et al., 2023; Feng et al., 2024). Concretely, we consider a fixed number of beams N and a beam width M. At the end of the search we have N final answer candidates, to which we apply best-of-N weighted selection to make our final answer prediction.
**Lookahead search.** Lookahead search modifies how beam search evaluates each step. At each step in the search, rather than using the PRM score at the current step to select the top options, lookahead search performs a simulation, rolling out k steps. We stop early if the end of a solution is reached.

---

### ✅ 문장 5.2-2-1
**Best-of-N weighted.** We sample N answers independently from the base LLM and then select the best answer according to the PRM's final answer judgment.

**🔍 단위 해석**
**가중치 적용 Best-of-N.** 베이스 LLM에서 N개의 답변을 독립적으로 샘플링한 다음, PRM의 최종 답변 판정(점수)에 따라 가장 좋은 답변을 선택합니다.

---

### ✅ 문장 5.2-2-2
**Beam search.** Beam search optimizes the PRM by searching over its per-step predictions.

**🔍 단위 해석**
**빔 서치(Beam Search).** 빔 서치는 PRM의 단계별(per-step) 예측값을 탐색하여 PRM을 최적화합니다.

---

### ✅ 문장 5.2-2-3
Our implementation is similar to BFS-V (Yao et al., 2023; Feng et al., 2024).

**🔍 단위 해석**
우리의 구현 방식은 BFS-V(너비 우선 탐색 + 검증기)와 유사합니다.

---

### ✅ 문장 5.2-2-4
Concretely, we consider a fixed number of beams N and a beam width M. At the end of the search we have N final answer candidates, to which we apply best-of-N weighted selection to make our final answer prediction.

**🔍 단위 해석**
구체적으로, 우리는 고정된 빔 개수 N과 빔 너비 M을 고려합니다. 탐색이 끝나면 N개의 최종 답변 후보를 얻게 되며, 여기에 가중치 적용 Best-of-N 선택을 적용하여 최종 답변을 예측합니다.

---

### ✅ 문장 5.2-2-5
**Lookahead search.** Lookahead search modifies how beam search evaluates each step.

**🔍 단위 해석**
**룩어헤드 서치(Lookahead search).** 룩어헤드 서치는 빔 서치가 각 단계를 평가하는 방식을 수정합니다.

---

### ✅ 문장 5.2-2-6
At each step in the search, rather than using the PRM score at the current step to select the top options, lookahead search performs a simulation, rolling out k steps.

**🔍 단위 해석**
탐색의 각 단계에서, 상위 옵션을 선택하기 위해 현재 단계의 PRM 점수를 사용하는 대신, 룩어헤드 서치는 k 단계만큼 더 진행해보는(rolling out) 시뮬레이션을 수행합니다.

---

### ✅ 문장 5.2-2-7
We stop early if the end of a solution is reached.

**🔍 단위 해석**
만약 솔루션의 끝에 도달하면 조기에 중단합니다.

---

## 📘 단락 5.3-1
**5.3 ANALYSIS RESULTS: TEST-TIME SCALING FOR SEARCH WITH VERIFIERS**
We now present our results comparing various search algorithms and identify a prompt difficulty dependent compute-optimal scaling strategy for search methods.
**Comparing search algorithms.** We first conduct a sweep over different search settings. In addition to the standard best-of-N approach, we sweep over the two main parameters that distinguish these methods: beam-width M and number of lookahead steps k. While we are not able to exhaustively sweep all configurations, we sweep over the following settings with a maximum budget of 256: 1) Beam search with the beam width set to $\sqrt{N},$ where N is the generation budget; 2) Beam search with a fixed beam width of 4; 3) Lookahead search with $k=3$ applied to both beam-search settings 1) and 2); 4) Lookahead search with $k=1$ applied to beam-search setting 1).

---

### ✅ 문장 5.3-1-1
**5.3 ANALYSIS RESULTS: TEST-TIME SCALING FOR SEARCH WITH VERIFIERS**
We now present our results comparing various search algorithms and identify a prompt difficulty dependent compute-optimal scaling strategy for search methods.

**🔍 단위 해석**
**5.3 분석 결과: 검증기를 활용한 탐색의 테스트 시간 스케일링**
이제 다양한 탐색 알고리즘을 비교한 결과를 제시하고, 탐색 방법을 위한 '프롬프트 난이도 의존적 연산 최적화 스케일링 전략'을 규명합니다.

---

### ✅ 문장 5.3-1-2
**Comparing search algorithms.** We first conduct a sweep over different search settings.

**🔍 단위 해석**
**탐색 알고리즘 비교.** 먼저 다양한 탐색 설정에 대해 스윕(sweep, 전수 조사)을 수행합니다.

---

### ✅ 문장 5.3-1-3
In addition to the standard best-of-N approach, we sweep over the two main parameters that distinguish these methods: beam-width M and number of lookahead steps k.

**🔍 단위 해석**
표준 Best-of-N 접근법 외에도, 우리는 이 방법들을 구분 짓는 두 가지 주요 매개변수인 빔 너비 $M$과 룩어헤드 단계 수 $k$에 대해 스윕을 수행합니다.

---

### ✅ 문장 5.3-1-4
While we are not able to exhaustively sweep all configurations, we sweep over the following settings with a maximum budget of 256: 1) Beam search with the beam width set to $\sqrt{N},$ where N is the generation budget; 2) Beam search with a fixed beam width of 4; 3) Lookahead search with $k=3$ applied to both beam-search settings 1) and 2); 4) Lookahead search with $k=1$ applied to beam-search setting 1).

**🔍 단위 해석**
모든 구성을 완벽하게 스윕할 수는 없지만, 최대 예산 256 내에서 다음 설정들을 스윕합니다:
1) 빔 너비를 $\sqrt{N}$으로 설정한 빔 서치 (여기서 N은 생성 예산),
2) 고정 빔 너비 4를 사용하는 빔 서치,
3) 1번과 2번 빔 서치 설정에 $k=3$을 적용한 룩어헤드 서치,
4) 1번 빔 서치 설정에 $k=1$을 적용한 룩어헤드 서치.

---

## 📘 단락 5.3-2
To compare search methods as a function of generation budget fairly, we estimate the inference-time cost of each method. For beam search and best-of-N the generation budget corresponds to the number of beams and N respectively. Lookahead search utilizes additional compute: at each step, we sample k additional steps ahead. Therefore, the cost of lookahead-search is $N \times (k+1)$ samples. Querying the verifier also adds a 2x overhead for all methods but we account for this in our analysis.

---

### ✅ 문장 5.3-2-1
To compare search methods as a function of generation budget fairly, we estimate the inference-time cost of each method.

**🔍 단위 해석**
생성 예산(generation budget)에 따른 탐색 방법들을 공정하게 비교하기 위해, 각 방법의 추론 시간 비용을 추정합니다.

---

### ✅ 문장 5.3-2-2
For beam search and best-of-N the generation budget corresponds to the number of beams and N respectively.

**🔍 단위 해석**
빔 서치와 Best-of-N의 경우, 생성 예산은 각각 빔의 개수와 N에 해당합니다.

---

### ✅ 문장 5.3-2-3
Lookahead search utilizes additional compute: at each step, we sample k additional steps ahead.

**🔍 단위 해석**
룩어헤드 서치는 추가적인 연산을 사용합니다: 각 단계에서 k개의 추가 단계를 앞서 샘플링합니다.

---

### ✅ 문장 5.3-2-4
Therefore, the cost of lookahead-search is $N \times (k+1)$ samples.

**🔍 단위 해석**
따라서 룩어헤드 서치의 비용은 $N \times (k+1)$ 샘플입니다.

---

### ✅ 문장 5.3-2-5
Querying the verifier also adds a 2x overhead for all methods but we account for this in our analysis.

**🔍 단위 해석**
검증기에 쿼리하는 것 또한 모든 방법에 대해 2배의 오버헤드를 추가하지만, 우리는 분석에서 이를 감안합니다.

---

## 📘 단락 5.3-3
**Results.** As shown in Figure 3 (left), with small budgets, beam search outperforms best-of-N. However, at high budgets, these improvements diminish, with beam search underperforming. Additionally, lookahead-search underperforms other methods, likely due to the additional computation induced by looking-ahead. It is possible that with further test-time scaling or with an online MCST trained value function, lookahead search may perform better; we leave further exploration of this to future work. The diminishing returns from search are likely due to exploitation of the PRM's predictions. For example, we see instances (such as in Figure 32), where search causes the model to generate repetitive low-information steps. In other cases, we find that over-optimizing search can result in overly short solutions, of just 1-2 steps. We include several of these examples in Appendix R.

---

### ✅ 문장 5.3-3-1
**Results.** As shown in Figure 3 (left), with small budgets, beam search outperforms best-of-N.

**🔍 단위 해석**
**결과.** 그림 3 (왼쪽)에서 볼 수 있듯이, 적은 예산에서는 빔 서치가 Best-of-N보다 성능이 뛰어납니다.

**💡 보조 설명**
* **[PDF 참조]:** Figure 3 (Left) 그래프를 보면, x축(예산)이 낮을 때는 빔 서치(파란/보라색 선)가 Best-of-N(주황색 선)보다 위에 있지만, 예산이 커질수록 역전되는 양상을 보입니다.

---

### ✅ 문장 5.3-3-2
However, at high budgets, these improvements diminish, with beam search underperforming.

**🔍 단위 해석**
그러나 높은 예산에서는 이러한 개선 효과가 줄어들며, 빔 서치의 성능이 더 낮아집니다.

---

### ✅ 문장 5.3-3-3
Additionally, lookahead-search underperforms other methods, likely due to the additional computation induced by looking-ahead.

**🔍 단위 해석**
또한, 룩어헤드 서치는 다른 방법들보다 성능이 떨어지는데, 이는 아마도 '미리 보기(looking-ahead)'로 인해 유발된 추가적인 연산 비용 때문일 것입니다.

---

### ✅ 문장 5.3-3-4
It is possible that with further test-time scaling or with an online MCST trained value function, lookahead search may perform better; we leave further exploration of this to future work.

**🔍 단위 해석**
더 많은 테스트 시간 스케일링이나 온라인 MCTS(Monte Carlo Tree Search)로 훈련된 가치 함수를 사용하면 룩어헤드 서치가 더 나은 성능을 낼 수도 있겠으나, 이에 대한 탐구는 추후 연구로 남겨둡니다.

---

### ✅ 문장 5.3-3-5
The diminishing returns from search are likely due to exploitation of the PRM's predictions.

**🔍 단위 해석**
탐색의 수확 체감(diminishing returns, 이득이 줄어드는 현상)은 아마도 PRM 예측에 대한 '악용(exploitation)' 때문일 것입니다.

**💡 보조 설명**
* **[Exploitation / Goodharting]:** 모델이 실제로 좋은 답을 내는 것이 아니라, PRM 점수만 높게 받으려고 꼼수(예: 무의미한 반복, 너무 짧은 답)를 부리는 현상을 말합니다.

---

### ✅ 문장 5.3-3-6
For example, we see instances (such as in Figure 32), where search causes the model to generate repetitive low-information steps.

**🔍 단위 해석**
예를 들어, 우리는 탐색으로 인해 모델이 반복적이고 정보가 적은 단계들을 생성하는 사례들(그림 32 등)을 봅니다.

---

### ✅ 문장 5.3-3-7
In other cases, we find that over-optimizing search can result in overly short solutions, of just 1-2 steps.

**🔍 단위 해석**
다른 경우에는, 탐색을 과도하게 최적화하면 겨우 1~2단계에 불과한 너무 짧은 솔루션이 나오는 것을 발견했습니다.

---

### ✅ 문장 5.3-3-8
We include several of these examples in Appendix R.

**🔍 단위 해석**
우리는 이러한 사례들 중 몇 가지를 부록 R에 포함시켰습니다.

---

## 📘 단락 5.3-4
**Which problems does search improve?** To understand how to scale search adaptively per problem, we conduct a difficulty bin analysis. Specifically, we compare beam-search ($M=4$) against best-of-N. In Figure 3 (right), we find that, despite performing similarly in aggregate, the two methods exhibit very different behavior across difficulty levels. For example, on easy questions (levels $1/2$), the stronger optimizer of the two, beam search, degrades in performance as the budget increases, suggesting possible exploitation of the PRM signal. In contrast, on the harder questions (levels $3/4$), beam search outperforms best-of-N. Finally, on the most difficult questions (level 5), no method makes meaningful progress. These findings match intuition: we might expect that on the easy or medium difficulty questions, the verifier will make mostly correct assessments of correctness. Therefore, by optimizing further, we may be only further amplifying any spurious features learned by the verifier, causing performance degradation. On more difficult questions, the base model is less likely to sample the correct answer, so using search can help steer the model.

---

### ✅ 문장 5.3-4-1
**Which problems does search improve?** To understand how to scale search adaptively per problem, we conduct a difficulty bin analysis.

**🔍 단위 해석**
**탐색은 어떤 문제들을 개선하는가?** 문제별로 탐색을 어떻게 적응적으로 확장할지 이해하기 위해, 우리는 '난이도 구간 분석(difficulty bin analysis)'을 수행합니다.

---

### ✅ 문장 5.3-4-2
Specifically, we compare beam-search ($M=4$) against best-of-N.

**🔍 단위 해석**
구체적으로, 우리는 빔 서치($M=4$)와 Best-of-N을 비교합니다.

---

### ✅ 문장 5.3-4-3
In Figure 3 (right), we find that, despite performing similarly in aggregate, the two methods exhibit very different behavior across difficulty levels.

**🔍 단위 해석**
그림 3 (오른쪽)에서, 우리는 두 방법이 전체적으로는 비슷하게 수행됨에도 불구하고, 난이도 레벨에 따라 매우 다른 거동을 보인다는 것을 발견했습니다.

**💡 보조 설명**
* **[PDF 참조]:** Figure 3 (Right) 막대그래프를 보면, 쉬운 문제(bin 1, 2)에서는 빔 서치(파란 막대)가 예산이 늘수록 성능이 떨어지거나 정체되지만, 중간 난이도(bin 3, 4)에서는 빔 서치가 Best-of-N(주황 막대)보다 우월함을 보여줍니다.

---

### ✅ 문장 5.3-4-4
For example, on easy questions (levels $1/2$), the stronger optimizer of the two, beam search, degrades in performance as the budget increases, suggesting possible exploitation of the PRM signal.

**🔍 단위 해석**
예를 들어, 쉬운 문제(레벨 1/2)에서는 둘 중 더 강력한 최적화 도구인 빔 서치가 예산이 증가함에 따라 성능이 저하되는데, 이는 PRM 신호를 악용했을 가능성을 시사합니다.

---

### ✅ 문장 5.3-4-5
In contrast, on the harder questions (levels $3/4$), beam search outperforms best-of-N.

**🔍 단위 해석**
반대로, 더 어려운 문제(레벨 3/4)에서는 빔 서치가 Best-of-N보다 성능이 뛰어납니다.

---

### ✅ 문장 5.3-4-6
Finally, on the most difficult questions (level 5), no method makes meaningful progress.

**🔍 단위 해석**
마지막으로, 가장 어려운 문제(레벨 5)에서는 어떤 방법도 의미 있는 진전을 보이지 못합니다.

---

### ✅ 문장 5.3-4-7
These findings match intuition: we might expect that on the easy or medium difficulty questions, the verifier will make mostly correct assessments of correctness.

**🔍 단위 해석**
이러한 결과는 직관과 일치합니다: 우리는 쉽거나 중간 난이도의 문제에서는 검증기가 정답 여부를 대체로 올바르게 평가할 것이라 예상할 수 있습니다.

---

### ✅ 문장 5.3-4-8
Therefore, by optimizing further, we may be only further amplifying any spurious features learned by the verifier, causing performance degradation.

**🔍 단위 해석**
따라서 (이미 잘 맞추는데) 더 최적화하는 것은 검증기가 학습한 거짓된 특징(spurious features)만을 증폭시켜 성능 저하를 일으킬 수 있습니다.

---

### ✅ 문장 5.3-4-9
On more difficult questions, the base model is less likely to sample the correct answer, so using search can help steer the model.

**🔍 단위 해석**
더 어려운 문제에서는 베이스 모델이 정답을 샘플링할 확률이 낮으므로, 탐색을 사용하는 것이 모델을 올바른 방향으로 조종(steer)하는 데 도움을 줄 수 있습니다.

---

## 📘 단락 5.3-5
**Compute-optimal search.** Given the above, it is clear that question difficulty is a useful statistic for predicting the best search strategy at each budget. Additionally, the selected best search strategy varies as a function of difficulty. We visualize this "compute-optimal" scaling trend, as represented by the best performing search strategy, between best-of-N and beam search $(M=4)$, at each difficulty level in Figure 4. Interestingly, we see that with low budgets, using both the oracle and predicted difficulty, compute-optimal scaling can nearly outperform best-of-N using up to 4x less test-time compute (e.g. 16 versus 64 generations). While at higher budgets, some of these benefits diminish with the use of predicted difficulty, but the oracle bins still see improvements from optimal scaling. This result demonstrates that there are clear performance gains to be obtained by adaptively allocating test-time compute during search using predicted difficulty as an input statistic.

---

### ✅ 문장 5.3-5-1
**Compute-optimal search.** Given the above, it is clear that question difficulty is a useful statistic for predicting the best search strategy at each budget.

**🔍 단위 해석**
**연산 최적화 탐색.** 위 내용을 고려할 때, 문제 난이도는 각 예산에서 최적의 탐색 전략을 예측하는 데 유용한 통계량임이 분명합니다.

---

### ✅ 문장 5.3-5-2
Additionally, the selected best search strategy varies as a function of difficulty.

**🔍 단위 해석**
게다가, 선택된 최적의 탐색 전략은 난이도에 따라 달라집니다.

---

### ✅ 문장 5.3-5-3
We visualize this "compute-optimal" scaling trend, as represented by the best performing search strategy, between best-of-N and beam search $(M=4)$, at each difficulty level in Figure 4.

**🔍 단위 해석**
우리는 각 난이도 레벨에서 Best-of-N과 빔 서치($M=4$) 중 가장 성능이 좋은 전략으로 대표되는 이 "연산 최적화" 스케일링 추세를 그림 4에서 시각화합니다.

**💡 보조 설명**
* **[PDF 참조]:** Figure 4는 '연산 최적화(Compute Optimal)' 전략이 적은 예산(x축 좌측)에서 높은 정확도(y축)를 달성하여 효율적임을 보여주는 핵심 그래프입니다.

---

### ✅ 문장 5.3-5-4
Interestingly, we see that with low budgets, using both the oracle and predicted difficulty, compute-optimal scaling can nearly outperform best-of-N using up to 4x less test-time compute (e.g. 16 versus 64 generations).

**🔍 단위 해석**
흥미롭게도, 낮은 예산에서는 오라클 난이도와 예측 난이도 모두를 사용할 때, 연산 최적화 스케일링이 최대 4배 더 적은 테스트 시간 연산(예: 64회 대신 16회 생성)을 사용하고도 Best-of-N을 거의 능가할 수 있음을 확인했습니다.

---

### ✅ 문장 5.3-5-5
While at higher budgets, some of these benefits diminish with the use of predicted difficulty, but the oracle bins still see improvements from optimal scaling.

**🔍 단위 해석**
높은 예산에서는 예측 난이도를 사용할 경우 이러한 이점이 일부 줄어들지만, 오라클 구간(bins)을 사용할 때는 여전히 최적 스케일링을 통한 개선이 나타납니다.

---

### ✅ 문장 5.3-5-6
This result demonstrates that there are clear performance gains to be obtained by adaptively allocating test-time compute during search using predicted difficulty as an input statistic.

**🔍 단위 해석**
이 결과는 예측된 난이도를 입력 통계량으로 사용하여 탐색 중 테스트 시간 연산을 적응적으로 할당함으로써 얻을 수 있는 분명한 성능 이득이 있음을 입증합니다.

---

## 📘 단락 5.3-6 (Takeaways)
**Takeaways for compute-optimal scaling of verifiers**
We find that the efficacy of any given verifier search method depends critically on both the compute budget and the question at hand. Specifically, beam-search is more effective on harder questions and at lower compute budgets, whereas best-of-N is more effective on easier questions and at higher budgets. Moreover, by selecting the best search setting for a given question difficulty and test-time compute budget, we can nearly outperform best-of-N using up to 4x less test-time compute.

---

### ✅ 문장 5.3-6-1
**Takeaways for compute-optimal scaling of verifiers**
We find that the efficacy of any given verifier search method depends critically on both the compute budget and the question at hand.

**🔍 단위 해석**
**검증기의 연산 최적화 스케일링에 대한 시사점**
우리는 어떤 검증기 탐색 방법이든 그 효용성이 연산 예산과 당면한 문제 모두에 결정적으로 의존한다는 것을 발견했습니다.

---

### ✅ 문장 5.3-6-2
Specifically, beam-search is more effective on harder questions and at lower compute budgets, whereas best-of-N is more effective on easier questions and at higher budgets.

**🔍 단위 해석**
구체적으로, 빔 서치는 더 어려운 문제와 더 낮은 연산 예산에서 더 효과적인 반면, Best-of-N은 더 쉬운 문제와 더 높은 예산에서 더 효과적입니다.

---

### ✅ 문장 5.3-6-3
Moreover, by selecting the best search setting for a given question difficulty and test-time compute budget, we can nearly outperform best-of-N using up to 4x less test-time compute.

**🔍 단위 해석**
게다가, 주어진 문제 난이도와 테스트 시간 연산 예산에 맞는 최적의 탐색 설정을 선택함으로써, 우리는 최대 4배 더 적은 연산을 사용하고도 Best-of-N을 거의 능가할 수 있습니다.

---

# 6 REFINING THE PROPOSAL DISTRIBUTION

## 📘 단락 6-1
Now we study how the proposal distribution can be used for test-time scaling (Section 2). Concretely, we enable to improve its own distribution at test-time, by revising answers iteratively. Simply prompting existing LLMs to correct themselves tends to be largely ineffective on reasoning. Therefore, we finetune LLMs to iteratively revise their answers.

---

### ✅ 문장 6-1-1
Now we study how the proposal distribution can be used for test-time scaling (Section 2).

**🔍 단위 해석**
이제 우리는 제안 분포(proposal distribution)가 테스트 시간 스케일링(섹션 2)에 어떻게 사용될 수 있는지 연구합니다.

---

### ✅ 문장 6-1-2
Concretely, we enable to improve its own distribution at test-time, by revising answers iteratively.

**🔍 단위 해석**
구체적으로, 우리는 모델이 답안을 반복적으로 수정(revising)하게 함으로써 테스트 시간에 자신의 분포를 개선할 수 있도록 합니다.

---

### ✅ 문장 6-1-3
Simply prompting existing LLMs to correct themselves tends to be largely ineffective on reasoning.

**🔍 단위 해석**
기존 LLM에게 단순히 스스로 수정하라고 프롬프팅하는 것은 추론 문제에서 대체로 효과가 없는 경향이 있습니다.

---

### ✅ 문장 6-1-4
Therefore, we finetune LLMs to iteratively revise their answers.

**🔍 단위 해석**
따라서, 우리는 LLM이 자신의 답안을 반복적으로 수정하도록 미세 조정(finetune)합니다.

---

## 📘 단락 6.1-1
**6.1 TRAINING AND USING REVISION MODELS**
Our procedure for finetuning revision models is similar to Qu et al. (2024b), though we introduce some crucial differences. For finetuning, we need trajectories consisting of a sequence of incorrect answers followed by a correct answer, that we can then run SFT on. To do this, we sampled 64 responses in parallel and post-hoc constructed multi-turn rollouts from these independent samples. These rollouts consist of up to four incorrect attempts in context followed by a correct revision. We include more details on our revision model finetuning procedure in Appendix L.

---

### ✅ 문장 6.1-1-1
**6.1 TRAINING AND USING REVISION MODELS**
Our procedure for finetuning revision models is similar to Qu et al. (2024b), though we introduce some crucial differences.

**🔍 단위 해석**
**6.1 수정 모델 훈련 및 사용**
우리의 수정 모델 미세 조정 절차는 Qu et al. (2024b)과 유사하지만, 몇 가지 중요한 차이점을 도입했습니다.

---

### ✅ 문장 6.1-1-2
For finetuning, we need trajectories consisting of a sequence of incorrect answers followed by a correct answer, that we can then run SFT on.

**🔍 단위 해석**
미세 조정을 위해서는, 지도 미세 조정(SFT)을 수행할 수 있도록 '일련의 오답들 뒤에 정답이 오는' 궤적(trajectories)이 필요합니다.

---

### ✅ 문장 6.1-1-3
To do this, we sampled 64 responses in parallel and post-hoc constructed multi-turn rollouts from these independent samples.

**🔍 단위 해석**
이를 위해, 우리는 64개의 응답을 병렬로 샘플링한 뒤, 이 독립적인 샘플들로부터 사후적으로(post-hoc) 멀티턴 롤아웃을 구성했습니다.

---

### ✅ 문장 6.1-1-4
These rollouts consist of up to four incorrect attempts in context followed by a correct revision.

**🔍 단위 해석**
이 롤아웃들은 문맥(context) 내에 최대 4개의 오답 시도가 포함되고 그 뒤에 정답 수정본이 이어지는 형태로 구성됩니다.

---

### ✅ 문장 6.1-1-5
We include more details on our revision model finetuning procedure in Appendix L.

**🔍 단위 해석**
수정 모델 미세 조정 절차에 대한 더 자세한 내용은 부록 L에 포함되어 있습니다.

---

## 📘 단락 6.1-2
**Using revisions at inference-time.** Given a finetuned model, we can then sample a sequence of revisions from the model at test time. While our revision model is only trained with up to four previous answers in-context, we can sample longer chains by truncating the context to the most recent four revisions. In Figure 9 (left), we see longer chains gradually improve pass@k demonstrating that we are able to effectively teach the model to learn from mistakes in previous answers.

---

### ✅ 문장 6.1-2-1
**Using revisions at inference-time.** Given a finetuned model, we can then sample a sequence of revisions from the model at test time.

**🔍 단위 해석**
**추론 시간에 수정 사용하기.** 미세 조정된 모델이 주어지면, 테스트 시간에 모델로부터 수정본 시퀀스(sequence of revisions)를 샘플링할 수 있습니다.

---

### ✅ 문장 6.1-2-2
While our revision model is only trained with up to four previous answers in-context, we can sample longer chains by truncating the context to the most recent four revisions.

**🔍 단위 해석**
우리 수정 모델은 문맥 내에 최대 4개의 이전 답변까지만 학습되었지만, 문맥을 가장 최근의 4개 수정본으로 잘라냄(truncating)으로써 더 긴 체인을 샘플링할 수 있습니다.

---

### ✅ 문장 6.1-2-3
In Figure 9 (left), we see longer chains gradually improve pass@k demonstrating that we are able to effectively teach the model to learn from mistakes in previous answers.

**🔍 단위 해석**
그림 9 (왼쪽)에서 우리는 더 긴 체인이 pass@k를 점진적으로 향상시키는 것을 볼 수 있는데, 이는 우리가 모델에게 이전 답변의 실수로부터 배우도록 효과적으로 가르칠 수 있음을 입증합니다.

---

## 📘 단락 6.1-3
That said, there is a distribution shift at inference time: the model was trained on only sequences with incorrect answers in context, but at test-time the model may sample correct answers. Thus, the model may turn a correct answer into an incorrect one. Similar to Qu et al. (2024b), around 38% of correct answers get converted to incorrect with our model. Thus, we employ either sequential majority voting or verifier-guided selection to select the correct answer from the sequence of revisions (see Figure 5). Querying the verifier adds a 2x compute overhead, and we account for this in our analysis.

---

### ✅ 문장 6.1-3-1
That said, there is a distribution shift at inference time: the model was trained on only sequences with incorrect answers in context, but at test-time the model may sample correct answers.

**🔍 단위 해석**
그렇긴 하지만, 추론 시간에는 '분포 변화(distribution shift)'가 발생합니다. 모델은 오답들만 문맥에 있는 시퀀스로 훈련받았지만, 테스트 시간에는 정답을 샘플링할 수도 있기 때문입니다.

---

### ✅ 문장 6.1-3-2
Thus, the model may turn a correct answer into an incorrect one.

**🔍 단위 해석**
따라서 모델이 (이미 맞은) 정답을 오답으로 바꿔버릴 수도 있습니다.

---

### ✅ 문장 6.1-3-3
Similar to Qu et al. (2024b), around 38% of correct answers get converted to incorrect with our model.

**🔍 단위 해석**
Qu et al. (2024b)과 유사하게, 우리 모델에서도 약 38%의 정답이 오답으로 변환되었습니다.

---

### ✅ 문장 6.1-3-4
Thus, we employ either sequential majority voting or verifier-guided selection to select the correct answer from the sequence of revisions (see Figure 5).

**🔍 단위 해석**
그래서 우리는 수정본 시퀀스 중에서 정답을 고르기 위해 '순차적 다수결 투표(sequential majority voting)'나 '검증기 유도 선택(verifier-guided selection)'을 사용합니다(그림 5 참조).

**💡 보조 설명**
* **[PDF 참조]:** Figure 5는 **Sequential revisions**(한 줄로 쭉 수정해 나가는 방식)와 **Parallel sampling**(여러 줄을 동시에 생성하는 방식)을 비교하며, 검증기(Verifier)가 이들 중 최고를 뽑는 과정을 보여줍니다.

---

### ✅ 문장 6.1-3-5
Querying the verifier adds a 2x compute overhead, and we account for this in our analysis.

**🔍 단위 해석**
검증기에 쿼리하는 것은 2배의 연산 오버헤드를 추가하지만, 우리는 분석에서 이를 감안합니다.

---

## 📘 단락 6.2-1
**6.2 ANALYSIS RESULTS: TEST-TIME SCALING WITH REVISIONS**
We see that sampling sequentially outperforms in parallel. We might expect however, that these approaches have different properties. Intuitively, sampling in parallel acts as a global search process that could, in principle, provide coverage over many different approaches for solving a problem. Sequential sampling, on the other hand, may work more as a local refinement process. This motivates striking a balance between these two approaches by allocating some of our budget to parallel sampling (e.g. $\sqrt{N}$) and the rest to sequential (e.g. $\sqrt{N}$). We will now show the existence of a compute-optimal ratio between sequential and parallel sampling, and understand their pros and cons based on the difficulty of a prompt.

---

### ✅ 문장 6.2-1-1
**6.2 ANALYSIS RESULTS: TEST-TIME SCALING WITH REVISIONS**
We see that sampling sequentially outperforms in parallel.

**🔍 단위 해석**
**6.2 분석 결과: 수정을 활용한 테스트 시간 스케일링**
우리는 순차적으로 샘플링하는 것이 병렬로 하는 것보다 성능이 우수함을 확인했습니다.

---

### ✅ 문장 6.2-1-2
We might expect however, that these approaches have different properties.

**🔍 단위 해석**
하지만 우리는 이 두 접근법이 서로 다른 특성을 가질 것이라 예상할 수 있습니다.

---

### ✅ 문장 6.2-1-3
Intuitively, sampling in parallel acts as a global search process that could, in principle, provide coverage over many different approaches for solving a problem.

**🔍 단위 해석**
직관적으로, 병렬 샘플링은 원칙적으로 문제를 해결하기 위한 다양한 접근법들을 커버할 수 있는 '전역 탐색(global search)' 과정으로 작용합니다.

---

### ✅ 문장 6.2-1-4
Sequential sampling, on the other hand, may work more as a local refinement process.

**🔍 단위 해석**
반면, 순차적 샘플링은 '국소적 정제(local refinement)' 과정으로 더 많이 작용할 수 있습니다.

---

### ✅ 문장 6.2-1-5
This motivates striking a balance between these two approaches by allocating some of our budget to parallel sampling (e.g. $\sqrt{N}$) and the rest to sequential (e.g. $\sqrt{N}$).

**🔍 단위 해석**
이는 예산의 일부는 병렬 샘플링(예: $\sqrt{N}$)에, 나머지는 순차적 샘플링(예: $\sqrt{N}$)에 할당함으로써 두 접근법 사이의 균형을 맞추도록 유도합니다.

---

### ✅ 문장 6.2-1-6
We will now show the existence of a compute-optimal ratio between sequential and parallel sampling, and understand their pros and cons based on the difficulty of a prompt.

**🔍 단위 해석**
이제 우리는 순차적 샘플링과 병렬 샘플링 사이에 '연산 최적화 비율(compute-optimal ratio)'이 존재함을 보여주고, 프롬프트 난이도에 따른 장단점을 이해해 볼 것입니다.

---

## 📘 단락 6.2-2
**Trading off sequential and parallel compute.** To understand how to allocate sequential and parallel compute, we perform a sweep over different configurations. We see, in Figure 7 (left), that indeed, at a given budget, there exists an ideal sequential to parallel ratio. We also see in Figure 7 (right) that this ideal ratio varies depending on question difficulty. Easy questions benefit more from revisions, whereas on difficult questions it is optimal to strike a balance between sequential and parallel computation. This finding supports the hypothesis that sequential revisions (i.e., varying the proposal distribution) and parallel sampling (i.e., search with verifiers) are complementary axes for scaling test-time compute, which may be more effective on a per-prompt basis. We include examples of our model's generations in Appendix Q. Additional results are in Appendix D.

---

### ✅ 문장 6.2-2-1
**Trading off sequential and parallel compute.** To understand how to allocate sequential and parallel compute, we perform a sweep over different configurations.

**🔍 단위 해석**
**순차 및 병렬 연산의 트레이드오프.** 순차적 연산과 병렬 연산을 어떻게 할당할지 이해하기 위해, 우리는 다양한 구성에 대해 스윕을 수행합니다.

---

### ✅ 문장 6.2-2-2
We see, in Figure 7 (left), that indeed, at a given budget, there exists an ideal sequential to parallel ratio.

**🔍 단위 해석**
우리는 그림 7 (왼쪽)에서, 주어진 예산에서 실제로 이상적인 순차 대 병렬 비율이 존재한다는 것을 확인합니다.

**💡 보조 설명**
* **[PDF 참조]:** Figure 7 (Left) 그래프는 x축(순차/병렬 비율)에 따라 성능이 산 모양으로 솟아올라, 너무 한쪽으로 치우치지 않는 최적점(Peak)이 있음을 보여줍니다.

---

### ✅ 문장 6.2-2-3
We also see in Figure 7 (right) that this ideal ratio varies depending on question difficulty.

**🔍 단위 해석**
또한 그림 7 (오른쪽)에서 이 이상적인 비율이 문제 난이도에 따라 달라짐을 확인합니다.

---

### ✅ 문장 6.2-2-4
Easy questions benefit more from revisions, whereas on difficult questions it is optimal to strike a balance between sequential and parallel computation.

**🔍 단위 해석**
쉬운 문제들은 수정(revision)으로부터 더 많은 이득을 얻는 반면, 어려운 문제들에서는 순차적 연산과 병렬 연산 사이의 균형을 맞추는 것이 최적입니다.

---

### ✅ 문장 6.2-2-5
This finding supports the hypothesis that sequential revisions (i.e., varying the proposal distribution) and parallel sampling (i.e., search with verifiers) are complementary axes for scaling test-time compute, which may be more effective on a per-prompt basis.

**🔍 단위 해석**
이 발견은 순차적 수정(즉, 제안 분포 변화)과 병렬 샘플링(즉, 검증기를 이용한 탐색)이 테스트 시간 연산 스케일링을 위한 상호 보완적인 축이며, 프롬프트별로 적용할 때 더 효과적일 수 있다는 가설을 뒷받침합니다.

---

### ✅ 문장 6.2-2-6
We include examples of our model's generations in Appendix Q. Additional results are in Appendix D.

**🔍 단위 해석**
모델 생성 예시는 부록 Q에, 추가 결과는 부록 D에 포함되어 있습니다.

---

## 📘 단락 6.2-3
**Compute-optimal revisions.** Given our finding that the efficacy of sequential and parallel sampling depends on difficulty, we can select the ideal ratio of sequential to parallel compute per difficulty bin (we describe the specific ratios in Appendix O). In Figure 6, we plot results using our compute-optimal scaling when employing both oracle and predicted difficulty. In both cases, we substantially improve test-time compute scaling by optimally scaling the proposal distribution. In particular, we see that at higher generation budgets, parallel sampling plateaus, whereas compute-optimal scaling continues to improve. For both oracle and predicted difficulty, we see that compute-optimal scaling can outperform best-of-N using up to 4x less test-time compute (e.g. 64 samples versus 256). Overall, these results demonstrate the potential for improved test-time compute scaling by adjusting the proposal distribution on a per-prompt basis.

---

### ✅ 문장 6.2-3-1
**Compute-optimal revisions.** Given our finding that the efficacy of sequential and parallel sampling depends on difficulty, we can select the ideal ratio of sequential to parallel compute per difficulty bin (we describe the specific ratios in Appendix O).

**🔍 단위 해석**
**연산 최적화 수정.** 순차 및 병렬 샘플링의 효용성이 난이도에 의존한다는 발견을 바탕으로, 우리는 난이도 구간별로 이상적인 순차/병렬 연산 비율을 선택할 수 있습니다(구체적인 비율은 부록 O에 기술).

---

### ✅ 문장 6.2-3-2
In Figure 6, we plot results using our compute-optimal scaling when employing both oracle and predicted difficulty.

**🔍 단위 해석**
그림 6에서 우리는 오라클 난이도와 예측 난이도 모두를 사용할 때의 연산 최적화 스케일링 결과를 도표로 나타냅니다.

**💡 보조 설명**
* **[PDF 참조]:** Figure 6는 Revisions(수정) 전략에 대한 그래프로, 여기서도 'Compute Optimal' 선이 'Best-of-N'보다 훨씬 효율적임(더 적은 샘플로 더 높은 점수)을 보여줍니다.

---

### ✅ 문장 6.2-3-3
In both cases, we substantially improve test-time compute scaling by optimally scaling the proposal distribution.

**🔍 단위 해석**
두 경우 모두, 우리는 제안 분포를 최적으로 스케일링함으로써 테스트 시간 연산 스케일링을 상당히 개선합니다.

---

### ✅ 문장 6.2-3-4
In particular, we see that at higher generation budgets, parallel sampling plateaus, whereas compute-optimal scaling continues to improve.

**🔍 단위 해석**
특히, 높은 생성 예산에서 병렬 샘플링은 성능이 정체(plateaus)되는 반면, 연산 최적화 스케일링은 계속해서 개선되는 것을 볼 수 있습니다.

---

### ✅ 문장 6.2-3-5
For both oracle and predicted difficulty, we see that compute-optimal scaling can outperform best-of-N using up to 4x less test-time compute (e.g. 64 samples versus 256).

**🔍 단위 해석**
오라클 및 예측 난이도 모두에서, 연산 최적화 스케일링은 최대 4배 더 적은 테스트 시간 연산(예: 256개 대신 64개 샘플)을 사용하고도 Best-of-N을 능가할 수 있음을 확인했습니다.

---

### ✅ 문장 6.2-3-6
Overall, these results demonstrate the potential for improved test-time compute scaling by adjusting the proposal distribution on a per-prompt basis.

**🔍 단위 해석**
종합적으로, 이 결과들은 프롬프트별로 제안 분포를 조절함으로써 테스트 시간 연산 스케일링을 개선할 수 있는 잠재력을 입증합니다.

---

## 📘 단락 6.2-4 (Takeaways)
**Takeaways for compute-optimal scaling by refining the proposal distribution with revisions**
We find that there exists a tradeoff between sequential (e.g. revisions) and parallel (e.g. standard best-of-N) test-time computation, and the ideal ratio of sequential to parallel test-time compute depends on both the compute budget and the specific question at hand. Specifically, easier questions benefit from purely sequential test-time compute, whereas harder questions often perform best with an ideal ratio of sequential to parallel compute. By selecting the best setting for a given question difficulty and compute budget, we can outperform the parallel best-of-N baseline using up to 4x less test-time compute.

---

### ✅ 문장 6.2-4-1
**Takeaways for compute-optimal scaling by refining the proposal distribution with revisions**
We find that there exists a tradeoff between sequential (e.g. revisions) and parallel (e.g. standard best-of-N) test-time computation, and the ideal ratio of sequential to parallel test-time compute depends on both the compute budget and the specific question at hand.

**🔍 단위 해석**
**수정을 통한 제안 분포 정제의 연산 최적화 스케일링에 대한 시사점**
우리는 순차적(예: 수정) 연산과 병렬(예: 표준 Best-of-N) 테스트 시간 연산 사이에 트레이드오프가 존재하며, 이상적인 순차 대 병렬 비율은 연산 예산과 당면한 특정 문제 모두에 의존한다는 것을 발견했습니다.

---

### ✅ 문장 6.2-4-2
Specifically, easier questions benefit from purely sequential test-time compute, whereas harder questions often perform best with an ideal ratio of sequential to parallel compute.

**🔍 단위 해석**
구체적으로, 쉬운 문제들은 순전히 순차적인 테스트 시간 연산으로부터 이득을 보는 반면, 더 어려운 문제들은 종종 순차와 병렬 연산의 이상적인 비율을 맞췄을 때 가장 성능이 좋습니다.

---

### ✅ 문장 6.2-4-3
By selecting the best setting for a given question difficulty and compute budget, we can outperform the parallel best-of-N baseline using up to 4x less test-time compute.

**🔍 단위 해석**
주어진 문제 난이도와 연산 예산에 맞는 최적의 설정을 선택함으로써, 우리는 최대 4배 더 적은 테스트 시간 연산을 사용하여 병렬 Best-of-N 베이스라인을 능가할 수 있습니다.

---

# 7 EXCHANGING PRETRAINING AND TEST-TIME COMPUTE

## 📘 단락 7-1
We saw that utilizing additional test-time compute can enable us to represent more complex distributions than the one predicted by the base LLM, thereby increasing performance. We now posit that this increased flexibility of representing distributions means that we can expect additional test-time compute to make up for the lack of a higher-capacity model or training for more FLOPs during pretraining. In this section, we study to what extent this is possible. We pose the following question:

---

### ✅ 문장 7-1-1
We saw that utilizing additional test-time compute can enable us to represent more complex distributions than the one predicted by the base LLM, thereby increasing performance.

**🔍 단위 해석**
우리는 추가적인 테스트 시간 연산을 활용하는 것이 베이스 LLM이 예측하는 것보다 더 복잡한 분포를 표현할 수 있게 하여, 결과적으로 성능을 높일 수 있음을 보았습니다.

---

### ✅ 문장 7-1-2
We now posit that this increased flexibility of representing distributions means that we can expect additional test-time compute to make up for the lack of a higher-capacity model or training for more FLOPs during pretraining.

**🔍 단위 해석**
이제 우리는 이러한 분포 표현의 유연성 증가는, 추가적인 테스트 시간 연산이 '더 높은 용량의 모델'이나 '사전 학습 중 더 많은 FLOPs 투입'의 부재를 보완(make up for)할 수 있음을 의미한다고 가정(posit)합니다.

---

### ✅ 문장 7-1-3
In this section, we study to what extent this is possible. We pose the following question:

**🔍 단위 해석**
이 섹션에서는 이것이 어느 정도까지 가능한지 연구하며, 다음과 같은 질문을 던집니다.

---

## 📘 단락 7-2 (Question Box)
**Question: Exchanging pretraining and test-time compute**
Suppose a model was pre-trained with X FLOPs. Assume that we plan to run Y FLOPs of inference with this model. If we want to improve performance by increasing the total FLOPs budget by a factor of M (i.e., $M(X+Y)$ total FLOPs across both pretraining and inference), should we spend our FLOPS on increased pretraining compute or on additional test-time compute?

---

### ✅ 문장 7-2-1
**Question: Exchanging pretraining and test-time compute**
Suppose a model was pre-trained with X FLOPs. Assume that we plan to run Y FLOPs of inference with this model.

**🔍 단위 해석**
**질문: 사전 학습과 테스트 시간 연산의 교환**
어떤 모델이 X FLOPs로 사전 학습되었다고 가정합시다. 그리고 이 모델로 Y FLOPs만큼의 추론을 실행할 계획이라고 가정해 봅시다.

---

### ✅ 문장 7-2-2
If we want to improve performance by increasing the total FLOPs budget by a factor of M (i.e., $M(X+Y)$ total FLOPs across both pretraining and inference), should we spend our FLOPS on increased pretraining compute or on additional test-time compute?

**🔍 단위 해석**
만약 우리가 총 FLOPs 예산을 M배로 늘려서 성능을 개선하고자 한다면(즉, 사전 학습과 추론을 합쳐 총 $M(X+Y)$ FLOPs), 우리는 이 FLOPs를 '사전 학습 연산 증대'에 써야 할까요, 아니면 '추가적인 테스트 시간 연산'에 써야 할까요?

---

## 📘 단락 7-3
Increasing pretraining FLOPS introduces the additional design decision of whether to allocate compute to training with more data or more parameters. We focus on the setting in which model parameters are scaled up and training data amount is fixed, matching the canonical approach from the LLaMA series of models.

---

### ✅ 문장 7-3-1
Increasing pretraining FLOPS introduces the additional design decision of whether to allocate compute to training with more data or more parameters.

**🔍 단위 해석**
사전 학습 FLOPs를 늘리는 것은 더 많은 데이터로 훈련할지, 아니면 더 많은 파라미터(모델 크기)로 훈련할지에 대한 추가적인 설계 결정을 도입합니다.

---

### ✅ 문장 7-3-2
We focus on the setting in which model parameters are scaled up and training data amount is fixed, matching the canonical approach from the LLaMA series of models.

**🔍 단위 해석**
우리는 LLaMA 시리즈 모델들의 표준적인(canonical) 접근 방식에 맞춰, 훈련 데이터 양은 고정한 채 모델 파라미터를 확장하는 설정에 집중합니다.

---

## 📘 단락 7-4
**Exchanging FLOPs.** We use the common formula for pretraining FLOPS $X=6ND_{pretrain}$, and for inference FLOPs, we use $Y=4ND_{inference}$, which multiplies the standard $2ND_{inference}$ by two to account for the overhead of calling the verifier. Here N represents model parameters, $D_{pretrain}$ is the total tokens used for pretraining, and $D_{inference}$ the total tokens generated at inference. If we multiply N by a factor of M, then both the pretraining and inference FLOPs (due to the cost of greedy decoding with the larger model) increase by a factor of M, giving a total of $M(X+Y)$ FLOPs.

---

### ✅ 문장 7-4-1
**Exchanging FLOPs.** We use the common formula for pretraining FLOPS $X=6ND_{pretrain}$, and for inference FLOPs, we use $Y=4ND_{inference}$, which multiplies the standard $2ND_{inference}$ by two to account for the overhead of calling the verifier.

**🔍 단위 해석**
**FLOPs 교환.** 우리는 사전 학습 FLOPs에 대한 일반적인 공식 $X=6ND_{pretrain}$을 사용하고, 추론 FLOPs에 대해서는 검증기 호출 오버헤드를 고려하여 표준 $2ND_{inference}$에 2를 곱한 $Y=4ND_{inference}$를 사용합니다.

---

### ✅ 문장 7-4-2
Here N represents model parameters, $D_{pretrain}$ is the total tokens used for pretraining, and $D_{inference}$ the total tokens generated at inference.

**🔍 단위 해석**
여기서 N은 모델 파라미터 수, $D_{pretrain}$은 사전 학습에 사용된 총 토큰 수, $D_{inference}$는 추론 시 생성된 총 토큰 수를 나타냅니다.

---

### ✅ 문장 7-4-3
If we multiply N by a factor of M, then both the pretraining and inference FLOPs (due to the cost of greedy decoding with the larger model) increase by a factor of M, giving a total of $M(X+Y)$ FLOPs.

**🔍 단위 해석**
만약 우리가 N을 M배로 늘리면, (더 큰 모델을 사용한 그리디 디코딩 비용 때문에) 사전 학습과 추론 FLOPs 모두 M배로 증가하여, 총 $M(X+Y)$ FLOPs가 됩니다.

---

## 📘 단락 7-5
To match the FLOPs between scaling parameters and scaling test-time compute, we multiply the smaller model's inference compute by $M + \frac{3}{2}(D_{pre}/D_{inf})(M-1)$. Notably, this multiplier depends on the ratio $D_{pre}/D_{inf}$. We refer to the inverse of this ratio as $R=D_{inf}/D_{pre}$. Depending on the specific production setting, we should expect very different values of R. In particular, in large scale production settings, we may expect more inference tokens than pretraining tokens, in which case we have $R \gg 1$. On the other hand, in many self-improvement setups, we would likely generate fewer inference tokens than pretraining tokens, giving $R \ll 1$. Therefore, since the scale of test-time compute depends on this ratio, we expect differing conclusions depending on the specific setting.

---

### ✅ 문장 7-5-1
To match the FLOPs between scaling parameters and scaling test-time compute, we multiply the smaller model's inference compute by $M + \frac{3}{2}(D_{pre}/D_{inf})(M-1)$.

**🔍 단위 해석**
파라미터 스케일링과 테스트 시간 연산 스케일링 간의 FLOPs를 일치시키기 위해, 우리는 작은 모델의 추론 연산에 $M + \frac{3}{2}(D_{pre}/D_{inf})(M-1)$를 곱합니다.

---

### ✅ 문장 7-5-2
Notably, this multiplier depends on the ratio $D_{pre}/D_{inf}$. We refer to the inverse of this ratio as $R=D_{inf}/D_{pre}$.

**🔍 단위 해석**
주목할 점은 이 승수(multiplier)가 비율 $D_{pre}/D_{inf}$에 의존한다는 것입니다. 우리는 이 비율의 역수를 $R=D_{inf}/D_{pre}$라고 부릅니다.

**💡 보조 설명**
* **[Ratio R]:** $R$은 '사전 학습 토큰 대비 추론 토큰의 비율'입니다. 모델을 한 번 훈련해서 얼마나 많이 써먹느냐(추론하느냐)를 나타내는 지표입니다.

---

### ✅ 문장 7-5-3
Depending on the specific production setting, we should expect very different values of R. In particular, in large scale production settings, we may expect more inference tokens than pretraining tokens, in which case we have $R \gg 1$.

**🔍 단위 해석**
구체적인 프로덕션 설정에 따라 우리는 매우 다른 R값을 예상해야 합니다. 특히 대규모 프로덕션 설정에서는 사전 학습 토큰보다 더 많은 추론 토큰을 예상할 수 있으며, 이 경우 $R \gg 1$입니다.

---

### ✅ 문장 7-5-4
On the other hand, in many self-improvement setups, we would likely generate fewer inference tokens than pretraining tokens, giving $R \ll 1$.

**🔍 단위 해석**
반면, 많은 자기 개선(self-improvement) 설정에서는 사전 학습 토큰보다 더 적은 추론 토큰을 생성할 가능성이 높으며, 이는 $R \ll 1$을 의미합니다.

---

### ✅ 문장 7-5-5
Therefore, since the scale of test-time compute depends on this ratio, we expect differing conclusions depending on the specific setting.

**🔍 단위 해석**
따라서 테스트 시간 연산의 규모가 이 비율에 의존하기 때문에, 구체적인 설정에 따라 다른 결론이 도출될 것으로 예상합니다.

---

## 📘 단락 7-6
In Figure 8, we use this approach to exchanging test-time and pretraining compute to compare our compute-optimal scaling against scaling up model parameters by a factor of ~ 14. We conduct comparisons for 3 values of R: 0.08 ($R \ll 1$), 0.40 ($R \sim 1$), and 11 ($R \gg 1$), with each ratio corresponding to an inference budget. Observe that if we only expect to see difficult questions (e.g. bins 4/5) or have a larger $D_{inference}$ (i.e., larger R value), then it is often more effective to allocate compute towards pretraining (e.g. the star is above the line). If instead, we expect mostly easy or intermediate difficulty questions (e.g. bins 1-3 and sometimes 4) or have lower inference requirements (as is the case in self-improvement pipelines), then scaling test-time compute is preferred.

---

### ✅ 문장 7-6-1
In Figure 8, we use this approach to exchanging test-time and pretraining compute to compare our compute-optimal scaling against scaling up model parameters by a factor of ~ 14.

**🔍 단위 해석**
그림 8에서, 우리는 이러한 테스트 시간 및 사전 학습 연산 교환 접근법을 사용하여 우리의 연산 최적화 스케일링과 모델 파라미터를 약 14배 확장하는 것을 비교합니다.

**💡 보조 설명**
* **[PDF 참조]:** Figure 8은 파라미터를 키우는 것(별표 ★)과 테스트 시간 연산을 늘리는 것(선 그래프) 중 어느 것이 더 위에 있는지(성능이 좋은지) 보여줍니다.

---

### ✅ 문장 7-6-2
We conduct comparisons for 3 values of R: 0.08 ($R \ll 1$), 0.40 ($R \sim 1$), and 11 ($R \gg 1$), with each ratio corresponding to an inference budget.

**🔍 단위 해석**
우리는 3가지 R값에 대해 비교를 수행합니다: 0.08 ($R \ll 1$), 0.40 ($R \sim 1$), 11 ($R \gg 1$). 각 비율은 특정 추론 예산에 해당합니다.

---

### ✅ 문장 7-6-3
Observe that if we only expect to see difficult questions (e.g. bins 4/5) or have a larger $D_{inference}$ (i.e., larger R value), then it is often more effective to allocate compute towards pretraining (e.g. the star is above the line).

**🔍 단위 해석**
만약 우리가 어려운 문제(예: 구간 4/5)만 볼 것으로 예상하거나 $D_{inference}$가 더 크다면(즉, R값이 크다면), 연산을 사전 학습에 할당하는 것이 종종 더 효과적이라는 점을 관찰할 수 있습니다(예: 별표가 선 위에 있음).

---

### ✅ 문장 7-6-4
If instead, we expect mostly easy or intermediate difficulty questions (e.g. bins 1-3 and sometimes 4) or have lower inference requirements (as is the case in self-improvement pipelines), then scaling test-time compute is preferred.

**🔍 단위 해석**
대신, 만약 우리가 주로 쉽거나 중간 난이도의 문제(예: 구간 1-3, 때로는 4)를 예상하거나 추론 요구량이 낮다면(자기 개선 파이프라인의 경우처럼), 테스트 시간 연산을 확장하는 것이 선호됩니다.

---

## 📘 단락 7-7 (Takeaways)
**Takeaways for exchanging pretraining and test-time compute**
Test-time and pretraining compute are not 1-to-1 "exchangeable". In settings with a small inference requirement or on questions of moderate difficulty, test-time compute can substitute for pretraining. However, on challenging questions or under higher inference loads, pretraining is likely more effective.

---

### ✅ 문장 7-7-1
**Takeaways for exchanging pretraining and test-time compute**
Test-time and pretraining compute are not 1-to-1 "exchangeable".

**🔍 단위 해석**
**사전 학습과 테스트 시간 연산 교환에 대한 시사점**
테스트 시간 연산과 사전 학습 연산은 1대1로 "교환 가능"하지 않습니다.

---

### ✅ 문장 7-7-2
In settings with a small inference requirement or on questions of moderate difficulty, test-time compute can substitute for pretraining.

**🔍 단위 해석**
추론 요구량이 적은 설정이나 중간 난이도의 문제에서는 테스트 시간 연산이 사전 학습을 대체할 수 있습니다.

---

### ✅ 문장 7-7-3
However, on challenging questions or under higher inference loads, pretraining is likely more effective.

**🔍 단위 해석**
그러나 도전적인(어려운) 문제나 더 높은 추론 부하 하에서는 사전 학습이 더 효과적일 가능성이 높습니다.

---
