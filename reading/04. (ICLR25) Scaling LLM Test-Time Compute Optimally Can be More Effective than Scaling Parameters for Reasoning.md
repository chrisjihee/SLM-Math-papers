# ABSTRACT

---

## 📘 단락 본문 텍스트

Enabling LLMs to improve their outputs by using more test-time compute is a critical step towards building self-improving agents that can operate on open-ended natural language. In this paper, we scale up inference-time computation in LLMs, with a focus on answering: if an LLM is allowed to use a fixed but non-trivial amount of inference-time compute, how much can it improve its performance on a challenging prompt? Answering this question has implications not only on performance, but also on the future of LLM pretraining and how to tradeoff inference-time and pre-training compute.

---

### ✅ 문장 1-2

Enabling LLMs to improve their outputs by using more test-time compute is a critical step towards building self-improving agents that can operate on open-ended natural language. In this paper, we scale up inference-time computation in LLMs, with a focus on answering: if an LLM is allowed to use a fixed but non-trivial amount of inference-time compute, how much can it improve its performance on a challenging prompt?

**단위 해석**
LLM이 test-time compute(테스트 시점 연산)를 더 많이 사용하여 그들의 출력 결과를 개선할 수 있도록 하는 것은, 개방형 자연어 환경에서 동작할 수 있는 self-improving agents(자기 개선 에이전트)를 구축하기 위한 중요한 단계입니다. 본 논문에서 우리는 LLM의 inference-time computation(추론 시점 연산)을 확장(scale up)하며 다음 질문에 답하는 데 초점을 맞춥니다: 만약 LLM이 고정되어 있지만 적지 않은 양의(non-trivial) inference-time compute를 사용할 수 있다면, 어려운 프롬프트에 대해 성능을 얼마나 개선할 수 있을까?

**보조 설명**

* **Test-time compute (Inference-time compute):** 모델을 학습시키는 단계(Training)가 아니라, 이미 학습된 모델이 답변을 생성하는 단계(Inference)에서 더 많은 연산 자원(시간, 토큰 생성 수 등)을 사용하는 것을 의미합니다. 예를 들어, 한 번만 답변을 생성하는 것이 아니라 여러 번 생성하여 가장 좋은 것을 고르거나, 단계별로 검증하며 생성하는 방식 등이 있습니다.
* **Research Question:** 이 논문의 핵심 질문은 "모델 크기를 키우는 대신 추론 때 계산을 더 많이 하면 얼마나 좋아지는가?"입니다.

---

### ✅ 문장 3

Answering this question has implications not only on performance, but also on the future of LLM pretraining and how to tradeoff inference-time and pre-training compute.

**단위 해석**
이 질문에 답하는 것은 성능뿐만 아니라, LLM pretraining(사전 학습)의 미래, 그리고 inference-time compute와 pre-training compute 간의 tradeoff(트레이드오프, 균형 조절)를 어떻게 할지에 대해서도 시사점을 줍니다.

**보조 설명**

* **Tradeoff:** 자원이 한정되어 있을 때, 모델을 엄청나게 크게 학습시키는 데(Pre-training) 비용을 쓸 것인지, 아니면 모델은 적당히 만들고 추론할 때(Inference) 비용을 더 쓸 것인지에 대한 전략적 선택을 의미합니다.

---

## 📘 단락 본문 텍스트

Little research has attempted to understand the scaling behaviors of test-time inference methods, with current work largely providing negative results for a number of these strategies. In this work, we analyze two primary mechanisms to scale test-time computation: (1) searching against dense, process-based verifier reward models (PRMs); and (2) updating the model's distribution over a response adaptively, given the prompt at test time.

---

### ✅ 문장 4

Little research has attempted to understand the scaling behaviors of test-time inference methods, with current work largely providing negative results for a number of these strategies.

**단위 해석**
Test-time inference 방법들의 scaling behavior(확장 거동/법칙)를 이해하려는 연구는 거의 없었으며, 현재의 연구들은 이러한 전략들에 대해 대체로 부정적인 결과를 제시하고 있습니다.

**보조 설명**

* 기존 연구들은 추론 시점에 계산량을 늘려도 복잡한 문제 해결 능력이 크게 향상되지 않았다는 부정적인 결론을 낸 경우가 많았다는 점을 지적하며, 이 논문의 차별성을 강조합니다.

---

### ✅ 문장 5

In this work, we analyze two primary mechanisms to scale test-time computation: (1) searching against dense, process-based verifier reward models (PRMs); and (2) updating the model's distribution over a response adaptively, given the prompt at test time.

**단위 해석**
본 연구에서 우리는 test-time computation을 확장하기 위한 두 가지 주요 메커니즘을 분석합니다: (1) dense, process-based verifier reward models (PRMs, 과정 중심 검증 보상 모델)을 사용하여 탐색(searching)하는 방법, 그리고 (2) 테스트 시점에 주어진 프롬프트에 맞춰 반응에 대한 모델의 분포를 적응적으로 업데이트하는 방법입니다.

**보조 설명**

* **PRM (Process-based Reward Model):** 최종 정답만 채점하는 것이 아니라, 풀이 과정의 각 단계(Step)마다 점수를 매겨주는 모델입니다. 이를 통해 모델이 올바른 풀이 과정을 탐색(Tree Search 등)할 수 있게 돕습니다.
* **Updating distribution:** 모델 자체를 재학습하는 것이 아니라, 프롬프트를 통해 모델이 생성하는 답변의 확률 분포를 바꾸는 것(예: 스스로 답변을 수정하게 하는 Self-correction 등)을 의미합니다.

---

## 📘 단락 본문 텍스트

We find that in both cases, the effectiveness of different approaches to scaling test-time compute critically varies depending on the difficulty of the prompt. This observation motivates applying a "compute-optimal" scaling strategy, which acts to, as effectively as possible, allocate test-time compute per prompt in an adaptive manner. Using this compute-optimal strategy, we can improve the efficiency of test-time compute scaling for math reasoning problems by more than 4x compared to a best-of-N baseline. Additionally, in a FLOPs-matched evaluation, we find that on problems where a smaller base model attains somewhat non-trivial success rates, test-time compute can be used to outperform a 14x larger model.

---

### ✅ 문장 6-7

We find that in both cases, the effectiveness of different approaches to scaling test-time compute critically varies depending on the difficulty of the prompt. This observation motivates applying a "compute-optimal" scaling strategy, which acts to, as effectively as possible, allocate test-time compute per prompt in an adaptive manner.

**단위 해석**
우리는 두 경우 모두, test-time compute를 확장하는 다양한 접근 방식의 효율성이 프롬프트의 난이도에 따라 결정적으로 달라진다는 것을 발견했습니다. 이러한 관찰은 "compute-optimal(연산 최적화)" 확장 전략을 적용하는 동기가 되며, 이 전략은 가능한 한 효과적으로 각 프롬프트마다 적응적인 방식으로 test-time compute를 할당하는 역할을 합니다.

**보조 설명**

* **Key Insight:** 모든 문제에 똑같은 방법을 쓰는 게 아니라, 쉬운 문제는 쉬운 방법으로, 어려운 문제는 어려운 방법(더 많은 연산)으로 접근하는 것이 최적이라는 뜻입니다.

---

### ✅ 문장 8-9

Using this compute-optimal strategy, we can improve the efficiency of test-time compute scaling for math reasoning problems by more than 4x compared to a best-of-N baseline. Additionally, in a FLOPs-matched evaluation, we find that on problems where a smaller base model attains somewhat non-trivial success rates, test-time compute can be used to outperform a 14x larger model.

**단위 해석**
이 compute-optimal 전략을 사용하여, 우리는 수학 추론 문제에서 Best-of-N 기준선(baseline)과 비교하여 test-time compute 확장의 효율성을 4배 이상 개선할 수 있었습니다. 또한, FLOPs-matched(연산량 일치) 평가에서, 작은 베이스 모델이 어느 정도 의미 있는 성공률을 달성하는 문제들의 경우, test-time compute를 사용하여 14배 더 큰 모델보다 더 뛰어난 성능을 낼 수 있음을 발견했습니다.

**보조 설명**

* **Best-of-N:** 모델에게 N개의 답변을 생성하게 한 뒤, 그중 가장 좋은 것(또는 다수결)을 선택하는 가장 단순하고 강력한 베이스라인 기법입니다.
* **FLOPs-matched evaluation:** 공정한 비교를 위해, '작은 모델 + 추가 추론 연산'의 총 연산량(Floating Point Operations)과 '큰 모델(14배) + 기본 추론'의 총 연산량을 같게 맞추고 성능을 비교했다는 뜻입니다.

---

# 1 INTRODUCTION

---

## 📘 단락 본문 텍스트

Given a challenging input, can we enable LLMs to most effectively make use of additional computation at test-time to improve their responses? In theory, additional test-time compute should enable an LLM to do better than what it was trained to do in zero-shot. Such a capability at test-time bears the potential to unlock agentic and reasoning abilities (Shinn et al., 2023; Qu et al., 2024b). Additionally, if pre-trained model size can be traded off for additional inference compute, this would enable the deployment of smaller on-device models in place of datacenter scale LLMs. Automating the inference-time improvement of model outputs also provides a path towards a general self-improvement algorithm that can function with reduced human supervision.

---

### ✅ 문장 10-12

Given a challenging input, can we enable LLMs to most effectively make use of additional computation at test-time to improve their responses? In theory, additional test-time compute should enable an LLM to do better than what it was trained to do in zero-shot. Such a capability at test-time bears the potential to unlock agentic and reasoning abilities (Shinn et al., 2023; Qu et al., 2024b).

**단위 해석**
어려운 입력이 주어졌을 때, 우리는 LLMs가 답변을 개선하기 위해 test-time에서 추가적인 연산을 가장 효과적으로 활용하도록 할 수 있을까요? 이론적으로, 추가적인 test-time compute는 LLM이 zero-shot으로 수행하도록 훈련받은 것보다 더 나은 성능을 낼 수 있게 해야 합니다. Test-time에서의 이러한 능력은 agentic(에이전트적) 능력과 reasoning(추론) 능력을 잠금 해제할 잠재력을 지니고 있습니다.

**보조 설명**

* **Zero-shot:** 추가적인 예시나 튜닝 없이 모델이 학습된 상태 그대로 문제를 푸는 것.

---

### ✅ 문장 13-14

Additionally, if pre-trained model size can be traded off for additional inference compute, this would enable the deployment of smaller on-device models in place of datacenter scale LLMs. Automating the inference-time improvement of model outputs also provides a path towards a general self-improvement algorithm that can function with reduced human supervision.

**단위 해석**
게다가, 만약 pre-trained 모델의 크기를 추가적인 inference compute와 맞바꿀(trade off) 수 있다면, 이는 데이터센터 규모의 LLM 대신 더 작은 on-device(온디바이스) 모델의 배포를 가능하게 할 것입니다. 모델 출력의 inference-time 개선을 자동화하는 것은 또한 사람의 감독을 줄인 상태에서 작동할 수 있는 일반적인 self-improvement(자기 개선) 알고리즘을 향한 길을 제공합니다.

---

## 📘 단락 본문 텍스트

Prior work studying inference-time computation provides mixed results. On the one hand, some prior work shows that current LLMs can use test-time computation to improve their outputs (Bai et al., 2022; Madaan et al., 2023; Du et al., 2023; Saunders et al., 2022; Yao et al., 2023), on the other hand, several other works show that the effectiveness of these methods on complex tasks such as math reasoning remains limited (Huang et al., 2023; Stechly et al., 2023; Valmeekam et al., 2023; Wang et al., 2024a; Olausson et al., 2024). However, reasoning is a domain we should expect to benefit from test-time compute, since reasoning involves drawing inferences from existing knowledge as opposed to acquiring new knowledge. Therefore, the disagreement in these prior findings motivates the need for a systematic analysis of different approaches for scaling test-time compute.

---

### ✅ 문장 15

Prior work studying inference-time computation provides mixed results. On the one hand, some prior work shows that current LLMs can use test-time computation to improve their outputs , on the other hand, several other works show that the effectiveness of these methods on complex tasks such as math reasoning remains limited.

**단위 해석**
Inference-time computation을 연구한 이전 연구들은 엇갈린(mixed) 결과를 제공합니다. 한편으로는 현재의 LLM들이 test-time computation을 사용하여 출력을 개선할 수 있음을 보여주는 연구들이 있는 반면, 다른 한편으로는 수학 추론과 같은 복잡한 작업에서 이러한 방법들의 효과가 여전히 제한적임을 보여주는 연구들도 있습니다.

**보조 설명**

* 선행 연구들 사이의 상충되는 결과(된다 vs 안 된다)를 제시하며, 이 문제를 명확히 할 필요성을 제기합니다.

---

### ✅ 문장 16-17

However, reasoning is a domain we should expect to benefit from test-time compute, since reasoning involves drawing inferences from existing knowledge as opposed to acquiring new knowledge. Therefore, the disagreement in these prior findings motivates the need for a systematic analysis of different approaches for scaling test-time compute.

**단위 해석**
하지만 reasoning(추론)은 새로운 지식을 습득하는 것이 아니라 기존 지식으로부터 추론을 이끌어내는 과정을 포함하기 때문에, 우리는 추론 영역이 test-time compute의 혜택을 받을 것이라 기대해야 합니다. 따라서, 이전 연구 결과들의 불일치는 test-time compute를 확장하는 다양한 접근 방식에 대한 체계적인 분석의 필요성을 불러일으킵니다.

**보조 설명**

* 추론은 '지식 암기'가 아니라 '논리적 사고' 과정이므로, 시간을 더 들이면(더 많이 생각하면) 결과가 좋아져야 한다는 직관을 설명합니다.

---

## 📘 단락 본문 텍스트

In this paper we understand the pros and cons of scaling up test-time compute, and how it compares with scaling up pre-training compute. While the simplest approach for scaling test-time compute is best-of-N sampling - sampling N outputs in "parallel" from a base LLM and selecting the one that scores the highest per a learned verifier or a reward model (Cobbe et al., 2021; Lightman et al., 2023) - there are many other ways we could conceivably scale up test-time compute. We unify methods into those that modify either the proposal distribution from which responses are sampled (for e.g., by asking the base model to revise its responses (Qu et al., 2024b)) or those that alter how the verifier is used for searching directly in the output space (e.g. by training a PRM (Lightman et al., 2023)).

---

### ✅ 문장 18

In this paper we understand the pros and cons of scaling up test-time compute, and how it compares with scaling up pre-training compute.

**단위 해석**
본 논문에서 우리는 test-time compute를 확장하는 것의 장단점을 이해하고, 그것이 pre-training compute를 확장하는 것과 어떻게 비교되는지 알아봅니다.

---

### ✅ 문장 19-20

While the simplest approach for scaling test-time compute is best-of-N sampling - sampling N outputs in "parallel" from a base LLM and selecting the one that scores the highest per a learned verifier or a reward model  - there are many other ways we could conceivably scale up test-time compute. We unify methods into those that modify either the proposal distribution from which responses are sampled (for e.g., by asking the base model to revise its responses ) or those that alter how the verifier is used for searching directly in the output space (e.g. by training a PRM ).

**단위 해석**
Test-time compute를 확장하는 가장 간단한 접근법은 Best-of-N sampling — 베이스 LLM에서 N개의 출력을 "병렬로" 샘플링하고 학습된 verifier(검증기)나 reward model(보상 모델)을 통해 가장 높은 점수를 받은 것을 선택하는 것 — 이지만, 우리가 상상할 수 있는 test-time compute를 확장하는 다른 많은 방법들이 있습니다. 우리는 방법론들을, 반응이 샘플링되는 proposal distribution(제안 분포)을 수정하는 것(예: 베이스 모델에게 답변을 수정하도록 요청)과 출력 공간에서 직접 탐색하기 위해 verifier가 사용되는 방식을 변경하는 것(예: PRM 훈련)으로 통합하여 분류합니다.

**보조 설명**

* **두 가지 축:** 저자들은 Test-time compute 방법을 크게 두 가지로 나눕니다.
1. **Proposal Distribution 수정:** 모델이 답변을 생성하는 방식 자체를 개선 (예: "다시 생각해봐"라고 하여 수정하기).
2. **Verifier 최적화 (Search):** 생성된 답변들 사이를 탐색하는 방식 개선 (예: PRM을 이용한 Tree Search).



---

## 📘 단락 본문 텍스트

To understand the benefits of scaling up test-time compute, we carry out experiments on MATH (Hendrycks et al., 2021) using PaLM-2 (Anil et al., 2023) models fine-tuned to either revise incorrect answers (Qu et al., 2024b) (e.g. improving the proposal distribution; Section 6) or verify the correctness of individual steps in an answer using a process-based reward model (PRM) (Lightman et al., 2023; Wang et al., 2023) (Section 5). We find that the efficacy of different test-time strategies depends on both the nature of the specific problem at hand and the base LLM used. For example, on "easier" problems, for which the base LLM can already produce reasonable-looking responses, allowing the model to sequentially revise its initial answer (i.e., modifying the proposal distribution) is a more effective use of compute than reranking N independent answers sampled in parallel. On the other hand, on difficult problems which require searching over many high-level strategies, re-sampling new responses independently in parallel or deploying tree-search against a process reward model is more effective. This underscores the need to deploy an adaptive "compute-optimal" strategy for scaling test-time compute, wherein the specific approach for utilizing test-time compute is selected depending on the prompt, so as to make the best use of additional computation.

---

### ✅ 문장 21

To understand the benefits of scaling up test-time compute, we carry out experiments on MATH using PaLM-2 models fine-tuned to either revise incorrect answers  (e.g. improving the proposal distribution; Section 6) or verify the correctness of individual steps in an answer using a process-based reward model (PRM)  (Section 5).

**단위 해석**
Test-time compute 확장의 이점을 이해하기 위해, 우리는 틀린 답을 수정하도록 finetuned(미세 조정된) PaLM-2 모델(예: 제안 분포 개선; 섹션 6) 또는 PRM을 사용하여 답변의 개별 단계의 정확성을 검증하도록 finetuned된 모델(섹션 5)을 사용하여 MATH 벤치마크에서 실험을 수행합니다.

---

### ✅ 문장 22-24

We find that the efficacy of different test-time strategies depends on both the nature of the specific problem at hand and the base LLM used. For example, on "easier" problems, for which the base LLM can already produce reasonable-looking responses, allowing the model to sequentially revise its initial answer (i.e., modifying the proposal distribution) is a more effective use of compute than reranking N independent answers sampled in parallel. On the other hand, on difficult problems which require searching over many high-level strategies, re-sampling new responses independently in parallel or deploying tree-search against a process reward model is more effective.

**단위 해석**
우리는 다양한 test-time 전략의 효용성이 당면한 특정 문제의 본질과 사용된 베이스 LLM에 따라 달라진다는 것을 발견했습니다. 예를 들어, 베이스 LLM이 이미 그럴싸한 반응을 생성할 수 있는 "더 쉬운" 문제들의 경우, 모델이 초기 답변을 순차적으로 수정하도록 허용하는 것(즉, 제안 분포를 수정하는 것)이 N개의 독립적인 답변을 병렬로 샘플링하여 reranking(재순위화)하는 것보다 더 효과적인 연산 사용법입니다. 반면, 많은 고차원적 전략을 탐색해야 하는 어려운 문제들의 경우, 새로운 반응을 독립적으로 병렬 재샘플링하거나 process reward model(PRM)을 사용하여 tree-search(트리 탐색)를 배포하는 것이 더 효과적입니다.

**보조 설명**

* **핵심 발견:**
* **쉬운 문제:** 답변을 고쳐쓰기(Revision)가 유리함. (이미 정답에 근접했으므로 다듬는 게 효율적)
* **어려운 문제:** 새로 많이 뽑기(Parallel Sampling)나 탐색(Search)이 유리함. (처음부터 방향이 틀렸을 수 있으므로 다양한 시도가 필요)



---

### ✅ 문장 25

This underscores the need to deploy an adaptive "compute-optimal" strategy for scaling test-time compute, wherein the specific approach for utilizing test-time compute is selected depending on the prompt, so as to make the best use of additional computation.

**단위 해석**
이는 test-time compute를 확장하기 위해 적응형 "compute-optimal" 전략을 배포할 필요성을 강조하는데, 여기서 test-time compute를 활용하는 구체적인 접근 방식은 추가적인 연산을 최적으로 활용하기 위해 프롬프트에 따라 선택됩니다.

---

## 📘 단락 본문 텍스트

We also show that a notion of question difficulty (Section 4) from the perspective of the base LLM can be used to predict the efficacy of test-time computation, enabling us to practically instantiate this "compute-optimal" scaling. By appropriately allocating test-time compute in this way, we are able to greatly improve test-time compute scaling, surpassing the performance of a best-of-N baseline while only using ~ 4x less computation with both revisions and search (Sections 5 and 6).

---

### ✅ 문장 26-27

We also show that a notion of question difficulty (Section 4) from the perspective of the base LLM can be used to predict the efficacy of test-time computation, enabling us to practically instantiate this "compute-optimal" scaling. By appropriately allocating test-time compute in this way, we are able to greatly improve test-time compute scaling, surpassing the performance of a best-of-N baseline while only using ~ 4x less computation with both revisions and search (Sections 5 and 6).

**단위 해석**
우리는 또한 베이스 LLM 관점에서의 질문 난이도 개념(섹션 4)이 test-time computation의 효용성을 예측하는 데 사용될 수 있으며, 이를 통해 이러한 "compute-optimal" 확장을 실제로 구현(instantiate)할 수 있음을 보여줍니다. 이런 방식으로 test-time compute를 적절히 할당함으로써, 우리는 revisions와 search 모두에서 Best-of-N 베이스라인의 성능을 능가하면서도 약 4배 적은 연산량만을 사용하여 test-time compute 확장을 크게 개선할 수 있습니다.

---

## 📘 단락 본문 텍스트

Using our scaling strategy, we then study to what extent test-time computation can substitute for additional pretraining. Specifically, we conduct a FLOPs-matched comparison between a smaller model with test-time compute and pretraining a 14x larger model. We find that on easy and intermediate questions, additional test-time compute is often preferable to scaling pretraining. This finding suggests that rather than focusing purely on scaling pretraining, in some settings it is more efficient to pretrain smaller models with less compute, and then apply test-time compute to improve outputs. That said, with the most challenging questions, we observe few benefits from scaling up test-time compute. Instead, on these questions, it is more effective to make progress by applying additional pretraining, demonstrating that current approaches to scaling test-time compute may not be 1-to-1 exchangeable with scaling pretraining. Overall, this suggests that even with a fairly naïve methodology, scaling up test-time computation can already be preferable to pretraining in some settings, with only more improvements as test-time strategies mature. Longer term, this hints at a future where fewer FLOPs are spent during pretraining and more FLOPs are spent at inference.

---

### ✅ 문장 28-29

Using our scaling strategy, we then study to what extent test-time computation can substitute for additional pretraining. Specifically, we conduct a FLOPs-matched comparison between a smaller model with test-time compute and pretraining a 14x larger model.

**단위 해석**
우리의 확장 전략을 사용하여, 우리는 test-time computation이 추가적인 pretraining을 어느 정도까지 대체할 수 있는지 연구합니다. 구체적으로, 우리는 test-time compute를 사용하는 더 작은 모델과 14배 더 큰 모델을 pretraining하는 것 사이의 FLOPs-matched 비교를 수행합니다.

---

### ✅ 문장 30-31

We find that on easy and intermediate questions, additional test-time compute is often preferable to scaling pretraining. This finding suggests that rather than focusing purely on scaling pretraining, in some settings it is more efficient to pretrain smaller models with less compute, and then apply test-time compute to improve outputs.

**단위 해석**
우리는 쉬운 문제와 중간 난이도 문제에서, 추가적인 test-time compute가 pretraining을 확장하는 것보다 종종 더 선호된다(유리하다)는 것을 발견했습니다. 이 발견은 단순히 pretraining 확장에만 집중하기보다는, 어떤 설정에서는 더 적은 연산으로 작은 모델을 pretraining한 다음, 출력을 개선하기 위해 test-time compute를 적용하는 것이 더 효율적임을 시사합니다.

---

### ✅ 문장 32-33

That said, with the most challenging questions, we observe few benefits from scaling up test-time compute. Instead, on these questions, it is more effective to make progress by applying additional pretraining, demonstrating that current approaches to scaling test-time compute may not be 1-to-1 exchangeable with scaling pretraining.

**단위 해석**
그렇기는 하지만, 가장 도전적인(어려운) 문제들에서는, test-time compute를 확장하는 것으로부터 얻는 이점이 거의 없음을 관찰했습니다. 대신, 이러한 문제들에서는 추가적인 pretraining을 적용하여 진전을 이루는 것이 더 효과적이며, 이는 현재의 test-time compute 확장 접근 방식들이 pretraining 확장과 1대1로 교환 가능하지 않을 수 있음을 보여줍니다.

**보조 설명**

* **한계점:** 정말 어려운 문제는 추론 시간에 아무리 시간을 쏟아도(test-time compute), 모델 자체의 지능(pretraining)이 부족하면 해결이 안 된다는 중요한 통찰입니다.

---

### ✅ 문장 34-35

Overall, this suggests that even with a fairly naïve methodology, scaling up test-time computation can already be preferable to pretraining in some settings, with only more improvements as test-time strategies mature. Longer term, this hints at a future where fewer FLOPs are spent during pretraining and more FLOPs are spent at inference.

**단위 해석**
종합적으로, 이는 꽤 단순한(naïve) 방법론을 사용하더라도 일부 설정에서는 test-time computation을 확장하는 것이 pretraining보다 이미 더 나을 수 있으며, test-time 전략이 성숙해짐에 따라 더 많은 개선이 있을 것임을 시사합니다. 장기적으로, 이는 pretraining 동안에는 더 적은 FLOPs를 소비하고 inference(추론) 동안에 더 많은 FLOPs를 소비하는 미래를 암시합니다.

---

## 📘 Figure 1 텍스트 (Caption)

Figure 1: Summary of results. Left: Compute-optimal scaling for revisions and search. We compare the compute-optimal scaling policy with PaLM 2-S* against baselines in the revision (top) and PRM search (bottom) settings. In the revision setting, we find that compute-optimal scaling outperforms best-of-N with 4x less compute. Similarly, with PRM search, compute-optimal scaling shows large early improvements over best-of-N, nearly outperforming best-of-N with 4x less compute at points (see Sec. 5 and 6). Right: Comparing test-time compute and parameter scaling. We compare compute-optimal test-time scaling with PaLM 2-S* against a $ \sim 14 $x larger model without additional test-time compute. We expect X tokens of pretraining for both models and Y tokens of inference. A larger model, multiplies the FLOPs for both. If we were to apply additional test-time compute to the smaller model, to match this FLOPs multiplier, we see that for the revisions (top) when $ Y \ll X $, test-time compute is preferable to pretraining. As inference to pretraining ratio increases, test-time compute is preferable on easy questions. However, on hard questions, pretraining is preferable.

---

### ✅ Figure 1 설명

**단위 해석 및 보조 설명**

* **Left (Compute-optimal scaling):** Revision(수정)과 Search(탐색) 두 설정 모두에서, 저자들이 제안한 'Compute-optimal' 전략(난이도에 따라 방법 선택)이 단순히 많이 생성하는 'Best-of-N' 방식보다 약 4배 적은 연산량으로 비슷한 성능을 냅니다.
* **Right (Test-time vs Parameter scaling):** 작은 모델(PaLM 2-S*)에 추론 연산을 더 쓰는 것과, 14배 큰 모델을 그냥 쓰는 것을 비교했습니다.
* **결과:** 추론 토큰 수가 사전 학습 토큰 수보다 훨씬 적을 때(), 그리고 쉬운/중간 문제에서는 **Test-time compute**를 늘리는 게 더 효율적입니다. 하지만 아주 어려운 문제에서는 여전히 **Pretraining(모델 크기)**이 중요합니다.



---

# 2 UNIFIED PERSPECTIVE ON TEST-TIME COMPUTE: PROPOSER & VERIFIER

---

## 📘 단락 본문 텍스트

We first provide an unified abstraction of test-time compute to situate contemporary approaches. We view the use of test-time compute through the lens of modifying the model's distribution on a given prompt, adaptively at test-time. Ideally, test-time compute should allow for the ability to express more complex distributions than naïvely sampling from the LLM. In general, there are two knobs to modify an LLM's distribution: (1) at the input level: by augmenting the given prompt with an additional set of tokens that the LLM conditions on to obtain a modified proposal distribution, or (2) at the output level: by sampling multiple candidates from the standard LLM and performing surgery on them, using some post-hoc verifiers or scorers. This process is reminiscent of Markov chain Monte Carlo (MCMC) sampling from a complex distribution by combining a simple proposal distribution and a score function. Modifying the proposal distribution by altering inputs tokens and using a verifier form the two independent axes of our study.

---

### ✅ 문장 1-3

We first provide an unified abstraction of test-time compute to situate contemporary approaches. We view the use of test-time compute through the lens of modifying the model's distribution on a given prompt, adaptively at test-time. Ideally, test-time compute should allow for the ability to express more complex distributions than naïvely sampling from the LLM.

**단위 해석**
먼저 우리는 현대의 접근 방식들을 정립하기 위해 test-time compute(테스트 시점 연산)에 대한 통일된 추상화(abstraction)를 제공합니다. 우리는 test-time compute의 사용을, 주어진 프롬프트에 대해 test-time에 적응적으로 모델의 분포를 수정하는 관점에서 바라봅니다. 이상적으로, test-time compute는 LLM에서 단순하게(naïvely) 샘플링하는 것보다 더 복잡한 분포를 표현할 수 있는 능력을 허용해야 합니다.

**보조 설명**

* **Unified Abstraction:** 다양한 기법(Re-ranking, Self-correction 등)을 하나의 틀(분포 수정)로 통합하여 설명하겠다는 의미입니다.
* **Complex Distributions:** 기본 모델은 단순히 학습된 확률대로 단어를 뱉어내지만, 연산을 더 쓰면 '논리적으로 더 타당한 답'이나 '여러 번 검토된 답'과 같이 더 정교한 확률 분포를 따르는 답변을 만들 수 있다는 뜻입니다.

---

### ✅ 문장 4-6

In general, there are two knobs to modify an LLM's distribution: (1) at the input level: by augmenting the given prompt with an additional set of tokens that the LLM conditions on to obtain a modified proposal distribution, or (2) at the output level: by sampling multiple candidates from the standard LLM and performing surgery on them, using some post-hoc verifiers or scorers. This process is reminiscent of Markov chain Monte Carlo (MCMC) sampling from a complex distribution by combining a simple proposal distribution and a score function. Modifying the proposal distribution by altering inputs tokens and using a verifier form the two independent axes of our study.

**단위 해석**
일반적으로, LLM의 분포를 수정하는 데는 두 가지 조절 장치(knobs)가 있습니다: (1) 입력 레벨에서: 수정된 proposal distribution(제안 분포)을 얻기 위해 LLM이 조건부로 삼을 추가 토큰 세트로 주어진 프롬프트를 증강(augmenting)하는 방법, 또는 (2) 출력 레벨에서: 표준 LLM으로부터 여러 후보를 샘플링하고 사후(post-hoc) verifiers(검증기)나 scorers(채점기)를 사용하여 그들에 대해 수술(surgery, 수정/선별 작업)을 수행하는 방법입니다. 이 과정은 단순한 제안 분포와 점수 함수(score function)를 결합하여 복잡한 분포로부터 샘플링하는 MCMC(Markov Chain Monte Carlo)를 연상시킵니다. 입력 토큰을 변경하여 제안 분포를 수정하는 것과 verifier를 사용하는 것은 우리 연구의 두 가지 독립적인 축을 형성합니다.

**보조 설명**

* **Input Level (Proposal Distribution):** 모델에게 "다시 생각해봐", "이 단계가 틀렸어"와 같은 추가 텍스트(토큰)를 입력으로 주어, 모델이 내놓을 답변의 확률 분포 자체를 바꾸는 방식입니다. (예: Self-correction)
* **Output Level (Verifier):** 모델이 뱉어낸 답변들(Output) 중에서 좋은 것을 골라내거나(Best-of-N), 나쁜 것을 가지치기하는 방식입니다. (예: PRM Search)
* **MCMC 비유:** 통계학에서 복잡한 확률 분포를 근사하기 위해 쉬운 분포에서 샘플을 뽑고 이를 평가(Accept/Reject)하는 과정과 비슷하다는 비유입니다.

---

## 📘 단락 본문 텍스트

**(1) Modifying the proposal distribution.** One way to improve the proposal distribution is to directly optimize the model for a given reasoning task via RL-inspired finetuning methods such as STaR or . These techniques specifically finetune the model to directly improve the proposal distribution, rather than generating additional tokens at test-time. Instead, techniques such as self-critique enable the model to improve its own proposals at test time by instructing it to critique and revise its outputs iteratively. Since prompting off-the-shelf models is not effective at enabling effective revisions at test time, we specifically finetune models to iteratively revise their answers for complex reasoning, using Best-of-N guidance.

---

### ✅ 문장 7-8 (Modifying the proposal distribution)

**(1) Modifying the proposal distribution.** One way to improve the proposal distribution is to directly optimize the model for a given reasoning task via RL-inspired finetuning methods such as STaR or . These techniques specifically finetune the model to directly improve the proposal distribution, rather than generating additional tokens at test-time.

**단위 해석**
**(1) 제안 분포 수정하기.** 제안 분포를 개선하는 한 가지 방법은 STaR나 $ ReST^{EM} $과 같은 RL(강화학습) 기반 finetuning 방법을 통해 주어진 추론 작업에 대해 모델을 직접 최적화하는 것입니다. 이러한 기술들은 test-time에 추가 토큰을 생성하는 대신, 제안 분포 자체를 직접 개선하도록 모델을 finetuning하는 데 특화되어 있습니다.

**보조 설명**

* **STaR / ReSTEM:** 모델이 스스로 만든 데이터 중 정답인 것만 골라 다시 학습하는 방식입니다. 이는 모델 자체의 성능(Base Model Performance)을 높이는 것이지, test-time compute(추론 시 추가 연산)를 활용하는 방식과는 조금 다릅니다. 저자들은 이 방식과 구분되는 'Test-time' 기법에 집중하려 합니다.

---

### ✅ 문장 9-10

Instead, techniques such as self-critique enable the model to improve its own proposals at test time by instructing it to critique and revise its outputs iteratively. Since prompting off-the-shelf models is not effective at enabling effective revisions at test time, we specifically finetune models to iteratively revise their answers for complex reasoning, using Best-of-N guidance.

**단위 해석**
대신, self-critique(자기 비평)와 같은 기술들은 모델에게 자신의 출력을 반복적으로 비평하고 수정하도록 지시함으로써 test-time에 자신의 제안을 개선할 수 있게 합니다. 상용(off-the-shelf) 모델에 프롬프팅만 하는 것은 test-time에 효과적인 수정(revisions)을 가능하게 하는 데 효과적이지 않기 때문에, 우리는 Best-of-N guidance를 사용하여 복잡한 추론에 대해 답을 반복적으로 수정하도록 모델을 특별히 finetuning합니다.

**보조 설명**

* **핵심 전략 1 (Revision):** 단순히 "고쳐봐"라고 프롬프트만 입력하는 것은 성능이 떨어지므로, 저자들은 **"수정하는 방법"을 별도로 학습(Finetuning)**시킨 모델을 사용하여 실험을 진행합니다.

---

## 📘 단락 본문 텍스트

**(2) Optimizing the verifier.** The verifier selects the best answer from the proposal distribution. The most canonical way to use such a verifier is by applying best-of-N sampling, wherein we sample N solutions and then select the best one with a verifier. This approach can be further improved by training a process-based reward model (PRM), which produces a prediction of the correctness of each intermediate step in a solution. We can then utilize these per-step predictions to perform tree search over the solution space, enabling a more effective modification of the proposal distribution.

---

### ✅ 문장 11-12 (Optimizing the verifier)

**(2) Optimizing the verifier.** The verifier selects the best answer from the proposal distribution. The most canonical way to use such a verifier is by applying best-of-N sampling, wherein we sample N solutions and then select the best one with a verifier.

**단위 해석**
**(2) 검증기 최적화하기.** Verifier(검증기)는 제안 분포에서 나온 답 중 가장 좋은 것을 선택합니다. 이러한 verifier를 사용하는 가장 정석적인(canonical) 방법은 Best-of-N sampling을 적용하는 것인데, 이는 N개의 해답을 샘플링한 다음 verifier를 이용해 가장 좋은 하나를 선택하는 방식입니다.

---

### ✅ 문장 13-14

This approach can be further improved by training a process-based reward model (PRM), which produces a prediction of the correctness of each intermediate step in a solution. We can then utilize these per-step predictions to perform tree search over the solution space, enabling a more effective modification of the proposal distribution.

**단위 해석**
이 접근법은 해결 과정의 각 중간 단계(intermediate step)의 정확성에 대한 예측을 생성하는 process-based reward model (PRM)을 훈련시킴으로써 더욱 개선될 수 있습니다. 그러면 우리는 이러한 단계별(per-step) 예측을 활용하여 해답 공간(solution space)에 대해 tree search(트리 탐색)를 수행할 수 있으며, 이를 통해 제안 분포를 더욱 효과적으로 수정할 수 있게 됩니다.

**보조 설명**

* **핵심 전략 2 (Verifier & Search):** 단순히 답만 고르는 게 아니라, PRM을 이용해 **풀이 과정 단계별로 점수**를 매기고, 이를 바탕으로 더 좋은 경로를 찾아가는 탐색(Search) 기법을 사용합니다.

---

# 3 HOW TO SCALE TEST-TIME COMPUTATION OPTIMALLY

---

## 📘 단락 본문 텍스트

Using this unified view of different methods, we would like to understand and characterize how to most effectively use test-time computation to improve performance on a given prompt by answering the question below. When either refining the proposal distribution or searching against a verifier, there are numerous choices on how to allocate test-time compute. For example, when using a model finetuned for revisions as the proposal distribution and an ORM verifier, we could either spend the full test-time budget on generating N independent samples in parallel from the model and then apply best-of-N, or we could sample N revisions in sequence using a revision model and then select the best answer in the sequence with an ORM, or strike a balance between these extremes.

---

### ✅ 문장 15-16

Using this unified view of different methods, we would like to understand and characterize how to most effectively use test-time computation to improve performance on a given prompt by answering the question below. When either refining the proposal distribution or searching against a verifier, there are numerous choices on how to allocate test-time compute.

**단위 해석**
이러한 다양한 방법들에 대한 통합된 관점을 사용하여, 우리는 아래 질문에 답함으로써 주어진 프롬프트에 대해 성능을 향상시키기 위해 test-time computation을 가장 효과적으로 사용하는 방법을 이해하고 규명(characterize)하고자 합니다. 제안 분포를 정제(refining)하거나 verifier를 이용해 탐색(searching)할 때, test-time compute를 어떻게 할당할지에 대한 수많은 선택지가 존재합니다.

---

### ✅ 문장 17

For example, when using a model finetuned for revisions as the proposal distribution and an ORM verifier, we could either spend the full test-time budget on generating N independent samples in parallel from the model and then apply best-of-N, or we could sample N revisions in sequence using a revision model and then select the best answer in the sequence with an ORM, or strike a balance between these extremes.

**단위 해석**
예를 들어, 수정(revision)을 위해 finetuned된 모델을 제안 분포로 사용하고 ORM verifier를 사용할 때, 우리는 전체 test-time 예산(budget)을 모델로부터 N개의 독립적인 샘플을 병렬(parallel)로 생성한 뒤 Best-of-N을 적용하는 데 쓸 수도 있고, 혹은 revision 모델을 사용하여 순차적으로(in sequence) N번의 수정을 샘플링한 뒤 ORM으로 시퀀스 내에서 가장 좋은 답을 선택할 수도 있으며, 아니면 이 극단들 사이에서 균형을 맞출 수도 있습니다.

**보조 설명**

* **ORM (Outcome-based Reward Model):** PRM과 달리 최종 결과만 보고 점수를 주는 모델입니다.
* **Allocation Dilemma:** 예산이 100번의 생성 기회라면, 100개를 동시에 뽑을지(Parallel), 아니면 하나를 100번 고칠지(Sequential), 아니면 10개를 뽑아서 각각 10번씩 고칠지(Balance) 결정해야 한다는 문제입니다.

---

## 📘 단락 본문 텍스트

Intuitively, we might expect that problems where the initial samples are more likely to be on the right track to benefit more from revisions. On the other hand, problems that require exploration over high-level problem solving strategies might benefit from sampling more independent answers in parallel. Finally, in the case of verifiers, we also can choose between different search algorithms (e.g. beam-search, lookahead-search, best-of-N), each of which may exhibit different properties depending on the quality of the verifier and proposal distribution at hand. More sophisticated search procedures might be more useful in harder problems compared to a much simpler best-of-N or majority baseline.

---

### ✅ 문장 18-19

Intuitively, we might expect that problems where the initial samples are more likely to be on the right track to benefit more from revisions. On the other hand, problems that require exploration over high-level problem solving strategies might benefit from sampling more independent answers in parallel.

**단위 해석**
직관적으로, 초기 샘플들이 올바른 방향에 있을 가능성이 높은 문제들은 수정(revisions)으로부터 더 많은 이득을 볼 것이라 예상할 수 있습니다. 반면, 고차원적인 문제 해결 전략에 대한 탐색(exploration)이 필요한 문제들은 병렬로 더 많은 독립적인 답을 샘플링하는 것으로부터 이득을 볼 수 있습니다.

**보조 설명**

* **직관:** 쉬운 문제는 이미 답에 가까우니 고쳐 쓰는 게 낫고, 어려운 문제는 아예 접근 방식이 틀렸을 수 있으니 다양하게 시도해보는 게 낫다는 가설입니다.

---

### ✅ 문장 20-21

Finally, in the case of verifiers, we also can choose between different search algorithms (e.g. beam-search, lookahead-search, best-of-N), each of which may exhibit different properties depending on the quality of the verifier and proposal distribution at hand. More sophisticated search procedures might be more useful in harder problems compared to a much simpler best-of-N or majority baseline.

**단위 해석**
마지막으로, verifier의 경우에도 다양한 탐색 알고리즘(예: beam-search, lookahead-search, best-of-N) 중에서 선택할 수 있으며, 각각은 사용 중인 verifier와 제안 분포의 품질에 따라 다른 특성을 보일 수 있습니다. 훨씬 더 단순한 Best-of-N이나 majority(다수결) 베이스라인에 비해, 더 정교한 탐색 절차가 더 어려운 문제에서 유용할 수 있습니다.

---

## 📘 단락 본문 텍스트 (Problem Setup)

> **Problem setup.** We are given a prompt and a test-time compute budget within which to solve the problem. Under the abstraction above, there are different knobs we can tune when utilizing test-time computation. How can we determine the most effective way to utilize test-time compute for a given prompt? And how well would this do against simply utilizing a much bigger pretrained model?

---

### ✅ 문장 22-25

> **Problem setup.** We are given a prompt and a test-time compute budget within which to solve the problem. Under the abstraction above, there are different knobs we can tune when utilizing test-time computation. How can we determine the most effective way to utilize test-time compute for a given prompt? And how well would this do against simply utilizing a much bigger pretrained model?

**단위 해석**

> **문제 설정.** 우리에게 프롬프트와, 그 안에서 문제를 해결해야 하는 test-time compute 예산(budget)이 주어집니다. 앞서 설명한 추상화 하에서, test-time computation을 활용할 때 조절할 수 있는 다양한 장치(knobs)들이 있습니다. 주어진 프롬프트에 대해 test-time compute를 활용하는 가장 효과적인 방법을 어떻게 결정할 수 있을까요? 그리고 이것이 단순히 훨씬 더 큰 pretrained 모델을 사용하는 것과 비교해 얼마나 잘 수행될까요?

---

## 📘 단락 본문 텍스트

### 3.1 COMPUTE-OPTIMAL TEST-TIME SCALING STRATEGY

Per the discussion above, we would like to prescribe the optimal allocation of our test-time compute budget onto a given problem. To this end, for any given approach of utilizing test-time compute (e.g., revisions and search against a verifier in this paper, some combination or other methods in general), we define the "test-time compute-optimal scaling strategy" as the strategy that chooses hyperparameters appearing in a given approach for maximal performance benefits on a given prompt at test time.

---

### ✅ 문장 26-27

Per the discussion above, we would like to prescribe the optimal allocation of our test-time compute budget onto a given problem. To this end, for any given approach of utilizing test-time compute (e.g., revisions and search against a verifier in this paper, some combination or other methods in general), we define the "test-time compute-optimal scaling strategy" as the strategy that chooses hyperparameters appearing in a given approach for maximal performance benefits on a given prompt at test time.

**단위 해석**
위의 논의에 따라, 우리는 주어진 문제에 대해 우리의 test-time compute 예산을 최적으로 할당(optimal allocation)하는 방법을 처방하고자 합니다. 이를 위해, test-time compute를 활용하는 어떤 주어진 접근 방식(예: 본 논문의 revisions 및 verifier 탐색, 또는 일반적인 다른 방법들의 조합)에 대해서든, "test-time compute-optimal scaling strategy(테스트 시점 연산 최적화 확장 전략)"를 주어진 프롬프트에 대해 test-time에 최대의 성능 이득을 얻기 위해 해당 접근 방식에 있는 하이퍼파라미터를 선택하는 전략으로 정의합니다.

---

## 📘 단락 본문 텍스트

Formally, define  as the distribution over natural language output tokens induced by the model for a given prompt $ q $, using test-time compute hyper-parameters $ \theta $, and a compute budget of . We would like to select the hyper-parameters  which maximize the accuracy of the target distribution for a given problem. We express this formally as:

where  denotes the ground-truth correct response for input query $ q $, and  represents the test-time compute-optimal scaling strategy for the problem  with compute budget . We note that our definition of test-time compute-optimal scaling differs slightly from that of concurrent work in that our notion of scaling is question dependent.

---

### ✅ 문장 28-29

Formally, define  as the distribution over natural language output tokens induced by the model for a given prompt $ q $, using test-time compute hyper-parameters $ \theta $, and a compute budget of . We would like to select the hyper-parameters  which maximize the accuracy of the target distribution for a given problem.

**단위 해석**
형식적으로, $ Target(\theta, N, q) $를 주어진 프롬프트 $ q $에 대해 test-time compute 하이퍼파라미터 $ \theta $와 연산 예산 $ N $을 사용하여 모델에 의해 유도된 자연어 출력 토큰들의 분포로 정의합시다. 우리는 주어진 문제에 대해 이 목표 분포의 정확도를 최대화하는 하이퍼파라미터 $ \theta $를 선택하고자 합니다.

---

### ✅ 문장 30-31

We express this formally as:

where  denotes the ground-truth correct response for input query $ q $, and  represents the test-time compute-optimal scaling strategy for the problem  with compute budget .

**단위 해석**
이를 수식으로 표현하면 위와 같습니다 (식 1). 여기서 $ y^*(q) $는 입력 쿼리 $ q $에 대한 정답(ground-truth)을 나타내며, $ \theta_{q,y^*(q)}^*(N) $는 연산 예산 $ N $을 가진 문제 $ q $에 대한 test-time compute-optimal scaling 전략을 나타냅니다.

**보조 설명**

* **수식 의미:** 예산 N이 주어졌을 때, 정답()을 맞출 기댓값(확률)을 최대화하는 설정()을 찾는다는 뜻입니다.

---

### ✅ 문장 32

We note that our definition of test-time compute-optimal scaling differs slightly from that of concurrent work in that our notion of scaling is question dependent.

**단위 해석**
우리는 우리의 test-time compute-optimal scaling 정의가 확장의 개념이 질문에 의존적(question dependent)이라는 점에서 동시대 연구들과 약간 다르다는 점을 주목합니다.

**보조 설명**

* 다른 연구들은 "전체적으로 이 파라미터가 좋다"를 찾는다면, 이 연구는 "문제 A에는 이 설정, 문제 B에는 저 설정이 좋다"는 식의 **적응형(Adaptive)** 전략을 추구합니다.

---

## 📘 단락 본문 텍스트

### 3.2 QUESTION DIFFICULTY IS A GOOD APPROXIMATION FOR THE OPTIMAL STRATEGY

In order to effectively analyze the test-time scaling properties of the different mechanisms discussed in Section 2 (e.g. proposal distribution and verifier), we will prescribe an approximation to this optimal strategy  as a function of a statistic of a given prompt. Our approximation estimates a notion of difficulty for a given prompt. The compute-optimal strategy is then defined as a function of the difficulty of a prompt. Despite being only an heuristic approach to solve Equation 1, we find that it can still induce substantial improvements in performance over a baseline strategy of allocating this inference-time compute in an ad-hoc manner.

---

### ✅ 문장 33-35

In order to effectively analyze the test-time scaling properties of the different mechanisms discussed in Section 2 (e.g. proposal distribution and verifier), we will prescribe an approximation to this optimal strategy  as a function of a statistic of a given prompt. Our approximation estimates a notion of difficulty for a given prompt. The compute-optimal strategy is then defined as a function of the difficulty of a prompt.

**단위 해석**
섹션 2에서 논의된 다양한 메커니즘들(예: 제안 분포 및 verifier)의 test-time scaling 속성을 효과적으로 분석하기 위해, 우리는 이 최적 전략 $ \theta_{q,y^*(q)}^*(N) $에 대한 근사값(approximation)을 주어진 프롬프트의 통계량에 대한 함수로서 처방할 것입니다. 우리의 근사는 주어진 프롬프트에 대한 '난이도(difficulty)' 개념을 추정합니다. 그러면 compute-optimal 전략은 프롬프트의 난이도에 대한 함수로 정의됩니다.

---

### ✅ 문장 36

Despite being only an heuristic approach to solve Equation 1, we find that it can still induce substantial improvements in performance over a baseline strategy of allocating this inference-time compute in an ad-hoc manner.

**단위 해석**
이것이 식 1을 풀기 위한 경험적(heuristic) 접근법일 뿐임에도 불구하고, 우리는 이것이 inference-time compute를 임의적인(ad-hoc) 방식으로 할당하는 베이스라인 전략에 비해 상당한 성능 향상을 유도할 수 있음을 발견했습니다.

---

## 📘 단락 본문 텍스트

Our estimate of question difficulty assigns a given question to one of five discrete difficulty levels. We then use these bins to estimate  on a validation set (given a compute budget), and apply the optimal strategy on the test set. Thus, question difficulty acts as a sufficient statistic for designing the compute-optimal strategy. For example, to optimally allocate test-time compute between parallel best-of-N and sequential sampling, we first pre-compute the accuracy of both techniques within each difficulty bin using a held-out set. Given a new test question, we then determine the difficulty bin it belongs to and select the best performing strategy within that bin.

---

### ✅ 문장 37-39

Our estimate of question difficulty assigns a given question to one of five discrete difficulty levels. We then use these bins to estimate  on a validation set (given a compute budget), and apply the optimal strategy on the test set. Thus, question difficulty acts as a sufficient statistic for designing the compute-optimal strategy.

**단위 해석**
우리의 질문 난이도 추정은 주어진 질문을 5개의 이산적인(discrete) 난이도 레벨 중 하나로 할당합니다. 그런 다음 우리는 이 구간(bins)들을 사용하여 검증 세트(validation set)에서 (주어진 연산 예산 하에) $ \theta_{q,y^*(q)}^*(N) $를 추정하고, 테스트 세트에 그 최적 전략을 적용합니다. 따라서, 질문 난이도는 compute-optimal 전략을 설계하기 위한 충분통계량(sufficient statistic) 역할을 합니다.

---

### ✅ 문장 40-41

For example, to optimally allocate test-time compute between parallel best-of-N and sequential sampling, we first pre-compute the accuracy of both techniques within each difficulty bin using a held-out set. Given a new test question, we then determine the difficulty bin it belongs to and select the best performing strategy within that bin.

**단위 해석**
예를 들어, 병렬 Best-of-N과 순차적 샘플링(sequential sampling) 사이에 test-time compute를 최적으로 할당하기 위해, 우리는 먼저 보류된(held-out) 세트를 사용하여 각 난이도 구간 내에서 두 기술의 정확도를 미리 계산합니다. 새로운 테스트 질문이 주어지면, 우리는 그것이 속한 난이도 구간을 결정하고 그 구간 내에서 가장 성능이 좋은 전략을 선택합니다.

**보조 설명**

* **구체적 방법:**
1. 검증 데이터셋의 문제들을 난이도 1~5로 분류.
2. 난이도 1에서는 전략 A가 좋고, 난이도 5에서는 전략 B가 좋다는 것을 미리 파악.
3. 실제 테스트 때 문제가 난이도 5로 판별되면 전략 B를 사용.



---

## 📘 단락 본문 텍스트

**Defining question difficulty.** Following Lightman et al. (2023), we define question difficulty as a function of the given base LLM. Specifically, we bin the model's pass@1 rate estimated from 2048 samples on each test question into five quantiles, each corresponding to increasing difficulty levels. We find this notion of model-specific difficulty bins to be more predictive of the efficacy of using test-time compute compared to the hand-labeled difficulty bins in the MATH dataset.

---

### ✅ 문장 42-43

**Defining question difficulty.** Following Lightman et al. (2023), we define question difficulty as a function of the given base LLM. Specifically, we bin the model's pass@1 rate estimated from 2048 samples on each test question into five quantiles, each corresponding to increasing difficulty levels.

**단위 해석**
**질문 난이도 정의하기.** Lightman et al. (2023)을 따르며, 우리는 질문 난이도를 주어진 베이스 LLM의 함수로 정의합니다. 구체적으로, 우리는 각 테스트 질문에 대해 2048개의 샘플로부터 추정된 모델의 pass@1 비율(정답률)을 5개의 분위수(quantiles)로 나누어(bin), 각각이 증가하는 난이도 레벨에 해당하도록 합니다.

**보조 설명**

* **Pass@1 rate:** 모델이 한 번에 정답을 맞힐 확률. 즉, 2048번 풀어봤을 때 정답률이 높으면 쉬운 문제, 낮으면 어려운 문제입니다.

---

### ✅ 문장 44

We find this notion of model-specific difficulty bins to be more predictive of the efficacy of using test-time compute compared to the hand-labeled difficulty bins in the MATH dataset.

**단위 해석**
우리는 이러한 모델 특화 난이도 구간(bin)의 개념이 MATH 데이터셋에 사람이 라벨링한 난이도 구간보다 test-time compute 사용의 효용성을 예측하는 데 더 예측력이 높다는 것을 발견했습니다.

**보조 설명**

* 사람이 어렵다고 느낀 것보다, **모델이 실제로 어려워하는지**가 최적화 전략을 세우는 데 더 중요하다는 의미입니다.

---

## 📘 단락 본문 텍스트

That said, we note that assessing difficulty as described assumes oracle access to a correctness checker, which is unavailable at deployment. To enable a realistic estimate of difficulty, we approximate difficulty via a model-predicted notion of difficulty, which constructs the bins by averaging the score of a learned verifier on the same 2048 samples per problem. We refer to this setting as model-predicted difficulty and the setting which relies on ground-truth correctness as oracle difficulty. Predicted difficulty removes the reliance on ground truth labels, but still incurs computational cost. Our experiments do not account for this cost largely for simplicity, since our goal is to present some of the first results of what is in fact possible by effectively allocating test-time compute.

---

### ✅ 문장 45-46

That said, we note that assessing difficulty as described assumes oracle access to a correctness checker, which is unavailable at deployment. To enable a realistic estimate of difficulty, we approximate difficulty via a model-predicted notion of difficulty, which constructs the bins by averaging the score of a learned verifier on the same 2048 samples per problem.

**단위 해석**
그렇기는 하지만, 우리는 앞서 설명한 난이도 평가가 배포 시점에는 사용할 수 없는 정답 확인기(correctness checker)에 대한 oracle access(오라클 접근/정답 접근)를 가정한다는 점을 언급합니다. 현실적인 난이도 추정을 가능하게 하기 위해, 우리는 문제당 동일한 2048개 샘플에 대해 학습된 verifier의 점수를 평균내어 구간을 구성하는 model-predicted(모델 예측) 난이도 개념을 통해 난이도를 근사합니다.

**보조 설명**

* **Oracle Difficulty:** 실제 정답을 보고 "너 이거 많이 틀렸으니 어려운 문제네"라고 하는 것. (실전 불가)
* **Model-predicted Difficulty:** 정답을 모르지만, Verifier가 "이 답변들 점수가 다 낮은 걸 보니 어려운 문제 같아"라고 추측하는 것. (실전 가능)

---

### ✅ 문장 47-49

We refer to this setting as model-predicted difficulty and the setting which relies on ground-truth correctness as oracle difficulty. Predicted difficulty removes the reliance on ground truth labels, but still incurs computational cost. Our experiments do not account for this cost largely for simplicity, since our goal is to present some of the first results of what is in fact possible by effectively allocating test-time compute.

**단위 해석**
우리는 이 설정을 model-predicted difficulty(모델 예측 난이도)라고 부르며, 정답(ground-truth)의 정확성에 의존하는 설정을 oracle difficulty(오라클 난이도)라고 부릅니다. 예측된 난이도는 정답 라벨에 대한 의존성을 제거하지만, 여전히 연산 비용을 발생시킵니다. 우리의 실험에서는 주로 단순함을 위해 이 비용을 고려하지 않는데, 이는 우리의 목표가 test-time compute를 효과적으로 할당함으로써 실제로 무엇이 가능한지에 대한 첫 번째 결과들을 제시하는 것이기 때문입니다.

---

