# 0 Abstract

## 📘 단락 0-1
A promising approach for improving reasoning in large language models is to use process reward models (PRMs). PRMs provide feedback at each step of a multi-step reasoning trace, improving credit assignment over outcome reward models (ORMs) that only provide feedback at the final step. However, collecting dense, per-step human labels is not scalable, and training PRMs from automatically-labeled data has thus far led to limited gains. With the goal of using PRMs to improve a base policy via test-time search and reinforcement learning (RL), we ask: "How should we design process rewards?" Our key insight is that, to be effective, the process reward for a step should measure progress: a change in the likelihood of producing a correct response in the future, before and after taking the step, as measured under a prover policy distinct from the base policy. Such progress values can distinguish good and bad steps generated by the base policy, even though the base policy itself cannot. Theoretically, we show that even weaker provers can improve the base policy, as long as they distinguish steps without being too misaligned with the base policy. Our results show that process rewards defined as progress under such provers improve the efficiency of exploration during test-time search and online RL. We empirically validate our claims by training process advantage verifiers (PAVs) to measure progress under such provers and show that compared to ORM, they are > 8% more accurate, and $1.5-5\times$ more compute-efficient. Equipped with these insights, our PAVs enable one of the first results showing a 6x gain in sample efficiency for a policy trained using online RL with PRMs vs. ORMs.

---

### ✅ 문장 0-1-1
A promising approach for improving reasoning in large language models is to use process reward models (PRMs).

**🔍 단위 해석**
대형 언어 모델(LLM)의 추론 능력을 향상시키기 위한 유망한 접근법 중 하나는 과정 보상 모델(PRM)을 사용하는 것입니다.

**💡 보조 설명**
* **[키워드] 과정 보상 모델 (Process Reward Models, PRMs):** 최종 결과뿐만 아니라 문제를 해결해 나가는 중간 과정의 각 단계(Step)에 대해 보상을 부여하여 모델을 훈련시키는 기법입니다.

---

### ✅ 문장 0-1-2
PRMs provide feedback at each step of a multi-step reasoning trace, improving credit assignment over outcome reward models (ORMs) that only provide feedback at the final step.

**🔍 단위 해석**
PRM은 다단계 추론 과정의 각 단계마다 피드백을 제공함으로써, 마지막 단계에서만 피드백을 제공하는 결과 보상 모델(ORM)에 비해 기여도 할당(credit assignment) 문제를 개선합니다.

**💡 보조 설명**
* **[키워드] 결과 보상 모델 (Outcome Reward Models, ORMs):** 최종 답이 맞았는지 틀렸는지에 대해서만 보상을 주는 모델입니다.
* **[키워드] 기여도 할당 (Credit Assignment):** 최종 성공이나 실패가 전체 과정 중 어떤 구체적인 행동(단계) 덕분인지 혹은 때문인지 판단하는 문제입니다.

---

### ✅ 문장 0-1-3
However, collecting dense, per-step human labels is not scalable, and training PRMs from automatically-labeled data has thus far led to limited gains.

**🔍 단위 해석**
하지만, 매 단계마다 인간이 조밀하게 라벨을 수집하는 것은 확장성이 떨어지며, 지금까지 자동 라벨링된 데이터를 통해 PRM을 훈련시키는 것은 제한적인 성능 향상만을 가져왔습니다.

**💡 보조 설명**
* 인간이 일일이 각 추론 단계를 평가하는 것은 비용과 시간이 너무 많이 들어 대규모 훈련에 적합하지 않음을 지적합니다.

---

### ✅ 문장 0-1-4
With the goal of using PRMs to improve a base policy via test-time search and reinforcement learning (RL), we ask: "How should we design process rewards?"

**🔍 단위 해석**
테스트 시간 탐색(test-time search)과 강화 학습(RL)을 통해 기본 정책(base policy)을 개선하기 위해 PRM을 사용한다는 목표를 가지고, 우리는 "과정 보상을 어떻게 설계해야 하는가?"라는 질문을 던집니다.

**💡 보조 설명**
* **[키워드] 기본 정책 (Base Policy):** 강화 학습에서 학습 대상이 되는 기본 언어 모델입니다.

---

### ✅ 문장 0-1-5
Our key insight is that, to be effective, the process reward for a step should measure progress: a change in the likelihood of producing a correct response in the future, before and after taking the step, as measured under a prover policy distinct from the base policy.

**🔍 단위 해석**
우리의 핵심 통찰은 과정 보상이 효과적이려면 '진척도(progress)'를 측정해야 한다는 것입니다. 즉, 기본 정책과는 구별되는 '증명자 정책(prover policy)' 하에서 측정했을 때, 해당 단계를 수행하기 전과 후의 미래에 올바른 응답을 생성할 가능성의 변화량을 측정해야 합니다.

**💡 보조 설명**
* **[키워드] 진척도 (Progress):** 단순히 그 단계의 정답 여부가 아니라, 이 단계를 밟음으로써 최종 정답에 도달할 확률이 얼마나 높아졌는지를 의미합니다.
* **[키워드] 증명자 정책 (Prover Policy):** 기본 정책과 분리되어 단계의 가치를 평가하는 참조 모델 역할을 하는 정책입니다.

---

### ✅ 문장 0-1-6
Such progress values can distinguish good and bad steps generated by the base policy, even though the base policy itself cannot.

**🔍 단위 해석**
이러한 진척도 값은 기본 정책 자체가 스스로 구별하지 못할지라도, 기본 정책에 의해 생성된 좋은 단계와 나쁜 단계를 구별할 수 있게 해줍니다.

**💡 보조 설명**
* 모델 자신이 생성한 문장의 가치를 스스로 평가하기는 어렵지만, 외부의 다른 관점(증명자 정책)을 활용하면 이를 효과적으로 판별할 수 있다는 뜻입니다.

---

### ✅ 문장 0-1-7
Theoretically, we show that even weaker provers can improve the base policy, as long as they distinguish steps without being too misaligned with the base policy.

**🔍 단위 해석**
이론적으로 우리는 증명자가 기본 정책과 너무 어긋나지(misaligned) 않으면서 단계들을 구별할 수 있기만 하다면, 기본 정책보다 성능이 떨어지는(weaker) 증명자를 사용하더라도 기본 정책을 개선할 수 있음을 보여줍니다.

**💡 보조 설명**
* 반드시 더 크고 훌륭한 모델만이 평가자 역할을 할 수 있는 것은 아니며, 특정 조건만 만족하면 더 작은 모델도 평가자로 활용할 수 있음을 시사하는 중요한 이론적 발견입니다.

---

### ✅ 문장 0-1-8
Our results show that process rewards defined as progress under such provers improve the efficiency of exploration during test-time search and online RL.

**🔍 단위 해석**
우리의 연구 결과는 이러한 증명자 하에서 진척도로 정의된 과정 보상이 테스트 시간 탐색 및 온라인 강화 학습 과정에서 탐색(exploration)의 효율성을 향상시킨다는 것을 보여줍니다.

**💡 보조 설명**
* **[키워드] 탐색 (Exploration):** 강화 학습 모델이 정답을 찾기 위해 다양한 시도와 경로를 뻗어나가는 과정을 의미합니다.

---

### ✅ 문장 0-1-9
We empirically validate our claims by training process advantage verifiers (PAVs) to measure progress under such provers and show that compared to ORM, they are > 8% more accurate, and $1.5-5\times$ more compute-efficient.

**🔍 단위 해석**
우리는 이러한 증명자 하에서 진척도를 측정하기 위해 과정 이점 검증기(PAV)를 훈련시킴으로써 우리의 주장을 경험적으로 검증하며, ORM과 비교했을 때 8% 이상 더 높은 정확도와 $1.5-5\times$ 더 높은 연산 효율성을 보임을 입증합니다.

**💡 보조 설명**
* **[키워드] 과정 이점 검증기 (Process Advantage Verifiers, PAVs):** 본 논문에서 제안하는 모델로, 단순한 Q-value가 아닌 단계 전후의 '이점(Advantage, 즉 확률의 변화)'을 예측하도록 훈련된 검증 모델입니다.

---

### ✅ 문장 0-1-10
Equipped with these insights, our PAVs enable one of the first results showing a 6x gain in sample efficiency for a policy trained using online RL with PRMs vs. ORMs.

**🔍 단위 해석**
이러한 통찰을 바탕으로, 우리의 PAV는 PRM을 사용한 온라인 강화 학습으로 훈련된 정책이 ORM을 사용한 경우에 비해 샘플 효율성 측면에서 6배의 이득을 얻을 수 있음을 보여주는 최초의 결과 중 하나를 가능하게 합니다.

**💡 보조 설명**
* **[키워드] 샘플 효율성 (Sample Efficiency):** 동일한 목표 성능에 도달하기 위해 필요한 데이터(샘플)의 양을 의미하며, 6배 좋아졌다는 것은 훨씬 적은 시도만으로도 성공적으로 학습됨을 뜻합니다.


# 1 Introduction

## 📘 단락 1-1
Trained reward models or verifiers are often used to improve math reasoning in large language models, either by re-ranking solutions at test-time (Collins, 2000) or during reinforcement learning (RL) (Uesato et al., 2022). Typically, verifiers are trained to predict the outcome of an entire reasoning trace, often referred to as outcome reward models (ORM) (Cobbe et al., 2021b; Hosseini et al., 2024). However, ORMs only provide a sparse signal of correctness, which is hard to learn from and inefficient to search against. This challenge is alleviated by fine-grained supervision, in theory. For reasoning, prior works train process reward models (PRMs) that assign intermediate rewards after each step of search (Snell et al., 2024) or during RL. While Lightman et al. (2023) obtains PRM annotations from human raters, this approach is not scalable. More recent works (Wang et al., 2024; Luo et al., 2024) train PRMs to predict automatically-generated annotations that estimate future success of solving the problem, akin to value functions in RL. So far, automated PRMs, especially as dense rewards in RL, only improve by 1-2% over ORMs (Shao et al., 2024), raising doubts about their utility.

---

### ✅ 문장 1-1-1
Trained reward models or verifiers are often used to improve math reasoning in large language models, either by re-ranking solutions at test-time (Collins, 2000) or during reinforcement learning (RL) (Uesato et al., 2022).

**🔍 단위 해석**
훈련된 보상 모델이나 검증기(verifier)는 종종 대형 언어 모델의 수학적 추론을 향상시키는 데 사용되며, 이는 테스트 시점에 솔루션의 순위를 재조정(re-ranking)하거나 강화 학습(RL) 과정에서 활용됩니다.

**💡 보조 설명**
* 생성된 여러 개의 답변 중 가장 그럴듯한 정답을 찾아내는 역할(검증기)을 설명합니다.

---

### ✅ 문장 1-1-2
Typically, verifiers are trained to predict the outcome of an entire reasoning trace, often referred to as outcome reward models (ORM) (Cobbe et al., 2021b; Hosseini et al., 2024).

**🔍 단위 해석**
일반적으로 검증기는 전체 추론 과정의 최종 결과를 예측하도록 훈련되며, 이를 흔히 결과 보상 모델(ORM)이라고 부릅니다.

**💡 보조 설명**
* 기존에 널리 쓰이는 방식이 전체 풀이 과정의 마지막 도출된 정답만을 채점하는 방식임을 설명합니다.

---

### ✅ 문장 1-1-3
However, ORMs only provide a sparse signal of correctness, which is hard to learn from and inefficient to search against.

**🔍 단위 해석**
하지만 ORM은 정답 여부에 대한 희소한(sparse) 신호만을 제공하기 때문에, 학습하기 어렵고 탐색 과정에서도 비효율적입니다.

**💡 보조 설명**
* 긴 수식을 10줄에 걸쳐 풀었는데 마지막 10번째 줄의 답이 틀렸다고 0점을 줘버리면, 모델 입장에서는 앞의 9줄이 맞았는지조차 알 길이 없어 학습이 느려짐(Sparse signal 문제)을 뜻합니다.

---

### ✅ 문장 1-1-4
This challenge is alleviated by fine-grained supervision, in theory.

**🔍 단위 해석**
이러한 문제는 이론적으로 세밀한 수준의 감독(fine-grained supervision)을 통해 완화될 수 있습니다.

**💡 보조 설명**
* 최종 결과뿐만 아니라 풀이 단계별로 채점하는 방식을 의미합니다.

---

### ✅ 문장 1-1-5
For reasoning, prior works train process reward models (PRMs) that assign intermediate rewards after each step of search (Snell et al., 2024) or during RL.

**🔍 단위 해석**
추론을 위해, 이전 연구들은 탐색 과정의 각 단계 이후나 RL 도중에 중간 보상을 부여하는 과정 보상 모델(PRM)을 훈련시켰습니다.

**💡 보조 설명**
* PRM의 도입 배경과 기존 연구 사례를 설명합니다.

---

### ✅ 문장 1-1-6
While Lightman et al. (2023) obtains PRM annotations from human raters, this approach is not scalable.

**🔍 단위 해석**
Lightman 등(2023)은 인간 평가자로부터 PRM 주석을 얻었지만, 이 접근법은 확장성(scalable)이 없습니다.

**💡 보조 설명**
* 사람이 직접 하나하나 피드백을 다는 것은 비용이 기하급수적으로 늘어나기 때문에 한계가 있다는 점을 지적합니다.

---

### ✅ 문장 1-1-7
More recent works (Wang et al., 2024; Luo et al., 2024) train PRMs to predict automatically-generated annotations that estimate future success of solving the problem, akin to value functions in RL.

**🔍 단위 해석**
더 최근의 연구들은 문제 해결의 미래 성공 가능성을 추정하는 자동 생성 주석을 예측하도록 PRM을 훈련시켰으며, 이는 강화 학습의 가치 함수(value functions)와 유사합니다.

**💡 보조 설명**
* 인간을 대체하기 위해 모델 스스로 시뮬레이션을 돌려 해당 단계에서 최종 정답이 나올 확률(가치 함수)을 계산해 라벨로 삼는 방식을 말합니다.

---

### ✅ 문장 1-1-8
So far, automated PRMs, especially as dense rewards in RL, only improve by 1-2% over ORMs (Shao et al., 2024), raising doubts about their utility.

**🔍 단위 해석**
지금까지 자동화된 PRM은, 특히 RL의 밀집(dense) 보상으로 사용되었을 때 ORM에 비해 불과 1-2% 정도의 성능 향상만을 보여 그 효용성에 대한 의구심을 불러일으켰습니다.

**💡 보조 설명**
* 자동화된 PRM이 엄청난 기대와 달리 실제 RL 훈련 시 성능 향상 폭이 미미하여 한계에 봉착했음을 보여줍니다.

---

## 📘 단락 1-2
To resolve these uncertainties, in this paper, our goal is to train automated PRMs that can improve a base policy, via compute efficient test-time search, and sample-efficient online RL with PRMs as dense rewards. For this, we first ask: (i) what should the per-step process rewards measure, and (ii) what kind of automated data collection strategy should we use to train PRMs that predict this measure. For (i), conventional belief (Lightman et al., 2023; Uesato et al., 2022) has been to measure mathematical correctness or relevance of steps. But, it is unclear if this supervision yields the most improvement in the base policy (e.g., a policy may need to generate simpler, repetitive, and even incorrect steps to explore and discover the final answer during test-time search and RL). Our key insight is that per-step, process rewards that measure notion of progress: change in the likelihood of arriving at a correct final answer before and after taking the step, are effective, for both test-time beam search and online RL. Reinforcing promising steps that make progress regardless of whether they appear in a correct or incorrect trace results in overall better exploration of possible answers at initial steps, which is crucial when the approach to solve a problem is not clear. Formally, such rewards correspond to per-step advantages of steps from the RL literature (Sutton & Barto, 2018). We empirically show that using advantages in conjunction with ORM rewards outperforms using future probabilities of success or Q-values (Wang et al., 2024) for both search and RL. This is because, in the combinatorial space of responses, Q-values mainly "exploit" states whereas advantages also "explore" steps that make the most progress towards the final answer.

---

### ✅ 문장 1-2-1
To resolve these uncertainties, in this paper, our goal is to train automated PRMs that can improve a base policy, via compute efficient test-time search, and sample-efficient online RL with PRMs as dense rewards.

**🔍 단위 해석**
이러한 불확실성을 해결하기 위해, 본 논문에서 우리의 목표는 연산 효율적인 테스트 시간 탐색과 PRM을 밀집 보상으로 사용하는 샘플 효율적인 온라인 RL을 통해 기본 정책을 향상시킬 수 있는 자동화된 PRM을 훈련시키는 것입니다.

**💡 보조 설명**
* 논문의 궁극적인 목적을 선언하는 문장입니다. 효율적인 자동 생성 과정을 통해 PRM의 진짜 잠재력을 이끌어내려 합니다.

---

### ✅ 문장 1-2-2
For this, we first ask: (i) what should the per-step process rewards measure, and (ii) what kind of automated data collection strategy should we use to train PRMs that predict this measure.

**🔍 단위 해석**
이를 위해 우리는 먼저 질문합니다: (i) 단계별 과정 보상은 무엇을 측정해야 하는가? 그리고 (ii) 이 측정값을 예측하는 PRM을 훈련시키기 위해 어떤 종류의 자동화된 데이터 수집 전략을 사용해야 하는가?

**💡 보조 설명**
* 논문이 풀고자 하는 두 가지 핵심 연구 질문을 제시합니다.

---

### ✅ 문장 1-2-3
For (i), conventional belief (Lightman et al., 2023; Uesato et al., 2022) has been to measure mathematical correctness or relevance of steps.

**🔍 단위 해석**
(i)에 대하여, 기존의 통념은 각 단계의 수학적 정확성이나 관련성을 측정하는 것이었습니다.

**💡 보조 설명**
* 지금까지 사람들은 단계별 보상을 줄 때 '이 수식 전개가 수학적으로 참(True)인가 거짓(False)인가'를 기준으로 채점해왔습니다.

---

### ✅ 문장 1-2-4
But, it is unclear if this supervision yields the most improvement in the base policy (e.g., a policy may need to generate simpler, repetitive, and even incorrect steps to explore and discover the final answer during test-time search and RL).

**🔍 단위 해석**
하지만, 이러한 감독(supervision)이 기본 정책의 성능을 가장 크게 향상시키는지는 불분명합니다(예를 들어, 정책은 테스트 시간 탐색이나 RL 도중에 최종 답을 탐색하고 발견하기 위해 더 단순하거나, 반복적이거나, 심지어 틀린 단계를 생성해야 할 수도 있습니다).

**💡 보조 설명**
* 정답만을 강요하는 피드백 방식이 모델의 '창의적인 탐색(trial and error)' 능력을 저해할 수 있음을 비판합니다. 일시적으로 틀린 방향으로 가보는 것이 최종 답을 찾는 데 도움이 될 수도 있다는 논리입니다.

---

### ✅ 문장 1-2-5
Our key insight is that per-step, process rewards that measure notion of progress: change in the likelihood of arriving at a correct final answer before and after taking the step, are effective, for both test-time beam search and online RL.

**🔍 단위 해석**
우리의 핵심 통찰은 단계별로 '진척도'라는 개념—해당 단계를 밟기 전과 후의 올바른 최종 답에 도달할 가능성의 변화—을 측정하는 과정 보상이 테스트 시간 빔 서치(beam search)와 온라인 RL 모두에 효과적이라는 것입니다.

**💡 보조 설명**
* **[키워드] 빔 서치 (Beam Search):** 텍스트를 생성할 때 여러 유망한 경로(Beam)를 동시에 탐색하여 최종적으로 가장 확률이 높은 답을 찾아내는 알고리즘입니다.

---

### ✅ 문장 1-2-6
Reinforcing promising steps that make progress regardless of whether they appear in a correct or incorrect trace results in overall better exploration of possible answers at initial steps, which is crucial when the approach to solve a problem is not clear.

**🔍 단위 해석**
최종적으로 맞거나 틀린 추론 과정에 나타나는지와 상관없이 진척도를 만들어내는 유망한 단계를 강화하는 것은, 초기 단계에서 가능한 답변들에 대한 전반적인 탐색을 더 원활하게 만들며, 이는 문제 해결 접근법이 명확하지 않을 때 매우 중요합니다.

**💡 보조 설명**
* 전체 풀이가 오답으로 끝났더라도, 중간에 정말 기발하고 유망한 시도가 있었다면 그 시도 자체에는 보상을 줘서 모델이 그 방향의 탐색을 계속하도록 독려해야 한다는 의미입니다.

---

### ✅ 문장 1-2-7
Formally, such rewards correspond to per-step advantages of steps from the RL literature (Sutton & Barto, 2018).

**🔍 단위 해석**
공식적으로 이러한 보상은 강화 학습 문헌의 단계별 어드밴티지(advantage)에 해당합니다.

**💡 보조 설명**
* **[키워드] 어드밴티지 (Advantage):** 강화 학습에서 특정 상태에서 평균적으로 기대할 수 있는 보상(Value) 대비, 특정 행동(Action)을 취했을 때 추가로 얻을 수 있는 보상의 차이를 의미합니다.

---

### ✅ 문장 1-2-8
We empirically show that using advantages in conjunction with ORM rewards outperforms using future probabilities of success or Q-values (Wang et al., 2024) for both search and RL.

**🔍 단위 해석**
우리는 경험적으로 ORM 보상과 결합하여 어드밴티지를 사용하는 것이 탐색과 RL 모두에서 미래의 성공 확률이나 Q-값(Q-values)을 사용하는 것보다 성능이 우수함을 보여줍니다.

**💡 보조 설명**
* 단순히 기대 확률(Q-value)만 쓰는 기존 연구들보다 진척도(Advantage)를 썼을 때가 훨씬 좋았다는 실험적 결론입니다.

---

### ✅ 문장 1-2-9
This is because, in the combinatorial space of responses, Q-values mainly "exploit" states whereas advantages also "explore" steps that make the most progress towards the final answer.

**🔍 단위 해석**
그 이유는 답변의 조합적 공간(combinatorial space)에서 Q-값은 주로 현재 상태를 "활용(exploit)"하는 데 치중하는 반면, 어드밴티지는 최종 답을 향해 가장 큰 진척을 이루는 단계를 "탐색(explore)"하도록 돕기 때문입니다.

**💡 보조 설명**
* **[키워드] 활용(Exploit) vs 탐색(Explore):** Q-값은 이미 점수가 높을 것으로 확실히 예상되는 안전한 길만 쫓으려 하지만, 어드밴티지는 기존 상태 대비 점수를 얼마나 '끌어올렸는가'에 집중하므로 새로운 해법 경로를 탐험하도록 촉진합니다.

---

## 📘 단락 1-3
To answer (ii) above, we first note that advantages under a poor base policy are $\approx 0$ on most steps, and thus will not be informative for test-time search. More generally, we argue that regardless of the strength of the base policy, using its own per-step advantages as process rewards in RL will result in updates equivalent to only using outcome rewards. Hence, we propose to use advantages estimated via rollouts under a different "prover" policy as process rewards (Fig. 1). How should we choose this prover policy? A natural guess would be to use a very capable prover policy, akin to how strong reward models are found to be effective for RLHF (Gao et al., 2023). However, we show advantages under an overly capable prover, that can succeed from any step, fail to distinguish between good and bad steps. A similar argument holds for very weak provers. We formalize this intuition to define good provers as policies that are complementary to the base policy, i.e., policies that can distinguish steps, while producing advantages correlated with the base policy. For example, when restricted to the special case of Best-of-K policies corresponding to a base policy, the set of good provers consists of policies for $K > 1$ with not too large values of K. Contrary to intuition, in general, this set also contains policies that are worse than the base policy, but can still improve it. We show that dense verifiers trained to predict advantages of these prover policies, which we call process advantage verifiers (PAVs), accelerate sample and compute efficiency of RL and search.

---

### ✅ 문장 1-3-1
To answer (ii) above, we first note that advantages under a poor base policy are $\approx 0$ on most steps, and thus will not be informative for test-time search.

**🔍 단위 해석**
위의 질문 (ii)에 답하기 위해, 우리는 성능이 떨어지는 기본 정책 하에서 산출된 어드밴티지는 대부분의 단계에서 0에 가까우며($\approx 0$), 따라서 테스트 시간 탐색에 아무런 정보도 주지 못한다는 점에 먼저 주목합니다.

**💡 보조 설명**
* 베이스 모델이 너무 멍청해서 어떤 행동을 취하든 결국 정답을 맞출 확률이 0이라면, 행동 전후의 확률 차이(Advantage)도 항상 0이 되어 피드백으로써의 가치가 사라진다는 의미입니다.

---

### ✅ 문장 1-3-2
More generally, we argue that regardless of the strength of the base policy, using its own per-step advantages as process rewards in RL will result in updates equivalent to only using outcome rewards.

**🔍 단위 해석**
더 일반적으로 우리는 기본 정책의 강력함과 상관없이, RL에서 정책 자신의 단계별 어드밴티지를 과정 보상으로 사용하면 결국 결과 보상(ORM)만을 사용하는 것과 동등한 업데이트 결과를 초래한다고 주장합니다.

**💡 보조 설명**
* 자기가 만든 행동을 자기 자신의 기준으로 평가하면, 결국 최종 점수를 따라가는 것과 다를 바 없는 수학적 한계가 존재함을 의미합니다.

---

### ✅ 문장 1-3-3
Hence, we propose to use advantages estimated via rollouts under a different "prover" policy as process rewards (Fig. 1).

**🔍 단위 해석**
따라서, 우리는 과정 보상으로서 모델 자신과 다른 "증명자(prover)" 정책 하에서 롤아웃을 통해 추정된 어드밴티지를 사용할 것을 제안합니다 (그림 1).

**💡 보조 설명**
* **[키워드] 롤아웃 (Rollout):** 특정 상태에서부터 정책 모델을 이용해 시뮬레이션을 끝까지 진행해보고 최종 결과를 얻어내는 과정입니다.
* **[PDF 참조]:** Fig 1(a)를 보면, 기본 정책이 아닌 보라색(Strong prover)과 주황색(Complementary prover) 정책이 개별적으로 롤아웃을 돌려보고 평가를 진행하는 것을 볼 수 있습니다.

---

### ✅ 문장 1-3-4
How should we choose this prover policy?

**🔍 단위 해석**
그렇다면 이 증명자 정책을 어떻게 선택해야 할까요?

**💡 보조 설명**
* 논리의 흐름을 유도하는 수사적 질문입니다.

---

### ✅ 문장 1-3-5
A natural guess would be to use a very capable prover policy, akin to how strong reward models are found to be effective for RLHF (Gao et al., 2023).

**🔍 단위 해석**
자연스러운 추측은 RLHF에서 강력한 보상 모델이 효과적이라고 밝혀진 것처럼, 매우 능력이 뛰어난 증명자 정책을 사용하는 것일 겁니다.

**💡 보조 설명**
* 당연히 똑똑하고 성능이 압도적인 큰 모델을 평가자로 쓰는 것이 좋을 것이라는 직관을 설명합니다.

---

### ✅ 문장 1-3-6
However, we show advantages under an overly capable prover, that can succeed from any step, fail to distinguish between good and bad steps.

**🔍 단위 해석**
그러나, 우리는 어떤 단계에서 시작하든 무조건 성공해내는 지나치게 능력이 뛰어난 증명자 하에서는 어드밴티지가 좋은 단계와 나쁜 단계를 구별하는 데 실패한다는 것을 보여줍니다.

**💡 보조 설명**
* **[PDF 참조]:** Fig 1(a)의 파란색(Very capable prover policy)을 보면, 첫 번째 줄의 식 전개를 하든(step 1), 엉뚱한 식을 쓰든(step 2), 너무 똑똑해서 어차피 혼자 알아서 최종 정답을 도출해냅니다. 그러면 두 스텝 모두 롤아웃 성공률이 1.0이 되므로, 어드밴티지 차이가 0.0이 되어버려 피드백이 불가능해짐을 시각적으로 확인할 수 있습니다.

---

### ✅ 문장 1-3-7
A similar argument holds for very weak provers.

**🔍 단위 해석**
유사한 주장이 매우 약한 증명자들에게도 적용됩니다.

**💡 보조 설명**
* 반대로 너무 약하면 어떤 단계에서 롤아웃을 하든 실패율 100%라서 차이가 발생하지 않습니다.

---

### ✅ 문장 1-3-8
We formalize this intuition to define good provers as policies that are complementary to the base policy, i.e., policies that can distinguish steps, while producing advantages correlated with the base policy.

**🔍 단위 해석**
우리는 이러한 직관을 공식화하여, 기본 정책에 '보완적인(complementary)' 정책, 즉 기본 정책과 상관관계가 있는 어드밴티지를 산출하면서 단계들을 구별할 수 있는 정책을 좋은 증명자로 정의합니다.

**💡 보조 설명**
* 기본 정책이 어려워하는 부분을 적당히 풀어줄 수 있으면서도, 너무 압도적이지 않아서 각 단계의 좋고 나쁨(차이)을 민감하게 캐치해내는 모델을 뜻합니다. (Fig 1(a)의 주황색 모델)

---

### ✅ 문장 1-3-9
For example, when restricted to the special case of Best-of-K policies corresponding to a base policy, the set of good provers consists of policies for $K > 1$ with not too large values of K.

**🔍 단위 해석**
예를 들어, 기본 정책에 상응하는 'Best-of-K' 정책이라는 특수한 경우로 한정 지어 보면, 좋은 증명자 집합은 K값이 너무 크지 않은 $K > 1$ 인 정책들로 구성됩니다.

**💡 보조 설명**
* K가 너무 크면 '너무 똑똑한' 모델이 되어버리므로, 적당히 여러 번 시도하는 수준의 K를 설정한 모델이 좋은 증명자가 됩니다.

---

### ✅ 문장 1-3-10
Contrary to intuition, in general, this set also contains policies that are worse than the base policy, but can still improve it.

**🔍 단위 해석**
일반적인 직관과 반대로, 이 집합에는 기본 정책보다 성능이 떨어지는 정책들도 포함될 수 있으며, 그럼에도 불구하고 기본 정책을 향상시킬 수 있습니다.

**💡 보조 설명**
* 선생님(Prover)이 학생(Base Model)보다 전반적인 성적은 떨어질지라도, 특정 문제에 대해 학생의 오류를 민감하게 짚어주는 역할이라면 충분히 훌륭한 선생님이 될 수 있다는 혁신적인 관점입니다.

---

### ✅ 문장 1-3-11
We show that dense verifiers trained to predict advantages of these prover policies, which we call process advantage verifiers (PAVs), accelerate sample and compute efficiency of RL and search.

**🔍 단위 해석**
우리는 이러한 증명자 정책들의 어드밴티지를 예측하도록 훈련된 밀집 검증기, 이른바 과정 이점 검증기(PAVs)가 RL 및 탐색 과정의 샘플 효율성과 연산 효율성을 가속화한다는 것을 보여줍니다.

**💡 보조 설명**
* 본 논문에서 제안하는 핵심 솔루션인 PAV 모델에 대한 정의와 그 효과를 명시합니다.

---

## 📘 단락 1-4
With the conceptual design of PAVs in place, we prescribe practical workflows for training PAVs and demonstrate their efficacy on a series of 2B, 9B, and 27B Gemma2 models (Gemma Team et al., 2024). PAV training data is gathered by sampling "seed" solution traces from the prover and partial rollouts from the same to estimate the Q-value at each prefix of the seed trace. Our workflow prescribes favorable ratios for seed and partial rollouts. Our first set of empirical results show that for an equal budget of test-time compute, combining beam search with trained PAVs is > 8% better in accuracy, and $1.5-5\times$ more compute efficient compared to best-of-N re-ranking against an ORM. Dense rewards from PAVs improve the efficiency of step-level exploration during search by pruning the combinatorial space of solutions aggressively and honing in on a diverse set of possible sequences. Finally, we demonstrate for the first time, that using PAVs as dense rewards in RL scales up data efficiency by 6x compared to only using outcome rewards. Moreover, base policies trained with PAVs also achieve 8x better Pass@N performance (chance of sampling the correct solution in N attempts), and consequently afford a higher ceiling on the performance of any test-time re-ranker.

---

### ✅ 문장 1-4-1
With the conceptual design of PAVs in place, we prescribe practical workflows for training PAVs and demonstrate their efficacy on a series of 2B, 9B, and 27B Gemma2 models (Gemma Team et al., 2024).

**🔍 단위 해석**
PAV의 개념적 설계가 마련된 상태에서, 우리는 PAV를 훈련시키기 위한 실용적인 작업 절차를 규정하고, 2B, 9B, 27B 파라미터 크기의 다양한 Gemma2 모델 시리즈를 통해 그 효능을 입증합니다.

**💡 보조 설명**
* 구글의 오픈소스 모델인 Gemma2 시리즈를 베이스 모델로 삼아 실험했음을 밝히고 있습니다.

---

### ✅ 문장 1-4-2
PAV training data is gathered by sampling "seed" solution traces from the prover and partial rollouts from the same to estimate the Q-value at each prefix of the seed trace.

**🔍 단위 해석**
PAV 훈련 데이터는 증명자로부터 "시드(seed)" 해결 궤적(traces)을 샘플링하고, 시드 궤적의 각 접두사(prefix)에서 Q-값을 추정하기 위해 동일하게 증명자로부터 부분적인 롤아웃을 수행함으로써 수집됩니다.

**💡 보조 설명**
* **[키워드] 접두사 (Prefix):** 전체 문장에서 중간 특정 단계까지의 부분적인 문장/수식 상태를 의미합니다.

---

### ✅ 문장 1-4-3
Our workflow prescribes favorable ratios for seed and partial rollouts.

**🔍 단위 해석**
우리의 작업 절차는 시드 샘플링과 부분 롤아웃 간의 유리한 비율을 규정합니다.

**💡 보조 설명**
* 데이터를 모을 때 처음부터 끝까지 풀어보는 횟수(seed)와 중간에 끊고 여러 번 시뮬레이션 해보는 횟수(partial rollout) 사이의 황금비를 찾았다는 의미입니다.

---

### ✅ 문장 1-4-4
Our first set of empirical results show that for an equal budget of test-time compute, combining beam search with trained PAVs is > 8% better in accuracy, and $1.5-5\times$ more compute efficient compared to best-of-N re-ranking against an ORM.

**🔍 단위 해석**
우리의 첫 번째 실증적 결과 세트는 동일한 테스트 시간 연산 예산 하에서, 훈련된 PAV와 빔 서치를 결합하는 것이 ORM에 기반한 Best-of-N 재랭킹 방식과 비교하여 정확도 측면에서 8% 이상 더 우수하며, 연산 효율성은 1.5배에서 5배 더 높음을 보여줍니다.

**💡 보조 설명**
* **[PDF 참조]:** 이 결과는 Fig 1(b) 그래프와 정확히 일치합니다. x축이 샘플 개수(Compute budget)일 때, PAV 선형(주황색)이 ORM 선형(파란색)보다 훨씬 위에 있으며(정확도 향상), 10% 더 높은 곳에 도달하기 위해 샘플을 훨씬 덜 소모(5배 효율)함을 보여줍니다.

---

### ✅ 문장 1-4-5
Dense rewards from PAVs improve the efficiency of step-level exploration during search by pruning the combinatorial space of solutions aggressively and honing in on a diverse set of possible sequences.

**🔍 단위 해석**
PAV가 제공하는 밀집 보상(dense rewards)은 조합적으로 확장되는 해결책의 공간을 적극적으로 가지치기(pruning)하고 다양하고 가능한 시퀀스 집합에 집중함으로써, 탐색 과정 중 단계 수준의 탐색 효율성을 향상시킵니다.

**💡 보조 설명**
* 매 스텝마다 채점하므로 가망이 없는 루트는 즉각 잘라내고(Pruning), 유망한 여러 루트들에 컴퓨팅 파워를 집중할 수 있게 합니다.

---

### ✅ 문장 1-4-6
Finally, we demonstrate for the first time, that using PAVs as dense rewards in RL scales up data efficiency by 6x compared to only using outcome rewards.

**🔍 단위 해석**
마지막으로, 우리는 RL에서 PAV를 밀집 보상으로 사용하는 것이 결과 보상(ORM)만을 사용하는 것에 비해 데이터 효율성을 6배 더 향상시킨다는 사실을 최초로 증명합니다.

**💡 보조 설명**
* **[PDF 참조]:** Fig 1(c)에서 볼 수 있듯, x축인 Training Iterations이 진행됨에 따라 PAV-RL(주황선)이 ORM-RL(파란선)보다 훨씬 가파르게 성능이 올라가, 동일 목표 달성 시점이 약 6배 빠릅니다.

---

### ✅ 문장 1-4-7
Moreover, base policies trained with PAVs also achieve 8x better Pass@N performance (chance of sampling the correct solution in N attempts), and consequently afford a higher ceiling on the performance of any test-time re-ranker.

**🔍 단위 해석**
더 나아가, PAV로 훈련된 기본 정책들은 Pass@N 성능(N번의 시도 중 정답 솔루션을 샘플링할 확률) 또한 8배 더 우수하며, 결과적으로 어떠한 테스트 시간 재랭킹 모델을 적용하더라도 그 성능의 상한선(ceiling)을 더 높여줍니다.

**💡 보조 설명**
* **[키워드] Pass@N:** 모델에게 N번의 생성 기회를 줬을 때, 그 중 적어도 1개 이상의 완벽한 정답이 포함되어 있을 확률입니다. 기본 베이스 모델 자체가 훌륭한 후보를 잘 생성해내므로, 나중에 어떤 검증기가 골라내든 유리해진다는 의미입니다.

---

### 🖼️ 시각 자료 Figure 1
**Figure 1:** Process advantage verifier (PAV): Process reward for a step is defined as progress (advantage) under the prover policy, i.e., change in prover policy's success rate before and after the step. (a): The base policy samples both correct and incorrect steps but struggles to succeed from either. A strong prover completes the solution from any step, making no progress on either 1 or 2 (both scored 0.0). Conversely, a complementary prover policy distinguishes 1, 2 more prominently (only succeeds from 1). (b,c): Compared to ORMs, PAVs are 5x more compute efficient, 10% more accurate in test-time search, and 6x more sample efficient for RL.

**🔍 캡션 해석**
그림 1: 과정 이점 검증기(PAV): 한 단계에 대한 과정 보상은 증명자 정책 하에서의 진척도(어드밴티지), 즉 해당 단계 전후의 증명자 정책 성공률 변화로 정의됩니다. (a): 기본 정책은 옳은 단계와 틀린 단계를 모두 샘플링하지만, 어느 쪽에서도 성공적으로 끝마치지 못합니다. 매우 강력한 증명자는 어떤 단계에서든 솔루션을 완성해내기 때문에, 1과 2 모두에서 진척도를 얻지 못합니다 (두 경우 모두 보상이 0.0). 반면, 보완적인 증명자 정책은 1과 2를 더 명확하게 구별합니다(오직 1번 경로에서만 성공함). (b,c): ORM과 비교하여, PAV는 테스트 시간 탐색에서 5배 더 연산 효율적이고 10% 더 정확하며, RL 훈련에서 6배 더 샘플 효율적입니다.

**📊 PDF 시각 분석**
* **Panel (a):** 좌측의 다이어그램은 Base Policy(회색)가 첫 스텝에서 맞는 방향(1.0)과 틀린 방향(0.0)을 내뱉었을 때, 이를 검증하는 두 모델을 비교합니다.
  - Very Capable Prover(파란색): 너무 똑똑해서 틀린 스텝(2번 경로)에서 시작해도 기어코 정답을 맞추기 때문에 스텝 1과 2의 Advantage 값에 차이가 없어 피드백이 불가능합니다. (둘 다 진척도 0.0)
  - Complementary Prover(주황색): 적당한 수준이어서, 맞는 스텝(1)에서 출발하면 성공하지만 틀린 스텝(2)에서 출발하면 실패합니다. 이 덕분에 좋은 스텝과 나쁜 스텝을 확연히 구분해낼 수 있게 됩니다.
* **Panel (b):** $x$축은 Base Policy로부터 뽑은 샘플 수(Search Compute), $y$축은 Accuracy입니다. PAV 선이 ORM 선보다 높게 위치하여, 훨씬 적은 샘플(좌측으로 5x 위치)로도 동일 성능을 내거나 동일 샘플로 10% 더 높은 정확도를 보여줍니다.
* **Panel (c):** RL 훈련 시 Iteration 횟수 대비 정확도를 보여줍니다. 학습 속도(기울기)가 압도적으로 빨라, ORM-RL 대비 6배 더 빠르게 목표치에 도달함을 시각적으로 명확히 입증합니다.

---