# RELATED WORK MATRIX

아래 표는 현재 메인 논문화에 직접 영향을 주는 **P0 중심 related work matrix**다. 목적은 단순 요약이 아니라, `무엇을 빌릴 수 있는지`, `무슨 주장을 하면 안 되는지`, `어디서 안전하게 차별화할 수 있는지`를 빠르게 확인하는 것이다.

| Paper | Year / Venue | 실제로 무엇을 하는가 | 선택/배분 단위 | SC | Verifier / PRM | Search / Reflection | Adaptive TTC | 우리 novelty risk | 빌릴 수 있는 것 | 하면 안 되는 주장 | 안전한 차별화 지점 | KIISE S/A target 관점 의미 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| Chain-of-Thought Prompting | 2022 / NeurIPS 2022 | few-shot exemplar에 중간 추론 단계(rationale)를 넣어 단일 자연어 reasoning path를 유도; 충분히 큰 모델에서 reasoning이 emergent하게 발현 | prompt, path | N (greedy 단일 path) | N | N | N | 낮음 (origin baseline; claim 정의 측면) | origin CoT(greedy) baseline, direct vs CoT 기준선, GSM8K/SVAMP/ASDiv/AQuA/MAWPS, prompt-diversity vs structural 분리 ablation, emergent-at-scale framing | CoT/rationale 유도 자체 novelty, `CG > CoT`, `prompt에 reasoning을 넣으면 좋아진다`를 새 관찰처럼 쓰면 안 됨; raw GSM8K/MATH SOTA 경쟁 금지 | CoT를 개선/대체하는 것이 아니라, CoT 포함 heterogeneous path family를 제한 TTC 안에서 state-conditioned하게 acquire/STOP; CG는 auxiliary structured family | 모든 reasoning path-family 비교의 origin baseline anchor이자 few-shot CoT greedy를 모든 실험표의 기준선으로 고정 |
| Self-Consistency | 2023 / ICLR 2023 | same-prompt CoT sample을 여러 개(temperature) 뽑아 final answer를 majority vote(answer marginalization)로 집계; 고정 K, verifier 없음 | sample, answer (rationale은 점수화 안 함) | Y | N | N | N | 매우 높음 | fixed SC@K(K=1/4/8/16/32/64) origin multi-sample baseline, Maj@K, sample-scaling curve, homogeneous SC vs heterogeneous pool 대비 | self-consistency 자체, majority vote, homogeneous multi-sample reasoning, sample scaling을 우리가 처음 한다고 말하면 안 됨; SC를 폄하하지도 말 것 | fixed homogeneous same-prompt SC가 아니라 heterogeneous path-family / strategy-level allocation과 state-conditioned STOP으로 이동 | 모든 core target의 필수 baseline이자 RASC/CISC·adaptive-TTC의 substrate; 반드시 넘어야 할 fixed homogeneous SC baseline |
| Reasoning-Aware Self-Consistency | 2025 / NAACL 2025 | reasoning-answer pair에서 answer-level/ reasoning-level feature를 뽑아 sufficiency score를 계산하고 early stopping, weighted majority voting, rationale selection 수행 | sample, rationale, answer | Y | lightweight outcome-style scorer | best-of-n | Y | 매우 높음 | stopping rule, rationale-aware weighting, sample-efficiency framing, cost/runtime reporting | SC efficiency, reasoning-aware stopping, weighted voting, rationale scoring을 우리가 처음 한다고 말하면 안 됨 | homogeneous CoT-style sample scoring이 아니라 heterogeneous path-family / macro-strategy acquisition과 state-conditioned stopping으로 이동 | ACL/EMNLP/NAACL/AAAI에서 직접 부딪히는 핵심 baseline이자 strongest nearby precedent |
| Confidence Improves Self-Consistency | 2025 / ACL Findings 2025 | sampled reasoning paths에 model-derived confidence를 부여하고 temperature-scaled confidence-weighted majority vote 수행 | sample, path, answer | Y | lightweight self-confidence signal | best-of-n | Y | 높음 | confidence-weighted aggregation, P(True), WQD, budget-accuracy curve | confidence voting, SC cost reduction, self-confidence 기반 path scoring을 우리가 처음 한다고 말하면 안 됨 | aggregation 개선을 strong baseline으로 수용하고, 그보다 앞선 heterogeneous path-family acquisition / strategy allocation으로 이동 | ACL/EMNLP/NAACL 계열에서 반드시 넣어야 할 aggregation baseline |
| Learning How Hard to Think | 2025 / ICLR 2025 | query별 marginal reward를 예측해 adaptive best-of-k sample 수와 weak/strong decoder routing을 배정 | budget, sample, decoder | mixed | outcome verifier / reward model 사용 | N | Y | 매우 높음 | query-level compute allocation framing, uniform vs adaptive baseline, oracle allocation, query-only predictor | adaptive TTC, difficulty prediction, math adaptive best-of-k를 우리가 처음 한다고 말하면 안 됨 | query-only allocation이 아니라 current reasoning pool을 보는 state-conditioned heterogeneous path-family / macro-strategy allocation으로 이동 | ICLR/ICML/NeurIPS stretch뿐 아니라 ACL/EMNLP/NAACL/AAAI에서도 직접 대응이 필요한 대표 adaptive TTC 선행 |
| Scaling LLM Test-Time Compute Optimally | 2025 / ICLR 2025 | `best-of-N`, `PRM beam/lookahead search`, `sequential revision`을 비교하고 problem difficulty별 compute-optimal TTC strategy를 분석 | budget, strategy, path | optional | process PRM | search / revision | Y | 매우 높음 | compute-optimal framing, difficulty-conditioned baseline, search-vs-revision decomposition, verifier-overhead accounting | adaptive TTC, problem-level strategy selection, best-of-N inefficiency를 우리가 처음 말했다고 하면 안 됨 | difficulty-only strategy selection이 아니라 current reasoning state를 보는 heterogeneous path-family / macro-strategy allocation으로 이동 | ICLR/ICML/NeurIPS stretch와 ACL/EMNLP/NAACL/AAAI positioning에서 모두 직접 부딪히는 TTC framing anchor |
| Let's Verify Step by Step | 2024 / ICLR 2024 | `MATH` multi-sample solutions에 대해 process supervision으로 PRM을 학습하고 best-of-N candidate selection에서 ORM / majority voting과 비교 | step, path, answer | optional | process PRM | best-of-N rerank | indirect | 높음 | process vs outcome supervision distinction, verifier-selection baseline, fixed candidate pool after generation framing | verifier/reranker 도입 자체, best-of-N + verifier selection, candidate selection 문제를 우리가 처음 다룬다고 말하면 안 됨 | fixed candidate pool after generation이 아니라 upstream path acquisition / heterogeneous pool construction / state-conditioned stopping으로 이동 | verifier-aware main paper나 extension paper에서 반드시 언급해야 할 canonical PRM citation |
| Math-Shepherd | 2024 / ACL 2024 | 자동 process supervision으로 PRM을 학습해 step-level verification, best-of-N reranking, step-level PPO에 활용 | step, path, answer | optional | process PRM | best-of-N rerank + PPO | indirect | 높음 | auto process supervision, PRM baseline, step-level vs outcome-level distinction, verifier cost accounting | verifier/reranker 도입 자체, best-of-N reranking, automatic process annotation을 우리가 처음 한다고 말하면 안 됨 | step-level candidate verification이 아니라 upstream path acquisition / heterogeneous pool construction / state-conditioned stopping으로 이동 | verifier / PRM extension 및 main paper의 strongest baseline 중 하나 |
| Process Reward Models That Think | 2025 / arXiv / unverified | long verification CoT를 생성하는 generative PRM(THINKPRM)으로 best-of-N, guided beam search, parallel/sequential verifier scaling을 수행 | step, path, answer | Y | generative PRM | best-of-N rerank + beam + verifier rounds | Y | 매우 높음 | generative verifier framing, compute-matched verifier scaling, long verification CoT, process-based filtering | verifier compute scaling, generative PRM, best-of-N verifier selection을 우리가 처음 제안한다고 말하면 안 됨 | verifier가 이미 있는 상황에서 candidate pool construction / generation-verification budget split / state-conditioned stopping으로 이동 | generative verifier와 lightweight orchestration을 비교할 때 반드시 넣어야 할 P1 baseline |
| Self-Discover | 2024 / NeurIPS 2024 | atomic reasoning modules를 SELECT-ADAPT-IMPLEMENT하여 task-specific reasoning structure를 만들고, 그 구조로 instance를 풂 | reasoning module, structure, strategy | baseline only | N | meta-prompt structure construction | indirect | 매우 높음 | strategy-card framing, structure composition language, task-level vs instance-level strategy distinction, compute-efficiency framing | structured reasoning이 CoT보다 낫다, 여러 reasoning strategy를 조합하는 것이 새롭다, strategy selection 자체가 novelty라고 말하면 안 됨 | one-shot task-level structure composition이 아니라 state-conditioned path-family acquisition, macro-strategy routing, verifier-aware stopping으로 이동 | strategy selection / structured prompting 축의 P0 핵심 선행이며 core target venue에서 반드시 정면 대응 필요 |
| Automatic Model Selection with LLMs for Reasoning | 2023 / EMNLP Findings 2023 | `CoT`와 `PAL` 해법을 모두 생성한 뒤, LLM selector가 더 나은 method / answer를 post-hoc으로 고름 | method, strategy, answer | Y | N | N | N | 매우 높음 | method routing problem definition, heterogeneous-vs-similar method contrast, `Delta UpperBound`, `Success Rate`, option-order bias | reasoning method selection, heterogeneous selection의 비용 효율성, LLM selector novelty를 우리가 처음 한다고 말하면 안 됨 | one-shot post-hoc selection이 아니라 state-conditioned path acquisition / macro strategy allocation / STOP decision으로 이동 | selection-focused related work의 핵심 P0 선행이며 ACL/EMNLP/NAACL/AAAI positioning에서 직접 대응 필요 |
| PAL: Program-aided Language Models | 2023 / ICML 2023 | 자연어 문제를 Python program으로 변환한 뒤 interpreter 실행으로 계산과 symbolic state tracking을 offload하는 tool-use reasoning path를 제안 | path, sample, tool | optional | N | tool execution | N | 높음 | tool-executed path family, language-vs-execution split, arithmetic/state-tracking robustness, `SC-PAL` 및 mixed-path budget comparison | tool-use novelty, natural-language-plus-execution novelty, CoT arithmetic failure 발견을 우리가 처음 주장하면 안 됨 | 모든 문제에 PAL 하나를 고정 적용하는 것이 아니라 CoT / CG / PAL / direct / verifier-guided path 중 무엇을 언제 더 획득할지 결정하는 state-conditioned path allocation으로 이동 | math reasoning mainline에서 빠지면 안 되는 canonical tool-use baseline이며 path-family completeness를 보여주는 P0 reference |
| Program of Thoughts Prompting | 2023 / TMLR | LLM이 Python program을 생성한 뒤 computation은 external interpreter에 맡겨 reasoning과 computation을 분리하는 executable numerical reasoning path를 제안 | path, sample, tool | optional | N | tool execution | N | 높음 | executable path family, computation-vs-reasoning split, `SC-PoT`, symbolic/state-tracking robustness, budget-matched tool-use baseline | tool-use novelty, computation offloading novelty, CoT 계산 오류 발견을 우리가 처음 주장하면 안 됨 | PoT를 고정 baseline으로만 보지 말고, CoT / PAL / CG / direct / verifier-guided path 중 무엇을 언제 더 샘플링할지 결정하는 state-conditioned pool construction으로 이동 | math reasoning mainline에서 반드시 포함해야 할 canonical executable-path baseline |
| ToRA | 2024 / ICLR 2024 | 자연어 reasoning과 program 실행·tool 호출을 한 trajectory에서 interleave하는 tool-integrated agent를 GPT-4 trajectory imitation learning + output space shaping으로 7B~70B(LLaMA/CodeLLaMA)에 학습 | trajectory, tool_call, path | optional | N | tool execution feedback loop (명시적 search 아님) | N | 높음 | tool-interleaved path family baseline(ToRA/ToRA-Code 7B~70B), CoT/PAL/PoT 대비축, output space shaping diversity, GSM8K/MATH + tool-call·exec cost | tool-use, program execution, reasoning↔tool interleaving을 우리가 처음 한다고 말하면 안 됨; raw MATH/GSM8K SOTA 경쟁 금지 | ToRA-like path 자체를 새로 제안하는 게 아니라, ToRA-like 포함 heterogeneous path family 중 무엇을 제한 TTC 안에서 언제 추가 획득/STOP할지 결정하는 state-conditioned orchestration; tool execution cost를 TTC budget에 포함 | PAL/PoT보다 강한 tool-interleaved path family reference이자 P0 baseline 후보 |
| Tree of Thoughts | 2023 / NeurIPS 2023 | thought(중간 단위)를 노드로 한 트리에서 thought 생성(sample/propose) + self-eval 상태평가(value/vote) + BFS/DFS(backtracking/lookahead) explicit search; GPT-4, planning/search 과제(Game of 24 등) | thought, state, node | N (CoT-SC는 비교 baseline) | N (self-evaluation; 외부 PRM 아님) | Y (BFS/DFS, lookahead, backtracking, self-eval) | N | 중간 (search/self-eval/deliberate solving 선점) | IO/CoT/CoT-SC/ToT(-lite) deliberation-spectrum baseline, generation(sample/propose)·evaluation(value/vote) framing, LM-call/tree-node cost accounting, budget-matched comparison | reasoning search·tree search·BFS/DFS·lookahead/backtracking·state self-evaluation·deliberate problem solving을 우리가 처음 한다고 말하면 안 됨; CG를 ToT/GoT식 tree/graph search처럼 과장 금지; raw SOTA 경쟁 금지 | thought-level explicit tree search가 아니라 heterogeneous path-family acquisition + STOP + macro strategy allocation; search/eval cost를 TTC budget에 계상; small LM + offline/route-level | search-heavy contrastive boundary reference (GoT·MCTS·rStar-Math cluster); full reproduction 비대상, 필요 시 budget-matched ToT-lite |
| Graph of Thoughts | 2024 / AAAI 2024 | LLM reasoning을 thought=vertex, dependency=edge의 arbitrary directed graph로 모델링하고 generate/aggregate(merge)/refine transformation + thought scoring + graph controller(GoO/GRS)로 실행; GPT-3.5, sorting 등 elaborate 과제 | thought, vertex, graph state(GRS) | N (CoT-SC는 비교 baseline) | N (thought self-scoring/ranking; 외부 PRM 아님) | Y (graph transformations, aggregation/merge, refinement loop) | N | 중간 (graph-structured reasoning/thought graph 선점; CG naming 혼동 위험 큼) | latency–volume(cost–quality) tradeoff framing, IO/CoT/CoT-SC/ToT 비교축, decompose→solve→merge, LM-call/graph-operation cost accounting | graph-structured reasoning·arbitrary thought graph·aggregation/merging·refinement loop·transformation reasoning을 우리가 처음 한다고 말하면 안 됨; CG를 GoT식 graph search/traversal처럼 과장 금지; GoT를 GPT-4 실험으로 잘못 기록 금지; raw SOTA 경쟁 금지 | thought-graph 변환·실행이 아니라 heterogeneous path-family acquisition + STOP + macro strategy allocation; CG는 graph-preserving traversal이 아니라 linearized structured rationale; graph operation cost를 TTC budget에 계상 | graph-structured contrastive boundary reference (ToT·MCTS·rStar-Math cluster); reviewer가 CG로 가장 먼저 연상; full reproduction 비대상 |
| rStar-Math | 2025 / ICML 2025 | code-augmented CoT, Python execution, MCTS, PPM, 4-round self-evolution을 결합해 1.5B~7B SLM의 System 2 math reasoning을 frontier 수준까지 끌어올림 | step, trajectory | optional | process preference model | MCTS + verifier-guided search | Y | 매우 높음 | System 1 vs System 2 framing, trajectory-scaling curve, PPM-style pairwise preference supervision, heavy-search contrast for positioning | SLM+TTC math improvement, MCTS math search, verifier-guided candidate selection, heavy System 2 reasoning novelty를 우리가 처음 한다고 말하면 안 됨 | step-level MCTS/PPM 경쟁이 아니라 heterogeneous path-family / macro-strategy allocation과 state-conditioned stopping으로 이동 | math mainline에서 가장 강한 heavy System 2 frontier reference 중 하나이며, top venue positioning에서 직접 대응이 필요한 P0 위협 논문 |
| DeepSeek-R1 | 2025 / arXiv | human reasoning label 없이 pure RL(GRPO)로 long CoT·self-verification 행동을 창발시키고(R1-Zero), cold-start + multi-stage RL로 안정화한 뒤 1.5B~70B 소형 모델로 distillation | model weights(training), trajectory(single long CoT) | N | rule-based reward(정답/format) 사용, step-level PRM 아님 | emergent self-verification / reflection (명시적 search 없음) | N (training-time; long CoT length는 emergent) | 매우 높음 (claim boundary) | distilled reasoner backbone baseline(R1-Distill-Qwen 1.5B/7B), AIME/MATH-500 + thinking-token budget, GRPO·cold-start·self-verification terminology | RL로 reasoning 유도, distilled small reasoner, long CoT/self-verification, SLM math reasoning을 우리가 처음 한다고 말하면 안 됨 | training-time reasoner 학습이 아니라 fixed/given backbone 위 state-conditioned heterogeneous path-pool construction / STOP / lightweight orchestration으로 이동 | training-heavy / reasoning-RL cluster의 P0 contrastive frontier reference이며, raw SOTA 경쟁이 아니라 claim boundary 제한·strong backbone 용도로 정면 배치 |
| DeepSeekMath | 2024 / arXiv / unverified | 7B code-pretrained backbone에 120B math corpus pretraining, CoT/PoT/tool-integrated SFT, GRPO RL, SC@64를 결합해 strong open math model을 만듦 | answer, sample | Y | reward / RL supervision 사용 | N | N | 높음 | strong 7B math backbone, homogeneous SC baseline, `Pass@K` vs `Maj@K` 분석, training-vs-inference distinction | sLLM math improvement, CoT/PoT/tool-use mixing, self-consistency gain을 우리가 처음 보였다고 말하면 안 됨 | training-heavy backbone 개선이 아니라 heterogeneous path-pool construction, fixed homogeneous SC를 넘는 budget-aware allocation으로 이동 | math mainline에서 빠질 수 없는 backbone/reference baseline이며, evaluation에서 `Pass@K / Maj@K / fixed SC@K`를 반드시 포함하게 만드는 P0 선행 |
| s1 | 2025 / EMNLP 2025 | single reasoning trace의 thinking length를 budget forcing과 `"Wait"` 삽입으로 제어해 sequential test-time scaling 구현 | budget, thinking length | baseline only | N | long CoT / self-correction-like continuation | Y | 중간~높음 | single-path TTC baseline, Control/Scaling/Performance metric, sequential-vs-parallel framing | test-time scaling, thinking length control, simple TTC improvement를 우리가 처음 한다고 말하면 안 됨 | longer same-path thinking이 아니라 heterogeneous path-family / strategy-level allocation으로 이동 | core target에서 반드시 비교해야 할 single-path sequential TTC baseline |
| Self-Refine | 2023 / NeurIPS 2023 | 단일 LLM이 generator/feedback/refiner를 모두 맡아 generate→self-feedback→refine을 반복(추가 학습·RL 없음); GPT-3.5/GPT-4, 7개 과제(수학 포함) | path (동일 출력 iterative 개선) | N | N (self-feedback; 외부 verifier 아님) | Y (reflection / iterative refinement, self-feedback loop) | N | 중간 (reflection/self-refinement 선점) | reflection macro-strategy baseline(self_refine_k), parallel SC vs sequential refine budget-matched 비교, iteration/token cost accounting | reflection·self-feedback·iterative refinement를 우리가 처음 한다고 말하면 안 됨; Self-Refine의 math(GSM8K) 약세로 reflection 무효를 일반화 금지; raw SOTA 경쟁 금지 | 동일 path 반복 개선이 아니라 heterogeneous path-family acquisition + STOP + macro strategy allocation; reflection은 macro strategy card 후보; reflection cost를 TTC budget에 계상 | reflection/self-feedback cluster의 P1 baseline reference (Reflexion과 묶음) |
| Reflexion | 2023 / NeurIPS 2023 | weight update 없이 language agent가 task feedback에 자연어로 self-reflect한 텍스트를 episodic memory에 저장해 다음 trial을 개선(verbal RL); GPT-3.5/GPT-4, ALFWorld/HotPotQA/HumanEval | trial (agentic 반복 + memory) | N | N (Evaluator/self-reflection; 외부 PRM 아님) | Y (self-reflection + episodic memory, trial-and-error) | N | 중간 (reflection/verbal-RL/memory 선점) | reflection macro-strategy(reflexion-like), feedback 신호 종류 ablation, trial/memory/LM-call cost accounting | verbal feedback·self-reflection·episodic memory·trial-and-error 개선을 우리가 처음 한다고 말하면 안 됨; reflection 무효 일반화 금지; raw SOTA 경쟁 금지 | agentic multi-trial+memory가 아니라 단일 budget heterogeneous path-family acquisition + STOP; reflection은 macro strategy 후보; memory/trial cost를 TTC budget에 계상 | reflection/self-feedback cluster의 P1 reference (Self-Refine과 묶음) |
| Least-to-Most Prompting | 2023 / ICLR 2023 | 복잡한 문제를 simpler subproblem sequence로 분해(1단계)하고 이전 답을 활용해 순차 해결(2단계)하는 decompose-first prompting; GPT-3 code-davinci-002, GSM8K/DROP/SCAN | subproblem, strategy (decompose-first) | optional | N | N | N | 중간 (decomposition/planning prompting 선점) | decompose-first strategy card, easy-to-hard 난이도 곡선, prompt-diverse vs decompose ablation, 분해 generation cost accounting | decomposition·planning prompting·문제 분해 효용을 우리가 처음 한다고 말하면 안 됨; strategy card를 단순 prompt engineering으로 정당화 금지; raw SOTA 경쟁 금지 | 단일 decompose-first 고정이 아니라 어떤 strategy card를 현재 state·budget에서 선택·중단할지의 macro strategy allocation; decompose cost를 TTC budget에 계상 | decomposition/structured-prompting cluster의 P1 reference (Plan-and-Solve·Self-Discover와 묶음) |
| Plan-and-Solve Prompting | 2023 / ACL 2023 | Zero-shot-CoT를 개선해 먼저 계획을 세우고(plan) 단계별로 수행(solve)하도록 지시하는 zero-shot plan-first prompting(PS/PS+); GPT-3 text-davinci-003, GSM8K 등 | strategy (plan-first), prompt | optional | N | N | N | 중간 (plan-first/zero-shot reasoning prompt 선점) | plan-first strategy card(PS/PS+), zero-shot-CoT 오류유형(계산/누락/의미) 분해, prompt-diverse vs plan-first ablation, plan generation cost accounting | plan-first·planning prompting·zero-shot reasoning prompt 개선을 우리가 처음 한다고 말하면 안 됨; strategy card를 단순 prompt engineering으로 정당화 금지; raw SOTA 경쟁 금지 | 단일 plan-first 고정이 아니라 budget 하 strategy card 선택·중단의 macro strategy allocation; plan cost를 TTC budget에 계상 | decomposition/structured-prompting cluster의 P1 reference (Least-to-Most·Self-Discover와 묶음) |
| Large Language Models are Zero-Shot Reasoners | 2022 / NeurIPS 2022 | 답 앞에 "Let's think step by step"를 붙여 exemplar 없이 multi-step reasoning을 유도하는 Zero-shot-CoT 원형; InstructGPT text-davinci-002/PaLM, MultiArith/GSM8K 등 | prompt, path | optional | N | N | N | 낮음~중간 (zero-shot-CoT origin/anchor) | zero-shot-CoT anchor baseline, prompt-diversity 축(zero-shot vs few-shot vs prompt-diverse), small-LM 이득 약화 곡선, 2단계 generation cost accounting | zero-shot step-by-step prompting·exemplar 없는 reasoning 유도를 우리가 처음 한다고 말하면 안 됨; prompt family를 단순 prompt engineering으로 정당화 금지; raw SOTA 경쟁 금지 | 단일 minimal prompt 고정이 아니라 budget 하 heterogeneous path-family acquisition + STOP; zero-shot-CoT는 anchor baseline | prompt-family/structured-prompting cluster의 P1 anchor reference (CoT·Least-to-Most·Plan-and-Solve와 묶음) |
| Distilling Step-by-Step | 2023 / ACL 2023 Findings | teacher(PaLM 540B) rationale을 추출해 student(T5 220M/770M/11B)를 multi-task(label+rationale)로 학습, 적은 데이터·작은 모델로 LLM 능가 | rationale (학습 supervision) | N | N | N | N | 중간 (rationale distillation·CG distillation 선점) | rationale-as-supervision multi-task framing, data-efficiency/student-size metric, distilled student를 fixed backbone substrate로 | rationale distillation·step-by-step distillation·CG distillation 자체를 우리가 처음/핵심 기여로 말하면 안 됨; training-time gain을 inference-time 기여로 혼동 금지 | training-time distillation이 아니라 fixed backbone 위 test-time path-pool allocation; distillation은 backbone 확보 수단 | distillation/rationale boundary cluster의 P1 reference (CG distillation 축 boundary; UniCoTT·MAGDi·STaR와 묶음) |
| Rewarding Progress (PAV) | 2024 / ICLR 2025 | process reward를 prover policy 하 "progress"(정답 likelihood advantage)로 정의·자동 측정하는 Process Advantage Verifiers; test-time search·RL credit assignment; Gemma2 2B/9B/27B, GSM8K/MATH | step, path | optional | Y (process advantage verifier, PAV) | verifier-guided search(beam/best-of-N) + RL | indirect (compute efficiency) | 중간 (process verifier/progress reward 선점) | outcome vs process reward framing, progress/advantage/prover policy, compute-efficiency(verifier overhead) metric, generation-verification budget split | process verifier·process reward·verifier-guided selection·progress reward를 우리가 처음 한다고 말하면 안 됨; verifier compute 무시 금지 | verifier 설계(downstream)가 아니라 upstream heterogeneous path-pool construction; verifier는 final-decision/STOP 후보; verifier compute를 TTC budget에 계상 | verifier/PRM cluster의 P1 reference (Let's Verify·Math-Shepherd·ThinkPRM과 묶음) |
| STaR (Self-Taught Reasoner) | 2022 / NeurIPS 2022 | few-shot로 rationale+answer 생성→정답인 것만 채택해 fine-tune, 틀린 문제는 rationalization(정답 hint)으로 보강하는 self-bootstrap 반복; GPT-J 6B, GSM8K/CQA | rationale (self-generated, correctness-filtered) | N | N (correctness filtering; 외부 verifier 아님) | self-bootstrapping loop (training) | N | 중간 (rationale bootstrapping/self-improvement 선점) | self-generation+correctness filtering framing, bootstrap iteration별 gain, bootstrapped backbone substrate | rationale bootstrapping·self-generated rationale training·self-improvement via rationales를 우리가 처음 한다고 말하면 안 됨; training-time gain을 inference-time 기여로 혼동 금지 | training-time self-improvement가 아니라 fixed backbone 위 test-time path-pool allocation; bootstrap은 backbone 확보 수단 | distillation/bootstrapping boundary cluster의 P1 reference (rStar-Math self-evolution 계보 원형) |
| UniCoTT | 2025 / ICLR 2025 | chain·tree·graph 등 다양한 구조의 CoT를 iterative construction + structured constraints로 작은 SLM(BERT class)에 통합 증류; CoT distillation의 hallucination·구조 무시 해결 | structured rationale (학습 supervision) | N | N | N | N | 중간~높음 (structural CoT distillation·CG distillation 직접 boundary) | structure-aware distillation framing(chain/tree/graph), structured constraints, distilled backbone substrate | structural CoT distillation·chain/tree/graph 증류·CG distillation/CoT_Lv route를 새 structural distillation으로/우리 main novelty로 말하면 안 됨 | training-time structural distillation이 아니라 fixed backbone 위 test-time path-pool allocation; CG는 structured path family 중 하나로 보존(structure type 학습 아님) | distillation boundary cluster의 P1 reference (CG distillation 축의 가장 가까운 boundary; Distilling·MAGDi·STaR와 묶음) |
| MAGDi | 2025 / ICLR 2025 Spotlight | 여러 LLM agent의 multi-round 상호작용을 graph로 표현하고 base student에 GNN을 augment해 작은 LM에 structured distillation; 단일 효율 모델로 multi-agent 비용 제거 | structured rationale / graph (학습 supervision) | N | N | N | N | 중간 (graph distillation·CG graph claim 직접 boundary) | multi-agent 비용·단일 효율 모델 동기, structured distillation objectives(contrastive + graph structure), distilled backbone substrate | multi-agent interaction graph distillation·GNN-augmented student를 우리가 처음 한다고/우리 CG가 graph reasoning이라고 말하면 안 됨 | training-time graph distillation이 아니라 fixed backbone 위 test-time path-pool allocation; CG는 linearized structured rationale path family(graph 아님) | distillation boundary cluster의 P1 reference ('진짜 graph distillation' 기준점; GoT graph-claim 절제와 연결) |
| Mentor-KD | 2024 / EMNLP 2024 | reasoning distillation(CoT KD)의 teacher rationale 품질·다양성 부족 + soft label 부재를 intermediate "mentor" 모델로 보강(rationale augmentation + soft label); teacher LLM → small student | rationale, soft label (학습 supervision) | N | N | N | N | 중간 (SLM reasoning distillation/teacher-guided improvement 선점) | teacher rationale 품질·다양성 framing, soft label, mentor augmentation, distilled backbone substrate | small model multi-step reasoning distillation·teacher-guided rationale augmentation·soft label KD·SLM reasoning improvement를 우리가 처음 한다고 말하면 안 됨 | training-time KD가 아니라 fixed backbone 위 test-time path-pool allocation; distillation은 backbone 확보 수단 | distillation boundary cluster의 P1 reference (Distilling·UniCoTT·MAGDi·STaR와 묶음) |
| Unveiling Key Factors (CoT distillation) | 2025 / ACL 2025 Findings | CoT distillation 성능을 좌우하는 요인(rationale granularity·format·teacher model)을 4 teacher × 7 student × 7 dataset로 체계 분석; SLM≠LLM 거동, student capacity confound | rationale (분석 대상) | N | N | N | N | 낮음~중간 (CoT distillation factor analysis 선점; 우리 ablation 통제 근거) | distillation confound(granularity/format/teacher/capacity) 통제 변수, SLM≠LLM 거동, factor 민감도 분석 | CoT distillation factor analysis·rationale 형식/teacher 선택 효과를 우리가 처음 밝혔다고 말하면 안 됨; route 차이를 confound 미통제로 path-family 효과라 단정 금지 | training-time distillation 분석이 아니라 fixed backbone 위 test-time path-pool allocation; route-family 비교에 distillation/teacher/capacity confound 통제 명문화 | distillation boundary cluster의 P1 analysis reference (ablation 통제 근거) |
| SWITCH | 2025 / NAACL 2025 Findings | LLM KD에서 student-generated outputs(SGO)의 noise/bias로 인한 teacher misguidance를, teacher가 token-level divergence 큰 토큰에서만 선택 개입(studying with teacher)해 완화; instruction-following 중심 | token (학습 supervision) | N | N | N | N | 낮음~중간 (teacher-student KD/studying-with-teacher 선점) | train-inference mismatch·SGO framing, selective(전체가 아닌) 개입 아이디어, distilled backbone substrate | teacher-student KD·studying with teacher·KD dynamics·SGO 개선을 우리가 처음 한다고 말하면 안 됨 | training-time token-level KD가 아니라 fixed backbone 위 test-time route-family selection; route family 생성(training)과 test-time 선택을 분리 | distillation boundary cluster의 P1 reference (Mentor-KD/Distilling/UniCoTT/MAGDi와 묶음); ⚠️ venue NAACL 2025(legacy EMNLP25 오기) |
| Self-Instruct | 2023 / ACL 2023 | 모델 자체 생성으로 instruction·input·output을 부트스트랩·필터링해 fine-tune하는 self-generated instruction tuning; vanilla GPT-3 +33%(SUPERNI) | instruction/instance data (학습 supervision) | N | N | N | N | 낮음~중간 (self-generated instruction/instruction tuning 선점) | self-generated data 활용·diversity 필터링·instruction-tuned backbone substrate | self-generated instruction·synthetic instruction generation·instruction tuning을 우리가 처음 한다고 말하면 안 됨; prompt family/route card를 self-instruction 데이터 생성으로 정당화 금지 | training-time instruction tuning이 아니라 fixed backbone 위 test-time path-pool allocation; prompt family/route card는 inference-time 선택 단위 | instruction-tuning boundary reference (Phased Instruction FT와 묶음; distillation cluster 인접) |
| Phased Instruction FT | 2024 / ACL 2024 Findings | GPT-4로 instruction 난이도 평가 → 난이도 증가 subset 분할 → easy-to-hard 단계적 fine-tuning(Phased IFT); One-off IFT 대비 개선; Llama-2/3·Mistral-7B | instruction subset / training phase (학습 supervision) | N | N | N | N | 낮음~중간 (phased/staged instruction tuning 선점; difficulty 시점 혼동 주의) | 학습 점진성·GPT-4 difficulty scoring·easy-to-hard framing·instruction-tuned backbone substrate | phased/staged instruction tuning·training curriculum·난이도 단계화를 우리가 처음 한다고 말하면 안 됨; training-time difficulty curriculum과 test-time difficulty/state-conditioned allocation 동일시 금지 | training-time curriculum이 아니라 fixed backbone 위 inference-time state-conditioned path-pool allocation(난이도 적용 시점 분리) | instruction-tuning boundary reference (Self-Instruct와 묶음); difficulty 시점 분리 핵심 |
| TeleMath | 2025 / arXiv / unverified | telecom domain의 mathematical reasoning benchmark를 제공 | answer, strategy | majority vote 가능 | N | N | optional | 중간 | domain bridge benchmark, pass@1 vs majority-vote framing | TeleMath를 NFM 전체 대표로 과장 금지 | math mainline의 selective bridge/application | NFM bridge의 immediate candidate |
| TeleTables | 2026 / arXiv 2601.04202 | 3GPP 표준 table을 retrieval·interpretation·multi-step reasoning하는 LLM 능력을 500 human-verified MCQ로 평가; Huawei NetOp | answer, strategy (table evidence) | cons@16(=SC majority vote) | N | N | N | 낮음 | telecom table reasoning bridge benchmark, pass@1/cons@16(=SC) metric, difficulty/table-evidence 분해 | table reasoning을 우리가 처음/일반 해결, NFM 전체 대표, domain benchmark 구축이 main contribution, cons@16이 우리 기여라고 말하면 안 됨 | NFM bridge stage 2; path-pool/STOP/verifier를 table 도메인으로 transfer; thesis core 아님 | NFM bridge stage 2 candidate (table/structured-evidence) |

## 짧은 해석

- 가장 위험한 선행연구 축은 `RASC`, `Learning How Hard to Think`, `Scaling LLM TTC Optimally`, `Self-Discover`, `rStar-Math`다.
- `DeepSeek-R1`, `rStar-Math`, `DeepSeekMath`는 training-heavy / reasoning-RL frontier cluster로 묶고, raw SOTA 경쟁 대상이 아니라 `RL reasoning 유도 / long CoT / self-verification / distilled small reasoner` claim을 제한하는 contrastive reference이자 strong backbone으로 다룬다.
- 가장 필수적인 baseline 축은 `Self-Consistency`, `RASC`, `s1`, `CISC`, `DeepSeekMath`다.
- verifier 관련 문헌은 `Let's Verify Step by Step`, `Math-Shepherd`를 최소 묶음으로 보는 편이 안전하다.
- NFM bridge는 현재 `TeleMath`만 immediate bridge로 두고, `TeleTables`, `TeleLogs`는 second phase로 분리하는 것이 맞다.
